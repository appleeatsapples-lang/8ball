# L48 override — PR #137 cleared on explicit controller word, no cross-lane read

This file is the L48 sighting log by construction (per the gate's own design:
"the override file IS the L48 sighting log"). **Sighting #22.**

- **PR:** #137 — test-coverage audit plus its P1–P9 list: boot sequence
  extracted to `ui/boot.js`, `index.html` ↔ `ui/` markup contract pinned,
  rising edge-cases converted from validity to correctness assertions, one
  product fix (silent pre-1900 submit dead-end), the countries loops collapsed,
  and an advisory coverage step. 21 files, +2731/−296, 20 commits.

- **This override is written by a lane that did not author the PR**, which is
  the one respect in which it is stronger than the recent run of self-overrides
  (#160 among them). It is still not an audit. It records a decision; nobody
  independent has read the change.

- **State at the word:** `mergeable_state: dirty` — the branch is **22 commits
  behind** (`1840afa` → `28c08ae`) and conflicts on exactly two files,
  `CLAUDE.md` and `index.html`. No `test` or `l48-gate` run exists on the head
  at all; the newest checks are Netlify's from ~05:00Z. **So this override does
  not make the PR mergeable** — it clears the artifact requirement and nothing
  else. A rebase has been requested from the authoring lane.

- **Cross-lane read: none.** `audits/codex_pr137_premerge_audit_2026-07-29_brief.md`
  exists on the branch and commissions one, but a brief has never satisfied the
  gate (the #129 F2 pin) and the lane it names is retired — see sighting #14 and
  the §10 amendment draft. The PR's own body records an adversarial pass by a
  same-vendor model and explicitly declines to offer it as gate satisfaction,
  which is the correct call and is preserved here rather than quietly upgraded.

- **Word:** explicit in-session controller instruction — *"yes merge 137"*,
  then *"write the override for pr137"* — given after this lane set out the
  three blockers and recommended a rebase over a merge. Controller override
  authority per DOCTRINE §10/§14.

- **What this override does not cover.** The 21-file change is unreviewed by
  any second lane, and three specific exposures are named rather than left
  implicit:
  1. The PR's own body discloses that **32 of its 39 mutation-testing claims
     rest on commit messages alone**; an adversarial pass sampled 7 and all 7
     held, so the remaining 32 are corroborated by nothing.
  2. The `index.html` boot extraction **cannot be verified by this repo's
     suite** — §12 forbids jsdom, so no test evaluates the page. #157 shipped a
     live paywall defect through a green suite for exactly this reason. The
     authoring lane verified in real Chromium; that verification has not been
     independently repeated.
  3. The countries collapse removes ~395 tests. Argued as redundant iteration
     rather than lost signal, and coverage is reported byte-identical — but the
     argument has not been checked by anyone else.

- **Placement, because it is load-bearing.** This file is committed on the PR's
  own branch, not to `main`. The gate matches
  `git diff --diff-filter=A ... $BASE...HEAD`, so an artifact already present on
  `main` is not *added by the PR* and would leave the gate red — while also
  firing the RECYCLED diagnostic. An override for a PR only functions from
  inside that PR. Worth stating: it is an easy and silent mistake.

- **Renumbered twice before this ever merged: #18 → #20 → #22.** #18 was free
  when this file was written and taken by
  `audits/L48_override_pr173_2026-07-29.md` minutes later. #20 was then free and
  taken by `audits/L48_override_pr178_2026-07-29.md` (#178), which merged to
  `main` first. Each correction was made here rather than allowed to land,
  because the already-merged record is never the one to move.

- **Ledger — the race is now pinned, and this file is its first catch.** The
  #17 duplicate this file originally reported (`pr160` and `pr168` both claiming
  it) was resolved on `main` by #177 (`f91e81c`), which also added
  `tests/l48_gate.test.js`'s uniqueness guard: every override must carry a
  sighting number, and no two may share one. That guard is what caught the
  #20 collision above — it failed on merge, which is exactly the outcome the
  earlier entries here asked for. Numbers are still allocated by reading the
  directory, so the race itself is unfixed; what changed is that a collision
  can no longer land silently.

  Three collisions in one day is the underlying signal, and a red suite is a
  late place to learn about it. An allocation scheme that does not require
  reading the directory — or a number derived from the PR rather than a
  sequence — would remove the race instead of detecting it.

- **Standing gap, unchanged.** A red check still does not block a merge; branch
  protection requiring `test` and `l48-gate` is repo-admin and unset. This
  override therefore removes a convention blocker, not a mechanical one.
