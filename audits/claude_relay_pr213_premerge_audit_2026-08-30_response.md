# Cross-model pre-merge audit — PR #213

**PR:** #213 — Filed §1.I relations in panels + four deterministic harmony frames
**Branch:** `claude/eight-ball-app-testing-rqphfo` (restarted from `main` @ `41915f0` after PR #212 merged)
**Heads:** `5aac72c` reviewed by both lanes; the fixes and the DOCTRINE v0.65 amendment land with this artifact's commit
**Date:** 2026-08-30
**Auditor:** in-container two-lane review (opus + sonnet), fresh context
each, reconciled by the authoring session (fable). Same-family lanes
with an author-reconciler, weighted accordingly; every finding
re-verified against source before disposition. Per L48 this response
does not self-clear the PR — merge stays with the controller.

## Verdicts

- sonnet: **MERGE WITH FIXES** (a governance gap + a DEMONSTRATED
  coverage loss, both reproduced by execution; nine of its own mutants run)
- opus: **MERGE WITH FIXES** (2 MAJOR + 3 MED + LOWs; 398 relation lines
  and 267 harmony sentences generated and voice-scanned — zero register
  violations; 18 mutants; live-fire at four rungs)
- **Reconciled: MERGE WITH FIXES — everything fix-class is landed in
  this artifact's commit, the §1.I scope now constitutionally recorded
  as DOCTRINE v0.65. SAFE TO MERGE per this artifact's own read,
  pending the controller's read and the explicit merge word (§10/L48).**

## Findings — fixed in this artifact's commit

1. **Register-law emissions (opus MAJOR).** The panel's relation line
   carried three of §1.I's six mandatory emissions and dropped the
   rest — the `recorded, not certified.` qualifier included, the one
   both other registry surfaces carry and `tests/dyad.test.js` pins by
   name. **Fixed:** the line now emits values, relation text, the
   textual status `registered`, the filing citation, the named
   registry, and the qualifier verbatim; emission-pinned per pair kind;
   the panel cap raised 640 → 720 for the grown line (style-pinned).
2. **Tier boundary unpinned for three pair kinds (opus MAJOR, driven
   end-to-end in Chromium).** A single `|| 2` default on the numerology
   partner value rendered a sealed compartment's number on a free-tier
   panel with all tests green. **Fixed:** sealed-partner pins for every
   pair kind (numerology, animal both directions, element both
   directions, sun/rising); the lane's mutant now reds.
3. **§1.I scope unrecorded (sonnet).** The intra-sheet application was
   a scope extension DOCTRINE never licensed — the class this repo's
   own precedent (§1.J) always ships with an amendment. **Fixed:**
   DOCTRINE v0.65, the §1.I intra-sheet amendment (controller order
   2026-08-30): the fixed pair list, registered-only rendering with the
   absence-claims-nothing rule, the emission set with the status
   narrowed to `registered`, the adverse-suppression and
   stale-panel-close safeguards, the sealed-DOM tier boundary, and the
   sole-importer preservation. Version log rotated (v0.64 → prior).
4. **Demonstrated coverage loss (sonnet).** After the frame-agnostic
   loosening, an injected theme/role misattribution passed the entire
   suite. **Fixed:** a token-order oracle over sentinel parts pins the
   exact theme↔role sequence per frame, plus a hand-assembled
   `harmonyFor` equality oracle built from registry parts the engine's
   internals cannot fake; the lane's injected bug now reds 2 pins.
5. **Cross-registry contradiction (opus MED, 96/96 adverse pairs).**
   The harmony algebra rendered one line above a filed
   chong/square/ke record. **Fixed:** an ADVERSE filed record (derived
   from the registries' own relation vocabulary) suppresses the
   algebra closing in that panel; a filed THEME tension still renders —
   friction above friction is consistent; pinned both ways.
6. **Artificial frame-reachability pin (opus MED).** The old pin's
   cross-product hid that `sun` never reaches frame 3 over its real
   twelve signs. **Fixed:** the pin enumerates each key's actual value
   domain — global all-four, every key at least two — and the modulus
   mutant now reds.
7. **Stale-panel false citation (opus MED; pre-existing shape, new
   §1.I consequence).** After a resubmission re-rendered the card under
   an open panel, the stale relation could cite a registered record for
   a same-value pair §1.I says must be unfiled. **Fixed:** the panel
   closes on any card re-render (guarded MutationObserver); pinned and
   live-fire verified.

## Ruled out / confirmed (the lanes' own verification)

Opus generated and voice-scanned ALL 398 assembled relation lines and
267 harmony sentences — zero register violations (the walker's 4-line
sample was a coverage gap, not a live defect; the walker still scans
one line per pair kind and every frame). The relation strings are
party-neutral intra-sheet — both registries key on signs and branches,
not people. The tier boundary held at every rung in live-fire BEFORE
the pins existed (the pins now keep it that way). Sun and rising panels
render the same record; the id-collision worry was checked and is not
real; frame grammar reads correctly with real role phrases under both
heads; suite/audit numbers reproduced exactly by both lanes.

## Verification after all fixes

Suite **57 files / 1970 tests green** (1965 + the emission pins, the
sealed-partner pins, the adverse pin, the real-domain reachability pin,
the token-order and equality oracles, the observer pin). Product audit
PASS, 0 blocking. Six lane mutants re-run post-fix — the role
misattribution, the `|| 2` partner default, the modulus change, the
qualifier drop, the adverse-suppression removal, the observer removal —
each red on exactly its pin; restored, green. Live-fire: the full
six-emission line renders on the sun panel; the panel closes on a card
mutation under it; zero console errors. This artifact's filename
carries pr213; with the DOCTRINE touch in the same commit, its journal
entry and this file satisfy the doctrine gate, and the l48-gate greens
on this push.
