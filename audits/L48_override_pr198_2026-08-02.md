# L48 artifact — PR: cards warm-paper integration (2026-08-02)
Controller override under APPROACH.md §22 amendment #19 (Codex lane parked; cross-model relay would reload the operator).
Nature: binary asset integration only — zero code, zero logic. Restores the 611 card renders that were LIVE in production until 2026-08-02 01:44Z, when the PR #196 merge auto-deployed pure main and regressed them (Codex return-handoff flag #5, predicted verbatim).
Evidence: 3-way SHA-256 on two samples (a01_rat, e1_wood): live==main≠worktree pre-PR; staged bytes == worktree bytes exactly (53d216c7…, 276b4397…). Source: frozen worktree /private/tmp/8ball-ui-refinement (read-only copy; worktree untouched).
Risk: none beyond re-publishing previously-live bytes; kills flag #5 permanently by making git the home of what production serves.
Verdict: SAFE TO MERGE on operator word.

Gate-shape note: filed at the ci.yml SHAPE path for PR #198; same content as staged pre-PR.
