# Card clarity — bounded implementation and review record

Date: 2026-09-05. Base: `e161ab9bf9bd02839aa3929d4b66ac252bb14d90` on `claude/pair-dossier-imprint`, isolated worktree `/private/tmp/8ball-build-phase1`.

Status at handback: uncommitted presentation work with passing tests and independent bounded runtime review. This is not a full-PR audit, release clearance, exact-new-SHA verification, or browser-acceptance record.

## Authority and implementation route

The operator explicitly authorized fixing the card or directing Claude Code. The bounded brief is `/Users/8ball/Documents/8ball/brief_card_clarity_2026-09-05.md`. Claude began partial work but did not complete it: its Bash patch commands repeatedly received tool denials, then its session was stopped. Codex took over the scoped patching route under that fix-or-direct authority, with independent agents reviewing separate pieces. This route deviation is disclosed; it is not a permanent change to repository lane discipline.

## Presentation contract

- Host and both standalone Pair sheets retain compact, permanently visible row labels; no live reveal/hide toggle remains. Existing label helpers/key are compatibility-only. The retained initializer supplies the mobile intrinsic-height style, not preference-controlled visibility.
- Real DOM whitespace/separators keep adjacent values distinct in copied text without modifying value nodes, coordinates, or share snapshots.
- Native written-entry attribution explains the sun-sign/year-animal catalog driver, life-path-anchored initial authored note, and `flip again` rotation.
- `SYMBOLIC ASSOCIATIONS` and `counterpoint` replace the visible domain-fit/anti-fit framing. A visible qualifier rejects aptitude/career-assessment interpretation. Native explanatory copy distinguishes symbolic family lookup/order from the Tarot-posture/birthday-method sentence.
- Shared fixed copy/native disclosure helpers use existing `ui/sheet.js`; `ui/public.js` remains the sole UI consumer of `core/public.js`. Host and Pair instances own independent disclosure state, cleared on invalid/cleared/sealed rerender; the master-mode bridge stays intact.
- Result-rail copy is `full sheet`; underlying density logic and honest unresolved fields remain.

Doctrine v0.87 is additive. The v0.86 chronology phrase is corrected to `regressions v0.85 referred to`, without rewriting the old journal entry.

## Verification of the uncommitted candidate

Full Vitest run passed: **64 files / 2497 tests**. Focused public/grouping/census checks passed 115 tests; label/meaning/desk/Pair checks passed 293 tests. The 23-test new suite drives actual host and Pair rendering with a text-node-preserving fixture parser: adjacent copied tokens, pure share-row values, native summary/details structure, visible qualifier placement, unavailable/sealed/clear cleanup, master-bridge preservation, and independent disclosure instances. This is DOM evidence, not browser/CSS/clipboard acceptance.

Retired toggle and old domain/census assertions were deliberately replaced, not skipped. The two About-copy assertions in `tests/payments_markup.test.js` now require symbolic associations and the current local-storage disclosure; every payment-behavior assertion stays intact. Initial integration failures exposed stale assertions, including the old separator expectation; the final full suite resolves them. No skipped/exclusive tests or probe placeholders were introduced.

Independent bounded runtime review: PASS after removing proposed separator flex items. Host rows and the shared builder instead contain literal whitespace-only text nodes between unchanged cell elements, preserving the original layout children. `git diff --check` passed. Product audit passed with **12 pass / 0 fail / 1 warning / 1 skip**, zero blocking failures (`audits/automated/product_audit_20260905_150050.json`); the warning is the explicitly uncommitted tree and the skip is missing worktree-local private configuration. Audit scripts were not edited.

Protected paths compared byte-identical to the base: `core/`, `content/`, `tests/fixtures.json`, package/lockfiles, `ui/payments.js`, `ui/tiers.js`, `ui/share.js`, and `ui/pairShare.js`. No calculation/value mapping changed. `index.html` is 699 lines, below 1500. The local preview returned HTTP 200 at `http://127.0.0.1:5173/`.

## Limits and exclusions

Browser automation is blocked before initialization by the known symlinked writable-root configuration. No browser screenshots, layout/assistive-technology acceptance, or native-device share verification are claimed; no alternate browser route or machine repair is part of this pass.

The unchanged default private-pattern scan exits 1 because the worktree-local configuration is absent: **NO ASSURANCE** from that invocation. A supplementary read-only configured invocation used the existing operator-local pattern file without copying or exposing its contents and returned **clean (909 files scanned)**. Only the repository/configuration paths were overridden in memory; the scan logic stayed unchanged. Public/static privacy checks are not substituted for this distinction.

No personal inputs are included in fixtures or this record. The separate `/Users/8ball/01_ACTIVE/dev/8ball` pricing checkout is not an implementation target. No calculation, authored-content, pricing, sharing, storage, network, dependency, or creative-expansion work is authorized here. No push, PR, merge, deploy, or production clearance is claimed.
