# L48 artifact — PR #199: kua block + optional gender input (§1.D v0.63) (2026-08-03)
Controller override of the product-surface freeze (vehicle, tier, and process were controller decisions of 2026-08-03; §1.G v0.44 precedent — override authority not re-litigated). Codex lane parked; the §8 gate 6 cross-model audit is ROUTED, not skipped: the relay packet is `audits/crossmodel_kua_gender_premerge_brief_2026-08-03.md`, run per the PR #198 relay precedent.
Nature: six-commit feature train — §6 headroom split (paywall specimen fill → ui/payments.js), core/kua.js (+tests), content/kua.v1.js (+tests), ui/kua.js + ladder/sheet/dyad wiring (+tests), optional gender persistence (+tests), DOCTRINE §1.D v0.63 + §5 v0.63 + journal + relay brief. The product's first gender-keyed surface and first non-DOB demographic input.
Evidence: suite 54 files / 1887 tests green at every commit; product audit PASS (13/14, one expected operator-local skip) on a clean tree; index.html 1486/1500; three live-fire passes (paywall specimen post-split; kua sealed-empty at free with density tail + dual-mode fill at ?paid=t3; gendered submit → storage → reload rehydration → forget clears) with zero page errors.
Risk: paid-surface addition behind the existing t3 entitlement — no new localStorage key, strict two-token vocabulary at every write seam, census unchanged at 15/15, existing coordinates untouched (fixtures byte-identical), registry-facts-only content with the no-guidance pin.
Verdict: NOT self-cleared. Merge waits on the controller's explicit audit-cleared signal after the cross-model relay — L48 proper. This file exists so the l48-gate can evaluate while the relay runs; it is a sighting record, not a clearance.

Gate-shape note: filed at the ci.yml SHAPE path for PR #199.

L48 override sighting #24 — controller-override (Codex lane parked), doctrine-touching feature train with the relay audit in flight; not a five-minute-window merge.
