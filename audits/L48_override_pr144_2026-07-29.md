# L48 override — PR #144 cleared on the brief's own instruction, no cross-model read

This file is the L48 sighting log by construction (per the gate's own design:
"the override file IS the L48 sighting log"). **Sighting #13** — the fifth
consecutive clearance in this run with no second model on the change, and the
first written **because the brief that commissioned the work said to write
one**, rather than after a controller instruction given in-session.

The brief's gate clause reads: *"L48 requires an audits/ artifact naming the
PR — Codex is dead, so file an explicit override artifact stating the
cross-model lane is unavailable and naming who reviewed. Do not self-certify
silently."* This file does that literally, including the last sentence, which
is the part that costs something.

## The change

- **PR:** #144 — public-tier computation engine and its spec. Five new files
  (`core/public.js`, `content/public.v1.js`, `tests/public.test.js`,
  `tests/public_tier.fixture.json`, `PUBLIC_TIER_SPEC.md`), two count lines in
  `CLAUDE.md`, one journal entry, and this artifact. Additive: no existing
  module, test, fixture, scanner list or doctrine clause is modified.

- **What it changes for a reader today: nothing.** Nothing in the repository
  imports `core/public.js`; a test in the PR asserts that, and another asserts
  the module names no tier / price / credit / entitlement token. No rendered
  coordinate moves. The blast radius on the live product is zero until a
  separate change wires a surface, which needs a §1.D amendment under §10
  first.

- **State at this writing:** suite **44 files / 1536 tests** green locally in
  a clean checkout (`npm ci` then `npm test`); was 43 / 1500 on the base. CI
  had not reported at the time this file was committed — it runs against the
  push that carries it.

## Who reviewed

**Named, because the brief asked for a name and a vague answer here is worth
less than none: one author, no reviewer.** This lane wrote the spec, the
tables, the engine, the tests, the fixtures, the journal entry, and this
clearance. No second model read any of it. Codex is retired; no other lane was
commissioned, and none is in flight.

That is five roles and one author, which is a worse ratio than sighting #12
recorded, and it is stated here so the record does not read as though a review
happened.

## What this override does not cover

The change is additive and unwired, which bounds the damage but does not make
the judgement calls right.

- **The eleven-mode divergence is a doctrine question answered by an
  implementer.** DOCTRINE §1.B v0.54 fixed active Pythagorean numerology at
  exactly nine terminal values. This tier's expression number retains the 11
  and 22 stops, on the argument that it is a date-derived driver private to
  the tier rather than the `expression/name number` coordinate that clause
  governs. That argument was constructed by the lane that wanted the eleven
  entries the brief specified. **It is the single item most worth a second
  read**, and the PR body, the spec (§6.1) and the journal entry all flag it
  as a controller ruling rather than settled. Collapsing to nine modes is one
  predicate plus two table entries.

- **The name collision is unresolved on purpose.** "Expression number" already
  means the name-derived coordinate in §1.B; this one is date-derived and is a
  different number. Nothing surfaces either today, so nothing is currently
  wrong on screen — but if the tier is ever rendered before this is settled,
  the sheet shows two different things under one word.

- **The strength model is coarse and this lane cannot judge how coarse.**
  Strength is 旺相休囚死 against the month-branch element alone. A full BaZi
  read weighs roots, hidden stems and the rest of the chart. Whether the
  simplification is acceptable for a date-only tier is a practitioner's call,
  and no practitioner has looked at it. The favourability rankings inside the
  ten entries are likewise conventional but authored — a reviewer who works
  in this tradition might order `[output, wealth, officer]` differently.

- **The tables are authored copy with no second reader.** Fifteen domain-family
  bodies, eleven mode methods and twenty-two posture stances were written by
  this lane. They pass the canonical register scans mechanically; nobody has
  read them for taste, for §4 cultural-lineage respect, or for the flattening
  that a five-way partition of all human work invites. The scans catch banned
  vocabulary, not bad judgement.

- **The fixtures were generated from the implementation.** Sixteen snapshot
  cases cannot falsify an engine that was wrong when they were taken. Four
  independent anchors are in the suite to cover that (the three calibrated day
  pillars, a hand-walked case, the sheng/ke re-derivation, and season/posture
  re-derived from the shipped modules) — but they anchor the *composition*,
  not the *convention*. Nothing outside this repository has confirmed that the
  four-step pipeline is a defensible reading of the tradition it cites.

- **The architecture edge is new and unreviewed.** `core/public.js` is the
  first `core/` module to import from `content/`. It is one-way and adds no
  runtime capability, but no second lane has agreed that it is the right shape.

- **The local PII audit did not run.** It needs `audits/local_personal_data.txt`,
  which is gitignored and absent in a fresh container. The in-suite CI PII
  scan passes; the pre-push local audit is outstanding and is controller-side.

- **Scope:** this override covers **#144 only**.

## Missing inputs, recorded because they change how much this clearance is worth

The brief opened with a reading list of four sources outside this repository —
a personal context file, an approach document with the amendments this tier
was supposed to extend, a sibling development checkout, and the newest session
handoff. **None exists in this container.** The engine was built from the
tracked repo alone.

So the ladder framing this tier belongs to (exposure radius: private ·
comparative · public, at $3 / $6 / $9) exists in this record only as the
brief's own words. `DOCTRINE.md` §1.D still describes a density ladder at
$1 / $2 / $3, with the §4.B v0.56 single-$3-offer sprint live through
2026-08-08. **Those two accounts contradict each other**, and this lane could
not read the document that would reconcile them. The PR therefore names no
price, param or rung anywhere in code — but a reviewer should know that the
work was specified against context the implementer never saw.

## Standing gap, unchanged by this PR

Nothing yet blocks a merge on a red check; branch protection requiring `test`
and `l48-gate` is repo-admin and is not set. This file records a choice, not a
constraint that had to be lifted.

**Recommendation attached to this sighting.** A cross-model read is worth
commissioning here more than it was for the three test/CI clearances that
preceded it, because this change is 700+ lines of new authored convention
rather than a two-line correction — and it is worth commissioning *before*
any surfacing change, not merely before merge. The spec is written to be
handed to a reviewer as-is; §6 lists the five questions in the order they
matter. Merging the engine unwired forecloses nothing, and the rollback is
five file deletions plus two count lines.
