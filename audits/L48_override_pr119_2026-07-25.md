# L48 override — PR #119 merged with the gate red and no cross-model verdict

This file is the L48 sighting log by construction (per the gate's own
design: "the override file IS the L48 sighting log"). **Sighting #8** —
the first since #7 closed in the post-spree sweep, and the first logged
*after* the merge rather than at the merge word.

- **PR:** #119 — `ci.yml` action-runtime bump (`actions/checkout@v4` →
  `@v5`, `actions/setup-node@v4` → `@v5`) clearing the node20 runtime
  deprecation. One file, +6/-2, no product surface touched.
- **State at merge:** CI red on the L48 step alone. Every other gate
  green — suite 38 files / 1369 tests, single-file rule 1464 lines,
  journal-touch gate pass, audit-artifact gate pass. The deprecation
  warning was verified absent from the run log, and `fetch-depth: 0`
  history verified intact under checkout v5 (both diff-dependent gates
  resolved `git diff "$BASE"...HEAD` correctly — including the one that
  failed, which failed for the right reason).
- **Cross-model read:** none run, none commissioned, none in flight.
  This is the material difference from sighting #6 (#98), where a Codex
  verdict was mid-relay at the merge word and landed ~2 minutes later.
  Here nothing is pending, so there is no closure commitment to carry:
  the change shipped without a second model reading it. That is the
  override, stated plainly.
- **Word:** no in-session verbal merge word. The controller merged the
  PR directly with the gate red at head `e0692f9`, squash-merged to
  `main` as `71f2e7b`. Controller override authority per DOCTRINE
  §10/§14; the gate preserves it by design. Recorded here as observable
  fact — this lane was not given a quotable word and does not
  manufacture one.
- **Lane position, on record once:** the implementer lane declined to
  clear the gate itself, in writing and twice before the merge (the PR
  body and the in-PR run comment). Both routes to green were the
  controller's to take: a self-authored artifact is precisely the
  sighting #7 shape (#103's implementer-authored "self-audit"), and the
  override file is the controller's instrument per the #98 precedent.
  That reasoning stands unchanged; this file is the record the gate
  asked for, written after the fact on explicit instruction.
- **Retroactive, and named as such.** Sighting #6 was logged
  self-documenting at the merge word — "exactly as the mechanism
  intends." This one is not. The gap worth carrying forward: the gate
  blocks the merge *button* on a red check, but nothing blocks a
  squash-merge performed with the red in view, so the artifact the gate
  exists to force can always be deferred to after the fact. Whether
  that is acceptable slack or the next thing to tighten is a controller
  call, queued here rather than decided.
