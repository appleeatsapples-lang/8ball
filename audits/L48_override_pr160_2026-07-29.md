# L48 override — PR #160 shipped on explicit controller word, no cross-vendor read

This file is the L48 sighting log by construction (per the gate's own design:
"the override file IS the L48 sighting log"). **Sighting #19** — filed
pre-merge, the self-documenting shape #6 set.

- **PR:** #160 — `--no-renames` on both `CHANGED=` computations (closing a
  reproduced P0), plus pinning the `ADDED=` invocation exactly instead of by
  prefix (closing the associated P1). Two files: `.github/workflows/ci.yml`,
  `tests/l48_gate.test.js`.

- **The gate governs its own modification — for the fourth time.** #98 (scope
  widening), #122 (job split), #133 (added-not-renamed), now #160. Each was
  blocked by the machinery it changed, and each cleared by override rather
  than by an independent read. That pattern is itself worth a controller
  decision; it is recorded here rather than left to accumulate silently.

- **State at the word:** `test` green — 45 files / 1568 tests, +2 from this
  PR's own new pins. `l48-gate` red solely for want of this file, and red
  *correctly*: the run reached the artifact-shape error, which means the
  modified `CHANGED=` executed through `--no-renames` and classified the PR as
  not-docs-only. Had the flag broken the diff, the failure mode would have been
  the opposite and quieter — the exemption branch firing and the gate going
  green. Netlify header/redirect checks green.

- **Cross-vendor read: none.** None run, none commissioned, none in flight.
  `audits/claude_l48_predicate_crossread_2026-07-29.md` (PR #159) exists and
  found the defect this PR fixes, but it is a Claude-lane read and disclaims
  satisfying §10 in its own second section. It does not clear this PR and is
  not offered as doing so.

- **Word:** explicit in-session controller instruction — *"write the override
  for pr160"* — given after this lane laid out both routes and argued, twice,
  for the Codex relay over an override. Controller override authority per
  DOCTRINE §10/§14; the gate preserves it by design.

- **What this override does not cover — the loop is closed single-vendor.**
  This is materially weaker than the ordinary override, and the difference
  should not be lost. In a normal case one lane writes a change and no second
  model reads it. Here the *entire chain* is one vendor: Claude found the
  defect, Claude judged its severity, Claude wrote the fix, and Claude wrote
  the mutation tests that certify the fix works. A wrong fix that also passes
  its own author's mutation tests is precisely the failure this arrangement
  cannot detect. The specific unexamined risk: `--no-renames` changes what
  **every** consumer of `CHANGED=` sees, including the journal-touch and
  audit-artifact gates in the `test` job, and no independent party has checked
  that a legitimate rename-bearing PR is not now wrongly blocked.

- **Counter-consideration, recorded because it cuts the other way.** The P0 is
  live on `main` right now: any PR can rename a non-`.md` file to a `.md` path
  and delete this gate's own regression pins, the PII scanner, or the
  dependency-discipline check with both checks green and no artifact. Holding
  this fix for a relay leaves that open. The override therefore trades an
  unaudited fix against a known-exploitable gate — not against a safe status
  quo. That is a real trade with a defensible answer either way, and naming it
  is the point of this file.

- **Ledger note.** With this file, 11 of the 17 recorded sightings clear by
  override, and 8 of the artifacts filed on this date are overrides rather
  than verdict responses. The ledger lens of the #159 audit raised exactly
  this: the override is becoming the default path rather than the exception.
  Filed as an observation for the controller, not a judgement.

- **Numbering correction, recorded rather than silently fixed.** This file
  first claimed **#16**. That number was correct against the ledger when it was
  written and wrong by the time it was pushed: `audits/L48_override_pr157_2026-07-29.md`
  took #16 on a concurrent lane. Corrected to #17 on sighting, per the standing
  mechanical-correction precedent.

  **Then it happened again, and that time it landed.** #17 collided with
  `audits/L48_override_pr168_2026-07-29.md`, which took the same number 20
  seconds later (`968d004` 15:51:37 vs `6a6d327` 15:51:57) and reached `main`
  before anyone noticed. A third collision followed on #18
  (`L48_override_pr173` vs the pr137 override). Renumbered here to **#19**.

  **Why this file moved rather than pr168's, despite landing first.** Twenty
  seconds between two lanes writing concurrently is not meaningful priority.
  What decided it: pr168's #17 is cited in `DOCTRINE.md`'s v0.59 footer, so
  renumbering that record would edit the constitution — which under §8 requires
  a journal touch plus an `audits/` artifact, and under §10 a cross-model audit
  from a lane that does not currently exist (sighting #14). Moving this file
  touches only records this lane wrote. The cheaper correction is the right one
  when the numbers carry no ordering guarantee anyway.

  **The scheme is the defect, not any individual number.** Three collisions in
  one day is not carelessness; it is a read-modify-write race with no lock.
  `tests/l48_gate.test.js` now pins uniqueness, so a fourth collision fails CI
  instead of landing. What the pin cannot restore is chronological ordering —
  after this correction the numbers are unique identifiers, not a sequence, and
  the dates and commits are the record of when each sighting occurred. Anyone
  reading the ledger as a timeline should stop.

  The original observation stands: sighting numbers
  are assigned by reading the directory, which is a read-modify-write race
  whenever two lanes file overrides the same day. Nothing in CI would have
  caught a duplicate; the `l48-gate` predicate checks the filename, never the
  contents. Worth a pin if the ledger is meant to be authoritative.

- **Standing gap, unchanged.** A red check still does not block a merge.
  Branch protection requiring `test` and `l48-gate` is repo-admin and unset;
  this lane's token returns 403 on the protection endpoint. So this fix
  hardens a gate that nothing yet enforces — worth holding both facts at once.
