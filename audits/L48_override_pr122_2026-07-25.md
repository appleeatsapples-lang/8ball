# L48 override — PR #122 shipped on explicit controller word, no cross-model read

This file is the L48 sighting log by construction (per the gate's own
design: "the override file IS the L48 sighting log"). **Sighting #9** —
and unlike #8, logged **self-documenting before the merge**, which is the
clean shape the mechanism intends and the shape #6 set.

- **PR:** #122 — splits the L48 gate out of the `test` job into its own
  `l48` job reporting as check `l48-gate`, plus the CLAUDE.md lockstep
  update naming both check contexts. Two files, +22/-4. The gate script
  itself is byte-identical: the `ci.yml` diff is a pure insertion of the
  job wrapper, zero deletions in the script body.

- **The gate governs its own modification.** Same recursion as sighting
  #6 (#98, the L48 scope widening blocked by its own widened step): this
  PR changes the L48 machinery and is gated by the L48 machinery. The red
  on `l48-gate` is the newly-split job correctly refusing its own PR for
  want of this file — the mechanism demonstrating itself.

- **State at the word:** `test` green (suite 38 files / 1369 tests,
  single-file rule 1464 lines, journal-touch and audit-artifact gates
  pass). `l48-gate` red in 5s, failing for exactly the right reason —
  `PR='122'` resolved, `git diff "$BASE"...HEAD` evaluated against the
  full history `fetch-depth: 0` preserves, artifact absent. Netlify
  header/redirect checks green.

- **Cross-model read:** none run, none commissioned, none in flight. Same
  as sighting #8, and the same material difference from #6, where a Codex
  verdict was mid-relay at the merge word and landed minutes later. No
  closure commitment attaches to this file: the change ships without a
  second model having read it.

- **Word:** explicit in-session controller instruction — *"write the
  override for pr122"* — given after this lane laid out both routes to
  green (cross-model verdict or override) and declined to choose between
  them. Controller override authority per DOCTRINE §10/§14; the gate
  preserves it by design.

- **Lane position, on record once:** the implementer lane authored the
  change under review and therefore declined to author its audit — a
  self-authored artifact is the sighting #7 shape (#103). Writing this
  override on an explicit word is a different act, and the distinction is
  the reason the override exists as a separate instrument: it *records
  the absence* of a cross-model read rather than simulating the presence
  of one.

- **What this override does not cover.** The change is unreviewed by any
  second model. Its risk concentrates in one place worth naming plainly:
  had the job split broken the gate's diff resolution, the failure mode
  would have been a silent **pass**, not a visible red — a gate that
  stops gating looks identical to a gate with nothing to catch. The run
  on this PR shows it still evaluates correctly, but that verification
  was performed by the same lane that wrote the change, which is exactly
  the coupling a cross-model read exists to break.

- **Standing gap, unchanged by this PR.** Nothing yet blocks a merge on a
  red check. Branch protection requiring `test` and `l48-gate` is
  repo-admin and is not set; this lane's token returns 403 on the
  protection endpoint, so it could not be set here. Until it is, this
  file records a choice that was made, not a constraint that had to be
  lifted — and the sighting #8 finding stands: the artifact this gate
  exists to force can still be deferred past the merge.
