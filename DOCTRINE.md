# 8ball — DOCTRINE.md

> One page. Read every release. Edit only deliberately.

This document is the constitution of `8ball`. The codebase obeys it. PRs that contradict it require an explicit doctrine amendment in the same change.

## §1. What this is

A fixed designed deck that knows you. Enter your name and date of birth once. Shake.

**Current free/paid composition (authoritative) — read this before the lineage prose below.** The free surface is **five** coordinates from the date of birth (tarot birth card · sun · public animal (year) · life path · catalog numeral), rendered on a constant compartment **specimen sheet** where higher-tier coordinates appear as sealed cells (§1.D v0.37); three paid rungs (t1/t2/t3) open the rest. The current per-surface authority is the §1.x clause family — §1.A rising · §1.B numerology · §1.C birth card · §1.D tier ladder · §1.E provenance · §1.F atlas+density. The two paragraphs immediately below describe the original v0.2.3 surface and are retained as lineage per L17; where they disagree with §1.A–§1.F, the clauses win.

The product calculates ten baseline calibrated coordinates from (name, DOB): sun sign, Chinese five-element, public animal (year-pillar), private animal (month-pillar), life path, expression/name number, personality, maturity, soul urge, and birthday. The two animals pair on a single line via an equilibrium arrow (`⇌`); three of the numerology coordinates (life path, expression/name number, soul urge) render as a text triplet on line 4 of the result card. The (sun sign, public animal) pair drives a 144-card catalog index — computed positionally in `core/engine.js` (sun-row × 12 + animal-col + 1, rendered as roman numeral). Life path drives bracket resolution (low/mid/high) within a card cell via `resolveBracket`, not the catalog index. The catalog index is the only card-derived field surfaced.

As of v0.2.3 the result card surfaces seven of the ten baseline coordinates (chinese five-element, sun, public animal, private animal, life path, expression/name number, soul urge), optional rising sign, and the catalog index. The remaining three baseline coordinates (personality, birthday, maturity) are computed on buildProfile but reserved for v0.3.0+ paid surfacing — see §1.B. The locked render path ships symbols only — no interpretation, no per-symbol explanation — and is the canonical free surface. The unlocked render path (gated by paid credits per §1 v0.22 amendment / §4.B / §5.C) surfaces the card-content layer (name, type, habit, note per cell) from `content/cards.v1.full.js`; private authoring source is preserved at `~/dev/8ball-private/cards.v1.full.js`.

The voice is declarative-observational, framed in strengths and weaknesses. Cards openly reference the symbol systems they draw from — sun sign, Chinese five-element, Chinese zodiac animals (year and month pillars), numerology life path, name number, soul urge — and name them as such.

**v0.22 amendment (v0.3.0).** As of v0.3.0, the public repo ships the full card-content layer at `content/cards.v1.full.js` (144 entries, name · type · habit · note × low/mid/high). Visibility is JS-gated behind the `eight_ball_credits_v1` localStorage flag — unset/zero renders the locked surface (symbols only), positive value renders the unlocked surface (symbols + card-content). The deck bytes are inspectable via View Source — the lock is a UI convention, not a server-gated fortress (see §5.C). **The free-surface invariant "symbols only — no interpretation, no per-symbol explanation" still applies to the locked render path; the unlocked render path is the named exception, gated by paid credits.**

**v0.36 amendment (v0.6.0).** As of v0.6.0 the free surface is **four coordinates**: tarot birth card (§1.C), sun sign (bare — rising joins at t1), public animal (year-pillar, alone — the private month-pillar animal joins at t1), and the catalog numeral. The v0.2.3 seven-coordinate free surface described above is superseded and preserved as lineage per L17; those coordinates relocate to the paid rungs per the §1.D tier ladder. The locked render path still ships symbols only — no interpretation, no per-symbol explanation — and remains the canonical free surface. The v0.22 unlocked card-content layer relocates to tier t3 (§1.D / §4.B v0.36): credits buy reads, the stored tier sets density at every render — and credits held with no stored tier are the pre-v0.6.0 purchase shape, grandfathered to t3 per §1.D.

**§1.A. Rising sign — surface coordinate, not a driver.**

As of v0.2.1 the result card surfaces an eighth coordinate when the operator provides birth time, country, latitude, and longitude: the rising sign (astronomical ascendant). Rising sign is computed from `(year, month, day, hour, minute, utcOffsetMinutes, lat, lng)` via standard ascendant math (Meeus); see `core/rising.js` for the exact algorithm. It is rendered on line 2 of the result card paired with the sun sign as `${sunSign} ↑ ${risingSign}`.

Rising sign is **surface-only**: it does not enter `getCard`, `resolveBracket`, or any catalog computation. The catalog driver remains `(sunSign, animal)` per §1. Rising adds a coordinate to the visual surface; it does not branch the card layer.

When required birth-time or location fields are missing, rising sign is `undefined` and line 2 falls back to the v0.2.0 bare-sun-sign render. Existing v0.2.0 profiles in localStorage continue to work without modification.

**v0.21 amendment (v0.2.7.2).** Birthplace input is city-level via `core/cities.js` — a 53,308-entry pop≥7500 subset of GeoNames cities5000, lazy-loaded from `assets/cities.json` per §5 (v0.21 same-origin lazy-load permission). Selecting a city atomically sets `tz` (IANA timezone), `lat`, `lng`, and country code on the form state. The country-code payload shape from v0.2.1 is retained for backward compatibility with stored v0.2.1–v0.2.7.1 profiles, but the UI no longer surfaces country selection.

Rising-sign math is now DST-aware and historical-tz-aware. The `computeRising({...tz...})` API in `core/rising.js` resolves the UTC offset for the (year, month, day, hour, minute) wall-time input via `Intl.DateTimeFormat` with `{ timeZone: tz, timeZoneName: 'longOffset' }` — gives correct offset including DST transitions and historical-rule changes (e.g. Indiana pre-2006 no-DST, Russia post-2014 permanent UTC+3, US summer DST). As of v0.5.2, stored legacy country-code profiles first resolve the country/zone code to a representative IANA timezone, then use the same `computeRising` path as fresh city profiles. Legacy `getRisingSign(..., utcOffsetMinutes, ...)` is retained only as a low-level fixed-offset helper; both APIs are polar-safe.

Polar latitudes (|lat| > 66.5°, strictly past the polar circles) return `null` from both rising APIs. The horizon-plane geometry degenerates at the poles and a rising sign is not astrologically meaningful inside the polar circle. The UI surfaces "rising unavailable at this latitude" at city-selection time when |lat| > 66.5°. Boundary cases (|lat| == 66.5°) return a valid sign — the rule is strict greater-than.

Pre-1970 dates: `Intl.DateTimeFormat`'s historical-tz coverage falls back to LMT (Local Mean Time) approximation for some timezones in pre-tzdata-coverage periods. This is acknowledged as a precision tradeoff; the about-modal disclosure copy lands at v0.2.8 (calculator-framing rewrite cycle).

**v0.43 coherence note (B1).** The about-modal disclosure copy named above in the future tense ("lands at v0.2.8") shipped long ago — the calculator-framing about-modal copy is pinned on every push by `tests/payments_markup.test.js` (its about-modal "disclosure copy" suite). Read it as past-tense (shipped); the LMT-precision tradeoff it discloses is unchanged. Per L17 the original wording is preserved verbatim above.

The v0.14 wording (fixed UTC offsets per country entry; DST and historical-tz changes "out of scope") is retired by this amendment. The catalog-driver rule (rising surface-only, never entering `getCard` or `resolveBracket`) is unchanged.

**§1.B. Numerology surface — text triplet, calc reserved.**

As of v0.2.3 the result-card numerology surface renders as a three-number text line: life path, expression/name number, soul urge. Values are always space-separated for readability (e.g. `3 8 3`, `3 11 3`). **v0.23 amendment (CiC Fix A): the v0.22 hasMaster-branched concatenation (`383` for all-single-digit, spaced when any master present) is retired — the concatenated form read cognitively as a single three-digit number rather than three independent coordinates.** Codified by `tests/numerology_display.test.js`.

The additional calc fields personality (consonants only), birthday (day-of-month reduced with master preservation), and maturity (life-path sum plus expression/name-number sum, reduced with master preservation) remain on `buildProfile` as data-only — computed but not surfaced. They are reserved for v0.3.0+ paid surfacing per §1.

Catalog driver (§1) and rising semantics (§1.A) are unchanged. v0.2.3 reverts the v0.2.2 hexagon surface; this is breaking-on-render and additive-on-data. Calc-version remains v1.

**v0.43 coherence amendment (B2 + B3).** Two clauses above describe superseded states; per L17 the original wording is preserved verbatim and superseded here:
- **Numerology surface — now a name-derived pair, not a triplet (B2).** The "three-number text line (life path, expression/name number, soul urge)" describes the pre-v0.38 surface. §1.D v0.38 split `lifePath` (DOB-derived) onto the free surface, leaving the t1 numerology a name-derived **pair** (expression/name number + soul urge). #48 noted this under the §1.D *table*; §1.B itself is updated here. The always-space-separated invariant (CI-pinned by `tests/numerology_display.test.js`) is unchanged.
- **Personality / birthday / maturity — surfaced at t2, no longer "reserved" (B3).** Those three fields, described above as "reserved for v0.3.0+ paid surfacing," were surfaced at tier **t2** by §1.D v0.6.0 — the reservation's release condition fired and the clause is updated here to record it. They remain surface-only (no catalog/bracket effect) per §1.D.

**§1.C. Tarot birth card — surface coordinate, not a driver (v0.5.0).**

As of v0.5.0 the result card surfaces a tarot birth card: a Major Arcana coordinate computed from the DOB alone, rendered as roman numeral + lowercase name (e.g. `XXI · the world`). It is the **lead row** of the free card, above the five-element coordinate. Computed in `core/birthcard.js` (`getBirthCard`), carried as the additive `birthCard` field on `buildProfile`.

This is a **free-surface coordinate**, inside the §1 symbols-only invariant — a labeled coordinate (numeral + arcana name), never interpretation. No per-card meaning text, no per-arcana explanation; that register stays the paid card-content layer (§1 v0.22). The clause adds a coordinate to the visual surface in the same way §1.A's rising sign does — it does **not** enter `getCard`, `resolveBracket`, or any catalog computation; the catalog driver remains `(sunSign, animal)` per §1.

Reduction tradition is **digit-sum**: sum every digit of the DOB, reduce by repeated digit-sum until ≤ 22. This is distinct from the life-path reduction in `core/profile.js` (which stops at master numbers 11/22/33 and floors at a single digit); the birth-card reduction floors at ≤ 22 with no master-number stop, because the target range is the 22 Major Arcana. A reduced value of 22 maps to `0 · the fool`, guaranteeing every DOB resolves to a real arcana (the out-of-range `XXII` never renders). Both choices are operator-locked (see journal); codified by `tests/birthcard.test.js`.

Because it is a free coordinate, the birth card is included in the §5.D share surface — the shared PNG and the on-screen free card carry the same five coordinates. It carries no PII (numeral + arcana name only, no name/DOB) and no paid-content layer, so the §5.D invariants (a)/(b) hold unchanged.

**v0.36 amendment (v0.6.0).** The "same five coordinates" count in the paragraph above describes the v0.5.0 free surface and is preserved verbatim as lineage per L17. As of §1 v0.36 / §1.D the free card carries four coordinates and the shared PNG matches the card at the user's current render tier (§5.D v0.36). The birth card itself is unchanged by the tier ladder: it remains the lead coordinate of every tier's card, including free, and stays inside the §5.D share surface at every tier.

**§1.D. Tier ladder — coordinate density, surface-only (v0.6.0).**

As of v0.6.0 the paid surface is a three-rung tier ladder that reveals progressively more of the coordinate sheet. The ladder is operator-locked (2026-06-11):

| Tier | Param | Price | Card contents (cumulative) |
|---|---|---|---|
| **free** | — | $0 | birth card · sun (no rising) · public animal (year) · catalog numeral — 4 coordinates |
| **t1** | `?paid=t1` | $3 | + rising (when computable) · five-element · private animal (month) · numerology triplet (life path / expression / soul urge) |
| **t2** | `?paid=t2` | $6 | + personality · birthday · maturity · day pillar (animal + stem-element) |
| **t3** | `?paid=t3` | $9 | + hour pillar (animal + stem-element; four pillars complete) · the written 144-card entry (name/type/habit/bracket note) as ceiling bonus |

**Table currency (per L17 — the table above is operator-locked 2026-06-11 and preserved verbatim).** Two cells are superseded by later amendments: the **free** row is now **five** coordinates — `life path` joined free at v0.38 (the DOB-derived numerology split, amendment below) — and the **t1** `numerology triplet` is now the name-derived **pair** (expression/name number + soul urge), the life path having moved to free. Since v0.37 the card renders the full sheet at every tier as a compartment specimen sheet; sealed (higher-tier) cells carry no value.

Clauses, mirroring the §1.A / §1.B / §1.C surface-clause pattern:

- **Surface-only.** Tier coordinates add to the visual surface the way §1.A's rising sign does — none of them enters `getCard`, `resolveBracket`, or any catalog computation. The catalog driver stays `(sunSign, animal)` per §1; day and hour pillars never enter the catalog.
- **Stored tier.** `eight_ball_tier_v1` (§5 allow-list) records the highest rung purchased; absent = free. Monotonic: `applyPaidReturn` sets tier = max(current, purchased) by ladder order — never downgrades. An upgrade purchase raises the tier and adds credits like any paid return.
- **Density rule (single, total — v0.6.0 remediation R1).** Render density = f(stored tier, credit state), resolved at render time by one helper (`getRenderTier` in `ui/payments.js`, delegating to the pure `resolveRenderTier` in `core/payments.js`) — never a function of boot circumstance or shake action. Cold-boot rehydration, same-card re-shake, same-pair re-submit, free-try renders, paid reads, and paid-return boots all render the device's entitled tier. Free devices (no tier, no credits) render the free card.
- **Legacy grandfather (v0.6.0 remediation R2).** Credits present with no stored tier are the pre-v0.6.0 purchase shape — that product sold the written-entry unlock, which now lives at t3 — so the device resolves to t3 and the tier is persisted on first detection. Deterministic, total, never downgrades.
- **Credits unchanged.** Every paid return (any rung) grants +3 credits via the existing `applyPaidReturn` path; tries/credits semantics, pending-profile mechanic, and β-idempotent re-shake are unchanged from v0.22. Credits and the §4.B tries cap govern how many readings; the stored tier alone governs how much sheet each one shows.
- **t3 carries the card-content unlock.** The §1 v0.22 unlocked card-content layer relocates to t3 — the written entry is the ceiling bonus, not a per-credit unlock.
- **Rollback flag.** Free-tier composition is defined by the exported `TIER_COORDS` render constant in `ui/tiers.js`; reverting free to the pre-v0.6.0 surface is a one-line change plus a test update, not surgery.
- **Element systems not reconciled.** The free/t1 five-element stays the simplified year element (§3 calc v2); the day/hour stem-elements are NEW pillars labeled as such ("day pillar", "hour pillar") in the §2 clinical register. No contradiction surface, no §3 calc-version bump.

**v0.37 amendment (v0.7.0) — compartment render.** The card renders the full coordinate sheet at every tier. Coordinates above the device's render tier appear as sealed compartments: row, label, and cell structure visible; the coordinate value absent from the DOM — absent, not hidden. The free-surface invariant is unchanged in substance: the free render carries exactly four coordinate VALUES per the §1.D table; v0.7.0 adds the visible sealed structure of the remaining sheet. Sealed ≠ unresolvable: a paywalled field renders the seal treatment; a field that cannot resolve from given inputs (rising without birth time/place; hour pillar without birth time) renders the `—` empty field at any entitled tier. The written 144-card entry remains a t3-only unlock; its sealed block carries no entry text at any lower tier.

**v0.38 amendment (v0.7.x) — free life-path split (override #3, controller-authorized 2026-06-12).** The §1.D numerology coordinate splits by derivation. `lifePath` (DOB-derived) joins the free surface — preserving the free = DOB-only invariant (every free coordinate derives from date of birth alone, no name input) — so the free render carries **five** coordinate VALUES: birth card · sun (bare) · public animal (year) · life path · catalog numeral. The name-derived numerology narrows to the inner **pair** (expression/name number and soul urge) and stays at t1; the t1 "numerology triplet" in the table above is superseded and preserved as lineage per L17 — it is now a pair, the life path having moved to free. Surface-only and catalog-isolated like every §1.D coordinate: life path continues to drive low/mid/high bracket resolution within a catalog cell per §1, never the catalog index `(sunSign, animal)`; `getCard` is unchanged. §5.D: life path being free, the free share-PNG and the on-screen free card carry the same five coordinates — the PNG now includes the life-path numeral. It is a DOB-derived number carrying no name/DOB string, so the §5.D PII invariants (labeled numeral/symbol only; never name or DOB) hold unchanged, the same basis as the birth card and the catalog numeral. Free composition stays the one-line `TIER_COORDS` rollback flag (`ui/tiers.js`): `FREE_COORDS` gains `lifePath`; `CELL_COORD` maps `lifePath → lifePath` (free) while `nameNumber` / `soulUrge → numerology` (t1).

**§1.E. Provenance placards — labeled derivation note, surface-only (Coordinate Legibility Pack, v0.40).** Each coordinate may carry a **provenance placard**: a labeled, surface-only note naming the DERIVATION of that coordinate — its method/source (e.g. `digit-sum reduction`, `tropical zodiac`, `ascendant`, `solar term`, `letter-value sum`) — rendered per row in the row's cell order, mirroring the `.coord-title` grammar. The placard is **clinical derivation only, never interpretation** (§2): it names HOW a coordinate is computed, never what it "means." It is **tier-invariant** (a coordinate's derivation does not change with seal state) and **catalog-isolated** like every §1.x surface coordinate (§1 / §1.D): the note is keyed to the coordinate, never to a profile value or the catalog driver `(sunSign, animal)`; `getCard` is unchanged. It adds **no coordinate VALUE** to any surface — the free-surface VALUE count is unchanged (still five per §1.D v0.38) — and carries **no PII**: it names a derivation method/source, never a name, DOB, or derived value. A **sealed** compartment's placard still names the coordinate's derivation (structure/label only, the value absent per the v0.37 compartment contract) — so a hatched cell reads as a real, sourced coordinate withheld at the user's tier, not a rendering failure (the F1-WATCH legibility fix). Visibility reuses the existing symbol-label toggle (`.card.labels-revealed`) — off by default, **no new localStorage key**. The placard is **NOT serialized into the §5.D share artifact**: the share path reads `.coord-val` + `.coord-title` (via `ui/tiers.js shareRowRefs`, which `ui/share.js` consumes as a snapshot), never `.coord-prov`, so the PNG is unchanged. Implementation: `ui/tiers.js` `PROV_NOTE` (coordinate → derivation note) + `provText` (pure, per-row join) + an init-time `.coord-prov` write pass; `index.html` carries the `.coord-prov` CSS gate only.

**§1.F. Legibility surfaces — atlas legend + density census, labeled, surface-only (Coordinate Legibility Pack cuts 2–3, v0.41).** Two further legibility surfaces extend the §1.E pattern; this clause grants them the literal coverage flagged at the cut-2 Codex audit (Hook 7). Both are **surface-only** and **catalog-isolated** (keyed to coordinates or the tier constants, never a profile value or the catalog driver `(sunSign, animal)`; `getCard` unchanged), add **no coordinate VALUE** and **no PII**, and stay in the §2 clinical register (a label/census, never interpretation, never a CTA). **(a) Atlas** — a per-coordinate system-name legend (e.g. `life-path number`, `soul-urge number`) that disambiguates the abbreviated multi-cell row titles; gated by the existing `.card.labels-revealed` toggle (off by default, **no new localStorage key**), rendered in `.coord-atlas` outside `.coord-title` so it is **not serialized into the §5.D PNG**. **(b) Density census** — an **always-on** aggregate count in the result rail, OUTSIDE `#card-face` (so off the §5.D PNG): `N of 15 coordinates open · M sealed at paid tiers` (the 14 sheet cells plus the always-open catalog numeral — base 15, and the free count 5, match the §1.D free-coordinate framing), computed purely from `TIER_COORDS` + `CELL_COORD` via the pure `tierDensitySummary` — never from a profile. It is the on-screen counted form of the §5.D v0.39 `sealed remainder` marker, with **no FOMO/urgency/price** (§2 v0.35 / §5.C). Both retire the F1-WATCH "hatch reads as broken" risk — atlas at the per-cell level, density at the whole-sheet level. §1.E remains the provenance clause; §1.F covers the atlas + density siblings.

## §2. What this is NOT

**Currently enforced (CI gates, see §7):**

- Not a "spiritual" product. The voice is declarative-observational and materialistic, not mystical or guidance-oriented. Banned-voice-register scan (`BANNED_VOICE_REGISTER` in `tests/profile.test.js`) ran on `cards.v1.js` content in v0.1.x. As of v0.3.0 (v0.22 amendment to §4 + §7 stage 1 extension) the scan runs against the tracked deck at `content/cards.v1.full.js` in CI; the private authoring pipeline at `~/dev/8ball-private/` continues to pre-scan before each release-prep `cp` to the public path.
- Not a SIRR product. Never references SIRR. Never imports from SIRR. The two repos are siblings, not parent/child. (See §9.) Enforced by `tests/pii_scan.test.js`.
- Not an analytics product. No tracking, no telemetry, no third-party scripts. (First-party server-side request logs — the host's own aggregate traffic analytics — are not "telemetry" under this rule: the product injects no client, makes no call, emits no beacon. See §5.) Network behavior is scope-restricted per §5: only same-origin lazy loads of static assets (e.g. `assets/cities.json`) and §5.B user-initiated calls (Netlify Forms feedback POST + Gumroad Buy Link redirect) are permitted. `fetch()`, `XMLHttpRequest`, and `navigator.sendBeacon` remain forbidden in tracked source. Enforced by `tests/privacy_scan.test.js`. The page uses system fonts only — no Google Fonts, no third-party CSS.

**Review-discipline (no current code surface to scan; rule fires when a feature is proposed):**

- Not a horoscope app. Not a numerology calculator. Not a guidance product. (Voice register; partly captured by the banned-list above.)
- Not a logged-in product. No accounts, no signup, no "save your reading."
- **Honest invitation permitted; manipulation forbidden (v0.35).** 8ball may be shared, posted, linked, and discussed by the operator as a person, and may carry a soft contextual invitation (a plain link, a "here's the thing"). Forbidden is manipulative/coercive distribution: dark patterns, engagement-bait, false urgency, undisclosed funneling, spam, extractive growth-hacking. Participating as a human in a community is not a funnel. The anti-funnel value protects against manipulation, not against being seen. This clause is doctrine; prior "not a funnel / no CTA / no link in bio" language that lived only in operating/session plans (never in this file) is superseded by it.
- **Arcade-toy carve-out (v0.22, v0.3.0).** Paid surface is one-time purchase, fixed-credit. Three dollars buys three reads with depth content unlocked. Tier ladders (v0.3.1+: $6 / 6 tries at depth+hexagon; $9 / 9 tries at depth+hexagon+four) extend the model without changing its shape. Not a subscription. Device-stored credit counter preserves the no-account constraint. The arcade frame is load-bearing — any future amendment that breaks `dollars == tries` or introduces recurring billing requires explicit re-amendment.
- **Arcade-toy carve-out re-amendment (v0.36, v0.6.0).** This is the explicit re-amendment the v0.22 clause requires. The shipped tier ladder (§1.D) is $3 / $6 / $9 rungs, each a one-time purchase granting **+3 reads at that rung's coordinate density** — `dollars == tries` is superseded: every rung buys exactly three reads; dollars buy density, not try count. The v0.22 contemplated ladder ($6 / 6 tries; $9 / 9 tries) is preserved above as lineage and never shipped. Everything else holds unchanged: one-time purchase, fixed-credit, no subscription, no recurring billing, device-stored counter, no account.

## §3. The calculation contract

`core/profile.js` is the calculation core. Changes fall into two categories:

**Breaking changes** — modify existing outputs (sunSign, animal, lifePath, nameNumber, chineseElement, soulUrge for any input). The contract:

1. Add or update fixtures in `tests/fixtures.json` reflecting the intended new behavior.
2. Update the algorithm.
3. Run `npm test`. All cases must pass.
4. Bump the calc-version note at the bottom of this file.

**Additive changes** — add new outputs without modifying existing ones (e.g. new functions exported, new fields on the `buildProfile` return value). The contract:

1. Add direct unit tests for the new outputs in `tests/profile.test.js`.
2. Run `npm test`. All cases must pass, including the new tests.
3. Existing `tests/fixtures.json` cases must remain byte-identical (no silent drift).
4. Calc-version stays the same major (additive doesn't break v1 consumers).

Algorithm versions documented:

- **calc v2** (v0.2.7.1) — Real lunar new year table for year-pillar cusps (year-pillar animal + 5-element pillar). Real solar-term table for month-pillar (`innerAnimal`). Both tables span 1900–2100, evaluated at date-precision in canonical Asia/Shanghai timezone (UTC+8); DOB given as `YYYY-MM-DD` is treated as the truth — no timezone conversion of user input. Lunar new year dates and the 12 month-start solar-term dates are computed in JS via Meeus astronomical algorithms (Chapter 25 sun position, Chapter 49 lunar phases with periodic + planetary corrections), mirroring the `core/rising.js` Meeus pattern; results match Hong Kong Observatory canonical tables byte-exact at the sanity-lock checkpoints in `tests/profile.test.js`. Zero runtime deps, zero devDeps, zero build step. The v1 Feb-4 fixed-cutoff approximation is retired. **`getInnerAnimal` signature changed from `(month, day)` to `(year, month, day)`** as a breaking calc change. All existing fixtures other than the v1-bug-locking fixture #6 (CNY Feb 4 cutoff) remain byte-identical; fixture #6 is intentionally re-spec'd because it was locking the bug being fixed.
- **calc v1** (initial through v0.2.7) — Pythagorean life path with master numbers 11/22/33 preserved at the final reduce. Western tropical sun signs at standard cusps. Chinese zodiac with Feb 4 cutoff approximation (retired in calc v2). Additively extended at v0.2.0 with: chinese five-element (2-year cycle, 1924 anchor), public/private animal split (year-pillar already present at `animal`; month-pillar added as `innerAnimal` via solar-term cutoff approximations), soul urge (vowel-only Pythagorean, masters preserved), and unreduced sums (lifePathSum, nameNumberSum, soulUrgeSum) exported for potential future surfacing. Additively extended at v0.2.1 with optional rising sign (surface-only ascendant) when birth time + country + lat/lng are present. All additions covered by direct unit tests in `tests/profile.test.js` and `tests/rising.test.js`; existing fixtures unchanged.

If a fixture changes silently, the test gate catches it. If a test changes silently, code review catches it. If both change in the same commit without a doctrine note, the reviewer rejects.

## §4. Content rules — copy & traits

- **No slurs.** Banned-pattern check runs in `tests/profile.test.js`.
- **No medical/diagnostic framing.** Cards do not adopt clinical or diagnostic vocabulary, including ironic adoption (e.g., "diagnosis," "syndrome," "disorder").
- **Cultural-symbol respect.** If the deck draws from any tradition (tarot, I Ching, runes, zodiac, etc.), cards respect that tradition's lineage. No syncretic flattening, no caricature of source traditions.
- **No targeting minors.** Copy assumes adult user. UI does not pander to children. As of v0.2.6, an 18+ acknowledgment gate is required before name/DOB entry — see §4.A.
- **No real-person targets.** No "you're like [public figure]" lines.
- **Universal floor.** Cards should land equally on a person who picked their own DOB.
- **No card-content strings in tracked files.** As of v0.2.0 the card content (cell `name`, `type`, `habit`, `note` bodies) lives privately at `~/dev/8ball-private/cards.v1.full.js`. No public-repo tracked file (source, tests, fixtures, `journal.md`, audit notes, this doctrine) may contain a card-content string. Audit dispositions and content-batch logs reference cells by coordinate path (e.g. `aries.rabbit.note.mid`); before/after strings are stored with the private deck. Forward-looking from doctrine v0.11; pre-v0.11 trait-pool / template-pool excerpts in journal entries (retired `traits.v1.js` / `templates.v1.js` system, deleted from repo in 2F-2) are out of scope. **v0.22 amendment (v0.3.0):** the rule is scoped, not retired — exactly one tracked file, `content/cards.v1.full.js`, is permitted to contain card-content strings (the 144-card content layer: `name`, `type`, `habit`, `note` × `{low, mid, high}` brackets). All other tracked files remain prohibited, including source modules in `core/`, all `tests/`, `tests/fixtures.json`, `journal.md`, audit notes under `audits/`, `DOCTRINE.md`, `8BALL.md`, `README.md`. Audit dispositions continue to reference cells by coordinate path; the carve-out covers the deck file alone, not its discussion in tracked text. Voice-register enforcement (`BANNED_VOICE_REGISTER` scan in `tests/profile.test.js`) now runs against `content/cards.v1.full.js`. The private location at `~/dev/8ball-private/cards.v1.full.js` is retained as the authoring source; `content/cards.v1.full.js` is a verbatim copy maintained via `cp` at release-prep time.
- **Versioned, not edited.** Shipped content batches are immutable. New release = new file (e.g. `traits.v2.js`, `cards.v2.js`). Diff lives in `journal.md`.

**§4.A — 18+ acknowledgment gate.**

As of v0.2.6 the product gates entry to the onboarding form behind a one-time 18+ acknowledgment. Mechanics:

- On first load, a modal blocks the onboarding form. Single button: "confirm." Tapping it is the acknowledgment.
- Acknowledgment persists via `eight_ball_age_ack_v1` localStorage flag (string `'true'` on confirm). On subsequent visits the gate is skipped.
- Acknowledgment is a click-through, not verification. Copy must be explicit about that — no implication of legal age-gating, no health-product warning theatre, no false claim of identity verification.
- Disclosure: the about-modal mentions the gate's existence and shape so the user knows what the storage flag is for.

If a future feature needs harder age verification (paid tier, adult-content layer, regulatory requirement), that requires a §4.A amendment and likely a §5 amendment for whatever data-collection surface the verification introduces.

**§4.B — Three-free-tries cap (v0.22, v0.3.0).**

Three free tries total per device. First three unique `(name, dob)` pairs render the locked card (symbols visible, depth bars dimmed, lock icon top-left — Pattern C surface per `~/Desktop/8ball/sessions/v03_pattern_c_locked_vs_unlocked.html`). The fourth new pair triggers the paywall modal. The cap is monotonic and per-device — wiping localStorage resets it. Paying does not reset the cap; paying adds paid credits separately (see §5 `eight_ball_credits_v1`). The about-modal discloses the cap exists ("first three readings are free. after that, three dollars buys three more reads…"). The localStorage-wipe reset is intentionally not advertised in user-facing copy — disclosing the cap shape is the load-bearing disclosure; advertising the bypass is not.

The cap exists to admit the offer plainly: this is a paid toy, not an ad-supported product, not a free-trial-with-aggressive-upsell. Three free tries is the demo.

**v0.36 amendment (v0.6.0):** the paywall is a three-rung ladder (t1 · $3 / t2 · $6 / t3 · $9 per §1.D) — three plain `<a href>` CTAs to the three Gumroad products (§5.B Call 2 v0.36). The three-free-tries cap is unchanged; every render — free tries included — shows the device's entitled density per the §1.D density rule (a device with no tier and no credits renders the free card). t3 carries the card-content unlock — the §1 v0.22 paid card-content layer relocates to t3. Paying still does not reset the cap; every rung adds +3 paid credits (`eight_ball_credits_v1`) and raises the stored tier monotonically (`eight_ball_tier_v1`, §5).

If a line lands but you can't tell whether it crosses any of the above, it crosses. Cut it.

**Safety-patch carve-out.** Locked-decision #9 (immutable v1 pools) protects against silent flavor drift. It does NOT protect a doctrine-rule violation caught post-ship. If a shipped trait, template, or card line violates §4, it is cut in a patch release; the journal records the cut and the diff carries the doctrine note. Immutability is for taste discipline, not error preservation.

## §5. Privacy primitive

The product persists only user-entered profile fields, in `localStorage` only, on the user's own device:

- `eight_ball_profile_v1` — JSON-stringified profile object containing the user-entered fields below. Persists the profile payload across loads; written by the onboarding submit handler and by `applyPaidReturn()` when consuming `eight_ball_pending_profile_v1` on a paid round-trip.
- `name` (string)
- `dob` (ISO date YYYY-MM-DD)
- optional `time` (HH:MM string, 24-hour)
- optional `country` (country/zone entry code from `core/countries.js`)
- optional `lat` (decimal latitude)
- optional `lng` (decimal longitude)
- optional `city` (city display name from `assets/cities.json` — user's birthplace selection. Added v0.2.7.2.)
- optional `cc` (ISO 3166-1 alpha-2 country code, paired with `city` for display rehydration. Added v0.2.7.2.)
- optional `tz` (IANA timezone string — drives DST-aware rising-sign computation per §1.A v0.21. Added v0.2.7.2.)
- `eight_ball_age_ack_v1` — separate boolean flag (string `'true'`) set when the user confirms the 18+ acknowledgment gate per §4.A. Independent of the profile payload.
- `eight_ball_labels_revealed_v1` — separate string flag (`'true'` or `'false'`) tracking whether the user has chosen to reveal the symbol-name labels on the result card. UI preference only; independent of the profile payload. Added v0.2.7.
- `eight_ball_tries_used_v1` — integer (serialized as string). Monotonic counter incrementing on every new-pair shake regardless of paid state. Read against the constant `3` for the §4.B cap check. Added v0.3.0.
- `eight_ball_credits_v1` — integer (serialized as string). `+= 3` on each `?paid=t1` handler invocation; decrements by 1 on each new-pair shake when `> 0`. Hits zero → next new-pair triggers paywall or locked render depending on `tries_used`. Non-negative invariant. Added v0.3.0.
- `eight_ball_pending_profile_v1` — JSON-stringified profile object, transient. Written when the paywall modal opens (both Path A new-pair shake and Path B lock-tap on already-rendered card per §5.C). Consumed on the `?paid=t1` return: one credit decremented, profile saved to `eight_ball_profile_v1`, key deleted. Lifetime is one round-trip; either absent or present-and-valid. Added v0.3.0.
- `eight_ball_tier_v1` — string tier flag (`'t1' | 't2' | 't3'`), the highest rung purchased per §1.D; absent = free. Monotonic — written only by the `?paid=t1|t2|t3` return handler as max(current, purchased) by ladder rank, plus the one-time §1.D legacy-grandfather write (credits with no tier key persist as `'t3'` on first render); never downgrades. UI preference-free, profile-free: it stores which density the device has bought, nothing about the person. Added v0.6.0 (§1.D / §4.B v0.36).

**v0.36 amendment (v0.6.0, allow-list semantics).** The `?paid=t1` references in the `eight_ball_credits_v1` and `eight_ball_pending_profile_v1` bullets above are preserved verbatim from their v0.3.0 wording per L17. As of v0.6.0 the same semantics apply to every `?paid=t1|t2|t3` return: +3 credits on any rung, identical pending-profile round-trip consumption (§1.D / §4.B / §5.B Call 2 v0.36). No other change to either key's behavior.

Nothing else. No derived profile is stored — it's recomputed on each load. No analytics. No third-party endpoints. No third-party scripts. System fonts only.

**Network behavior is scope-restricted, not absolute** (v0.21 amendment). The product makes no telemetry calls, no third-party calls, no fingerprinting calls, no out-of-band data flow.

**First-party server-side observability is not telemetry** (v0.35 clarification). Passive aggregate request logs generated by the hosting platform (e.g. Netlify's own server-side analytics) are server-side, carry no client-injected tracker, no fingerprint, no third-party script, and no identifier the product emits. Reading the operator's own origin logs is permitted. The ban is on out-of-band / third-party data flow the product *creates* — not on the operator observing first-party traffic to their own site. This clarification adds no code surface and changes no scanner: `tests/privacy_scan.test.js` still forbids `fetch()`/`XMLHttpRequest`/`navigator.sendBeacon`/third-party scripts in tracked source, because first-party server logs are produced by the host platform, never by tracked source.

The following same-origin asset loads are permitted:

- **Initial page load.** HTML and ES modules served from the deployed origin, plus same-origin static assets (favicons, og:image).
- **Same-origin lazy loads.** Dynamic ES `import()` of same-origin JSON or modules. As of v0.2.7.2, `assets/cities.json` is lazy-loaded the first time the user opens the rising-sign `<details>` element. Lazy loads carry no user state — they fetch a static asset whose bytes are identical for every visitor.
- **§5.B user-initiated network calls.** Two outbound network calls, both user-initiated (see §5.B): (1) feedback form POST to Netlify Forms, (2) Gumroad Buy Link redirect for paid-tier unlock. Added v0.3.0 (LS path); swapped to Gumroad at v0.3.0.3.

`fetch()`, `XMLHttpRequest`, and `navigator.sendBeacon` remain forbidden in tracked source under `core/`, `content/`, and `index.html` — enforced by `tests/privacy_scan.test.js`. Dynamic `import()` with import attributes (`with: { type: 'json' }`) is the permitted same-origin lazy-load mechanism for static JSON; it does not trip the `fetch(` ban because it is an ES module-loader call, not an XHR.

The v0.14 wording "zero network requests after page load" is retired by this amendment. The intent — no third-party traffic, no telemetry, no out-of-band data flow — is preserved verbatim.

If a future feature requires storing or transmitting more, that feature requires a doctrine amendment to §5 and a privacy-policy update before merge.

## §5.B — User-initiated network calls (feedback v0.2.5, LS redirect v0.3.0 retired, Gumroad redirect v0.3.0.3)

The site permits exactly two user-initiated network calls:

1. **Feedback POST** — submission of a feedback form to the Netlify Forms endpoint at the same origin (added v0.2.5).
2. **Gumroad Buy Link redirect** — navigation away to the operator's hosted Gumroad checkout when the user taps the paywall CTA (added v0.3.0 as Lemon Squeezy path; swapped to Gumroad at v0.3.0.3).

Both calls are out-of-band — neither echoes user profile state. These are the only network calls permitted in the product runtime; everything else in §5 stands.

### Call 1 — Feedback POST

Constraints:

- **User-initiated only.** The submission fires only on user click of the explicit submit button. No timer, no auto-fire, no submission on page load, on shake, on result-render, or on any other implicit trigger.
- **User-authored content only.** The submitted payload contains exactly two fields: a free-text `message` and an optional free-text `contact`, both typed by the user inside the form. No localStorage profile data (`name`, `dob`, `time`, `country`, `lat`, `lng`, `city`, `cc`, `tz`) is included. No derived coordinates. No name, no DOB, no IP enrichment from the page, no analytics tags, no UTM params, no fingerprint. The form has no hidden field carrying user-data state.
- **Single named endpoint.** The `<form>` action is the same-origin Netlify Forms handler — either no `action` attribute (Netlify intercepts the POST and serves its branded thanks page) or `action="/?sent=1"` for a same-origin success redirect that the v0.2.5.2 banner JS detects and swaps to the in-page "thanks. read." state, then `replaceState` strips the query. The v0.2.5.2 success redirect is the shipped form (`tests/feedback_surface.test.js` locks `action="/?sent=1"`). No third-party form provider, no webhook fan-out, no secondary destination.
- **Native form post, no JavaScript fetch.** The form uses HTML `<form method="POST" data-netlify="true">` semantics. No `fetch()`, no `XMLHttpRequest`, no `navigator.sendBeacon`. The privacy scan (`tests/privacy_scan.test.js`) is unchanged and remains the enforcement.
- **Fail-silent.** On submission failure (offline, provider outage, network error), the browser handles the error natively. No retry queue, no remote error reporting, no telemetry of failure. If the user's browser is offline, the submit fails and the user can decide what to do.
- **Submission visibility.** Submissions land in the Netlify dashboard under the Forms tab. The operator may configure email notifications per Netlify defaults. Submissions are not echoed back to the page or persisted in localStorage.
- **No retention by 8ball.** 8ball-the-codebase does not retain submissions. Netlify-the-platform does (for as long as operator keeps them in the dashboard); that is operator's responsibility, not 8ball's runtime concern.

The about-modal copy is updated to disclose this surface honestly. Disclosure is the load-bearing piece: anyone reading the modal must learn that a feedback form exists and what happens when they submit it.

### Call 2 — Gumroad Buy Link redirect (v0.3.0 LS path → v0.3.0.3 Gumroad path)

User taps `unlock · $3` on the paywall modal → browser navigates to `https://theeightball.gumroad.com/l/rzqezp` (hosted by Gumroad, which acts as Merchant of Record). Unlike the v0.3.0 Lemon Squeezy path, Gumroad does not accept a URL-encoded `success_url` query parameter on the Buy Link href; post-purchase redirect to `/?paid=t1` is handled entirely on Gumroad's side via the product's Content-tab "→ unlock your reads" Button (configured 2026-05-16 chat-27 directive F) which links to `https://the-eight-ball.netlify.app/?paid=t1`. The redirect mechanism is therefore single-source (Gumroad-side Content Button), not the belt-and-suspenders pattern of the prior LS path. Two-click post-purchase flow: paywall tap → Gumroad hosted checkout → Content-tab Button tap → `/?paid=t1`.

Gumroad processes payment via Stripe (Gumroad's internal Stripe Connect integration; operator's verified Connect account cleared 2026-05-16 per `~/Desktop/8ball/audits/gumroad_post_confirm_recheck_2026-05-16.md`). PayPal optional at buyer choice per Gumroad's default checkout. On successful payment, Gumroad displays the product Content page with the "→ unlock your reads" Button; on Button-tap, browser navigates to `https://the-eight-ball.netlify.app/?paid=t1`. The page-load JS handler at the same `/?paid=t1` query parser calls `applyPaidReturn()` from `core/payments.js` to credit the device (`+= 3`) and — if `eight_ball_pending_profile_v1` is present — consume it (decrement one credit, save the profile, render unlocked), then `replaceState({}, '', window.location.pathname)` strips the query for a clean URL.

Constraints:

- **User-initiated only.** Tap of the explicit paywall CTA. No timer, no auto-fire, no redirect on page load, on shake, on result-render, or on any other implicit trigger.
- **Hosted redirect, not in-page overlay.** Plain `<a href>` navigation. The Gumroad overlay/embed script is permanently rejected per §5's third-party-script ban — same constraint as the prior LS path, operator decision logged in the v0.3.0.3 cycle commit.
- **No user data leaves on the redirect.** The Buy Link URL is static — no profile fields encoded, no UTM tags, no tracking identifiers appended. The bare URL contains no query parameters at all (distinct from the v0.3.0 LS path which carried URL-encoded `checkout[success_url]`); `tests/payments_markup.test.js` v0.3.0.3 assertions lock this shape.
- **No SDK, no `fetch`, no webhook.** 8ball-the-codebase never talks to Gumroad programmatically; the redirect IS the entire payment surface.
- **Two-click post-purchase return (Gumroad-specific).** The post-purchase flow is two clicks (paywall → Gumroad checkout → Content-tab Button → `/?paid=t1`), structurally different from the LS path's single-click `checkout[success_url]` redirect. The Content-tab Button text is operator-controlled and ships as "→ unlock your reads" per chat-27 directive F.
- **Trust-based return.** `/?paid=t1` is unsigned. Manual URL entry grants credits without payment (see §5.C). Acceptable for the arcade-toy model and disclosed in the about-modal.
- **No customer object retained.** Gumroad captures buyer email for its own receipt and tax compliance; 8ball never sees it. No localStorage entry for purchase metadata beyond `eight_ball_credits_v1` count.
- **Disclosure.** The about-modal names Gumroad as the payment processor and the data-flow boundary (payment + email to Gumroad; reading stays on-device). The paywall modal carries a compact `.modal-disclosure` line mirroring the same disclosure.

**v0.36 amendment (v0.6.0, tier ladder).** Call 2 generalizes to **three Gumroad products — one per rung**, same mechanism per product: t1 · $3 `https://theeightball.gumroad.com/l/rzqezp` → Content-tab Button → `/?paid=t1`; t2 · $6 `https://theeightball.gumroad.com/l/neysyv` → `/?paid=t2`; t3 · $9 `https://theeightball.gumroad.com/l/xjpvp` → `/?paid=t3`. Each paywall-ladder CTA is a plain bare-href Buy Link (no query parameters, no UTM, no tracking identifiers — `tests/payments_markup.test.js` locks all three shapes); the post-purchase redirect stays single-source on Gumroad's side via each product's Content-tab Button. The page-load handler is generalized to `?paid=t1|t2|t3`: `applyPaidReturn` credits the device +3 and raises the stored tier to max(current, purchased) per §1.D; unknown `?paid=` values are ignored (replay-safe branch preserved). All Call 2 constraints — user-initiated only, hosted redirect not overlay, no user data on redirect, no SDK/fetch/webhook, trust-based return, no customer object retained, disclosure — apply to each product unchanged. This remains one mechanism with three product instances; the §5.B "exactly two user-initiated network calls" enumeration is unchanged in kind (feedback POST + Gumroad Buy Link redirect).

If a future feature would require a third endpoint, additional fields, or any non-user-initiated network behavior, that requires a further §5 amendment.

## §5.C — Content-delivery transparency (added v0.3.0)

The card-content layer (`content/cards.v1.full.js` — `name`, `type`, `habit`, `note` × 144 cards) ships in the public bundle. JS visibility is gated by the `eight_ball_credits_v1` flag; the data itself is inspectable via View Source, and `/?paid=t1` is not signed — manual URL entry will grant `+= 3` credits without payment. **The lock is a UI convention; the deck is in the source — we trust adults to tip when they enjoyed the toy.**

Backend-gated delivery (Netlify Functions + Stripe webhook verification, or any processor's server-validated license-key issuance) is doctrinally permitted as a future amendment but not built in v0.3.0. The about-modal discloses the bundle shape so payment is informed-tip, not deception. The disclosure line "the deck is visible in source; the lock is a convention, not a vault" is the load-bearing piece — users tip the operator for the toy, not for content scarcity.

## §5.D — Share surface (added v0.4.0)

The result surface MAY offer a user-initiated share affordance that renders the free card to a raster image (PNG) entirely on-device (SVG → canvas → `toBlob`; no network). Sharing uses the Web Share API where available (`navigator.share` with the image file); otherwise it falls back to a local file download plus a clipboard copy of the bare production URL. Invariants:

- **(a)** the shared artifact contains only the free coordinate symbols, their section labels, the catalog number, the 8ball wordmark, and the bare production URL — never the paid card-content layer (name/type/habit/note), never name or DOB;
- **(b)** no per-result link or query parameter encodes any profile field;
- **(c)** no telemetry, no share counting, and no network call of any kind is introduced — §5 same-origin/no-beacon and §7 no-telemetry are preserved;
- **(d)** the affordance is offered on the free surface to maximize organic reach.

The image builder reads only the rendered free coordinate symbol nodes (and their section titles + catalog number) from the live DOM; it never reads a profile object or the unlocked card-content slots, which is how invariants (a)/(b) are mechanically guaranteed. DOM-touching code lives in `ui/share.js` (`initShareUI({refs}, {hooks})` per §6), not in `index.html`. This clause is the privacy review contemplated by the §12 "until a privacy review explicitly clears it" wording.

**v0.36 amendment (v0.6.0, tier-aware share).** The share affordance renders the card **at the user's current render tier** — paid users emit denser PNGs. Invariant (a)'s "free coordinate symbols" generalizes to "the rendered coordinate symbols at the current tier"; every other protection in (a)–(d) is preserved verbatim and applies at every tier: labeled coordinate symbols + section labels + catalog number + 8ball wordmark + bare production URL only; never the written card-content layer (name/type/habit/note); never name or DOB; no per-result link or query parameter encoding any profile field; no telemetry, no share counting, no network call of any kind; affordance offered on the free surface (and every paid render). Mechanically the builder still reads only rendered coordinate symbol nodes from the live DOM and now skips tier-hidden rows (the `ui/tiers.js` gating sets them hidden); it still never reads a profile object or the card-content slots.

**v0.39 amendment (reach pass v1 — full-sheet share artifact).** The share artifact renders the FULL specimen sheet at every tier: the rendered coordinate values at the user's tier PLUS the SEALED STRUCTURE of the higher-tier compartments — section label + seal hatch, the coordinate VALUE absent. Rendering is **per compartment, not per row**: a mixed row (e.g. `SUN · RISING` on the free card) surfaces its open value beside its sealed compartment(s); the free PNG shows all 14 compartments (open values + seal hatches), mirroring the on-card v0.37 compartment sheet. This resolves the v0.37 logged deferral ("sealed-structure-in-PNG is a logged journal deferral; `ui/share.js` UNTOUCHED"). Every invariant (a)–(d) is preserved verbatim: a sealed coordinate's VALUE never appears in the artifact; never the written card-content layer (name/type/habit/note); never name or DOB; no per-result link or query parameter encodes any profile field; no telemetry, no share counting, no network call of any kind; the affordance is offered on the free surface. Invariant (a)'s "rendered coordinate symbols at the current tier" generalizes to "the rendered coordinate values at the current tier PLUS the seal-hatch structure (section label only, value absent) of the tier-hidden compartments." The mechanical guarantee is unchanged and is what makes the added structure safe: the builder reads ONLY the live DOM, where a sealed cell renders `textContent === ''` (the v0.37 compartment contract — absent, not hidden), so the sealed VALUE does not exist in the snapshot the builder serializes; `ui/tiers.js shareRowRefs` exposes a per-cell `{state, value}` snapshot derived from that same live cell state, never from a profile object or a tier constant (the prior tier-hidden row filter in `ui/share.js` is removed). NEW — share caption: the artifact MAY be accompanied by a clinical caption (the catalog numeral + the open coordinate VALUES + a `sealed remainder` marker + the bare production URL) carried in `navigator.share`'s text field where supported, and copied to the clipboard on the desktop fallback in place of the prior bare-URL-only copy. The caption is built from the SAME DOM snapshot and is bound by every (a)/(b) invariant — no name/DOB, no profile read, no per-result parameter, no sealed coordinate value — and stays in the §2 clinical register (no sales language). Sibling tracked-file changes co-versioned: `ui/share.js` (full-sheet builder — sealed cells draw an SVG `<pattern>` hatch + label, value absent; the per-cell `sealed` state replaces the tier-hidden row filter; `buildCaptionFromSnapshot` + caption coupled into the share flow), `ui/tiers.js` (`shareRowRefs` per-cell `{state, value}` snapshot, DOM-derived; the sealed value is never passed), `index.html` (one-line cold-landing `#mechanism-strip` near the registry header — clinical, no pricing/CTA; share-wiring comment updated; 1499/1500 held by offsetting compaction), `tests/share_surface.test.js` (full-sheet eight-row render, sealed = hatch + label + no value, aggregate paid-value-absence sentinel, F4 unresolved-vs-sealed in the PNG, caption PII/voice, share-flow caption coupling, mechanism-strip placement/no-CTA). Ships within the 0.7.x line — `package.json` deliberately not bumped (flagged for Procedure 4 version-truth review, as in v0.38). No calc change, no `content/` deck touch, no `core/` touch, no new dependency, no new localStorage key, no network call introduced — `privacy_scan` FORBIDDEN list + `LOCALSTORAGE_KEY_ALLOW` unchanged; `pii_scan` DOCTRINE_ALLOW unchanged. Trigger per §13: reach pass — the verified reach bottleneck (the 06-15 ship-gate read; orchestrator dissent on record since v0.38). Codex pre-build audit cleared this scope 2026-06-29 (P1 hook H2 → this amendment; disposition `~/Desktop/8ball/audits/codex_prebuild_reach_pass_v1_2026-06-29_response.md`); brief `~/Desktop/8ball/sessions/brief_reach_pass_v1_2026-06-29.md`.

## §6. Architecture

- Single repo, private on GitHub as of v0.2.0 (was public through v0.1.4; flipped private to protect the future paid card-content layer at `~/dev/8ball-private/`).
- Static site. Deployed on Netlify free tier from `main` branch.
- ES modules in the browser. No build step.
- Four-folder source layout: `core/` (pure-logic ES modules — **9** as of v0.41), `ui/` (DOM-touching ES modules with `init*UI({refs}, {hooks})` DI shape per the v0.23 amendment below — **6** as of v0.41), `content/` (data — `cards.v1.full.js` 144-card deck added v0.3.0 per §1/§4 amendments), `tests/` (gates — **24** test files + `fixtures.json` as of v0.41). This clause pins the layout *shape*, not the file list; the canonical module/test inventory lives in `CLAUDE.md` (with `README.md`), so this count never re-drifts here. Plus `index.html` and config. The private location at `~/dev/8ball-private/cards.v1.full.js` remains the authoring source; the public `content/cards.v1.full.js` is a verbatim copy maintained via `cp` at release-prep time.
- The single-file rule: `index.html` may not exceed 1500 lines. If it would, split into `ui/*.js` modules. Not before. **v0.23 amendment: the split-target shape is locked. A `ui/*.js` module is an ES module with pure exports (no DOM, vitest-testable without jsdom) alongside DOM-touching helpers gated by an `init*UI({refs}, {hooks})` injection point. Host-side `let` bindings stay in `index.html`; the module mutates them via host-injected setter callbacks (e.g. `setCurrentProfile`, `setSelectedCity`). Module-internal localStorage keys are bare-string `const KEY = '...'` definitions inside the module so `tests/privacy_scan.test.js`'s same-file identifier resolution catches them. Concrete precedents: `ui/payments.js` (v0.3.0 step 6, 165 LOC) and `ui/profile.js` (v0.3.0 step 7, 153 LOC). Future splits inherit the shape rather than re-deriving it.**

## §7. CI gate

`.github/workflows/ci.yml` runs on every push and PR to main. The gate has these stages:

1. **Calculation contract + engine pipeline** — `tests/profile.test.js` must pass (calc cases against `tests/fixtures.json`, `getCard` pipeline across the 144 positional catalog cells, `resolveBracket` cases). **v0.22 extension:** additionally verifies `content/cards.v1.full.js` parses as an ES module exporting `CARDS` with exactly 144 entries (12 sun signs × 12 animals), and runs the existing `BANNED_VOICE_REGISTER` scan against the deck's content strings (`name`, `type`, `habit`, `note.{low, mid, high}` fields).
2. **Privacy scan** — `tests/privacy_scan.test.js` must pass (no unpermitted network calls — only §5-permitted same-origin lazy loads and §5.B user-initiated calls are allowed; no third-party fonts/scripts; system fonts only). **v0.22 extension:** `LOCALSTORAGE_KEY_ALLOW` extended with `eight_ball_tries_used_v1`, `eight_ball_credits_v1`, `eight_ball_pending_profile_v1` per §5.
3. **PII scan** — `tests/pii_scan.test.js` must pass (operator-name leakage, SIRR cross-reference leakage, labeled-DOB leakage). **v0.22 extension:** scans `content/cards.v1.full.js` for the same banned patterns; no DOB strings should appear in card content but the scan codifies the invariant.
4. **Dependency discipline** — `tests/dependency_discipline.test.js` must pass (npm runtime dependencies absent/empty, no build step, devDependencies ≤ 5).
5. **Single-file rule** — `index.html` ≤ 1500 lines.
6. **Payments state machine** (added v0.3.0) — `tests/payments_state.test.js` must pass (`isNewPair`, `nextShakeState`, `applyPaidReturn` transitions covering credits, cap, paywall trigger, replay-attack `no-pending` branch, same-profile idempotence, pending-profile round-trip). Markup-static assertions for the paywall surface in `index.html` are covered by `tests/payments_markup.test.js` (counted under stage 5's file budget; runs in the same vitest pass).

(History note: the `cards.v*.js` content-scan retired in v0.2.0 when the deck moved private; it returns as of v0.3.0 against the new public-tracked `content/cards.v1.full.js` — see stage 1's v0.22 extension and stage 3's. The private content-authoring pipeline at `~/dev/8ball-private/` continues to run its own scans before each release-prep `cp` to the public path.)

Netlify is configured to auto-deploy on push to `main`. CI runs in parallel on the same push. Failed CI does not currently block the auto-deploy itself — this gap is acknowledged and acceptable while traffic is operator-only. Wiring a Netlify required-check on the GitHub Actions status is queued for the first traction milestone (see `journal.md`); at that point, this paragraph gets re-tightened to "a failed gate blocks the deploy."

**v0.43 amendment (A3 — Netlify-required-check trigger bound per §13).** "The first traction milestone" above is an unobservable park, against §13 v0.35's named-trigger rule. Bind it to the concrete proxy already used as the journal's ship-gate: **≥5 paid purchases plus ≥1 strong divination-niche signal**. Until that threshold is met, the failed-CI-does-not-block-deploy gap is an **indefinitely-accepted gap** while traffic is operator-scale — not parked work awaiting an unnamed event. (At the mid-June ship-gate read the count was 0 sales — reach is the verified bottleneck — so the trigger is nowhere near firing; this is wording precision, not a deploy-risk change.) When the threshold is met, the paragraph above re-tightens to "a failed gate blocks the deploy." Per L17 the original wording is preserved verbatim above.

## §8. Release ritual

Every release, however small, has automated gates and ritual gates.

**Automated gates (CI, blocking):**

1. CI green (6 stages — calc+pipeline / privacy / PII / dependency / single-file / payments-state). See §7 for the per-stage breakdown.
2. **Doctrine/content change requires journal entry.** PRs touching `DOCTRINE.md` or `content/*.js` must also touch `journal.md`. Enforced in `.github/workflows/ci.yml`.

**Ritual gates (operator/reviewer responsibility):**

3. PR opened with a one-line summary. (PR title discipline; CI doesn't validate title quality.)
4. Operator runs local PII audit (`audits/run_local_audit.sh`). The public CI cannot see the operator's personal data; the local audit closes that gap. Skipping it is the failure mode, so it's a checklist item, not a vibe.
5. Reviewer reads the diff. Asks: any new line cross §4? Any new path cross §5?
6. Cross-model audit on doctrine or content changes. See §10. Solo authority IS the failure mode. Doctrine and content changes go through Codex (or ChatGPT for content batches) before merge. Mechanical edits do not.
7. Merge.
8. Append to `journal.md` with the `===== YYYY-MM-DD · Title =====` shape: what shipped, what was rejected, what's deferred.
9. **Live-fire on local deploy preview.** Static audit cannot simulate DOM/CSS or engine runtime. Releases touching `index.html`, `core/engine.js`, or content batches require an operator-run local-server pass against the changed surface before merge. Phase-2F-1 made this load-bearing — the flip-state inversion bug was invisible to Codex's static audit and only surfaced when the operator shook the deploy preview.
10. Confirm Netlify auto-deployed. Open the live URL. Shake it.

**v0.43 amendment (A1 self-firing rule-kill step + B4 journal-header shape).** Two corrections to the ritual above:
- **Self-firing rule-kill step (A1, see §13).** Folded into gate 8 (the journal append, where the last-review date is both read and written): **at every doctrine-touching ship, if the newest rule-kill review entry in `journal.md` is more than 30 days old, run the §13 rule-kill review as part of this close-out and log it.** This is the event-based accelerator for the §13 cadence — the monthly date is the floor, the ship-event is the accelerator. The check is mechanical (read the newest review date from `journal.md`), not a remembered cadence.
- **Journal-header shape (B4).** Gate 8's `===== YYYY-MM-DD · Title =====` example names a retired header shape; the live journal uses `## YYYY-MM-DD — Title — STATUS`. Read the example as that shape. Per L17 the `=====` form is preserved verbatim above and superseded here.

## §9. The SIRR boundary

Tracked content of this repo MAY NOT contain the string "SIRR" (case-sensitive) or any SIRR-domain vocabulary (`sirr.studio`, `abjad`, `hebrew_gematria`, etc.) outside the boundary-naming files allow-listed in `tests/pii_scan.test.js` (`DOCTRINE_ALLOW`). SIRR is a sibling project, not a parent or child of `8ball`; the two repos share no code and no imports. Enforced by `tests/pii_scan.test.js`.

## §10. Multi-model lane system

**The author-judge problem is real.** A single instance writing AND reviewing its own work has a structural blind spot. SIRR's session L4 named this directly: "Solo authority on doctrine IS the failure mode."

For 8ball at its current scale, the system runs **five agent roles plus one always-on controller**. Full role docs live at `~/dev/8ball/agents/*.md`; platform-specific constraints at `~/dev/8ball/agents/PLATFORMS.md`; this § is the constitutional summary.

**Core agent roles** (v0.29):

| Role | Tool | Role doc | Read access | Write access |
|---|---|---|---|---|
| **Orchestrator** — context, briefs, dispositions, sequencing | Claude (chat) | [`agents/orchestrator.md`](agents/orchestrator.md) | Everywhere in `~/dev/8ball/` and `~/Desktop/8ball/` | Pragmatic for fast initial work; routes through implementer for production-grade edits |
| **Implementer** — multi-file production edits, git ops, repo filesystem | Claude Code (CC) — `/usr/local/bin/claude` | [`agents/implementer.md`](agents/implementer.md) | Repo + `~/Desktop/8ball/` | Repo + `~/Desktop/8ball/` |
| **Auditor** — adversarial pre-merge review on doctrine, content, release-gates | Codex — Mac desktop app, paste-and-relay | [`agents/auditor.md`](agents/auditor.md) | Pasted briefs only | Returns categorized verdicts (PASS / P3 / P2 / P1 / P0) |
| **Verifier** — live UX on deployed product, structured findings + screenshots | Claude in Chrome (CiC) — browser extension | [`agents/verifier.md`](agents/verifier.md) | Per-session domain allowlist (default = read-only/static; non-default requires controller approval) | Reports text + screenshots only; no irreversible action clicks |
| **Inspector** — read-only state on operator infrastructure dashboards (Gumroad, Netlify, GitHub, social), structured observations | Claude in Chrome (CiC) + osascript + operator-screenshot adjacents | [`agents/inspector.md`](agents/inspector.md) | Operator's own dashboards (project-operational scope, never personal accounts); per-session CiC allowlist for non-default domains | Reports observations only; no irreversible action clicks, no credentials, no payment entries, no terms acceptance |

**Controller** (always-on, never an agent): the **Operator**. Sole authority for merges, deploys, payments, account changes, sharing-permission changes, TOS acceptance, identity verification flows, and any irreversible third-party action. Full role doc at [`agents/controller.md`](agents/controller.md).

**Adjunct lanes** (not core agents; invoked ad-hoc, no project-specific role doc): **ChatGPT** for content/copy review (paste-relay); **Perplexity** for web search; **Gemini** for second opinion. Per `~/MUHAB.md` §3.

**Lane discipline (v0.24):**

- The orchestrator does NOT autonomously override §4 or §5. Borderline content gets flagged for ChatGPT review before merge.
- Doctrine amendments go through the auditor before merge. Mechanical edits do not.
- Periodic corpus drift-sweep audits run when state-doc age + cumulative shipped surface change warrant it. Drift-sweep brief shape, paste workflow, verdict format, and pre-budget-absorb expectation codified at `agents/auditor.md` Procedure 6. (v0.27)
- All filesystem and git operations route to the implementer for any change touching ≥3 files or modifying `core/`. Single-file documentation tweaks may stay in chat.
- Live-UX verification routes to the verifier post-deploy-preview (pre-merge for surface changes) and post-merge for the first 24–48h on production for any significant ship.
- Operational-dashboard state reads route to the inspector when canonical files (`8BALL.md`, journal) need to assert state of an operator-controlled third-party surface — ship-gate verification, processor activation banner state, deploy-status confirmation, social-profile state. Inspector directives originate from a named canonical-file-consumption purpose; never open-ended exploration. (v0.29)
- The controller is always the final approver. No agent auto-merges. Per L48: explicit audit-cleared signal before merge; five-minute-CI-green-to-merge windows are the L48 failure shape.

For per-cycle artifact locations (briefs, audits, directives, reports), see `agents/PLATFORMS.md` "Artifact-location matrix". For SIRR-specific lane discipline, see `~/dev/SIRR/SIRR.md` §7.

## §11. PII rule

Personal data of the operator, family, friends, or any other identifiable person is NEVER tracked content. The repo is private as of v0.2.0, but this rule is independent of repo visibility — tracked content is the durable boundary, not the current ACL state. Repos can flip; the rule survives the flip.

Specifically forbidden in any tracked file:

- The operator's full name in association with personal data (DOB, address, ID numbers).
- Test fixtures using real-person DOBs labeled to a real person.
- Any string matching combination patterns: `(operator-name) + (YYYY-MM-DD)` within 40 characters.
- SIRR cross-references outside of explicit boundary-naming doctrine.

The `tests/pii_scan.test.js` enforces a public-banned subset. The local audit at `audits/LOCAL_PII_AUDIT.md` closes the gap for personal data the public CI cannot know about.

**Fixture DOB rule (sub-rule of §11):** Fixture DOBs are chosen for the calc path they exercise, not anchored to any real person. If a calc path accidentally hits a real-person DOB, the fixture date is shifted by 12 years (preserves Chinese zodiac animal and life path mod-9 distribution while breaking real-DOB anchor) and the calc path is re-verified.

## §12. Out of scope

Two tiers. **[PERMANENT]** = load-bearing invariant (privacy, no-backend, no-oracle, SIRR); relaxing it changes what the object is — do not revisit. **[GUARDED]** = parked behind a named evidence trigger, not forbidden forever; revisit only on that trigger, never as idle feature-creep. (v0.35.)

- **[PERMANENT]** Account systems
- **[PERMANENT]** Email capture
- **[PERMANENT]** Push notifications
- **[PERMANENT]** AI-generated trait phrases at runtime (latency, cost, repeatability all break)
- **[GUARDED]** Multi-language until v2 of the trait pool exists in any language
- **[GUARDED]** Astrology charts, planetary aspects, anything beyond sun sign + optional rising sign — revisit only if it serves the object, never as feature-creep, and never in a way that crosses into horoscope/guidance identity
- **[PERMANENT]** Daily/weekly horoscopes ("today's reading")
- **[PERMANENT]** Sharing/social-card export of unconstrained content. The original wording — "Sharing/social-card export until a privacy review explicitly clears it" — is preserved here as lineage: that privacy review landed at v0.31 (§5.D), clearing a user-initiated, client-side, no-network export of the **free** card (labeled coordinate symbols + catalog number, no paid content, no PII). Permanently out of scope remains any export that carries the paid card-content layer (name/type/habit/note), any PII (name/DOB), or any per-result link / query parameter encoding a profile field.
- **[PERMANENT]** Anything touching the SIRR engine, codebase, or vocabulary

**v0.43 amendment (A2 — [GUARDED] gated-work pass, P1; corrects a 3-review-inherited error).** §13 v0.35 requires every [GUARDED] item to carry a NAMED, OBSERVABLE evidence trigger. There are exactly two; both fail it and are corrected here. Per L17 the two [GUARDED] lines above are preserved verbatim and superseded:
- **Astrology charts, planetary aspects → reclassified [GUARDED] → [PERMANENT].** Not genuinely guarded: §12 already permanently bans the identity they belong to — the [PERMANENT] header names *no-oracle* as a load-bearing invariant whose relaxation "changes what the object is," and "[PERMANENT] Daily/weekly horoscopes" bans the reading/guidance register. Charts and planetary aspects ARE that horoscope/guidance identity. The only genuinely-guarded astrology surface — optional rising sign — already shipped and cleared (§1.A). Relaxing the line would change what the object is, so it is [PERMANENT], not parked.
- **Multi-language trigger → re-keyed to a live observable.** Its trigger "until v2 of the trait pool exists in any language" references the trait-pool / template-pool system (`traits.v1.js` / `templates.v1.js`) **retired and DELETED in Phase-2F-2** (§4). A condition keyed to re-versioning a deleted, never-recurring system can never fire — the exact "parked with no defined release condition = drift" case §13 v0.35 exists to catch. (Flagged at the 2026-05-15 inaugural review; wrongly reversed 2026-05-30 — "string still present" conflated with "trigger valid"; inherited until the 2026-06-30 cross-verification caught it.) Re-keyed: **revisit multi-language only if a v2 of the cards deck (`cards.v2.*`) exists in any language.** Remains [GUARDED].

## §13. Refresh discipline

This file is **maintained by hand**, not generated. Update it when:

- A locked rule is added under any §-number
- A canonical path changes
- A doctrine version locks (note the version)
- A blocker is resolved or added
- A new tool/relay is automated (kill the old manual entry)

**Rule-kill review:** every locked rule that hasn't fired in 30 days → archive. Doctrine that doesn't breathe accumulates cost without producing safety. ~15 min/month.

**Gated-work discipline (v0.35):** parked/gated work carries a NAMED evidence trigger. "Parked" with no defined release condition is drift — at rule-kill review, either name the trigger or kill the item.

**Amendment discipline (L17, codified in-document, v0.42):** amendments **supersede, never edit** — historical clause text and prior footer version entries are preserved verbatim; a change is a new dated amendment block (or footer entry) that supersedes the old, and §-numbers are preserved under substance rewrite. This is the rule the in-document "per L17" citations refer to; its origin is the journal L17 lessons entry. Sibling standing lessons this constitution cites: **L48** — no merge before an explicit cross-model audit-cleared signal (§10); **L53** — describing labeled-DOB events in prose can re-fire the PII scan, so keep operator/role tokens away from ISO dates in files not on the scan's allow-list.

Cadence is **monthly** (amended v0.32, 2026-05-30 — was weekly). Rationale: weekly review at a solo cadence produced zero pruning (every review 2026-05-08 through 2026-05-30 was all-KEEP calibration — rules younger than the 30-day archive threshold have no firing surface to evaluate), and the weekly cadence silently lapsed for two consecutive Fridays (2026-05-22 + 2026-05-29) — the prune-discipline became the rule that stopped firing. Monthly aligns the review interval to the 30-day threshold it enforces, so each review actually has rules old enough to bite. Co-locate the monthly firing with other monthly discipline beats where possible (e.g. the v0.3.1 gate-close read 2026-06-15) so it has a recurring trigger and does not lapse.

First rule-kill review fired the first Friday after the doctrine aged 7 days (2026-05-15, inaugural). The catch-up review covering the two lapsed weekly slots fired 2026-05-30 (`~/Desktop/8ball/sessions/friday_rule_kill_review_2026-05-30.md`). Next review: 2026-06-15 (first monthly).

**v0.43 amendment (A1 — self-firing review trigger; the date cadence failed twice).** The monthly date cadence (v0.32, preserved verbatim above as lineage) is superseded as the *primary* trigger. It lapsed twice: first the weekly slots that prompted weekly→monthly, then the monthly review itself — due mid-June, it did not self-fire and ran 15 days late, firing only because a handoff carried it. A calendar a solo operator never watches cannot self-fire; swapping one date for another does not change the failure class. Replace it with an **event-based trigger backed by a date floor**, both first-class:
- **Accelerator (event).** Every doctrine-touching ship runs the rule-kill review in its §8 close-out (new ritual step, §8 v0.43) **if the last logged review is more than 30 days old** — binding the review to the journal-append ritual that already fires on every ship.
- **Floor (date).** Absent any doctrine-touching ship, the review still fires monthly as a backstop (the v0.32 cadence, retained as the floor).
- **Mechanical read, not memory.** "Last review date" is the date of the newest `## YYYY-MM-DD — … rule-kill review …` entry in `journal.md`; the >30-day check reads that date. This review logged 2026-06-30, so the next fires on the first doctrine-touching ship after 2026-07-30, or 2026-07-30 itself as the floor.

If you find yourself adding more locked rules than you're killing at review time, that's the recursion firing through the orchestrator. Stop drafting and ship something.

---

**calc version:** v2 (Pythagorean LP w/ 11/22/33 masters · tropical sun · real lunar new year tables · real solar-term tables · canonical Asia/Shanghai date-precision for cusps)
**content version:** v0.3.0-public-tier-1 (catalog + JS-gated card-content layer at `content/cards.v1.full.js`; compartment specimen-sheet render at every tier — sealed cells carry row/label structure, never values (§1.D v0.37); free surface = five DOB-derived coordinate values (§1.D v0.38) — tarot birth card (Major Arcana numeral + name, digit-sum reduction, §1.C, v0.5.0) · sun · public animal (year) · life path · catalog numeral; three-rung paid ladder (§1.D v0.36): t1 = rising (when computable) · five-element · private animal (month) · name-derived numerology pair (name number + soul urge, §1.B/§1.D v0.43 — the pre-v0.38 text triplet is superseded lineage); t2 = + personality · birthday · maturity number trio · day pillar; t3 = + hour pillar · the written 144-card entry (name/type/habit/note × low/mid/high), gated by paid credits per §1 v0.22 / §4 v0.22 / §4.B / §5.C; symbol-label visibility toggle; provenance placards (per-coordinate derivation note, labels-gated, surface-only, §1.E v0.40); atlas system-name legend (per-coordinate, labels-gated, §1.F v0.41); density census (always-on result-rail aggregate, off-PNG, §1.F v0.41); opt-in feedback surface §5.B Call 1; Gumroad Buy Link redirect §5.B Call 2 (LS path retired v0.3.0.3); 18+ acknowledgment gate §4.A; three-free-tries cap §4.B; content-delivery transparency §5.C; share surface §5.D — full specimen sheet (open coordinate values + sealed-compartment hatch structure) + catalog + wordmark → on-device PNG, Web Share API with a clinical caption (caption copied on the desktop fallback), v0.39 (was free-card-only PNG + bare-URL clipboard, v0.4.0); private authoring source preserved at `~/dev/8ball-private/cards.v1.full.js`)
**doctrine version:** 2026-07-01 · v0.43 (rule-kill review enactment — the 7 cross-verified AMENDs from the 2026-06-30 §13 rule-kill review (#49), additive/L17-clean; no historical or operator-locked text edited. **Governance/trigger (3):** §13 cadence → event-based self-firing trigger (every doctrine-touching ship runs the review in its §8 close-out when the last logged review is >30d old; the monthly date retained as the floor; "last review date" read mechanically from the newest rule-kill review entry in `journal.md`) + sibling §8 ritual step (gate 8); §12 [GUARDED] gated-work pass (P1) — astrology-charts/planetary-aspects reclassified [GUARDED]→[PERMANENT] (already the permanently-banned no-oracle/horoscope identity), multi-language trigger re-keyed off the deleted `traits.v1.js`/`templates.v1.js` system to a live observable (`cards.v2.*` exists in any language); §7 Netlify-required-check trigger bound to the concrete ≥5-paid + 1-signal ship-gate proxy (else an indefinitely-accepted gap). **Coherence/L17-staleness (4):** §1.A "lands at v0.2.8" → shipped/past-tense; §1.B numerology "three-number triplet" → name-derived pair (life path split free at v0.38); §1.B personality/birthday/maturity "reserved for v0.3.0+" → surfaced at t2 (v0.6.0); §8 gate-8 journal-header example `===== … =====` → the live `## … — STATUS` shape. No calc/content/core/code touch; `package.json` unbumped. `privacy_scan` FORBIDDEN + `LOCALSTORAGE_KEY_ALLOW` unchanged; `pii_scan` DOCTRINE_ALLOW unchanged. Trigger per §13: §13 rule-kill review enactment. Codex Procedure 4 + journal per §10/L48.)
- v0.42: 2026-06-30 (doctrine coherence — refinement campaign #3, additive/L17-clean. §1 gains a current-state anchor routing readers to the §1.D ladder (as amended through v0.38) ahead of the retired v0.2.3/v0.36 lineage prose; a table-currency note under the operator-locked §1.D table records that free is now FIVE coordinates (life path joined at v0.38) and t1 numerology is the name-derived pair; §6 architecture counts reconciled to 9 core / 6 ui / 24 tests with the file inventory deferred to CLAUDE.md (canonical) so they cannot re-drift in doctrine; §13 codifies L17 (supersede-not-edit + §-number preservation) in-document with L48/L53 sibling pointers, so the constitution no longer depends on externally-defined rules. No historical or operator-locked text edited; no calc/content/core/code touch; `package.json` unbumped. DEFERRED to a dedicated cut: the overdue §13 rule-kill review + a self-firing review trigger. `privacy_scan` FORBIDDEN + `LOCALSTORAGE_KEY_ALLOW` unchanged; `pii_scan` DOCTRINE_ALLOW unchanged. Trigger per §13: doc-coherence refinement. Codex Procedure 4 + journal per §10/L48.)
- v0.41: 2026-06-30 (Coordinate Legibility Pack cuts 2–3 doctrine coverage [NEW §1.F]. Documents the ATLAS system-name legend (per-coordinate, labels-gated, rendered in `.coord-atlas` outside `.coord-title`, off the §5.D PNG) and the DENSITY census (always-on aggregate `N of 15 coordinates open · M sealed at paid tiers` in the result rail, OUTSIDE `#card-face`, computed purely from `TIER_COORDS` + `CELL_COORD` via `tierDensitySummary`, never a profile; base 15 = the 14 sheet cells + the always-open catalog numeral, so the free count 5 matches §1.D). Both surface-only, catalog-isolated, no coordinate VALUE / no PII, no new localStorage key, §2 clinical / no-FOMO. Discharges the cut-2 Codex Hook-7 §1.F/§1.E-broaden debt — ATLAS (cut 2) and DENSITY (cut 3) both shipped clause-free under §1.D v0.37 + §5.D v0.39 + the §1.E precedent; §1.F now gives them literal coverage. `index.html` about-modal "the trick" copy updated to name what reveal-labels surfaces (derivation + system) and the result census. **Doc/copy only — no calc/content/core touch, no code-behavior change beyond the already-shipped cuts 2–3; `package.json` not bumped.** `privacy_scan` FORBIDDEN + `LOCALSTORAGE_KEY_ALLOW` unchanged; `pii_scan` DOCTRINE_ALLOW unchanged. Trigger per §13: §8 brand-coherence / legibility. Codex Procedure 4 + journal per §10/L48.)
- v0.40: 2026-06-29 (Coordinate Legibility Pack — provenance placards [NEW §1.E]. Each coordinate may carry a labeled, surface-only PROVENANCE PLACARD naming its derivation method — clinical, never interpretation (§2) — e.g. `digit-sum reduction`, `tropical zodiac`, `ascendant`, `solar term`, `letter-value sum`, rendered per row in cell order, gated by the existing `.card.labels-revealed` toggle (no new key). Tier-invariant + catalog-isolated: keyed to the coordinate, never a profile value or the catalog driver `(sunSign, animal)`; `getCard` unchanged. Adds NO coordinate VALUE (free VALUE count stays five per §1.D v0.38) and NO PII (names a method, never name/DOB/value). A sealed compartment's placard names its derivation (label/structure only, value absent per the v0.37 contract) — a hatched cell reads as a real coordinate withheld at the user's tier, not a failure (the F1-WATCH legibility fix). NOT serialized into the §5.D share PNG: the share path reads `.coord-val` + `.coord-title` (via `ui/tiers.js shareRowRefs`, consumed by `ui/share.js` as a snapshot), never `.coord-prov`. Step-0 §6 budget move: about/forget/age-gate modals + escape-to-close extracted to `ui/modals.js` (AGE_ACK_KEY relocated, no new key); index.html 1496 → ~1444. Sibling tracked-file changes co-versioned: `ui/tiers.js` (`PROV_NOTE` + `provText` + init-time `.coord-prov` write pass), `ui/modals.js` (NEW — modal controllers), `index.html` (`.coord-prov` CSS gate + modal extraction), `tests/` (NEW `tests/modals.test.js` (DI shape + no-new-key + boot wiring), NEW `tests/provenance.test.js` (derivation pins + paid-value-absence sentinel + clinical-voice pin + DOM-write/sealed-row F1-WATCH/idempotence), `tests/share_surface.test.js` placard-exclusion pin, `tests/age_gate.test.js` key re-home). Ships within the 0.7.x line — `package.json` not bumped (flagged for Procedure 4 version-truth, as v0.38/v0.39). No calc / `content/` / `core/` touch, no new dependency, no new localStorage key, no network call — `privacy_scan` FORBIDDEN + `LOCALSTORAGE_KEY_ALLOW` unchanged; `pii_scan` DOCTRINE_ALLOW unchanged. Trigger per §13: §8 brand-coherence / legibility — retires the F1-WATCH "hatch reads as broken" risk; explicitly NOT a measured conversion fix. Brief `~/Desktop/8ball/sessions/brief_clp_provenance_2026-06-29.md`.)
- v0.39: 2026-06-29 (reach pass v1 — full-sheet share artifact. **§5.D:** the share PNG renders the FULL specimen sheet — open coordinate values at the user's tier PLUS the sealed structure of higher-tier compartments (section label + seal hatch, the VALUE absent), resolving the v0.37 sealed-structure-in-PNG deferral; the sealed value never exists in the DOM snapshot the builder serializes (sealed cells hold `textContent === ''`), so invariants (a)–(d) hold; `ui/tiers.js shareRowRefs` exposes a DOM-derived per-cell `{state, value}` snapshot and the `ui/share.js` tier-hidden filter is removed. NEW share caption: catalog + open coordinate values + `sealed remainder` + bare URL, carried in `navigator.share` text and copied on the desktop fallback (replacing the bare-URL copy), same (a)/(b) PII bounds, §2 clinical voice. **Cold-landing:** one-line `#mechanism-strip` near the registry header — clinical, no pricing/CTA. Sibling tracked-file changes co-versioned: `ui/share.js` (full-sheet builder + SVG hatch `<pattern>` + `buildCaptionFromSnapshot` coupled into the flow), `ui/tiers.js` (`shareRowRefs` per-cell `{state, value}` snapshot, DOM-derived), `index.html` (mechanism strip + share-wiring comment, 1499/1500 held by offsetting compaction), `tests/share_surface.test.js` (full-sheet render, sealed = hatch + label + no value, aggregate paid-value-absence sentinel, F4 unres-vs-seal, caption PII/voice, share-flow coupling, mechanism strip). Ships within the 0.7.x line — `package.json` not bumped (flagged for Procedure 4 version-truth, as v0.38). No calc / `content/` / `core/` touch, no new dependency, no new localStorage key, no network call — `privacy_scan` FORBIDDEN + `LOCALSTORAGE_KEY_ALLOW` unchanged; `pii_scan` DOCTRINE_ALLOW unchanged. Reach lever per the 06-15 read; Codex pre-build audit cleared 2026-06-29 (P1 H2 → §5.D amendment). Trigger per §13: reach pass; brief `~/Desktop/8ball/sessions/brief_reach_pass_v1_2026-06-29.md`.)
- v0.38: 2026-06-25 (free life-path split — override #3, controller-authorized 2026-06-12, HELD by operator 2026-06-14, resumed 2026-06-25 — §1.D numerology splits by derivation: `lifePath` (DOB-derived) joins the free surface (free = DOB-only invariant preserved) so the free render carries FIVE coordinate VALUES — birth card · sun · public animal · life path · catalog numeral; the name-derived pair (expression/name number + soul urge) stays t1, the t1 "numerology triplet" demoted to lineage per L17. Surface-only / catalog-isolated — life path drives low/mid/high bracket resolution per §1, never the catalog index `(sunSign, animal)`; `getCard` unchanged. §5.D: the free share-PNG now carries the life-path numeral — a DOB-derived number with no name/DOB string, so §5.D PII invariants hold, same basis as the birth card and catalog numeral. One-line `TIER_COORDS` rollback (`ui/tiers.js`). Sibling tracked-file changes co-versioned: `ui/tiers.js` (FREE_COORDS + CELL_COORD split + renderTierSections lifePath cell), `index.html` about-modal + og/twitter/description meta (four→five coordinates; life path moved to the free list, out of the t1 list), test re-pins in `tiers` / `payments_markup` / `prose_coordinate_count`. No calc / `content/` / `core/` touch, no new dependency, no new localStorage key, no network call — `privacy_scan` FORBIDDEN + `LOCALSTORAGE_KEY_ALLOW` unchanged; `pii_scan` DOCTRINE_ALLOW unchanged. Orchestrator dissent on record: reach is the verified bottleneck (06-15 read), conversion is not the constraint; LOW public-perception reversibility — the free share-PNG exposes the life-path number, a later clawback reads as bait-and-switch. Trigger per §13: controller override (override #3); brief `~/Desktop/8ball/sessions/override3_free_lifepath_split_2026-06-12.md`.)
- v0.37: 2026-06-12 (compartment-render amendment, v0.7.0 — the card renders the full coordinate sheet at every tier; coordinates above the device's render tier appear as sealed compartments — row, label, and cell structure visible, the coordinate value ABSENT from the DOM (absent, not hidden). **§1.D:** v0.37 amendment appended — free-surface invariant unchanged in substance (the free render carries exactly four coordinate VALUES; v0.7.0 adds the visible sealed structure of the remaining sheet); sealed ≠ unresolvable (paywalled field → seal treatment; entitled-but-uncomputable field — rising without birth time/place, hour pillar without birth time — → `—` empty field at any entitled tier); the written 144-card entry stays a t3-only unlock, its sealed block carries no entry text at any lower tier. Sibling tracked-file changes co-versioned: `ui/tiers.js` render-contract rewrite (hidden→sealed, per-cell granularity — `setCell` state ∈ value | sealed | unres; paired-row title grammar: dot form names structure, relation glyph marks a resolved entitled pair — `SUN · RISING` / `SUN ↑ RISING`, `PUBLIC · PRIVATE` / `PUBLIC ⇌ PRIVATE`, bare sun-only title never renders; pure unseal-trigger decision `newlyEntitledCells` + `primeUnsealBaseline` — upgrade renders only, β-idempotent; §5.D `shareRowRefs` row-snapshot proxies — the share PNG keeps rendering OPEN coordinates only, sealed-structure-in-PNG is a logged journal deferral, `ui/share.js` UNTOUCHED); `index.html` per-cell compartment markup (14 `.coord-cell` + the sealed `#card-entry` block), F1 hatch seal as the single root token `.card.seal-hatch` (gradient defined once), `.locked-extras` bars retired, unseal keyframes + `prefers-reduced-motion` instant fallback, written-entry slots constant-skeleton and cleared below t3 (1499/1500); `tests/tiers.test.js` rewrite (constant skeleton, DOM purity at every rung, seal-iff-above-tier, F4 distinction, title grammar, unseal idempotence, share-row snapshots; tier-resolution/grandfather/monotonic suites carried unchanged); `tests/payments_markup.test.js` seal tokens + v0.6.1 geometry assertions kept; re-pins in labels_reveal / numerology_display / birthcard; `package.json` + lock 0.6.0 → 0.7.0. No calc change, no `content/` deck touch, no `core/` touch, no new dependency, no new localStorage key, no network call introduced — privacy_scan FORBIDDEN list + LOCALSTORAGE_KEY_ALLOW unchanged; pii_scan DOCTRINE_ALLOW unchanged. Trigger per §13: §8 brand-coherence on the tier surface, controller override recorded 2026-06-12 (same exception class as Build B 2026-06-11); brief `~/Desktop/8ball/sessions/brief_v070_compartment_card_2026-06-12.md`.)
- v0.36: 2026-06-11 (tier-ladder amendment, v0.6.0 — the paid surface becomes a three-rung coordinate-density ladder per the operator-locked table; the free surface shrinks to four coordinates. **§1:** free-surface invariant amended — free card = birth card · sun (bare) · public animal (year) · catalog numeral; the v0.2.3 seven-coordinate free surface demoted to lineage verbatim per L17; v0.22 unlocked card-content layer relocates to t3. **§1.D (NEW, mirrors §1.A/§1.B/§1.C):** tier-ladder surface clause — tiers reveal coordinates per the locked table; tier coordinates are surface-only; catalog driver stays `(sunSign, animal)`; day/hour pillars never enter the catalog; stored tier monotonic via `eight_ball_tier_v1`; +3 credits on any rung; density = f(stored tier, credit state) resolved by one helper at every render, never boot circumstance (PR #36 Codex remediation R1); legacy credits-without-tier grandfathered to t3 and persisted on first detection (remediation R2); `TIER_COORDS` rollback flag in `ui/tiers.js`; specimen-sheet §9.3 element reconciliation untouched (no §3 calc-version bump). **§2:** arcade-toy carve-out explicit re-amendment — `dollars == tries` superseded (every rung buys three reads; dollars buy density); one-time/fixed-credit/no-subscription unchanged. **§4.B:** paywall = three-rung ladder; 3-free-tries cap unchanged; t3 carries the card-content unlock. **§5:** allow-list + `eight_ball_tier_v1`. **§5.B Call 2:** three Gumroad products (rzqezp $3 / neysyv $6 / xjpvp $9), same bare-href + Content-tab-Button mechanism; `?paid=t1|t2|t3` handler, unknown values ignored. **§5.D:** share renders at the current tier; all invariants preserved (a's "free coordinate symbols" generalizes to "rendered coordinate symbols at the current tier"). Sibling tracked-file changes co-versioned: new `ui/tiers.js` (TIER_COORDS + render gating, §6 DI shape); `ui/payments.js` TIER_KEY + generalized handler; `core/payments.js` pure tier functions (TIER_ORDER/isTier/tierRank/maxTier + applyPaidReturn tier extension); `ui/share.js` tier-aware row filter + count-adaptive geometry; `index.html` three new pillar/number rows + paywall ladder + about/meta tier disclosure (1494/1500); new `tests/tiers.test.js`; re-pins in payments_state / payments_markup / labels_reveal / prose_coordinate_count / share_surface / numerology_display / pillars / birthcard tests; `tests/privacy_scan.test.js` LOCALSTORAGE_KEY_ALLOW + `eight_ball_tier_v1` (the §5-amendment-gated extension). No calc change, no `content/` deck touch, no new dependency, no network call introduced — privacy_scan FORBIDDEN list unchanged; pii_scan DOCTRINE_ALLOW unchanged. Trigger per §13: tier-model §8 brand-coherence exception, controller override recorded 2026-06-11; brief `~/Desktop/8ball/sessions/brief_v060_tier_ladder_2026-06-11.md`.)
- v0.35: 2026-06-02 (governance-repair amendment — decouples over-extended constraints from their load-bearing values without removing any privacy / voice / SIRR / no-backend invariant. **§2:** analytics bullet clarified (first-party server-side host logs are not "telemetry") + new review-discipline bullet "honest invitation permitted; manipulation forbidden" — supersedes "not a funnel / no CTA / no link in bio" language that lived only in operating/session plans, never in this file. **§5:** new "first-party server-side observability is not telemetry" clarification — reading the operator's own origin logs is permitted; the product still injects no client / makes no call / emits no beacon, and `tests/privacy_scan.test.js` is unchanged because first-party logs are produced by the host platform, never by tracked source. **§12:** retitled "Out of scope" with a two-tier **[PERMANENT]** vs **[GUARDED]** marking — accounts / email / push / runtime-AI-phrases / daily-weekly-horoscopes / unconstrained-export / SIRR stay PERMANENT (load-bearing); only multi-language + astrology-charts move to GUARDED. No item deleted. **§13:** gated-work-needs-a-named-trigger discipline added. Sibling state-doc co-edits in `8BALL.md`: §3 telemetry row (first-party logs ≠ telemetry), §1 deniability-layer clarified (privacy/identity split, not a status claim — 8ball may be a first-class public object; §9 no-SIRR-string rule unchanged), build-B parked behind a named evidence gate. No code, no calc, no `content/` deck touch, no shipped-surface change, no new dependency, no new localStorage key, no network call introduced — `tests/privacy_scan.test.js` FORBIDDEN list + `LOCALSTORAGE_KEY_ALLOW` unchanged; `tests/pii_scan.test.js` DOCTRINE_ALLOW unchanged. Product stays 0.5.2. Derivation: `~/Desktop/8ball/sessions/{constraint_decoupling_analysis,doctrine_patch_plan}_2026-06-02.md` + Codex constraint-audit package 2026-06-02.)
- v0.34: 2026-06-02 (§1.A amended — v0.5.2 legacy rising determinism. Stored v0.2.1–v0.2.7.1 country-code profiles now resolve their country/zone code to a representative IANA timezone before using the same `computeRising` path as fresh city profiles; legacy `getRisingSign(..., utcOffsetMinutes, ...)` is retained only as a low-level fixed-offset helper and both APIs remain polar-safe. §5.D(a) share-artifact wording now truthfully includes the 8ball wordmark. Sibling tracked-file changes co-versioned: `core/countries.js` exports `LEGACY_COUNTRY_TIMEZONES` for exact-keyset tests; `core/profile.js` legacy country path routes through representative IANA tz; `ui/share.js` stale fixed-count comment scrubbed; `tests/countries.test.js` adds exact keyset, alpha-2 largest-city parity, split-code snapshots, and DST/historical sign-impact regressions; `tests/prose_coordinate_count.test.js` derives the prose count from rendered coordinate rows plus share refs; `tests/rising.test.js` adds invalid-tz null regression; `package.json` and `package-lock.json` version truth reconciled to product v0.5.2. No calc-version change, no `content/` deck touch, no new dependency, no new localStorage key, no network call introduced — `tests/privacy_scan.test.js` FORBIDDEN list + `LOCALSTORAGE_KEY_ALLOW` unchanged; `tests/pii_scan.test.js` DOCTRINE_ALLOW unchanged.)
- v0.33: 2026-05-30 (§1.C added — tarot birth-card free coordinate. Major Arcana numeral + lowercase name (e.g. `XXI · the world`), computed DOB-only in new `core/birthcard.js` (`getBirthCard`), carried as additive `birthCard` field on `buildProfile`. Lead row of the free card, above five-element. Free-surface coordinate inside the §1 symbols-only invariant — labeled coordinate, never interpretation; the paid card-content layer is untouched. Surface-only like §1.A rising: does not enter `getCard` / `resolveBracket` / catalog computation; catalog driver remains `(sunSign, animal)`. Reduction tradition: digit-sum (sum all DOB digits, reduce ≤22), distinct from the master-number-stopping life-path reduction; 22 maps to `0 · the fool` so every DOB resolves to a real arcana. Included in the §5.D share surface (free coordinate → shared PNG matches the on-screen free card, five coordinates); no PII, no paid content, §5.D invariants (a)/(b) hold. Ships with product v0.5.0. Sibling tracked-file changes co-versioned: new `core/birthcard.js`; `core/profile.js` import + additive field; `index.html` arcana lead `.coord-section` + render wiring + share symbols array extended 4→5; new `tests/birthcard.test.js` (14 tests); `tests/labels_reveal.test.js` coordinate-count assertions 4→5. No calc-version change (DOB-only coordinate, no catalog/bracket effect), no `content/` deck touch, no new dependency, no new localStorage key, no network call introduced — `tests/privacy_scan.test.js` FORBIDDEN list + `LOCALSTORAGE_KEY_ALLOW` unchanged; `tests/pii_scan.test.js` DOCTRINE_ALLOW unchanged.)
- v0.32: 2026-05-30 (§13 cadence amended + §7 mirror — rule-kill review weekly → monthly. Weekly review at solo cadence produced zero pruning (all reviews 2026-05-08 → 2026-05-30 were all-KEEP calibration; rules younger than the 30-day archive threshold have no firing surface) and silently lapsed two consecutive Fridays (2026-05-22 + 2026-05-29) — the prune-discipline became the rule that stopped firing. Monthly aligns the review interval to the 30-day threshold it enforces and co-locates with other monthly beats (next: 2026-06-15, with the v0.3.1 gate-close read). Sibling change: `agents/orchestrator.md` gains Procedure 9 (re-grep canonical state before orienting/drafting) per the orchestrator-stale-context L promoted same session (2 sightings: chat-25 §5.C duplicate-analysis / 2026-05-30 handoff-orientation-shortcut). The Friday pre-stage's §12 "trait pool / template expansion" AMEND leg was DROPPED on verification — that string no longer exists in DOCTRINE (retired earlier); the surviving §12 "trait pool" multi-language clause is accurate, not drift. Doctrine-governance-only cycle: no code, no calc, no `content/` deck touch, no shipped-surface change, no new dependency, no test logic change — `tests/pii_scan.test.js` DOCTRINE_ALLOW unchanged, `tests/privacy_scan.test.js` unchanged.)
- v0.31: 2026-05-29 (§5.D added + §12 amended — share surface cleared. New §5.D codifies a user-initiated, on-device (SVG → canvas → `toBlob`) PNG export of the free card (labeled coordinate symbols + catalog number, no paid content, no PII), shared via the Web Share API with a local-download + bare-URL-clipboard fallback; invariants lock no-paid-content, no-PII, no-per-result-link, no-network, no-telemetry. §12 "Sharing/social-card export until a privacy review explicitly clears it" amended (line preserved as lineage per L17): the contemplated privacy review landed here — the §5.D-constrained free-card export is cleared; unconstrained / paid-content / PII / per-result-link export stays permanently out of scope. Sibling tracked-file changes co-versioned: new `ui/share.js` (`initShareUI` DI module per §6); `index.html` `#share-btn` + share-status surface + boot wiring; new `tests/share_surface.test.js`. Ships with product v0.4.0 — first new surface since the v0.3.0 paid layer. No calc change, no `content/` deck touch, no new dependency, no new localStorage key, no network call introduced — `tests/privacy_scan.test.js` FORBIDDEN list and `LOCALSTORAGE_KEY_ALLOW` both unchanged; `tests/pii_scan.test.js` DOCTRINE_ALLOW unchanged.)
- v0.30: 2026-05-17 (§5.C + §10 amended — Lemon Squeezy permanently retired from the project. LS rejected the operator's application 2026-05-17 after Step 3 KYC/KYB review (chat-25 logged Step 2 cleared; chat-28 chose Gumroad path per "whichever clears first becomes primary funnel" framing). The v0.28 lineage clause "LS path preserved at lemon-squeezy.com store but not wired through 8ball runtime; future re-route possible if LS Step 3 clears" is hereby superseded: LS path is permanently dead, no re-route possible, Gumroad is the single permanent processor at runtime. §5.C future-amendment example (license-key issuance via LS) updated to processor-agnostic backend-gated framing. §10 inspector dashboard list drops LS as a project surface (operator no longer has reason to check LS dashboards post-rejection). Sibling tracked-file scrub of ~13 active LS references in state docs (8BALL.md §4 inspector lane row + §11.11 (b) ship-gate row + §11.16 Live-fire testing row), agent docs (orchestrator.md / auditor.md / verifier.md / controller.md / PLATFORMS.md / inspector.md / implementer.md), README.md §5.B privacy scan description, ui/payments.js comment, tests/payments_markup.test.js comment header, audits/RELEASE_CHECKLIST.md §5.B + post-merge-smoke-test wording (mix of chat-28 Codex Procedure 4 P1 tier-1 backlog + post-rejection cleanup). All historical breadcrumbs in DOCTRINE §5 / §5.B Call 2 that explain Gumroad mechanism via comparison with prior LS path preserved — they're not drift, they're how the doctrine documents the transition. All lineage entries (v0.28 through v0.22) preserved verbatim per L17. All agents/*.md audit-history / firing-log blocks documenting past LS-dashboard inspector firings + LS domain-allowlist block at v0.3.0 first non-default-domain directive + L51 sightings against LS dashboard state-row drift preserved verbatim per L17. No code touch, no calc change, no test logic change, no shipped-surface change — v0.3.0.3 already removed LS from runtime; this cycle is the doctrine + state-doc + agent-doc cleanup that v0.3.0.3 deliberately did not bundle. `tests/pii_scan.test.js` DOCTRINE_ALLOW unchanged.)
- v0.29: 2026-05-16 (§10 amended — core-agent table extended from 4 rows to 5 rows; new **Inspector** row inserted between Verifier and Controller, codifying read-only state reads on operator-controlled third-party dashboards — payment processors (Gumroad, LS), hosting (Netlify), code (GitHub), social (IG / TikTok / Threads). Inspector promoted from sketch `~/Desktop/8ball/sessions/inspector_sketch_2026-05-13.md` after 9/9 directive-shape firings (8 fired, 7 unambiguously clean) across 8 distinct surfaces; T1 promotion gate over-satisfied by 4-firing buffer. §10 main paragraph "four agent roles" updated to "five agent roles"; Lane discipline gains new bullet routing operational-dashboard state reads to the inspector with canonical-file-consumption purpose requirement. Boundary clauses (no irreversible actions, no credentials, no scope-expansion synthesis) inherited from `agents/verifier.md` (adapted to inspector surface; meaning preserved) via chat-15 c13-c14-c15 L-mitigation bundle. Sibling tracked-file changes co-versioned: new `agents/inspector.md` role doc; `agents/PLATFORMS.md` new H2 "Inspector (operational dashboard read)" section between CiC (verifier) and ChatGPT entries; `agents/AGENTS.md` role table 4 → 5 core agents + opening-paragraph count + new audit history entry. No code touch, no calc change, no shipped-surface change, no test logic change — `tests/pii_scan.test.js` DOCTRINE_ALLOW extended in this cycle to add `agents/inspector.md` explicitly (sibling-to-existing-role-doc entries from v0.24 cycle); test change is one-line list addition.)
- v0.28: 2026-05-16 (§5.B Call 2 rewritten — Lemon Squeezy Buy Link redirect path retired, Gumroad Buy Link redirect codified as the live processor mechanism. §2 + §5 preamble + §5.B section header + §5.B Call 2 numbered item all updated. Mechanism differences: bare URL with no `checkout[success_url]` query parameter; post-purchase redirect handled single-source via Gumroad's product Content-tab Button rather than belt-and-suspenders LS dashboard Confirmation + URL-encoded query; two-click post-purchase flow paywall → Gumroad checkout → Content Button → `/?paid=t1` rather than single-click LS redirect; Stripe under Gumroad's Connect rather than Stripe under LS. Constraints preserved: user-initiated only, hosted redirect not overlay, no user data on redirect, no SDK/fetch/webhook, trust-based return, no customer object retained, disclosure in about-modal + paywall modal. Trigger: chat-25 LS Step 3 KYC/KYB review still vendor-staff-pending after 1-3 BD SLA window; chat-28 Gumroad Stripe Connect verification cleared 2026-05-16 same day after operator submission of corrected business-website + product-description fields. Per chat-27 "whichever clears first becomes primary funnel" — Gumroad cleared first, became live processor. LS path preserved at lemon-squeezy.com store but not wired through 8ball runtime; future re-route possible if LS Step 3 clears, but doctrine codifies single-processor-at-a-time at runtime.)
- v0.27: 2026-05-13 (§10 amended additively — periodic corpus drift-sweep audit procedure codified at `agents/auditor.md` Procedure 6 after three clean T1 firings, chat-8 + chat-9 + chat-10; absorbs the standing "Codex drift-sweep" tentative lane into a real auditor procedure. No new lanes, no role-doc additions; the existing auditor role gains an additional invocation entry point. No code touch, no calc change, no shipped-surface change.)
- v0.26: 2026-05-12 (drift-sweep tier-2 cleanup: §5.B Call 1 retires the stale `action="/?sent=1"` ban — the v0.2.5.2 same-origin success redirect is the shipped form; §5.B Call 2 documents the URL-encoded `checkout[success_url]` query-param mechanism shipped at v0.3.0.2 paired with the dashboard Button link as belt-and-suspenders; §6 main paragraph re-aligned with shipped `ui/` folder + 7-module `core/` + 14-test `tests/`. State docs (8BALL.md §1/§2/§8, README.md, root AGENTS.md, root CLAUDE.md, agents/auditor.md) reconciled with shipped reality. 8BALL.md §1 SIRR-paragraph metaphor/architecture split applied per controller disposition B (chat-10). No new doctrine clauses added; existing v0.21 / v0.22 / v0.23 / v0.25 amendments now reflected consistently across §5.B / §6 main bodies + state docs.)
- v0.25: 2026-05-12 (drift-sweep tier-1 cleanup: §1 main paragraph aligned with v0.22 amendment paid-content reality; §2 voice scan paragraph aligned with v0.22 §7 stage-1 CI scan extension; §2 third bullet + §7 stage 2 absolute "no network calls" wording replaced with scoped-model wording matching §5; §5 allow-list extended to declare `eight_ball_profile_v1` wrapper key explicitly. No new doctrine clauses added; existing v0.22 / v0.23 amendments now reflected consistently in §1 / §2 / §5 / §7 main bodies.)
- v0.24: 2026-05-12 (§10 restructured as 4 core agents + 1 always-on controller + adjuncts; agents/ folder formalized with `AGENTS.md`, `PLATFORMS.md`, and 5 role docs at `agents/orchestrator.md` / `implementer.md` / `auditor.md` / `verifier.md` / `controller.md`; v0.23 5-row table reshaped, lane labels aligned with role docs, ChatGPT moved from core lane to adjunct, verifier (Claude in Chrome) added as the fourth agent role).
- v0.23: 2026-05-11 (§1.B amended — numerology triplet always space-separated, v0.22 hasMaster branch retired post-CiC Fix A; §4.B amended — about-modal discloses cap shape, not the localStorage-wipe reset; §6 amended — ui/*.js split pattern locked with `ui/payments.js` + `ui/profile.js` as concrete precedents and DI shape specified).
- v0.22: §1 / §2 / §4 / §5 / §5.B / §5.C / §6 / §7 amended or added — paid surface (3 free + $3/3 reads via LS Buy Link redirect; public-bundle JS-gated deck delivery; arcade-toy carve-out; payments state-machine CI stage).
- v0.21: §1.A amended — DST + historical-tz handling in scope; city-level birthplace via core/cities.js; |lat|>66.5° returns null. §5 amended — same-origin lazy loads permitted, fetch() ban preserved. §5 allow-list extended with city/cc/tz.
- v0.20: §3 calc v2 — real lunar new year + solar-term tables; getInnerAnimal signature change; date-precision Asia/Shanghai cusp resolution.
- v0.19: §5 allow-list extended with `eight_ball_labels_revealed_v1`.
- v0.18: §4.A added — 18+ acknowledgment gate, click-through (no verification), one-time, persists via separate localStorage flag.
- v0.17: §5.B added — feedback surface, user-initiated only, single named endpoint, native form POST, fail-silent.
