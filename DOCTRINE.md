# 8ball — DOCTRINE.md

> One page. Read every release. Edit only deliberately.

This document is the constitution of `8ball`. The codebase obeys it. PRs that contradict it require an explicit doctrine amendment in the same change.

## §1. What this is

A fixed designed deck that knows you. Enter your name and date of birth once. Shake.

**Current free/paid composition (authoritative) — read this before the lineage prose below.** The free surface is **five** coordinates from the date of birth (tarot birth card · sun · public animal (year) · life path · catalog numeral), rendered on a constant compartment **specimen sheet** where higher-tier coordinates appear as sealed cells (§1.D v0.37); **four** paid rungs (t1/t2/t3 open the rest of the single sheet; **t5** adds a second person's complete sheet plus the §1.J relation layer — the ladder skips `t4`, which is retired, see §1.D v0.60/v0.61). The current per-surface authority is the §1.x clause family — §1.A rising · §1.B numerology · §1.C birth card · §1.D tier ladder · §1.E provenance · §1.F atlas+density · §1.G coordinate meanings · §1.H t3 written-entry rotation · §1.I registry + concordance · §1.J dyad relation layer. The two paragraphs immediately below describe the original v0.2.3 surface and are retained as lineage per L17; where they disagree with §1.A–§1.J, the clauses win.

The product calculates ten baseline calibrated coordinates from (name, DOB): sun sign, Chinese five-element, public animal (year-pillar), private animal (month-pillar), life path, expression/name number, personality, maturity, soul urge, and birthday. The two animals pair on a single line via an equilibrium arrow (`⇌`); three of the numerology coordinates (life path, expression/name number, soul urge) render as a text triplet on line 4 of the result card. The (sun sign, public animal) pair drives a 144-card catalog index — computed positionally in `core/engine.js` (sun-row × 12 + animal-col + 1, rendered as roman numeral). Life path drives bracket resolution (low/mid/high) within a card cell via `resolveBracket`, not the catalog index. The catalog index is the only card-derived field surfaced.

As of v0.2.3 the result card surfaces seven of the ten baseline coordinates (chinese five-element, sun, public animal, private animal, life path, expression/name number, soul urge), optional rising sign, and the catalog index. The remaining three baseline coordinates (personality, birthday, maturity) are computed on buildProfile but reserved for v0.3.0+ paid surfacing — see §1.B. The locked render path ships symbols only — no interpretation, no per-symbol explanation — and is the canonical free surface. The unlocked render path (gated by paid credits per §1 v0.22 amendment / §4.B / §5.C) surfaces the card-content layer (name, type, habit, note per cell) from `content/cards.v1.full.js`; private authoring source is preserved at `~/dev/8ball-private/cards.v1.full.js`.

The voice is declarative-observational, framed in strengths and weaknesses. Cards openly reference the symbol systems they draw from — sun sign, Chinese five-element, Chinese zodiac animals (year and month pillars), numerology life path, name number, soul urge — and name them as such.

**v0.22 amendment (v0.3.0).** As of v0.3.0, the public repo ships the full card-content layer at `content/cards.v1.full.js` (144 entries, name · type · habit · note × low/mid/high). Visibility is JS-gated behind the `eight_ball_credits_v1` localStorage flag — unset/zero renders the locked surface (symbols only), positive value renders the unlocked surface (symbols + card-content). The deck bytes are inspectable via View Source — the lock is a UI convention, not a server-gated fortress (see §5.C). **The free-surface invariant "symbols only — no interpretation, no per-symbol explanation" still applies to the locked render path; the unlocked render path is the named exception, gated by paid credits.**

**v0.36 amendment (v0.6.0).** As of v0.6.0 the free surface is **four coordinates**: tarot birth card (§1.C), sun sign (bare — rising joins at t1), public animal (year-pillar, alone — the private month-pillar animal joins at t1), and the catalog numeral. The v0.2.3 seven-coordinate free surface described above is superseded and preserved as lineage per L17; those coordinates relocate to the paid rungs per the §1.D tier ladder. The locked render path still ships symbols only — no interpretation, no per-symbol explanation — and remains the canonical free surface. The v0.22 unlocked card-content layer relocates to tier t3 (§1.D / §4.B v0.36): credits buy reads, the stored tier sets density at every render — and credits held with no stored tier are the pre-v0.6.0 purchase shape, grandfathered to t3 per §1.D.

**§1.A. Rising sign — surface coordinate, not a driver.**

As of v0.2.1 the result card surfaces an eighth coordinate when the operator provides birth time, country, latitude, and longitude: the rising sign (astronomical ascendant). Rising sign is computed from `(year, month, day, hour, minute, utcOffsetMinutes, lat, lng)` via standard ascendant math (Meeus); see `core/rising.js` for the exact algorithm. It is rendered on line 2 of the result card paired with the sun sign as `${sunSign} ↑ ${risingSign}`.

Rising sign is **surface-only**: it does not enter `getCard`, `resolveBracket`, or any catalog computation. The catalog driver remains `(sunSign, animal)` per §1. Rising adds a coordinate to the visual surface; it does not branch the card layer.

When required birth-time or location fields are missing, rising sign is `undefined` and line 2 falls back to the v0.2.0 bare-sun-sign render. Existing v0.2.0 profiles in localStorage continue to work without modification.

**v0.21 amendment (v0.2.7.2).** Birthplace input is city-level via `core/cities.js` — a 53,308-entry pop≥7500 subset of GeoNames cities5000, lazy-loaded from `assets/cities.json` per §5 (v0.21 same-origin lazy-load permission). Selecting a city atomically sets `tz` (IANA timezone), `lat`, `lng`, and country code on the form state. The country-code payload shape from v0.2.1 is retained for backward compatibility with stored v0.2.1–v0.2.7.1 profiles, but the UI no longer surfaces country selection.

Rising-sign math is now DST-aware and historical-tz-aware. The `computeRising({...tz...})` API in `core/rising.js` resolves the UTC offset for the (year, month, day, hour, minute) wall-time input via `Intl.DateTimeFormat` with `{ timeZone: tz, timeZoneName: 'longOffset' }` — gives correct offset including DST transitions and historical-rule changes (e.g. Indiana pre-2006 no-DST, Russia post-2014 permanent UTC+3, US summer DST). As of v0.5.2, stored legacy country-code profiles first resolve the country/zone code to a representative IANA timezone, then use the same `computeRising` path as fresh city profiles. Legacy `getRisingSign(..., utcOffsetMinutes, ...)` is retained only as a low-level fixed-offset helper; both APIs are polar-safe.

Polar latitudes (|lat| > 66.5°, strictly past the polar circles) return `null` from both rising APIs. The horizon-plane geometry degenerates at the poles and a rising sign is not astrologically meaningful inside the polar circle. The UI surfaces "rising unavailable at this latitude" at city-selection time when |lat| > 66.5°. Boundary cases (|lat| == 66.5°) return a valid sign — the rule is strict greater-than.

Pre-1970 dates: `Intl.DateTimeFormat`'s historical-tz coverage falls back to LMT (Local Mean Time) approximation for some timezones in pre-tzdata-coverage periods. This is acknowledged as a precision tradeoff; the about-modal disclosure copy lands at v0.2.8 (calculator-framing rewrite cycle).

**v0.43 coherence note (B1).** The about-modal disclosure copy named above in the future tense ("lands at v0.2.8") shipped long ago — the calculator-framing about-modal copy is pinned on every push by `tests/payments_markup.test.js` (its about-modal "disclosure copy" suite). Read it as past-tense (shipped); the LMT-precision tradeoff it discloses is unchanged. Per L17 the original wording is preserved verbatim above.

The v0.14 wording (fixed UTC offsets per country entry; DST and historical-tz changes "out of scope") is retired by this amendment. The catalog-driver rule (rising surface-only, never entering `getCard` or `resolveBracket`) is unchanged.

**§1.B. Numerology surface — text triplet, calc reserved.**

As of v0.2.3 the result-card numerology surface renders as a three-number text line: life path, expression/name number, soul urge. Values are always space-separated for readability (e.g. `3 8 3`, `3 11 3`). **v0.23 amendment (CiC Fix A): the v0.22 hasMaster-branched concatenation (`383` for all-single-digit, spaced when any master present) is retired — the concatenated form read cognitively as a single three-digit number rather than three independent coordinates.** Codified by `tests/numerology_display.test.js`.

The additional calc fields personality (consonants only), birthday (day-of-month reduced with master preservation), and maturity (life-path sum plus expression/name-number sum, reduced with master preservation) remain on `buildProfile` as data-only — computed but not surfaced. They are reserved for v0.3.0+ paid surfacing per §1.

Catalog driver (§1) and rising semantics (§1.A) are unchanged. v0.2.3 reverts the v0.2.2 hexagon surface; this is breaking-on-render and additive-on-data. Calc-version remains v1.

**v0.43 coherence amendment (B2 + B3).** Two clauses above describe superseded states; per L17 the original wording is preserved verbatim and superseded here:
- **Numerology surface — now a name-derived pair, not a triplet (B2).** The "three-number text line (life path, expression/name number, soul urge)" describes the pre-v0.38 surface. §1.D v0.38 split `lifePath` (DOB-derived) onto the free surface, leaving the t1 numerology a name-derived **pair** (expression/name number + soul urge). #48 noted this under the §1.D *table*; §1.B itself is updated here. The always-space-separated invariant (CI-pinned by `tests/numerology_display.test.js`) is unchanged.
- **Personality / birthday / maturity — surfaced at t2, no longer "reserved" (B3).** Those three fields, described above as "reserved for v0.3.0+ paid surfacing," were surfaced at tier **t2** by §1.D v0.6.0 — the reservation's release condition fired and the clause is updated here to record it. They remain surface-only (no catalog/bracket effect) per §1.D.

**v0.54 nine-number amendment (calc v3, controller correction, STAGED 2026-07-20).** Active Pythagorean numerology has exactly nine terminal values: `1` through `9`. Life path, expression/name number, soul urge, personality, birthday, and maturity all use repeated digit-sum until a single digit remains; historical master stops are retired, so `11 → 2`, `22 → 4`, and `33 → 6`. Maturity continues to combine the already-reduced life-path and name-number components before its final reduction. The unreduced `*Sum` fields remain available as calculation trails, but they are not coordinate values. When a name supplies no contributing letters to a subset (no standard vowel for soul urge or no consonant for personality), that coordinate is unresolved (`null` in core, `—` on the entitled sheet), never the tenth value `0` and never a fabricated 1–9 meaning; maturity is likewise unresolved if its required name number is absent. Every resolved numerology coordinate is interpreted through the same nine-entry meaning registry in its own role beside two other numerology coordinates; the context panel labels that integrated layer `with the other numbers`. This amendment supersedes earlier active-surface references to preserved masters while leaving the historical v1 content files immutable.

**v0.62 master-preserving amendment (calc v4, controller decision, STAGED 2026-07-31).** The v0.54 nine-number amendment above is **superseded**; its text stands as lineage per L17. Active Pythagorean numerology has exactly **twelve** terminal values: `1` through `9` plus the master stops `11`, `22` and `33`. Life path, expression/name number, soul urge, personality, birthday and maturity all reduce by repeated digit sum **while the value is above 9, stopping immediately at a master stop** — so `29 → 11` and `38 → 11`, while `44 → 8` (44 is not a stop and its digit sum is already single). Maturity is unchanged in method: it reduces each required component first, adds the canonical components, and reduces the result under the same rule — which means it can itself land on a master, as `22 + 11 = 33` does. The unreduced `*Sum` fields remain calculation trails, not coordinate values, and a master must never live only in a trail: it is the coordinate. The v0.54 unresolved guard **survives verbatim** — an absent contributing letter class is `null` in core and `—` on the entitled sheet, never the tenth value `0` — and it is the only part of the nine-number cut that outlives it. Every one of the twelve resolves a registered meaning through `content/meanings.v3.js` without fallback; the master entries are the immutable `content/meanings.v1.js` records, reused byte-for-byte and never re-authored. Persisted current and saved profiles need no migration: they store reconstruction inputs, not derived outputs, so reopening recomputes under calc v4.

**Why, on the controller's word.** This is a product and commercial decision, not a calculation correction: `11`, `22` and `33` are part of what the product sells, and the operator ruled *"master numbers sell so keep them."* It was forced by a truth defect rather than a preference — the locked X-offer specimen shows `11` and `22` while the shipped calculator reduced them, and the 2026-07-31 Codex preflight (`audits/codex_x_offer_preflight_2026-07-31_response.md`) returned STOP on exactly that mismatch. The repair the controller refused is the cheap one: deleting the masters from the creative. The product is made truthful instead.

**What v0.54 got right, and is not being un-learned.** Two of its rules survive this reversal because they were never about the domain's size: the unresolved-not-zero guard above, and the versioning discipline that kept the historical content files immutable through the narrowing — which is precisely why widening again costs a new active file and no edit to a shipped one.

**§1.C. Tarot birth card — surface coordinate, not a driver (v0.5.0).**

As of v0.5.0 the result card surfaces a tarot birth card: a Major Arcana coordinate computed from the DOB alone, rendered as roman numeral + lowercase name (e.g. `XXI · the world`). It is the **lead row** of the free card, above the five-element coordinate. Computed in `core/birthcard.js` (`getBirthCard`), carried as the additive `birthCard` field on `buildProfile`.

This is a **free-surface coordinate**, inside the §1 symbols-only invariant — a labeled coordinate (numeral + arcana name), never interpretation. No per-card meaning text, no per-arcana explanation; that register stays the paid card-content layer (§1 v0.22). **Superseded in part by §1.G (v0.44):** this coordinate (and sun / public animal / life path, the other three free-tier VALUES) may now surface a static, tradition-cited meaning entry on tap — citation of what a named tradition says, not the interpretation this clause originally reserved for the paid layer. The distinction §1.G draws is citation-vs-interpretation, not free-vs-paid; the paid card-content layer (name/type/habit/note for the compound catalog reading) remains untouched and is not given away by §1.G. The clause adds a coordinate to the visual surface in the same way §1.A's rising sign does — it does **not** enter `getCard`, `resolveBracket`, or any catalog computation; the catalog driver remains `(sunSign, animal)` per §1.

Reduction tradition is **digit-sum**: sum every digit of the DOB, reduce by repeated digit-sum until ≤ 22. This is distinct from the life-path reduction in `core/profile.js` (which preserves the master stops `11 / 22 / 33` per §1.B v0.62 / §3 calc v4 — the intervening wording "floors at a single digit `1..9`" was the v0.54 nine-number contract and is preserved here as lineage per L17, superseded when calc v4 restored the masters; calc v3 rewrote this same parenthetical in lockstep with its own calculation change, which is why calc v4 does too); the birth-card reduction floors at ≤ 22 because the target range is the 22 Major Arcana, and **that contract is untouched by calc v4** — `22 → 0 · the fool` still holds. A reduced value of 22 maps to `0 · the fool`, guaranteeing every DOB resolves to a real arcana (the out-of-range `XXII` never renders). Both choices are operator-locked (see journal); codified by `tests/birthcard.test.js`.

Because it is a free coordinate, the birth card is included in the §5.D share surface — the shared PNG and the on-screen free card carry the same five coordinates. It carries no PII (numeral + arcana name only, no name/DOB) and no paid-content layer, so the §5.D invariants (a)/(b) hold unchanged.

**v0.36 amendment (v0.6.0).** The "same five coordinates" count in the paragraph above describes the v0.5.0 free surface and is preserved verbatim as lineage per L17. As of §1 v0.36 / §1.D the free card carries four coordinates and the shared PNG matches the card at the user's current render tier (§5.D v0.36). The birth card itself is unchanged by the tier ladder: it remains the lead coordinate of every tier's card, including free, and stays inside the §5.D share surface at every tier.

**§1.D. Tier ladder — coordinate density, surface-only (v0.6.0).**

As of v0.6.0 the paid surface is a three-rung tier ladder that reveals progressively more of the coordinate sheet. The ladder is operator-locked (2026-06-11):

| Tier | Param | Price | Card contents (cumulative) |
|---|---|---|---|
| **free** | — | $0 | birth card · sun (no rising) · public animal (year) · catalog numeral — 4 coordinates |
| **t1** | `?paid=t1` | $3 | + rising (when computable) · five-element · private animal (month) · numerology triplet (life path / expression / soul urge) |
| **t2** | `?paid=t2` | $6 | + personality · birthday · maturity · day pillar (animal + stem-element) |
| **t3** | `?paid=t3` | $9 | + hour pillar (animal + stem-element; four pillars complete) · the written 144-card entry (name/type/habit/bracket note) as ceiling bonus |

**Table currency (per L17 — the table above is operator-locked 2026-06-11 and preserved verbatim).** Two cells are superseded by later amendments: the **free** row is now **five** coordinates — `life path` joined free at v0.38 (the DOB-derived numerology split, amendment below) — and the **t1** `numerology triplet` is now the name-derived **pair** (expression/name number + soul urge), the life path having moved to free. Since v0.37 the card renders the full sheet at every tier as a compartment specimen sheet; sealed (higher-tier) cells carry no value. The **Price** column is superseded by the v0.55 ownership amendment (below): the rungs repriced 2026-07-26 to **t1 · $1 / t2 · $2 / t3 · $3** (Gumroad live; product IDs and `?paid=` params unchanged). The coordinate composition of every row is NOT superseded — the density ladder is unchanged.

Clauses, mirroring the §1.A / §1.B / §1.C surface-clause pattern:

- **Surface-only.** Tier coordinates add to the visual surface the way §1.A's rising sign does — none of them enters `getCard`, `resolveBracket`, or any catalog computation. The catalog driver stays `(sunSign, animal)` per §1; day and hour pillars never enter the catalog.
- **Stored tier.** `eight_ball_tier_v1` (§5 allow-list) records the highest rung purchased; absent = free. Monotonic: `applyPaidReturn` sets tier = max(current, purchased) by ladder order — never downgrades. An upgrade purchase raises the tier and adds credits like any paid return.
- **Density rule (single, total — v0.6.0 remediation R1).** Render density = f(stored tier, credit state), resolved at render time by one helper (`getRenderTier` in `ui/payments.js`, delegating to the pure `resolveRenderTier` in `core/payments.js`) — never a function of boot circumstance or shake action. Cold-boot rehydration, same-card re-shake, same-pair re-submit, free-try renders, paid reads, and paid-return boots all render the device's entitled tier. Free devices (no tier, no credits) render the free card.
- **Legacy grandfather (v0.6.0 remediation R2).** Credits present with no stored tier are the pre-v0.6.0 purchase shape — that product sold the written-entry unlock, which now lives at t3 — so the device resolves to t3 and the tier is persisted on first detection. Deterministic, total, never downgrades.
- **Credits unchanged.** Every paid return (any rung) grants +3 credits via the existing `applyPaidReturn` path; tries/credits semantics, pending-profile mechanic, and β-idempotent re-shake are unchanged from v0.22. Credits and the §4.B tries cap govern how many readings; the stored tier alone governs how much sheet each one shows.
- **t3 carries the card-content unlock.** The §1 v0.22 unlocked card-content layer relocates to t3 — the written entry is the ceiling bonus, not a per-credit unlock.
- **Rollback flag.** Free-tier composition is defined by the exported `TIER_COORDS` render constant in `ui/tiers.js`; reverting free to the pre-v0.6.0 surface is a one-line change plus a test update, not surgery.
- **Element systems not reconciled.** The free/t1 five-element stays the simplified year element (§3 calc v2); the day/hour stem-elements are NEW pillars labeled as such ("day pillar", "hour pillar") in the §2 clinical register. No contradiction surface, no §3 calc-version bump.

**v0.37 amendment (v0.7.0) — compartment render.** The card renders the full coordinate sheet at every tier. Coordinates above the device's render tier appear as sealed compartments: row, label, and cell structure visible; the coordinate value absent from the DOM — absent, not hidden. The free-surface invariant is unchanged in substance: the free render carries exactly four coordinate VALUES per the §1.D table; v0.7.0 adds the visible sealed structure of the remaining sheet. Sealed ≠ unresolvable: a paywalled field renders the seal treatment; a field that cannot resolve from given inputs (rising without birth time/place; hour pillar without birth time) renders the `—` empty field at any entitled tier. The written 144-card entry remains a t3-only unlock; its sealed block carries no entry text at any lower tier.

**v0.38 amendment (v0.7.x) — free life-path split (override #3, controller-authorized 2026-06-12).** The §1.D numerology coordinate splits by derivation. `lifePath` (DOB-derived) joins the free surface — preserving the free = DOB-only invariant (every free coordinate derives from date of birth alone, no name input) — so the free render carries **five** coordinate VALUES: birth card · sun (bare) · public animal (year) · life path · catalog numeral. The name-derived numerology narrows to the inner **pair** (expression/name number and soul urge) and stays at t1; the t1 "numerology triplet" in the table above is superseded and preserved as lineage per L17 — it is now a pair, the life path having moved to free. Surface-only and catalog-isolated like every §1.D coordinate: life path continues to drive low/mid/high bracket resolution within a catalog cell per §1, never the catalog index `(sunSign, animal)`; `getCard` is unchanged. §5.D: life path being free, the free share-PNG and the on-screen free card carry the same five coordinates — the PNG now includes the life-path numeral. It is a DOB-derived number carrying no name/DOB string, so the §5.D PII invariants (labeled numeral/symbol only; never name or DOB) hold unchanged, the same basis as the birth card and the catalog numeral. Free composition stays the one-line `TIER_COORDS` rollback flag (`ui/tiers.js`): `FREE_COORDS` gains `lifePath`; `CELL_COORD` maps `lifePath → lifePath` (free) while `nameNumber` / `soulUrge → numerology` (t1).

**v0.55 amendment (ownership + open free surface, 2026-07-26).** A rung purchase is **permanent and unlimited**. This supersedes three clauses above: (1) the **Stored tier** bullet's "adds credits like any paid return" — `applyPaidReturn` now performs exactly one write, the monotonic `tier = max(current, purchased)`, plus the pending-profile render; no credits exist to add. (2) The **Density rule** bullet's "f(stored tier, credit state)" — density is now f(stored tier, legacy-credit grandfather signal): a stored tier governs every render; the R2 legacy read is the only surviving credit consultation. (3) The **Credits unchanged** bullet in full — retired. There is no counter, no cap, and no per-read debit anywhere: quantity is unlimited at every tier, free included (§4.B v0.55), and **depth is the only thing money buys**. The **Legacy grandfather (R2)** clause SURVIVES verbatim: legacy credits with no stored tier still resolve to t3, the tier is persisted on first detection, and the credits are ignored forever after — no existing buyer is ever downgraded. The rungs repriced 2026-07-26 to **$1 / $2 / $3** (Gumroad live: rzqezp → `?paid=t1`, neysyv → `?paid=t2`, xjpvp → `?paid=t3` — IDs and delivery links unchanged). The tier table's coordinate composition and the `TIER_COORDS` constant are UNCHANGED: free = 5 of 15 · t1 = 10 · t2 = 14 · t3 = 15 + the written card entry.

**v0.58 amendment (public rung — a fourth rung, t4 · $9, 2026-07-29).** The ladder gains a **fourth** rung, `t4`, param `?paid=t4`, price **$9**. It is an APPEND, and the append is the whole of its mechanical footprint: `TIER_ORDER` becomes `['t1','t2','t3','t4']`, every existing rung keeps its rank and its meaning, every stored tier keeps its entitlement, monotonicity is unchanged, and **no t1/t2/t3 owner is affected in any way**. The **R2 legacy grandfather does NOT follow the top of the ladder** — a device with legacy credits and no stored tier still resolves to `t3`, because that product sold the written entry, not a rung that did not exist when it was bought; `tests/public_surface.test.js` pins this so a later rung cannot silently widen it.

What t4 adds is **not a coordinate**. The coordinate sheet is complete at t3 (15 of 15, plus the written entry); t4 adds a **reading of it** — three ranked domain families, one anti-fit family, and one shape-of-role line, computed offline and deterministically by `core/public.js` from the birth **date alone** (no name, no birth time). Like `cardEntry` it is a BLOCK, not a compartment: it takes no cell in the 14-cell grid and is excluded from the §1.F density census, so the t4 census is byte-identical to t3's. The §1.D v0.37 sealed-DOM rule governs it unchanged — below t4 the block renders its seal with the value nodes empty, absent rather than hidden.

**Three limits this clause states rather than leaves to be found.** (1) The tier's mode-of-work driver is the **life path** — a coordinate the FREE sheet already shows — so nothing in the t4 read is new information; it is a re-reading of open coordinates against fixed tables. Whether that is the right basis for a paid rung is an open design question, recorded at `PUBLIC_TIER_SPEC.md` §6.1. (2) Day-master **strength is the seasonal-state model, month branch only** (旺相休囚死), a documented simplification a practitioner may disagree with. (3) **No cross-model audit has read any of it** — neither the engine (#144, L48 sighting #14) nor this rung. §10 asks for one on a doctrine change; the lane is unavailable and the clearance is an explicit controller override, which is a decision, not a substitute.

**Commercially the rung is not live, and fails closed until it is.** The Gumroad product does not exist; creating it is the controller's action, never an agent's. `T4_PRODUCT_URL` in `ui/payments.js` ships **empty**, and while it is empty the t4 CTA carries no `href` and stays hidden, so no visitor can reach a dead checkout. Filling that constant in is the whole of what makes the rung buyable. The **§4.B v0.56 single-$3-offer sprint is materially untouched** while it runs (to 2026-08-08): the result-rail offer control, the paywall's $3 headline and the t3 product are byte-unchanged, and the only new element is a hidden anchor.

**Price-column reconciliation, stated plainly.** The rung table's Price column was superseded to **$1 / $2 / $3** by the v0.55 ownership amendment. This clause adds a rung at **$9**, which does not fit that run and is not claimed to: the $9 comes from the exposure-radius ladder the commissioning brief describes (private · comparative · public), a framing recorded in **no other tracked file** and one this project's constitution has never adopted. This amendment defines the rung's mechanics and price and takes **no position** on the radius ladder, on re-pricing t1–t3, or on retiring any existing rung. Reconciling the two ladders remains open doctrine work. **Mooted, 2026-07-29, by the v0.60 retirement of this rung (its "the price column stops contradicting itself" paragraph is the record) — marker added 2026-08-31 with the v0.69 reconciliation so the zero-open-reconciliations count is literal on the page; corrected on sighting per the v0.48–v0.61 footer precedent (mechanical edit).** The §2 arcade-toy carve-out survives intact and needs no re-amendment: t4 is a one-time purchase of permanent, unlimited density at that rung — no subscription, no recurring billing, no counter, no clawback.

**v0.59 amendment (public rung — the mode driver moves off the free surface, 2026-07-29).** §1.D v0.58 shipped the t4 read with its mode of work keyed by the **life path** — a coordinate the FREE sheet has carried since §1.D v0.38. That clause named the problem in its own text ("nothing in the t4 read is new information; it is a re-reading of open coordinates") and shipped anyway on controller word. This amendment closes it: the mode is now keyed by the **birthday** number (day of the month reduced by the §1.B v0.54 nine-number rule), a **t2** coordinate.

Three properties make it a fix rather than a substitution. It is **date-only**, so the tier's input contract is unchanged and no birth time is introduced. Its **domain is exactly 1..9**, so the authored nine-mode table carries over unedited — no copy is re-authored under cover of a mechanical change. And it is **not free**, which is the whole point: a paid rung's driver is now itself paid information.

On the merits rather than the mechanics: §1.G's own meaning registry names the birthday **"the recurring skill"** and the life path **"the long route"**. A mode of work is a recurring skill. The first keying was defensible; this one is apt.

**Content versioning (§4).** `content/public.v1.js` is IMMUTABLE and untouched. `content/public.v2.js` imports every table byte-for-byte and changes exactly two things: the mode provenance string (which named the wrong coordinate), and the modes' self-naming `lifePath` field, re-derived as `birthday` — a table whose own field lies about what keys it is the drift §4's versioning rule exists to prevent.

**What this does NOT change.** The ladder, the price, the entitlement, the census, the sealed-DOM rule, the fail-closed offer, and every other rung: all unchanged. `T4_PRODUCT_URL` remains empty and the rung remains unbuyable, so **no purchaser has ever received the superseded keying** — this correction reaches nobody's paid reading, which is precisely why it was worth making now rather than after a sale. The two limits §1.D v0.58 recorded and this amendment does not close: strength is still the month-branch seasonal-state model, and no cross-model audit has read the engine, the rung, or this clause.

**v0.60 amendment (the public rung is retired; the read folds into t3, 2026-07-29).** §1.D v0.58 added a fourth rung `t4` at **$9**; §1.D v0.59 corrected its driver. This clause **retires the rung**. `TIER_ORDER` returns to `['t1','t2','t3']`, the `?paid=t4` param is no longer a tier, and the public read — three ranked domain families, one anti-fit, one shape-of-role line — becomes a **t3 ceiling block** alongside the written card entry. It is still a BLOCK, not a coordinate, so the §1.F census is unmoved: t3 remains 15 of 15.

**Why, on the controller's ruling.** Three reasons, recorded because the rung was defended in this document two clauses ago. **(1) The tier was named for an exposure radius it never delivered.** Its commissioning brief called it *"you + the world; work is its legible instance"* — but the block renders privately, is absent from the §5.D share surface, and cannot be shown to anyone. What shipped was a depth increment priced as a radius increment. **(2) The price ordering could not be defended.** t3 sells fifteen coordinates plus a written 144-card entry for $3; t4 sold five derived strings for $9, computing **no new coordinate** — it is a lookup over values the t3 buyer already sees. **(3) There are no sales to build on.** The revenue-reset diagnosis on record is that the payment path *"works and has zero outside orders"*; adding a rung above a price that has never converted optimizes the end that is not binding.

**Nobody loses anything, and that took explicit work.** The rung was never buyable, but its unsigned `?paid=t4` return was live (§5.C), so devices can hold a stored `'t4'`. A retired rung therefore resolves to the rung that absorbed it — `RETIRED_TIERS = { t4: 't3' }` in `core/payments.js` — and the UI persists that rewrite on first detection, the same shape as the §1.D R2 legacy grandfather. **The case this protects is not hypothetical:** the stored tier is the only record of a purchase, so a genuine t3 buyer who opened the t4 URL once would otherwise be dropped to **free** by the retirement. `tests/public_surface.test.js` pins the migration and that case by name.

**The price column stops contradicting itself.** §1.D v0.58 put a $9 rung in this clause while the Price column above read **$1 / $2 / $3** — a contradiction v0.58 named and did not resolve. With the rung retired, the column is correct again and the exposure-radius ladder the brief describes remains, as before, adopted by no clause of this constitution.

**What survives from v0.58/v0.59.** The engine (`core/public.js`), its tables (`content/public.v1.js` + `v2.js`), the render module (`ui/public.js`), the sealed block and its §1.D v0.37 treatment, and the birthday keying. Only the rung, its price and its unopened offer are withdrawn. §4.B v0.56's single-$3 sprint is byte-untouched and is again the only purchase surface.

**v0.62 amendment (calc v4 — the public read's mode driver keeps its master value, and reaches its table through a declared bridge).** §1.D v0.59 moved the t3 public read's mode of work onto the **birthday** number and rested on one property: *"its domain is exactly 1..9, so the authored nine-mode table carries over unedited."* Calc v4 (§1.B v0.62) restores the master stops, so that property is gone — a birthday of `11` or `22` is a real coordinate value and the mode table has nine entries. (`33` is not reachable from a day of the month; the bridge covers it anyway so the mapping is total over the domain rather than over what happens to be reachable.)

**The two wrong fixes, named so they are not re-proposed.** Reducing the birthday before keying the mode would put `11` on the sheet while the reading silently treated it as `2` — the truth defect this whole cycle repairs, reintroduced one clause later. Authoring three master work modes would be new paid copy shipped under cover of a calculation change, which is not what a calculation change is for.

**What ships instead.** The birthday coordinate keeps its master value everywhere it is shown and everywhere it is reported. Only the TABLE LOOKUP is bridged, through the declared `MASTER_MODE_BRIDGE` in `content/public.v3.js` — `11→2`, `22→4`, `33→6`, the same three links the immutable v1 Concordance registry files, and no other mapping. **The bridge is disclosed in the returned reading, not applied silently**: `mode.birthday` carries the master, `mode.modeKey` carries the table key actually read, `mode.bridged` is a boolean, and `mode.bridgeNote` states the substitution in prose. `PUBLIC_SOURCES.mode` names the bridge rather than a reduction rule that no longer runs on this coordinate. `tests/public.test.js` pins all of it, and pins that `WORK_MODES` still has exactly nine entries — so a tenth-through-twelfth appearing there is a CI failure, not an unreviewed piece of paid copy.

**The disclosure REACHES THE READER, and saying so cost a refused audit.** The paragraph above was true and insufficient, and this clause records that rather than quietly widening. The first implementation disclosed the bridge in the object `core/public.js` returns and stopped there: the sole production formatter (`formatPublicRead` in `ui/public.js`) emitted three strings and discarded `mode.bridgeNote`, so a reader whose sheet showed birthday `11` received mode-`2` copy with nothing connecting the two — **the silent substitution this very clause names as the wrong fix, recreated one layer down at the render surface.** A Codex pre-merge audit refused the branch on it (P1, 2026-07-31, `audits/codex_calc_v4_premerge_audit_2026-07-31_response.md`), and it survived a green suite because every existing surface test used a non-master birthday. The block now carries a **fourth line**: the engine's own `bridgeNote`, empty for an unbridged reading. It is rendered on the **host sheet and on BOTH dyad sheets** — a second person with a master birthday is exactly as entitled to the disclosure as the first — and it is cleared on the sealed branch and on a subsequent unbridged render, so no entitled string and no stale note survives (§1.D v0.37). `index.html` is untouched: `ui/public.js` injects the node and its scoped CSS at init in the §6 v0.23 DI shape `ui/meanings.js` and `ui/dyad.js` already use, and the host's four-id boot wiring is byte-unchanged. The node collapses on `:empty` rather than the `hidden` attribute, deliberately — this repo has a logged case of an author `display:` rule beating the UA `[hidden]` rule and shipping a "hidden" control visible in production. **`content/public.v1.js` and `content/public.v2.js` are IMMUTABLE and untouched**; `content/public.v3.js` imports every table byte-for-byte and adds only the bridge, its note template, and the corrected provenance string. Nothing else about the block moves: it is still a t3 ceiling BLOCK, still absent from the §1.F census, still governed by the §1.D v0.37 sealed-DOM rule, and `ui/public.js` renders the same three strings it rendered before.

**v0.61 amendment (the dyad rung — a fourth rung, `t5` · $6, 2026-07-30).** The ladder gains a fourth rung, **`t5`**, param `?paid=t5`, price **$6**. `TIER_ORDER` becomes `['t1','t2','t3','t5']`. It is an APPEND and the append is the whole of its mechanical footprint on the existing rungs: every one keeps its rank and its meaning, every stored tier keeps its entitlement, monotonicity is unchanged, and **no t1/t2/t3 owner is affected in any way**. The **R2 legacy grandfather does NOT follow the top of the ladder** — a device with legacy credits and no stored tier still resolves to `t3`, because that product sold the written entry and never a second person's sheet; `tests/dyad_surface.test.js` pins this beside the pin §1.D v0.58 left for the same reason.

**The gap at `t4` is load-bearing, not cosmetic.** The obvious token for a fourth rung is `t4`, and taking it would have been a silent, unrecoverable defect. `t4` was the public rung for part of 2026-07-29 (§1.D v0.58) and was retired the same day (v0.60), which left `RETIRED_TIERS = { t4: 't3' }` and devices that can hold a stored `'t4'` forever from the unsigned `?paid=` return. A dyad shipped as `t4` would put the same key in `TIER_ORDER` **and** in `RETIRED_TIERS`, so `normalizeTier` would rewrite every paying dyad device to `t3` before `isTier` ever saw it — **paying customers silently collapsed to the rung beneath the one they bought.** `t5` was never live as an unsigned return, so it is clean. The invariant is now derived rather than remembered: `RETIREMENT_COLLISIONS` in `core/payments.js` is computed from both tables at module load and pinned empty, so a future rung cannot reuse a retired token without failing CI. **Rank is ladder POSITION, never the digit in the token** — `t5` ranks 4 — and that too is pinned.

**What t5 adds is a relation, not a coordinate.** The coordinate sheet is complete at t3 (15 of 15, plus the written entry and the public read). t5 buys a **second complete sheet** plus a **relation layer** between the two, computed offline and deterministically by `core/dyad.js` from the two calculated profiles alone. Three axes, each a lookup or a join over frozen tables in `content/dyad.v1.js`: the directed five-element relation between the two **day masters**, one authored passage per direction (A→B and B→A are separate entries, never one sentence read twice); the **combined life path**, the digit-reduced sum of the two, whose number meaning is read from the existing nine-entry registry rather than re-authored; and the **card pair**, read across the two registers the four-slot grammar exposes to a pair — the year branches through the same five named branch families §1.I already uses, and the life-path brackets as an ordered pair against the deck's own arrival → construction → command arc. Like `cardEntry` and `publicRead` it is a **BLOCK**, not a compartment: it takes no cell in the 14-cell grid and is excluded from the §1.F density census, so the t5 census is byte-identical to t3's. The §1.D v0.37 sealed-DOM rule governs it unchanged — below t5 the block renders its seal with the value nodes empty, absent rather than hidden.

**The relation layer computes no new coordinate for either person.** The day master, the life path, the catalog index and the bracket are all values the single sheet already carries; what is new is the relation between two of them. That is the same limit §1.D v0.58 recorded against itself and it is recorded here rather than left to be found — with the difference that the second sheet IS new information, because it belongs to a second person the device has never read.

**Commercially the rung is not live, and fails closed until it is.** The Gumroad product does not exist; creating it is the controller's action, never an agent's (§10). `T5_PRODUCT_URL` in `ui/dyad.js` ships **empty**, and while it is empty the CTA carries no `href` and stays hidden, so no visitor can reach a dead checkout. **Filling that constant in alone does not make the rung buyable** (corrected post-`t5` audit, PR #187 R6 — a prior draft made the entry control's visibility react to this constant with no click path behind it, a dead button waiting to be exposed the moment the URL was set). `dyadEntryVisible` is entitlement-only and does not consult this constant; a real below-t5 offer needs its own coherent click path designed and shipped together with whatever control triggers it. The **§4.B v0.56 single-$3 sprint is byte-untouched** while it runs (to 2026-08-08): the paywall, its $3 headline, the result-rail offer control and the t3 product are unchanged, and `index.html` still contains exactly one Gumroad URL. **Price-column reconciliation, stated plainly:** the rung table's Price column reads $1 / $2 / $3 per v0.55; this clause adds a rung at **$6**, which does not fit that run and is not claimed to. The $6 comes from the entity-count ladder the commissioning brief describes (one person · two people), a framing this constitution has not adopted and takes no position on here. Reconciling the two ladders remains open doctrine work. **Resolved, 2026-08-31 — the v0.69 price-column reconciliation (appended after the v0.67 activation amendment) derives the $6 from the column's own unit — two complete sheets at the v0.55 $3 — and leaves the brief's ladder unadopted; the open-work sentence stands as the record of the interval, corrected on sighting per the v0.48–v0.61 footer precedent (mechanical edit).** The §2 arcade-toy carve-out survives intact and needs no re-amendment: t5 is a one-time purchase of permanent, unlimited density at that rung — no subscription, no recurring billing, no counter, no clawback.

**Corrected pre-merge on an independent audit (PR #187, 2026-07-30).** The first implementation of this clause was refused by a Codex pre-merge audit with six P1 findings, and three of them were contradictions between this document and the code shipped under it rather than defects in the code alone. They are named here because a clause that was wrong once should say so: the entry control and person B's sheet were reachable **below t5** while this clause said t5 buys them; the relation engine **recomputed** the day master while §1.J said it recomputes nothing; and the combined-life-path meaning was **re-authored** in `content/dyad.v1.js` while §1.J said it comes from the existing registry. All three are closed in code, and the sentences below now describe what ships. The audit's own record is `audits/codex_pr187_premerge_audit_2026-07-30_response.md`.

**Entitlement is all-or-nothing.** There is no partial dyad. Below t5 the entry control is absent, the screen refuses to open, a submission is refused, and the render produces nothing — four independent gates on one predicate (`dyadEntitled`), because the previous version's single gate on the relation passages left the whole second sheet open. A t3 device — which owns the complete SINGLE sheet, the written entry and the public read — receives no second sheet at all. That is the boundary the rung is priced on.

**Two limits this clause states rather than leaves to be found.** (1) The two dyad sheets are built by `ui/sheet.js`, a second DOM writer over the SAME pure value mapping (`cellRenderState`) and the same tier constants the host sheet renders through, rather than a second live instance of `ui/tiers.js`. That module is a singleton imported by 14 of the 51 test files, two of which assert on its source layout; converting it would have meant rewriting protected tests to fit a refactor, and the audit packet requires that existing single-reading behaviour remain unchanged — which a new module cannot regress and a rewrite of the shipped path can. The equivalence is pinned by a bounded differential test — one profile per life-path facet-anchor group, across every tier, every coordinate cell against a REAL `renderTierSections` host render (not a second call to the shared `cellRenderState` function standing in for it), plus the written-entry and public-read blocks each side varies independently of the cell grid (corrected post-`t5` audit, PR #187 R3 — the first version of this claim named "every profile" over a profile space that name-derived numerology makes unenumerable, and its own suite never actually drove `renderTierSections`, so it could not have caught R2's written-entry regression). The cost of the bound is named: sheets built there carry no element `id`, so they are deliberately **not tappable** (the §1.D v0.52 interaction clause covers the host sheet only) — emitting the host's ids would make `ui/meanings.js`'s `getElementById` lookups ambiguous and break the host's own panel. (2) **A fresh independent Codex re-audit has cleared the corrected branch.** The audit cited above read the previous implementation and returned DO NOT MERGE; a second, independent Codex re-audit read `e34458a` and returned SAFE TO MERGE, recorded at `audits/codex_pr187_premerge_reaudit_2026-07-30_response.md`. Nothing in that record makes the t5 checkout live — the Gumroad product still does not exist and `T5_PRODUCT_URL` still ships empty.

**v0.63 amendment (the kua block — the first gender-keyed surface, controller override, 2026-08-03).** The t3 ceiling gains a third BLOCK, `kuaRead`: the Eight Mansions personal trigram (Ba Zhai Ming Jing), computed by `core/kua.js` from the SOLAR birth year and the profile's optional `gender`. The solar-year boundary is Li Chun from `core/calendar.js`'s authority-pinned term table, at-or-after — deliberately not a fixed Feb-4 (Li Chun 2021 is Feb 3, and the test suite carries that discriminating case). Like `cardEntry` and `publicRead` the block takes no cell in the 14-cell grid and is excluded from the §1.F census, so t3 remains 15 of 15; the §1.D v0.37 sealed-DOM rule governs it unchanged; and it joins t3's coordinate list, so the t5 append carries it onto both dyad sheets via a handed-in read. **— The block's render was retired 2026-09-01 by the §1.D v0.70 amendment below; the registries, disclosures and scrubs this lineage defined survive as stated there.**

**Gender is the first non-DOB demographic input, and its rules are stated here rather than left to be found.** (1) It is OPTIONAL, forever: the control defaults to empty, an off-vocabulary value is dropped at every write seam (strict `'male' | 'female'`), and no surface may require it. (2) It never touches the free surface: the free = DOB-only invariant (§1.D v0.38) is untouched because, like the name input, the field exists on the form but feeds only a sealed paid block. (3) A profile WITHOUT gender renders BOTH classical values, labeled, with a register note naming the method's gender dependence — never a silent single-gender default. (4) The formula's raw 5 has no trigram and files male→2 / female→8; the remap is RETURNED by the engine (`{number, remapped}`) and rendered as a note — the §1.B v0.62 anti-silent-substitution rule applied to this coordinate. (5) A competing modern school changes the constants for post-2000 births (male 9−S / female 6+S); the product ships the single continuous rule (male 11−S / female 4+S over the solar-year digit sum, arithmetically identical to the classical 19xx statements and pinned so by a century sweep) and names the fork in `content/kua.v1.js`'s provenance. **The two wrong fixes, named so they are not re-proposed:** carrying both schools as a switchable table would put two values behind one coordinate and re-open the §1.B v0.62 truth defect; writing the tradition's directional guidance would ship oracle register.

**v0.64 amendment (the gender ask is retired, controller decision, 2026-08-30).** The optional gender input is removed from every surface — the primary form, the dyad's second-person entry, storage, and the recompute paths (PR #208, squash-merged as `4c99ab4`). The kua block STAYS at t3 and now has exactly ONE read for every profile: the both-classical-values render that rule (3) above specified as the no-gender mode — both values labeled, the method's gender dependence named in the register note. Superseded with this: the v0.63 computation clause above ("computed by `core/kua.js` from the SOLAR birth year and the profile's optional `gender`") — the engine input is now the solar birth year alone, both gender variants computed and rendered — and rules (1)–(3): there is no control to be optional, no write seam to guard, and the both-values render is the contract rather than the fallback. The Li Chun solar-year boundary and everything else in that clause stand. Rules (4) (the disclosed 5→2/5→8 remap) and (5) (the single continuous formula and its named post-2000 fork) stand unchanged, and `core/kua.js` keeps the gendered arithmetic as the method registry, consumed only through `getKuaBoth`. Stale `gender` tokens from the v0.63 cycle are actively erased at boot from all three §5 keys by read-verified single-key scrubs whose only-the-gender-key contract is mutation-pinned. One named residue: the §1.G-shape citation body (`KUA_TRIGRAMS[n].body`) lost its only renderer with the single-gender read — whether the both-values block should carry the citation prose or the removal stands recorded is an open product call (PR #208 audit, F4), stated here so the reduction is not silent. **Resolved, 2026-08-30, same date — the controller chose RENDER: PR #210 (squash-merged `d4de8aa`) ships each value's citation body byte-equal from the immutable registry on the host block and both dyad sheets, once-only when the two values coincide; the clause above stands as the record of the interval, corrected on sighting per the v0.48–v0.61 footer precedent (mechanical edit; review record in this correction's own L48 artifact, resolution record in `audits/claude_relay_pr210_premerge_audit_2026-08-30_response.md`).** §10 provenance, stated precisely: the underlying PRODUCT change was audited pre-merge by the in-container relay at `audits/claude_relay_pr208_premerge_audit_2026-08-30_response.md` — three Claude-family lanes with an author-reconciler, a composition that artifact weights itself, reconciled MERGE WITH FIXES with all six fix-class findings landed pre-merge. That relay's pen did not reach constitution text (its F5 prescribed this amendment without writing it), so this AMENDMENT's own §10 read is the separate two-lane in-container review recorded in this PR's L48 artifact. **— Superseded as a render 2026-09-01: the both-values contract ended when the block left the t3 ceiling (§1.D v0.70 amendment below); the boot scrubs and registry rules stated here stand.**

**Register law: registry facts only.** Eight Mansions' traditional use of the trigram filing is directional guidance — auspicious and inauspicious orientations — which is oracle register and permanently out per §12. `content/kua.v1.js` carries citation entries in the §1.G shape (what the named tradition FILES: trigram, glyph, element, seat direction, east/west group), and `tests/kua_content.test.js` pins the no-guidance register mechanically alongside the voice scans. The block is not tappable, matching `publicRead`'s recorded limit.

**Process note, recorded because §13 requires the trigger to be named.** Product-surface work is frozen on the reach-bottleneck finding; this feature ships as a **controller override** — the operator's explicit 2026-08-03 decision chose the vehicle (kua), the tier (t3 block), and the process (override + cross-model audit before merge) — per the §1.G v0.44 precedent; override authority not re-litigated. §8 gate 6 / §10 cross-model audit precedes merge; the Codex lane is parked, so the audit runs as an operator relay per the PR #198 precedent.

**v0.70 amendment (the kua block is retired from the render, controller decision, 2026-09-01).** The controller, reviewing the live t3 read, ordered the block removed; the stated ground is screen space — six rendered lines (two labeled values, two citation bodies, the register note, the title) on an already long sheet. The order followed and supersedes a same-day keep-as-is answer given minutes earlier, before the controller re-reviewed against the live render; both are on the journal record. What changes: `kuaRead` leaves the t3 coordinate list (the ceiling returns to two blocks — the written entry and the public read), the renderer `ui/kua.js` is deleted, the host card face, both dyad sheets and the standalone specimen sheet carry no kua section, and the paid copy no longer names the kua line. What stays, stated so the reduction is not silent: `content/kua.v1.js` remains in the tree unedited — §4 binds shipped content batches, and a retired render is not a licence to touch one (the §1.D v0.60 t4 retirement is the precedent for a rung or block leaving the render while its tables stand); `core/kua.js` remains as the method registry with its engine suite (the continuous formula, the Li Chun solar-year boundary, the disclosed raw-5 remap, the named post-2000 fork); and the v0.64 boot scrubs for stale gender tokens keep running — the retirement removes a render, not a privacy guarantee. Nothing else in the tier ladder moves: prices, entitlements and the §1.F census are untouched, and no clause may cite this amendment to re-open them. Owner impact, named per the v0.58/v0.60 precedent: a device that bought t3 or t5 while the copy promised the kua line keeps every entitlement and loses the block itself — the render is removed for every tier alike, and this sentence is the record that the reduction reaches existing owners, not only future buyers.

**§1.J. The dyad relation layer — named relations between two readings (v0.61, t5).**

§1.I licensed comparing two browser-local saved readings and bounded it with a **register law**. The dyad is the paid, authored-passage form of the same act, and it is bound by that same law rather than a looser one written for it. §1.I is the controlling precedent; this clause extends it and narrows nothing.

**Register law, extended.** Every dyad passage is a **citation of a named relation between two coordinates**, never an interpretation of two people. It MUST NOT emit a compatibility percentage, harmony score, ranking, prediction, advice, diagnosis, soulmate claim, recommendation, or invented synthesis across traditions — the §1.I list, verbatim — and it adds one rule §1.I did not need: **no person may be the grammatical subject of any sentence.** The subject of every authored line is a branch, an element, a bracket or a number. Both rules are mechanical, not aspirational: `tests/dyad_content.test.js` scans the tables AND the assembled runtime output through the canonical §2/§4 voice tables plus a compatibility-claim scan and a person-subject scan, each with a positive-fire sentinel (the `pii_scan` guard-the-guard pattern) so a scan that could never fail is itself a failure. Where a tradition's own English name would trip the law — 六合 *liuhe* is conventionally rendered "six harmony" — the passage carries the **transliteration**, which is both stricter and more faithful than the gloss.

**The unfiled state is mandatory.** As in §1.I, an unsupported or same-value pair is `unfiled` and **says so in the prose**: the branch tables name no relation for a branch with itself, so the dyad claims none. Fabricating an `adjacent` reading to avoid an empty cell is the failure mode this rule exists to prevent.

**Calculation isolation.** The dyad engine takes **two already-calculated profiles**, never two payloads — the order §1.I fixes for Concordance, and load-bearing here for the same reason. It recomputes no coordinate: the day master is **read off `profile.dayPillar`**, so a supplied coordinate that contradicts the raw date still wins and there is one calculation of record. (The first implementation re-derived it from `yyyy/mm/dd`. It agreed with the supplied value in every ordinary case, which is precisely why a green suite proved nothing and an audit had to find it; "usually identical" is not the contract.) A missing or internally incoherent pillar throws rather than resolving to a plausible wrong relation, and the surface seals the block. The engine does not enter `getCard` or `resolveBracket`'s rule — it calls both, per profile, exactly as the single render does — and returns the two profile objects it was handed, so each side is byte-identical to the standalone reading **by construction**. There is no second calculation path to keep in sync. `core/profile.js`, `tests/fixtures.json` and the catalog driver are untouched; the calc version does not move.

**One entry contract for both forms.** The second-person form takes the same fields as the primary onboarding form — name, date of birth, optional birth time, optional birthplace — and runs the **same validator** (`validateBirthInput` in `ui/profile.js`), not a second implementation of the same rules: trimmed name, a real local-today future gate, and the 1900 floor the calendar tables require. Person B therefore resolves a rising sign when a birthplace is given, which the first implementation could not. `ui/citysearch.js` was converted from a module singleton to a per-instance controller to make that safe: its handlers were module-scope closures over module-scope refs, so a second birthplace field would have silently repointed the **primary** form's listeners and dropped the rising sign from every shipped single reading.

**What renders, and what is cleared.** Each side renders a complete standalone sheet — every coordinate, its own written 144-card entry, its own public read — followed by the relation layer. Closing the screen, or an invalid re-submission, blanks **every** node the render can write, from one enumeration shared with the fill path; `hidden` is not deletion, and the first implementation left person B's first name and fourteen coordinate values in live hidden DOM. Person B's birthplace selection is dropped by the same path, so a stale city cannot hand the next second person someone else's timezone.

**Content and deck boundary.** Static tables live in `content/dyad.v1.js`, immutable and versioned per §4 — a revision ships as `dyad.v2.js`. The wuxing cycles, the branch-relation families and the `recorded, not certified.` qualifier are **imported from** `content/concordance.v1.js`, not redefined; the nine number meanings are the existing registry — the combined-path axis emits `NUMEROLOGY_MEANINGS[n].body` **byte-for-byte** beside a neutral clause that states only the arithmetic of the reduction. (The first implementation authored nine bodies here that restated the registry's own clauses — for 1, "starting a sequence rather than joining one" against the registry's "…joining one already underway" — while also looking the registry up and rendering only the copy. Two sources for one number's meaning is the drift §4's versioning rule exists to prevent.) **No deck string reaches the relation layer** — the t3 written entry is what t3 sells, and a t5 passage that paraphrased or recombined it would sell the ceiling twice. `tests/dyad.test.js` and `tests/content_shape.test.js` both sweep the assembled output against all 864 deck strings for leakage.

**v0.62 amendment (calc v4 — masters are valid dyad input and valid dyad output).** Three sentences of this clause described the retired nine-number contract and are superseded; everything else, including the register law, the unfiled state, the calculation isolation and the privacy boundary, is unchanged and narrowed in no respect. **(1) Input.** Both life paths are validated against the ACTIVE terminal domain (`1..9, 11, 22, 33`) **before they are summed**, so a master enters the relation as itself and is never reduced ahead of validation. A value outside the twelve still throws — `10` and `44` are pinned by name, because a plausible integer that no reduction terminates on is the sharp case a widened domain must still refuse. **(2) Output.** The combined path uses the same master-preserving reducer, so the sum runs `2..66` and can itself land on `11`, `22` or `33` — `9 + 2`, `11 + 11` and `11 + 22` all do. Its meaning is read from the active registry (`content/meanings.v3.js`) byte-for-byte, exactly as before; the registry simply has twelve entries now instead of nine. **(3) The reduction clause selects on `sum !== combined`, not `sum > 9`.** Those were the same test while every terminal value was a single digit and are not the same test now: a sum of `11` that STOPS at `11` would otherwise fire the reduced frame and announce that 11 reduces to 11 — a claim about a reduction that did not happen, which is the exact defect the two-frame split exists to prevent. **Content versioning.** `content/dyad.v1.js` is IMMUTABLE and untouched; the active tables are `content/dyad.v2.js`, which imports all 25 ordered element pairs, the six branch registers, the bracket arc and the nine ordered bracket registers unedited, and changes exactly two strings — the two combined-path frames and the numerology provenance line — each of which named the nine-number rule rather than describing a relation. The bracket axis needs no change: masters resolve to `high` per §1.H v0.62, so the nine ordered bracket registers stay total.

**Privacy boundary (§5 / §5.F).** No account, backend, sync, analytics event, remote database, third-party storage, endpoint, network call, runtime dependency, or calculation-engine change is introduced. **The second person is never persisted**: they live in one module-local binding for the life of the screen and are gone on close and on reload — the §5.F transient shape. `ui/dyad.js` names no `localStorage` key, reads none and writes none, so `tests/privacy_scan.test.js`'s key allow-list is **unchanged**. No dyad share artifact ships: the §5.D surface is the single sheet and is byte-untouched.

**§1.E. Provenance placards — labeled derivation note, surface-only (Coordinate Legibility Pack, v0.40).** Each coordinate may carry a **provenance placard**: a labeled, surface-only note naming the DERIVATION of that coordinate — its method/source (e.g. `digit-sum reduction`, `tropical zodiac`, `ascendant`, `solar term`, `letter-value sum`) — rendered per row in the row's cell order, mirroring the `.coord-title` grammar. The placard is **clinical derivation only, never interpretation** (§2): it names HOW a coordinate is computed, never what it "means." It is **tier-invariant** (a coordinate's derivation does not change with seal state) and **catalog-isolated** like every §1.x surface coordinate (§1 / §1.D): the note is keyed to the coordinate, never to a profile value or the catalog driver `(sunSign, animal)`; `getCard` is unchanged. It adds **no coordinate VALUE** to any surface — the free-surface VALUE count is unchanged (still five per §1.D v0.38) — and carries **no PII**: it names a derivation method/source, never a name, DOB, or derived value. A **sealed** compartment's placard still names the coordinate's derivation (structure/label only, the value absent per the v0.37 compartment contract) — so a hatched cell reads as a real, sourced coordinate withheld at the user's tier, not a rendering failure (the F1-WATCH legibility fix). Visibility reuses the existing symbol-label toggle (`.card.labels-revealed`) — off by default, **no new localStorage key**. The placard is **NOT serialized into the §5.D share artifact**: the share path reads `.coord-val` + `.coord-title` (via `ui/tiers.js shareRowRefs`, which `ui/share.js` consumes as a snapshot), never `.coord-prov`, so the PNG is unchanged. Implementation: `ui/tiers.js` `PROV_NOTE` (coordinate → derivation note) + `provText` (pure, per-row join) + an init-time `.coord-prov` write pass; `index.html` carries the `.coord-prov` CSS gate only.

**§1.F. Legibility surfaces — atlas legend + density census, labeled, surface-only (Coordinate Legibility Pack cuts 2–3, v0.41).** Two further legibility surfaces extend the §1.E pattern; this clause grants them the literal coverage flagged at the cut-2 Codex audit (Hook 7). Both are **surface-only** and **catalog-isolated** (keyed to coordinates or the tier constants, never a profile value or the catalog driver `(sunSign, animal)`; `getCard` unchanged), add **no coordinate VALUE** and **no PII**, and stay in the §2 clinical register (a label/census, never interpretation, never a CTA). **(a) Atlas** — a per-coordinate system-name legend (e.g. `life-path number`, `soul-urge number`) that disambiguates the abbreviated multi-cell row titles; gated by the existing `.card.labels-revealed` toggle (off by default, **no new localStorage key**), rendered in `.coord-atlas` outside `.coord-title` so it is **not serialized into the §5.D PNG**. **(b) Density census** — an **always-on** aggregate count in the result rail, OUTSIDE `#card-face` (so off the §5.D PNG): `N of 15 coordinates open · M sealed at paid tiers` (the 14 sheet cells plus the always-open catalog numeral — base 15, and the free count 5, match the §1.D free-coordinate framing), computed purely from `TIER_COORDS` + `CELL_COORD` via the pure `tierDensitySummary` — never from a profile. It is the on-screen counted form of the §5.D v0.39 `sealed remainder` marker, with **no FOMO/urgency/price** (§2 v0.35 / §5.C). Both retire the F1-WATCH "hatch reads as broken" risk — atlas at the per-cell level, density at the whole-sheet level. §1.E remains the provenance clause; §1.F covers the atlas + density siblings.

**§1.G. Coordinate meanings — static, tradition-cited descriptions (v0.44, controller override).** Four of the five free-tier coordinate VALUES — arcana, sun, public animal, life path — are tappable. Tapping opens a labeled panel carrying a static, hand-authored entry from `content/meanings.v1.js`: a short register tag plus one to two sentences stating what the *named tradition* (tarot / western tropical zodiac / Chinese zodiac / Pythagorean numerology) associates with that specific symbol. This is **citation, not interpretation**: entries name what a tradition says, never what the reading "means" for the person who drew it, never advice, never a prediction — same §2 clinical/anti-oracle boundary as everywhere else in this document, enforced here by `tests/meanings_content.test.js` (BANNED_VOICE_REGISTER + BANNED_PATTERNS scan against every entry, imported from the same canonical lists `tests/profile.test.js` uses against the deck — no duplicated policy table) plus a second-person-address scan (`you`/`your`) with zero tolerance. **The catalog index is deliberately excluded** — the compound `(sunSign, animal)` reading is the paid card entry (name/type/habit/note, §1/§4); a free meaning for the catalog value would give away what t3 sells. **Static, not generated:** no network call, no LLM, no DOB or name leaves the device — a stricter privacy posture than the alternative considered and rejected this cycle (an LLM-narrated derivation-trace prototype, `lab/`, built 2026-06-26 through 07-03, which required a serverless proxy and transmitted DOB-derived strings to an external API for two of its five coordinates; purged 2026-07-04 rather than shipped, tag `archive/interrogation-layer-purged-2026-07-04` preserves it as historical record per L17-adjacent "archive, don't delete" discipline). Implementation: `content/meanings.v1.js` (58 entries: 22 arcana + 12 sun + 12 animal + 12 life path incl. masters 11/22/33), `ui/meanings.js` (`initMeaningsUI(refs)`, DI shape per §6 — injects its own panel markup + scoped CSS at init time rather than touching `index.html`'s static markup/style block, keeping the §6 1500-line single-file budget to a 2-line net footprint there: one import, one init call). **Controller override, not a measured reach fix:** shipped while product features are FROZEN per the reach-bottleneck finding (`~/Desktop/8ball/reach/REACH_CONTROL.md`, 2026-06-24 — real reach remains ~2 orders of magnitude below the ~2–3k tripwire); same override class as the v0.38 free-life-path-split (§13 named-trigger discipline does not bind a controller override, per precedent). **Dissent on record:** the orchestrator's standing recommendation is that product-surface work stay frozen until reach moves, on the reasoning that a confusing paywall/no-visible-counter problem (separately fixed this same week, PR #69) compounds faster at low reach than a new explainer surface helps it; the operator elected to ship this anyway, override authority not re-litigated.

**v0.52 amendment — every coordinate compartment is interactive (controller override, STAGED 2026-07-20).** The v0.44 sentence limiting tap interaction to four free-tier values is superseded at the **interaction layer only**. All fourteen specimen-sheet coordinate compartments are tappable and keyboard-reachable at every tier, including sealed and unresolved compartments; the catalog numeral remains excluded. The four coordinates backed by `content/meanings.v1.js` continue to show their exact versioned tradition-cited entries. Every other resolved coordinate opens a clinical detail containing only its existing system name (`ATLAS_NOTE`) and derivation method (`PROV_NOTE`) plus an explicit statement that no added interpretation is assigned. An unresolved compartment states the additional birth input required to calculate it. A sealed compartment may name its coordinate system, derivation method, and entitlement state, but MUST NOT place the withheld coordinate value anywhere in the DOM or panel. These status/provenance details are not new meanings, do not expand the catalog driver, do not alter tier entitlement, and add no content generation, persistence, network call, PII transmission, share serialization, or engine input. Interaction minimum: native-equivalent button semantics, Enter/Space activation, Escape/close dismissal with focus restored to the invoking cell, visible focus, and a 44px minimum target. Implementation remains inside `ui/meanings.js`; `content/meanings.v1.js`, `core/`, and `tests/fixtures.json` remain unchanged.

**v0.53 amendment — value meaning in the context of the full sheet (controller correction, STAGED 2026-07-20).** The v0.52 provenance-only treatment for the ten newly interactive coordinates is superseded. Every **resolved** sheet coordinate now opens (a) a value-specific meaning and (b) a separately labeled `in this sheet` synthesis that states the role that meaning plays beside two resolved neighboring coordinates. The synthesis is deterministic and finite: it reads only the fourteen already-rendered coordinate value nodes, maps them through versioned static tables, and combines three registered themes through fixed coordinate roles (`frame`, `outward agenda`, `public instinct`, `long route`, and their sibling roles). It MUST NOT introduce advice, prediction, diagnosis, compatibility scoring, certainty, second-person address, a cross-user comparison, or any claim that one tradition scientifically validates another. `content/meanings.v2.js` re-exports the immutable v1 arcana/sun/animal/number tables, adds five static element meanings, grammatical number-theme aliases, and the fourteen fixed context roles; rising/private-animal/numerology coordinates reuse the matching v1 value tables through their own labeled coordinate role, while a resolved day/hour pillar combines its registered animal and element meanings. Unresolved cells state that no value is available to place in context and name the missing input; sealed cells state only that the absent value's meaning cannot yet be read, preserving sealed-value DOM purity. Catalog exclusion, tier entitlement, engine/calculation inputs, share output, persistence, network behavior, and PII boundaries remain unchanged. This correction intentionally changes the panel's job from “how it is derived” to “what this value means here, in harmony with the others.”

**v0.54 meaning-layer amendment.** `content/meanings.v2.js` exposes `NUMEROLOGY_MEANINGS` as the exact active `1..9` subset of immutable `content/meanings.v1.js`; master-number entries remain only in v1 as historical shipped content and are unreachable from the current calculation and UI. For each of the six numerology coordinates, both preferred partners and fallback partners are other numerology coordinates. Other traditions retain the `in this sheet` cross-coordinate context behavior defined at v0.53.

**v0.62 meaning-layer amendment (calc v4).** The first sentence above is superseded; the rest of the clause stands unchanged. The active registry is `content/meanings.v3.js`, which exposes `NUMEROLOGY_MEANINGS` over the **full twelve-value** terminal domain — no longer a subset of v1 but the whole of its life-path table, because narrowing was the only thing v2 did to it. `content/meanings.v1.js` and `content/meanings.v2.js` are IMMUTABLE and untouched; v3 imports both and adds exactly one field to the three master entries: `theme`, the process noun the `with the other numbers` synthesis needs (`11 → intuition`, `22 → construction`, `33 → guidance`), constrained to be consistent with v1's own bodies. **The master registers and bodies are reused byte-for-byte and pinned as such** — `tests/meanings_content.test.js` asserts identity with v1, not similarity, so a paraphrase in v3 fails CI rather than becoming a second authority for a number's meaning. Every numerology coordinate can now show a two-character value; the §1.B always-space-separated invariant is what makes that safe, and `tests/numerology_display.test.js` drives all six cells at `11 / 22 / 33` to pin it.

**meanings-v4 routing note (2026-08-30, mechanical).** The active meanings registry is `content/meanings.v4.js`, which imports every v3 table and re-exports it UNEDITED — v1, v2 and v3 all stay immutable on disk, and the master identity pin above still holds through the re-export byte-for-byte. Where a v0.62-era clause in this document names `content/meanings.v3.js` as "the active registry", it now resolves through that unedited re-export; no shipped entry moved or changed. What v4 adds is two tables: `NUMEROLOGY_SLOT_LINES` (one authored line per numerology slot × terminal value, APPENDED after the shared base body so a value repeated across slots stops rendering as a copy of itself) and `THEME_TENSIONS` (authored sentences replacing the closing harmony clause where a filed opposing theme pair meets). One boundary question is named here rather than decided: §1.G's body/context split predates prose of this kind, and whether the slot lines sit inside the "registered meaning" or constitute a third layer is a controller call recorded as open in the PR #212 audit artifact — the register laws (§2) bind them either way and are mechanically scanned. **Resolved, 2026-08-31 — the controller ordered the boundary drawn: the appended lines constitute a third authored layer, the placement layer, per the v0.68 boundary amendment below; the open-question sentence stands as the record of the interval, corrected on sighting per the v0.48–v0.61 footer precedent (mechanical edit; resolution review in the v0.68 amendment's own two-lane artifact).**

**meanings-v5 routing note (2026-08-30, mechanical).** The active meanings registry is `content/meanings.v5.js`, which imports every v4 export and re-exports it UNEDITED — v1 through v4 all stay immutable on disk, and the master identity pin and the v4 tables both hold through the re-export byte-for-byte. What v5 adds is one table, `PLACEMENT_LINES`: one authored line per (placement, value) for the two surfaces that reuse a shared table verbatim — the rising sign (which draws the sun-sign registry) and the private animal (which draws the animal registry). The line is APPENDED after the unedited shared base body under the same contract as `NUMEROLOGY_SLOT_LINES`, reading the value in that placement's own office (the ascendant as what meets an encounter first; the month branch as the off-stage working register), so a sign or branch repeated across its pair of compartments no longer renders two byte-identical bodies. The claim is scoped to the body string: because the base body must stay byte-identical (§4), the two panels still open with the same first sentence and the same register title — the placement line differentiates what follows, it does not and may not rewrite what precedes. The v4 note's open §1.G body/context boundary question covers these lines identically and remains a controller call; the register laws (§2) bind them either way and they pass through the same mechanical scans, completeness pins, and byte-exact assembly pins as the slot lines. **Resolved, 2026-08-31, with the v4 note's question — the v0.68 boundary amendment below draws the boundary (the placement layer); the "remains a controller call" clause stands as the record of the interval, corrected on sighting per the v0.48–v0.61 footer precedent (mechanical edit).**

**v0.68 boundary amendment — a §1.G panel has three authored layers, and the appended lines are the third (controller order, 2026-08-31).** This resolves the question the meanings-v4 and meanings-v5 routing notes named open. A meaning panel's prose is constituted of exactly three layers, each with its own charter, keying, and immutability unit:

1. **The registered body** — the v0.44 contract, unchanged: a citation of what the named tradition associates with the VALUE, keyed by value alone, immutable per §4, and byte-identical wherever the same value renders (the pin that makes the layer boundary mechanical rather than editorial). It opens every panel and nothing below it may rewrite, truncate, or reorder it.

2. **The placement layer** — the appended lines: `NUMEROLOGY_SLOT_LINES` and `PLACEMENT_LINES`. Keyed by position × value, the layer states the VALUE READ IN THE POSITION'S REGISTERED OFFICE — the office being what the named tradition files the slot or placement as doing (the ascendant as what fronts the encounter; the month branch as the off-stage working register; the birthday as the native knack), and the line being that value's reading in that office, distinct per value by pinned whole-line uniqueness. The boundary against the body is therefore not "no second prose about the value" — the layer exists to be exactly that — but this: the body remains the sole VALUE-ALONE citation, and a placement line may not restate, paraphrase, or rewrite it (the no-echo discipline the line rewordings already practice). The boundary against the context layer stands as before: no cross-coordinate synthesis. Its position contract, already mechanically pinned, becomes constitution here: rendered APPENDED after the unedited base body and before the context block; base-only is the DEFAULT for every surface — the two line families are the enumerated exceptions (the six numerology slots; the rising and private-animal placements), and the sun and public-animal panels are the pinned representatives of the default; versioned inside the meanings chain under §4's new-release-new-file rule SEPARATELY from the bodies it follows, so a placement-line revision is never a change to a registered meaning; bound by §2's register laws through the same canonical scans as the body, with completeness and whole-line uniqueness pinned per family. On the citation question, stated honestly rather than asserted: the offices themselves are genuinely filed by the traditions, and the tradition attribution rides the base sentence each line is appended to — the assembled string opens with the named tradition's citation, so the line extends an attributed passage rather than standing free; the lines' own register runs closer to characterization than the bodies' does, is bound by the same §2 scans, and stays under the controller's read like all authored content.

3. **The context layer** — the v0.53 `in this sheet` synthesis, unchanged, separately labeled, combining already-rendered values through fixed roles. `THEME_TENSIONS` is classified as authored TABLE INPUT to this layer (it replaces the closing harmony clause inside the synthesis), not as part of the placement layer — stated so every v4/v5 table has exactly one home. Its interaction with §1.I v0.65's adverse-record rule, stated the way the pin states it: an adverse record suppresses the HARMONY algebra, and a filed tension SURVIVES it — the tension is itself a registered opposition, not harmony prose, so suppressing it would assert less than the registries hold (pinned by name in `tests/meanings_content.test.js`).

Scope of the count: three is the number of §1.G's own authored layers. The panel's fourth rendered prose section — the `filed relation` block of the v0.65 intra-sheet amendment — is a §1.I emission under §1.I's register law, separately labeled on the same panel; it is named here so the panel's classification is exhaustive, and it is not annexed to §1.G by being named.

What the naming buys, and what it does not: the §4 immutability unit is per-layer, so the body's byte-identity pin is the boundary's enforcement and version files may revise one layer without touching another; no product behavior, table, pin, or scan changes with this amendment — it names what the shipped mechanics already hold, which is precisely why the question could wait for the controller's pen and the resolution could not be an implementer's. One property is stated so it cannot later read as an oversight: layers 1 and 2 render FUSED — one prose block, one DOM node, no label or separator between body and line — unlike the labeled context and relation sections. The boundary drawn here is a boundary of authorship, keying, and §4 versioning, not of presentation, and its invisibility in the DOM is exactly why the constitution has to draw it: nothing in the product does. Review record: the two-lane artifact filed with this amendment's PR.

**§1.H. T3 written-entry rotation — explicit paid flip (v0.49, controller override, SHIPPED #93).** On a t3 result, an explicit `flip again` advances the written entry through the shipped v1 note positions in strict round-robin order (`low → mid → high → low`) and decrements exactly one paid credit. The card's catalog position, name, type, habit, and symbols remain fixed; only the selected `note` position changes. The first visible position is anchored by the existing life-path grouping: life paths 1–3 start at `low`, 4–6 at `mid`, and 7–9 at `high` (pre-calc-v3 lineage: masters 11/22/33 also anchored `high` — retired by the v0.54 anchor amendment below). Free, t1, and t2 flips remain cosmetic and never debit credits; same-profile form submission remains idempotent. The currently visible zero-based position is persisted in `eight_ball_facet_index_v2` (§5; keyed `_v1` until the v0.54 calc-v3 amendment versioned it): a new profile or a consumed pending-profile paid return resets it to the life-path anchor, while reload and same-profile submit preserve it; `try another` preserves it until another profile is submitted, and `forget` clears it. A zero-credit t3 flip clears any stale pending-profile intent and opens the paywall without advancing the position or spending an unseen read; the paid return grants credits without auto-consuming a facet, and only the next explicit flip advances and debits. **Content truth:** this is the c.1 mechanical-reuse path over immutable `content/cards.v1.full.js` `note.low/mid/high` strings. It does not claim those strings were laterally re-authored as outward/inward/returning facets, does not create a v2 deck, and makes no new taxonomy claim. On 2026-07-19 the controller explicitly overrode both the Codex read-only lane restriction and the standing traction/c.2-content prerequisite to authorize this c.1 implementation. Shipped 2026-07-19: Grok + Claude round-1 review returned MERGE WITH FIXES, all absorbs landed, their delta re-audit returned SAFE TO MERGE, and the change was squash-merged to `main` as `0e49773` (#93) after the ordinary release gates.

**v0.54 anchor amendment.** Calc v3 narrows the only valid life-path inputs to `1..9`; therefore the anchor groups are exactly `1–3 → low`, `4–6 → mid`, and `7–9 → high`. `resolveBracket` and `anchorFacetIndex` reject `11/22/33` rather than treating them as active coordinates. A profile whose old raw path passed through a former master now anchors from its reduced single digit. The stored position key is versioned `eight_ball_facet_index_v1` → `eight_ball_facet_index_v2`: the retired v1 value (possibly a former-master `high`) is never read and is cleared once on the first facet read after calc-v3 load. Funded flip history is not representable in the single stored index, so the first v3 load re-anchors every device — accepted cost, named in the journal 2026-07-20 §3 row 8 supersession entry.

**v0.62 anchor amendment (calc v4).** The v0.54 anchor amendment above is superseded and stands as lineage per L17. Calc v4 widens the valid life-path inputs to `1..9, 11, 22, 33`, so the anchor groups are `1–3 → low`, `4–6 → mid`, and `7–9` **plus `11 / 22 / 33` → high** — the pre-calc-v3 contract, restored. `resolveBracket` and `anchorFacetIndex` accept the master values and reject everything else; the two are the SAME partition of the terminal domain and `tests/facet_rotation.test.js` pins their agreement across the whole of it, so widening one and not the other fails CI rather than diverging. **There is no fourth bracket**: a catalog cell has exactly three note positions, so a master must land in one of the three, and `high` is where it landed before v0.54 retired the values.

The stored position key is versioned again, `eight_ball_facet_index_v2` → **`eight_ball_facet_index_v3`** (§5). The reason is the v0.54 reason, running the other way: a position stored under calc v3 was computed against a life path that has since MOVED — a device that read `1970-01-04` stored `mid` for a reduced `4`, and that profile now anchors `high` as a restored `22` — so a v2 value read under calc v4 would render a note slot never computed for the life path on screen. Both retired generations are cleared once on the first v3 facet read, and both stay named in `ui/payments.js` (and on the §5 list) solely for that clear. As at v0.54, flip history is not representable in the single stored index, so the first v4 load re-anchors every device — the same accepted cost, named again rather than assumed.

**v0.55 amendment (ownership — the flip is owned, 2026-07-26).** Credits no longer exist (§1.D / §2 v0.55), so the funded-flip mechanics above are superseded: on a t3 device the explicit `flip again` **always** advances the note position in the same strict round-robin order — nothing is debited, and the zero-credit paywall-opening branch is retired (`nextFacetState` takes only the position; `consumeFacetShake` always advances). t3 is the highest rung; a device that owns it has nothing left to buy from a flip. Free/t1/t2 flips remain cosmetic. Every other §1.H rule holds: anchor by life path, reset on new/consumed-pending profile, preserve on reload and same-profile submit, `forget` clears, position stored in `eight_ball_facet_index_v2`, content truth unchanged (c.1 mechanical reuse of the immutable v1 note slots).

**§1.I. Registry + Concordance — finite post-calculation relations (v0.51, controller override 2026-07-19).** Previous Readings MAY let the user select exactly two valid saved entries and open a separate Concordance screen. Both stored input payloads are recomputed independently through the unchanged `profileFromPayload` → `buildProfile` path; only after both calculated profiles exist does `ui/concordance.js` look up matching axes. The calculation core, catalog driver, bracket resolution, card output, tries, and credits are not inputs to relation lookup and are not mutated; the stored device tier is read once, solely to decide whether the five-element axis is included at all (§1.D entitlement), and never alters any axis's status or output. The compared axes are sun sign × sun sign, public animal × public animal, life path × life path, birth card × birth card, and five-element × five-element only when the device tier entitles the element coordinate.

**Register law.** Every axis emits exactly one textual status from `registered | adjacent | unfiled`, plus the two coordinate values, relation text, named registry, filing citation, and the qualifier `recorded, not certified.` `registered` means a named finite registry contains that relation; `adjacent` means two records may be juxtaposed but no shared registry is claimed; `unfiled` means the checked registry contains no named relation for that pair. The MVP's matching-axis lookups do not fabricate an `adjacent` relation: unsupported or same-value pairs are `unfiled`. The v1 registered inventory — historical record since the v0.54 registry amendment below; `tests/concordance.test.js` now pins the active v2 registry, which reuses every non-numerology table and retires the master-reduction links — was: 66 distinct western sign-distance pairs, 33 named earthly-branch animal pairs, 10 distinct wuxing phase pairs with direction, 3 Pythagorean master-reduction links, and 21 adjacent project-trump sequence pairs. Static data lives in immutable `content/concordance.v1.js`; a changed registry ships as a new versioned file — the active registry is `content/concordance.v2.js`.

**v0.62 registry-currency amendment (2026-08-01).** Two version-specific claims inside the Register law clause above are superseded, and the clause itself is preserved verbatim per L17 rather than edited — including by this amendment, which is the point: the first draft of this correction rewrote that clause in place with inline qualifiers, and an independent pre-merge audit (PR #190) blocked it for violating the very rule the change was written to serve. Quoting fragments of a clause is not preserving it. **(1)** "`tests/concordance.test.js` now pins the active **v2** registry, which reuses every non-numerology table and **retires** the master-reduction links" was accurate for the v0.54-through-v0.61 window only. Calc v4 (§1.B v0.62) restored the master numbers 11/22/33, and the active registry **files** the three master-reduction links rather than retiring them. **(2)** "the active registry is `content/concordance.v2.js`" names the v0.54-through-v0.61 active file; the active registry as of v0.62 is `content/concordance.v3.js`. `content/concordance.v1.js` and `content/concordance.v2.js` both remain immutable historical record per §4 — their continued presence is deliberate, not drift.

**v0.54 registry amendment.** Current runtime imports `content/concordance.v2.js`. It reuses every non-numerology v1 table, changes the life-path source label to the nine-number system, and retires the three master-reduction links from active Concordance. Every distinct or same-value `1..9` life-path pair is `unfiled` absent a separately named finite registry; values outside `1..9` are invalid. The immutable v1 inventory remains historical record.

**v0.62 registry amendment (calc v4).** The v0.54 registry amendment is superseded and stands as lineage per L17. Current runtime imports `content/concordance.v3.js`, which reuses every v1/v2 table unedited and restores two things v2 withdrew: the life-path domain, now the full `1..9, 11, 22, 33`, and **exactly the three master-reduction links** `11↔2`, `22↔4`, `33↔6`. Those three are re-exported from the immutable v1 inventory rather than re-declared, so the active registry cannot acquire a fourth by hand. **Nothing else is filed.** All 66 distinct life-path pairs are checked; 3 are `registered`, 63 are `unfiled`, and no pair is ever `adjacent` — a same-value pair included, since a number does not reduce to itself. No compatibility claim, score, ranking or synthesis is added on any axis: the register law of the base clause is unchanged and this amendment narrows nothing in it. Values outside the twelve remain invalid — widening to admit the masters admits no other double-digit value, which `tests/concordance.test.js` pins against `10`, `12` and `44` by name. The v1 inventory's other counts (66 sign-distance pairs, 33 branch pairs, 10 wuxing pairs, 21 trump-sequence pairs) are untouched, so the v1 inventory is once again the active one in full.

**v0.65 intra-sheet amendment (controller order, 2026-08-30).** The §1.I registries MAY be read within a single sheet: on the §1.G meaning panels, the tapped coordinate's value is checked against a FIXED, SHORT list of defined intra-sheet pairs — sun × rising (sign-distance), public × private animal (branch relations), year element × day-pillar element (wuxing, direction preserved), and any two numerology values on the sheet (the three master-reduction links only). Nothing else pairs; the birth-card sequence axis stays two-reading-only, since one sheet holds one card. The Register law binds this surface with one narrowing: the panel renders **registered records only**, carrying five of the six emissions verbatim (the two values, relation text, the textual status `registered`, the named registry, the filing citation, and the qualifier `recorded, not certified.`) — the `unfiled`/`adjacent` statuses never render because an unfiled pair renders NOTHING; on a panel, absence claims nothing, whereas the compare screen must speak because the pair itself is the user's question. Two mechanical safeguards ride with it: an ADVERSE filed record (chong/hai/xing, square/quincunx/opposition, the ke cycle) suppresses the §1.G harmony algebra in the same panel, so no surface asserts "working through" one line above a filed opposition; and the panel closes on any card re-render, so a stale citation can never attest a record the current values do not support. Tier boundary is §1.D v0.37's sealed-DOM contract doing the work: pair members are read from the rendered card, where a sealed compartment's value is ABSENT, so a pair with a sealed member does not exist to look up. `content/concordance.v3.js` keeps `ui/concordance.js` as its sole importer — the panel reaches the registries through an exported pure helper, not a second content import. Review record: the pr213 two-lane artifact; the sonnet lane's scope finding is what this amendment answers.

**Anti-oracle boundary.** Concordance is structural citation, not interpersonal interpretation. It MUST NOT emit a compatibility percentage, harmony score, ranking, prediction, advice, diagnosis, soulmate claim, recommendation, or invented synthesis across traditions. Local saved titles may label the two on-screen columns; the screen does not show DOB. No comparison share artifact ships in this MVP. The screen discloses that it recalculates now, makes no compatibility claim, and stores no comparison. §5.F controls retention and failure behavior.



## §2. What this is NOT

**Currently enforced (CI gates, see §7):**

- Not a "spiritual" product. The voice is declarative-observational and materialistic, not mystical or guidance-oriented. Banned-voice-register scan (`BANNED_VOICE_REGISTER` in `tests/profile.test.js`) ran on `cards.v1.js` content in v0.1.x. As of v0.3.0 (v0.22 amendment to §4 + §7 stage 1 extension) the scan runs against the tracked deck at `content/cards.v1.full.js` in CI; the private authoring pipeline at `~/dev/8ball-private/` continues to pre-scan before each release-prep `cp` to the public path.
- Not a SIRR product. Never references SIRR. Never imports from SIRR. The two repos are siblings, not parent/child. (See §9.) Enforced by `tests/pii_scan.test.js`.
- Not an analytics product. No tracking, no telemetry, no third-party scripts. (First-party server-side request logs — the host's own aggregate traffic analytics — are not "telemetry" under this rule: the product injects no client, makes no call, emits no beacon. See §5.) Network behavior is scope-restricted per §5: only same-origin lazy loads of static assets (e.g. `assets/cities.json`) and §5.B user-initiated calls (Netlify Forms feedback POST + Gumroad Buy Link redirect) are permitted. `fetch()`, `XMLHttpRequest`, and `navigator.sendBeacon` remain forbidden in tracked source. Enforced by `tests/privacy_scan.test.js`. The page uses system fonts only — no Google Fonts, no third-party CSS.

**Review-discipline (no current code surface to scan; rule fires when a feature is proposed):**

- Not a horoscope app. Not a numerology calculator. Not a guidance product. (Voice register; partly captured by the banned-list above.)
- Not a logged-in product. No accounts, no signup, no "save your reading."

**v0.50 amendment (Saved Readings MVP, controller override 2026-07-19).** The final phrase above is superseded only for a browser-local archive of completed readings. 8ball remains not logged-in: no account, signup, identity layer, email capture, cloud sync, remote database, or third-party storage is introduced. A reading is saved only after the user presses the explicit `save reading` action, stays in that browser's `localStorage`, and can be reopened, renamed, deleted individually, or cleared in full. §5.E is the controlling scope; anything beyond it remains prohibited by the original line and §12's permanent account-system ban.

**v0.51 amendment (Registry + Concordance MVP, controller override 2026-07-19).** Comparing two browser-local saved readings does not turn 8ball into a compatibility, guidance, social-profile, or logged-in product. The §1.I register law and §5.F transient-retention boundary are controlling; the §2 anti-oracle, no-account, no-analytics, and no-remote-storage rules remain literal.
- **Honest invitation permitted; manipulation forbidden (v0.35).** 8ball may be shared, posted, linked, and discussed by the operator as a person, and may carry a soft contextual invitation (a plain link, a "here's the thing"). Forbidden is manipulative/coercive distribution: dark patterns, engagement-bait, false urgency, undisclosed funneling, spam, extractive growth-hacking. Participating as a human in a community is not a funnel. The anti-funnel value protects against manipulation, not against being seen. This clause is doctrine; prior "not a funnel / no CTA / no link in bio" language that lived only in operating/session plans (never in this file) is superseded by it.
- **Arcade-toy carve-out (v0.22, v0.3.0).** Paid surface is one-time purchase, fixed-credit. Three dollars buys three reads with depth content unlocked. Tier ladders (v0.3.1+: $6 / 6 tries at depth+hexagon; $9 / 9 tries at depth+hexagon+four) extend the model without changing its shape. Not a subscription. Device-stored credit counter preserves the no-account constraint. The arcade frame is load-bearing — any future amendment that breaks `dollars == tries` or introduces recurring billing requires explicit re-amendment.
- **Arcade-toy carve-out re-amendment (v0.36, v0.6.0).** This is the explicit re-amendment the v0.22 clause requires. The shipped tier ladder (§1.D) is $3 / $6 / $9 rungs, each a one-time purchase granting **+3 reads at that rung's coordinate density** — `dollars == tries` is superseded: every rung buys exactly three reads; dollars buy density, not try count. The v0.22 contemplated ladder ($6 / 6 tries; $9 / 9 tries) is preserved above as lineage and never shipped. Everything else holds unchanged: one-time purchase, fixed-credit, no subscription, no recurring billing, device-stored counter, no account.
- **Arcade-toy carve-out re-amendment (v0.55, ownership, 2026-07-26).** Second exercise of the v0.22 explicit-re-amendment requirement. The rungs reprice to **$1 / $2 / $3** and `fixed-credit` is superseded entirely: a rung is a one-time purchase of **permanent, unlimited** density at that rung — no read counter, no cap, no per-read debit; the free surface is likewise open (unlimited readings at free density; the three-free-tries demo is retired per §4.B v0.55). The load-bearing frame survives intact: one-time purchase, no subscription, no recurring billing, no account; the device-stored state shrinks to the monotonic tier flag (`eight_ball_tier_v1`) plus the read-only legacy-credit grandfather signal (§1.D R2). Any future amendment that introduces recurring billing or claws back a purchased rung still requires explicit re-amendment here.

## §3. The calculation contract

`core/profile.js` is the calculation core. Changes fall into two categories:

**Breaking changes** — modify existing outputs (sunSign, animal, lifePath, nameNumber, chineseElement, soulUrge for any input). The contract:

1. Add or update fixtures in `tests/fixtures.json` reflecting the intended new behavior.
2. Update the algorithm.
3. Run `npm test`. All cases must pass.
4. Bump the calc-version note at the bottom of this file.

**Additive changes** — add new outputs without modifying existing ones (e.g. new functions exported, new fields on the `buildProfile` return value). The contract:

1. Add direct unit tests for the new outputs in `tests/profile.test.js`.
2. Run `npm test`. All cases must pass, including the new tests.
3. Existing `tests/fixtures.json` cases must remain byte-identical (no silent drift).
4. Calc-version stays the same major (additive doesn't break v1 consumers).

Algorithm versions documented:

- **calc v3.1** (v0.57, 2026-07-29) — Lunar-year and solar-term cusps are resolved in the civil time of their own era: Beijing local mean time (UTC+7:45:40) before China Standard Time was adopted in 1929, UTC+8 from 1929 on. calc v2 evaluated every year at a flat UTC+8, which files a new moon or solar term landing in the first ~14 minutes after midnight one day late. Across the 1900–2100 table range that is four dates: lunar new year **1916 (Feb 4 → Feb 3)**, and three solar terms — 1911 lixia, 1912 xiaohan, 1927 bailu — each moving one day earlier. Only pre-1929 births are affected, and the visible defect was a 1916-02-03 birth filed under the previous year's animal (rabbit) instead of dragon. ~~Numerology, sun sign, rising sign, birth card, and the catalog index are untouched~~ **— erratum, 2026-07-29 P1-D: FALSE, struck per L17 rather than silently rewritten. The catalog index is `(sunSign, animal)`-driven (§1), and `animal` there is the public year-pillar animal — the exact coordinate this entry says moved. A 1916-02-03 birth's catalog index necessarily moves with it: Aquarius+rabbit was `cxxiv`, Aquarius+dragon (the corrected animal) is `cxxv`, verified against shipped `core/engine.js`. Numerology, sun sign, rising sign, and birth card genuinely are untouched — none of those inputs depend on the year-pillar animal — but the catalog index is not among them. This was a blast-radius/truth defect in the record, not a code defect: the shipped catalog computation was already correct; only this claim about it was wrong.** no stored reading needs migration, because only reconstruction inputs persist. Verified against three independent implementations — sxtwl (寿星万年历, astronomical) and the table-based lunardate and borax — which agree with each other on every year 1900–2050 and, after this change, with `core/calendar.js` on every year 1900–2100. **The Hong Kong Observatory tables named under calc v2 could not be reached from the implementing environment (egress policy blocked `hko.gov.hk`); the three-library consensus stands in for them and should be re-confirmed against HKO when an operator can reach it — done, 2026-07-29 (P1-D, journal-recorded): ~~the consensus disagreed with HKO at eight further boundaries outside this entry's four-date blast radius (1911 lixia, 1912 hanlu, 1912 xiaohan, 2014 jingzhe, 2016 xiaoshu, 2045 xiaoshu, 2047 jingzhe, 2097 lixia — see journal.md). All eight sit within the documented ~15-minute error budget of the low-accuracy solar-longitude formula, right at local midnight; `core/calendar.js` now carries a small, sourced correction table (`HKO_SOLAR_TERM_CORRECTIONS`) for exactly those eight, verified against the full 1901–2100 HKO index at zero remaining mismatches (2,400 solar + 200 lunar comparisons).~~ **— erratum, 2026-07-30 (Codex pre-merge audit, finding F): the struck sentence is wrong in three separate ways and is superseded per L17 rather than edited in place. (1) BLAST RADIUS — "outside this entry's four-date blast radius" is FALSE. Two of the eight, **1911 lixia and 1912 xiaohan**, are named in this entry's own four-date list two sentences above; they are inside it. The correction table restores exactly the HKO values this entry had moved away from for those two boundaries (1911 lixia back to May 7, 1912 xiaohan back to Jan 7), so for them calc v3.1's pre-1929 offset was a regression this table reverses, not a further discovery beyond it. The genuinely new boundaries are the other **six**. (2) ATTRIBUTION — "the consensus disagreed with HKO" is unsupported. The cited evidence file (`audits/calc_v3_1_pre1929_offset_evidence_2026-07-29.md`) uses sxtwl, lunardate and borax as **lunar-new-year** oracles only; its whole comparison table is lunar-new-year dates, and no artifact on file shows lunardate or borax evaluated at a solar-term boundary at all. Nothing establishes what those libraries say about these eight. The disagreement on record is between `core/calendar.js` and HKO — that and no more. (3) CAUSE — "all eight sit within the documented ~15-minute error budget of the low-accuracy solar-longitude formula" states a diagnosis the evidence does not reach. An error bound is not a deterministic threshold: a closer approximation can agree with the authority by luck while a farther one fails, so proximity to the bound is consistent with the claim but does not demonstrate it, and no independent high-precision ephemeris artifact exists to separate formula error from omitted time corrections. The eight are **authority-pinned residual corrections** — correct against HKO, cause not established. SCOPE, corrected in the same breath: the comparison covers **all 12 month-starting (jié) terms plus the lunar-new-year date, for every year 1901–2100** — 2,400 solar and 200 lunar comparisons at zero mismatches — NOT the "full HKO index." HKO publishes 12 major and 12 minor terms; only the 12 month-starting ones are compared here, and the same solver also drives major-term/leap-month logic that carries no direct coverage. What is unchanged by this erratum: the eight override values themselves, which are verified byte-exact against the 200 official HKO source files (`audits/fixtures/hko_calendar_authority_1901_2100.json`, all 200 SHA-256s independently re-derived 2026-07-30).**
- **calc v4** (v0.62, STAGED 2026-07-31) — Master-preserving Pythagorean numerology. All six resolved coordinate outputs reduce by repeated digit sum **while above 9, stopping at `11`, `22` or `33`**, so the terminal domain is exactly `1..9, 11, 22, 33` (`29 → 11`, `38 → 11`, `44 → 8`). Maturity is unchanged in method — reduce each component, add the canonical components, reduce again under the same rule — and can therefore land on a master itself. This is a breaking calculation change in `core/profile.js`, coordinated with `tests/fixtures.json`, direct unit tests, tier rendering, bracket/facet anchors, the v3 active meaning registry, the v3 Concordance registry, the v2 dyad tables, and the v3 public tables with their master-mode bridge. Calc v3's `null`-not-zero guard for an empty contributing letter subset is RETAINED unchanged; only the terminal domain moves. Lunar-year, solar-term, sun-sign, rising-sign, pillar, birth-card and catalog-index algorithms are untouched, and the tarot birth-card reduction (digit sum floored at ≤ 22, `22 → 0 · the fool`, §1.C) is a separate contract this does not touch. Persisted reading schemas need no migration because they store reconstruction inputs, not derived coordinates; reopen/reload recomputes under calc v4. The one persisted thing that DOES move is the t3 facet position, which is a derived anchor rather than an input — see §1.H v0.62 and §5.
- **calc v3** (v0.54 through v0.61) — All six resolved Pythagorean coordinate outputs reduce by repeated digit sum to exactly `1..9`; 11/22/33 are no longer terminal values, and an empty contributing letter subset is unresolved rather than zero. This is a breaking calculation change in `core/profile.js`, coordinated with `tests/fixtures.json`, direct unit tests, tier rendering, bracket/facet anchors, the v2 active meaning view, and the v2 Concordance registry. Lunar-year, solar-term, sun-sign, rising-sign, pillar, birth-card, and catalog-index algorithms are unchanged. Persisted reading schemas need no migration because they store reconstruction inputs, not derived coordinates; reopen/reload recomputes under calc v3.
- **calc v2** (v0.2.7.1 through v0.53) — Real lunar new year table for year-pillar cusps (year-pillar animal + 5-element pillar). Real solar-term table for month-pillar (`innerAnimal`). Both tables span 1900–2100, evaluated at date-precision in canonical Asia/Shanghai timezone (UTC+8); DOB given as `YYYY-MM-DD` is treated as the truth — no timezone conversion of user input. Lunar new year dates and the 12 month-start solar-term dates are computed in JS via Meeus astronomical algorithms (Chapter 25 sun position, Chapter 49 lunar phases with periodic + planetary corrections), mirroring the `core/rising.js` Meeus pattern; results match Hong Kong Observatory canonical tables byte-exact at the sanity-lock checkpoints in `tests/profile.test.js`. Zero runtime deps, zero devDeps, zero build step. The v1 Feb-4 fixed-cutoff approximation is retired. **`getInnerAnimal` signature changed from `(month, day)` to `(year, month, day)`** as a breaking calc change. All existing fixtures other than the v1-bug-locking fixture #6 (CNY Feb 4 cutoff) remain byte-identical; fixture #6 is intentionally re-spec'd because it was locking the bug being fixed.
- **calc v1** (initial through v0.2.7) — Pythagorean life path with master numbers 11/22/33 preserved at the final reduce. Western tropical sun signs at standard cusps. Chinese zodiac with Feb 4 cutoff approximation (retired in calc v2). Additively extended at v0.2.0 with: chinese five-element (2-year cycle, 1924 anchor), public/private animal split (year-pillar already present at `animal`; month-pillar added as `innerAnimal` via solar-term cutoff approximations), soul urge (vowel-only Pythagorean, masters preserved), and unreduced sums (lifePathSum, nameNumberSum, soulUrgeSum) exported for potential future surfacing. Additively extended at v0.2.1 with optional rising sign (surface-only ascendant) when birth time + country + lat/lng are present. All additions covered by direct unit tests in `tests/profile.test.js` and `tests/rising.test.js`; existing fixtures unchanged.

If a fixture changes silently, the test gate catches it. If a test changes silently, code review catches it. If both change in the same commit without a doctrine note, the reviewer rejects.

## §4. Content rules — copy & traits

- **No slurs.** Banned-pattern check runs in `tests/profile.test.js`.
- **No medical/diagnostic framing.** Cards do not adopt clinical or diagnostic vocabulary, including ironic adoption (e.g., "diagnosis," "syndrome," "disorder").
- **Cultural-symbol respect.** If the deck draws from any tradition (tarot, I Ching, runes, zodiac, etc.), cards respect that tradition's lineage. No syncretic flattening, no caricature of source traditions.
- **No targeting minors.** Copy assumes adult user. UI does not pander to children. **v0.48 amendment:** the v0.2.6 18+ acknowledgment gate (§4.A) is retired — see §4.A for the historical mechanics and the retirement rationale. This bullet's substance (adult-assuming copy, no pandering) stands on its own without a mechanical gate.
- **No real-person targets.** No "you're like [public figure]" lines.
- **Universal floor.** Cards should land equally on a person who picked their own DOB.
- **No card-content strings in tracked files.** As of v0.2.0 the card content (cell `name`, `type`, `habit`, `note` bodies) lives privately at `~/dev/8ball-private/cards.v1.full.js`. No public-repo tracked file (source, tests, fixtures, `journal.md`, audit notes, this doctrine) may contain a card-content string. Audit dispositions and content-batch logs reference cells by coordinate path (e.g. `aries.rabbit.note.mid`); before/after strings are stored with the private deck. Forward-looking from doctrine v0.11; pre-v0.11 trait-pool / template-pool excerpts in journal entries (retired `traits.v1.js` / `templates.v1.js` system, deleted from repo in 2F-2) are out of scope. **v0.22 amendment (v0.3.0):** the rule is scoped, not retired — exactly one tracked file, `content/cards.v1.full.js`, is permitted to contain card-content strings (the 144-card content layer: `name`, `type`, `habit`, `note` × `{low, mid, high}` brackets). All other tracked files remain prohibited, including source modules in `core/`, all `tests/`, `tests/fixtures.json`, `journal.md`, audit notes under `audits/`, `DOCTRINE.md`, `8BALL.md`, `README.md`. Audit dispositions continue to reference cells by coordinate path; the carve-out covers the deck file alone, not its discussion in tracked text. Voice-register enforcement (`BANNED_VOICE_REGISTER` scan in `tests/profile.test.js`) now runs against `content/cards.v1.full.js`. The private location at `~/dev/8ball-private/cards.v1.full.js` is retained as the authoring source; `content/cards.v1.full.js` is a verbatim copy maintained via `cp` at release-prep time.
- **Versioned, not edited.** Shipped content batches are immutable. New release = new file (e.g. `traits.v2.js`, `cards.v2.js`). Diff lives in `journal.md`.

**§4.A — 18+ acknowledgment gate. RETIRED v0.48 (2026-07-06).**

**Retirement rationale.** The gate was a v0.2.6 operator call ("kids shouldn't use it"), not an externally-mandated requirement — no app-store or payment-processor rule required it at the time. It was later cited as one of several disclosures in a pre-emptive Lemon Squeezy KYC response document, but that payment path was itself retired in favor of Gumroad by v0.3.0.3, so that citation no longer applies to anything live. Operator judgment on reflection: the modal's copy ("for adults" / "8 ball is a toy for adults") read as adult-content-warning theatre — exactly the framing §4.A's own mechanics clause said to avoid — rather than the intended clinical, self-attestation tone. Retired rather than reworded: removes the modal, the `isAgeAcknowledged` read, the `eight_ball_age_ack_v1` localStorage flag, and the about-modal's disclosure line. The underlying content posture (copy assumes an adult user, no pandering to minors) is unchanged — see the §4 "no targeting minors" bullet above, which no longer depends on a mechanical gate to hold.

Mechanics below preserved verbatim as history per L17 (supersede, don't edit) — this is what shipped from v0.2.6 through v0.47:

- On first load, a modal blocks the onboarding form. Single button: "confirm." Tapping it is the acknowledgment.
- Acknowledgment persists via `eight_ball_age_ack_v1` localStorage flag (string `'true'` on confirm). On subsequent visits the gate is skipped.
- Acknowledgment is a click-through, not verification. Copy must be explicit about that — no implication of legal age-gating, no health-product warning theatre, no false claim of identity verification.
- Disclosure: the about-modal mentions the gate's existence and shape so the user knows what the storage flag is for.

If a future feature needs age verification (paid tier, adult-content layer, regulatory requirement), that requires a new §4.A amendment and likely a §5 amendment for whatever data-collection surface the verification introduces — this retirement does not preclude reintroducing a gate, mechanically or in spirit, if a real need surfaces.

**§4.B — Three-free-tries cap (v0.22, v0.3.0).**

Three free tries total per device. First three unique `(name, dob)` pairs render the locked card (symbols visible, depth bars dimmed, lock icon top-left — Pattern C surface per `~/Desktop/8ball/sessions/v03_pattern_c_locked_vs_unlocked.html`). The fourth new pair triggers the paywall modal. The cap is monotonic and per-device — wiping localStorage resets it. Paying does not reset the cap; paying adds paid credits separately (see §5 `eight_ball_credits_v1`). The about-modal discloses the cap exists ("first three readings are free. after that, three dollars buys three more reads…"). The localStorage-wipe reset is intentionally not advertised in user-facing copy — disclosing the cap shape is the load-bearing disclosure; advertising the bypass is not.

The cap exists to admit the offer plainly: this is a paid toy, not an ad-supported product, not a free-trial-with-aggressive-upsell. Three free tries is the demo.

**v0.36 amendment (v0.6.0):** the paywall is a three-rung ladder (t1 · $3 / t2 · $6 / t3 · $9 per §1.D) — three plain `<a href>` CTAs to the three Gumroad products (§5.B Call 2 v0.36). The three-free-tries cap is unchanged; every render — free tries included — shows the device's entitled density per the §1.D density rule (a device with no tier and no credits renders the free card). t3 carries the card-content unlock — the §1 v0.22 paid card-content layer relocates to t3. Paying still does not reset the cap; every rung adds +3 paid credits (`eight_ball_credits_v1`) and raises the stored tier monotonically (`eight_ball_tier_v1`, §5).

**v0.55 amendment (open free surface, 2026-07-26):** the three-free-tries cap is **retired** — the base clause above and the cap language in the v0.36 amendment are superseded. Every render on a free device returns the free card: unlimited readings, unlimited profiles, no counter, no fourth-pair paywall trigger (Path A retired). The demo IS the product's free surface now; what the paywall sells is depth, priced t1 · $1 / t2 · $2 / t3 · $3 (§1.D / §2 v0.55), permanent on purchase. The paywall modal survives as the rung-purchase surface, reached from the lock-tap on the rendered sheet (Path B — §6.7): it stages the stored profile as pending and opens the ladder. The about-modal cap disclosure is superseded by open-surface copy ("readings are free and unlimited, on the free sheet"); disclosure of what a rung buys, that it is permanent, and the Gumroad boundary remains load-bearing. `eight_ball_tries_used_v1` is retired with the cap (§5 v0.55).

**v0.56 amendment (14-day single-offer sprint presentation, 2026-07-26 through 2026-08-08 inclusive):** for the sales sprint the buyer-facing paywall presents **one purchase choice** — the existing t3 product (`https://theeightball.gumroad.com/l/xjpvp`, `?paid=t3`) as `complete 8ball · $3 once`: every coordinate and the written card, permanently, for every reading on this device. The v0.36/v0.55 three-CTA ladder presentation is superseded **for the presentation only and for the sprint window only**: the t1/t2 products stay live on Gumroad, their `?paid=t1|t2` returns are honored unchanged, the stored tier stays monotonic, the §1.D R2 legacy grandfather survives, and every existing t1/t2 buyer retains exactly the access they own — `core/payments.js`, `ui/payments.js`, and `ui/tiers.js` are byte-untouched by this amendment. Three presentation surfaces are added, none a new flow: **(a)** the paywall MAY carry one compact **specimen preview** — the fixed catalog cell no. v (aries × dragon): its complete-sheet image from the already-public `/cards` hosted set plus its written entry filled at boot from the public deck bundle (`content/cards.v1.full.js` — the §5.C source-visible posture; the §4 carve-out file stays the only tracked home of card-content strings). The preview is labeled a fixed example, never the visitor's result; the visitor's own written entry remains t3-gated per §1.D / §5.C, and the §1.D v0.37 sealed-DOM purity of the visitor's sheet is unchanged. **(b)** A labeled result-rail **offer control** presents the same single Path B trigger (stage pending profile → open paywall) as the lock icon, visible below t3 and absent at t3 — the lock icon's own visibility rule; the §1.F density census stays price-free. **(c)** The cold first screen's mechanism strip and the head metadata read plain-English (`name + birth data in. one fixed identity sheet out`; descriptive page name `specimen registry`) — still §2 clinical, no predictive/therapeutic/guidance claim, pinned by `tests/reach_surface.test.js`. Sibling host-config change: `netlify.toml` adds five exact-match, non-forced 200 rewrites (`/r` `/x` `/ig` `/tt` `/pin` → the app) so channel attribution reads from the host's own first-party server logs per §5 v0.35 — no client event, cookie, pixel, query parameter, or third-party script; deterministically validated by `tests/channel_routes.test.js`. **Expiry:** the single-offer presentation lapses after 2026-08-08 unless the operator explicitly renews it; reverting is the mechanical restoration of the three-CTA ladder plus test re-pins, with no entitlement migration — nothing in the ownership model moved. **— The sprint, its presentation and every offer it governed retired 2026-09-02 with the storefront: see the §4.B v0.71 free amendment below; the product is completely free.**

**v0.61 amendment (the dyad rung does not touch the sprint presentation, 2026-07-30).** §1.D v0.61 appends a fourth rung, `t5` · $6. The v0.56 single-offer presentation above is **unchanged by it and is not superseded**: the paywall still presents exactly one purchase choice (the t3 product at $3), its headline, specimen preview and result-rail offer control are byte-identical, `index.html` still contains exactly one Gumroad URL, and `core/payments.js` / `ui/payments.js` / `ui/tiers.js` carry no new CTA. The dyad's own control is a labeled entry to a screen, not an offer — it carries no price and no urgency, and while `T5_PRODUCT_URL` is empty the rung's CTA has no `href` and stays hidden. The sprint expiry (2026-08-08) and the mechanical restoration it describes are unaffected; when the ladder presentation is restored, whether `t5` joins it is a separate controller decision, not a consequence of this amendment.

**v0.62 commercial-truth amendment (verified 2026-07-31; final-worktree audit closure 2026-08-01).** This supersedes the current-state storefront claims in §1.D v0.55/v0.61 and §4.B v0.56/v0.61; their entitlement and historical-price text remains lineage per L17. The product's entitlement ladder is still `t1 / t2 / t3 / t5`, stored tiers and valid paid-return handling remain monotonic, and every existing t1/t2 owner keeps exactly what the device owns. The live storefront is a different and narrower fact: authenticated inspection found `xjpvp` (**8 ball — private**, $3) published and wired as the only buyer-facing offer; `neysyv` (**8 ball — comparative**, $6) exists but is unpublished and not for sale; `rzqezp` (**8 ball — public**, $9) exists but is unpublished and not for sale. Therefore t1/t2 are not currently separate $1/$2 storefront offers, the t5 product does **exist** as the unpublished comparative listing, and the retired public listing remains residue rather than a live t4 rung. `T5_PRODUCT_URL` stays empty and no comparative checkout or fulfillment path is live; the runtime still fails closed. The sprint's 2026-08-08 expiry cannot mechanically restore the old three-CTA ladder: any later storefront expansion requires a fresh controller decision, operator-hand listing publication where applicable, coherent product wiring and tests, and the ordinary release gates. No Gumroad or account mutation was required or performed for this truth repair.

**v0.67 t5-activation amendment (2026-08-31, controller order).** Supersedes as CURRENT STATE the fail-closed storefront claims for the dyad rung — §1.J's "Commercially the rung is not live" paragraph AND the "Two limits" paragraph's closing "still ships empty" sentence, §4.B v0.61's empty-constant language, the v0.62 commercial-truth amendment's "`T5_PRODUCT_URL` stays empty", §7's v0.61-extension parenthetical describing the fail-closed-offer pin, and — re-purposing the listing from the rung it once served — §5.B v0.36's `neysyv → /?paid=t2` product↔param binding together with §1.D v0.55's restatement of it. All preserved verbatim per L17. On the controller's explicit activation order, `T5_PRODUCT_URL` carries the live comparative Buy Link (`neysyv`, $6, bare URL — no query), and the offer path the PR #187 R6 correction required ships WITH it, never behind it: a result-rail anchor (`#dyad-offer-link`, injected by `ui/dyad.js` beside the entry control) whose click IS the checkout — the §5.B Call 2 mechanism, a plain `<a href>` with target `_self`, no script handler, no fetch — with the third-party disclosure the modal checkout carries injected beside it in the same register. **The offer is scoped to t3 exactly** (owns the complete single sheet, not the dyad): that is the one rung the label's "a second sheet + the relation layer — $6" delta is true of; below t3 the rail presents the $3 complete-sheet step alone, and whether t5 ever joins that presentation remains the separate controller decision v0.61 reserved. `dyadEntryVisible` remains entitlement-only exactly as R6 fixed it; the offer is its own predicate (`dyadOfferVisible(tier, url)` — cardEntry-entitled AND not dyad-entitled AND url non-empty, the url parameter defaulting to the constant so the empty-URL degradation is runtime-testable), and the two controls swap on the same render sync with the href stripped whenever the offer hides. The `?paid=t5` return path needed no change — `handlePaidReturn` accepts whatever `isTier` accepts and has since v0.61; **the monotonic tier write is the entire grant (§1.D v0.55 — no credits exist to add)**. Blocked-storage exposure, decided rather than left to be found: the anchor carries no `stagePurchase`-style preflight because the t5 return needs no staged pending profile, and the return path is deliberately retry-preserving — a failed `setTier` keeps the `?paid=t5` URL and banners the allow-storage-then-reload instruction, so the recovery is the same one every rung's return has. Fail-closed degradation is retained: an emptied constant hides the offer, drops its href, and turns the exact-URL pin loudly red. The paywall presentation is untouched: the modal still carries exactly one Gumroad CTA (the t3 product), `index.html` is byte-unchanged. **Two operator-hand steps remain before a buyer can complete the path, named precisely because one of them is a trap (§10): publish the `neysyv` listing, and RE-POINT its EXISTING Content-tab button — wired to `/?paid=t2` in the listing's v0.6.0 life as the t2 product — to `/?paid=t5`. Publishing without the re-point silently grants a $6 buyer t2: one rung below the $3 product, no dyad, monotonic and permanent, undetectable by the runtime.** Until both steps are done the anchor resolves to Gumroad's own "not currently for sale" page — an inert storefront page, not a broken product surface; the runtime side is complete and waits on no further code. The §1.J price-column reconciliation stays open as v0.61 filed it. **Resolved, 2026-08-31, by the v0.69 price-column reconciliation appended below (mechanical sighting marker, same precedent).** **— MOOTED 2026-09-02 by the v0.71 free amendment below: the storefront retired before the operator-hand steps ran; the `neysyv` listing never published, no buyer ever completed the path, and the trap this paragraph names was never sprung.**

**v0.69 price-column reconciliation (§1.J, controller order, 2026-08-31).** Closes the open doctrine work v0.61 filed and v0.67 kept open — and it closes it by ADOPTING NOTHING: the $6 is derived from the Price column's own run rather than from the commissioning brief's ladder, which remains outside this constitution.

Stated as constitution: (1) **The column is a ledger of controller-set, one-time, FROM-ZERO rung prices, monotone up the ladder.** v0.55 set $1/$2/$3 by decision, not formula; no rung price has ever been derived, and upgrades have never been prorated — a t1 owner pays the full $3 to reach t3 (cumulative $4 for a $3 state). That non-proration is the column's established behavior, DISCLOSED here rather than left for subtraction to discover. (2) **t5's from-zero price is $6 — two complete sheets' worth at the ladder's own $3, the relation layer deliberately unpriced.** The t5 STATE is two complete sheets plus the relation layer; $3 is literally the complete-sheet price (the §4.B sprint sells it as `complete 8ball · $3 once`), and the direct path is real — the bare `neysyv` URL grants t5 from any tier at $6. On the presented t3→t5 path the buyer pays the unprorated rung price like every upgrade before it (cumulative $9); the constitution does NOT read that subtraction as pricing the relation layer, because subtraction has never priced anything in this column — the t1→t3 subtraction would "price" the last five coordinates plus the written entry at $3 while the same $3 buys all fifteen from zero. No clause quotes a standalone relation-layer price, so the defense v0.60 found impossible for the retired rung's "$9 for five derived strings" is never owed: the layer rides the pair, priced never. (3) **The brief's entity-count ladder (one person · two people) stays unadopted.** The reconciliation makes it unnecessary rather than making it law: the column accounts for its prices as its own ledger. (4) **The four-rung price reading is t1 · $1 / t2 · $2 / t3 · $3 / t5 · $6.** The operator-locked v0.2.3 rung table stays verbatim per L17 (it predates t5 and carries no t5 row); this amendment is a NEW supersession of the Price column standing beside the Table-currency paragraph's v0.55 note, not an extension of it. (5) **Both prior open-work sentences now carry markers** — §1.J's v0.61 sentence resolved by this amendment, and the v0.58 clause's sentence marked MOOTED by the v0.60 retirement — so the count of open price reconciliations is zero on the page, not by argument. (6) **Dominance, stated rather than discovered:** once `neysyv` is published, t5 at $6 entitles a strict superset of t3 at $3, so the direct $6 purchase dominates the presented $3-then-$6 path ($9 cumulative). That is a property of the declared column, named here as a commercial fact; whether any below-t3 surface should ever present the direct path remains the separate controller decision v0.61 reserved and v0.67 restated. (7) **Storefront truth, kept distinct from the ledger:** the amendment prices the LADDER; on the storefront today only the $3 product is live, the $6 listing is staged and unpublished pending the v0.67 operator-hand steps, and t1/t2 are not currently presented as separate storefront offers — the ledger records what each rung costs, not what the sprint-era presentation surfaces. No price changes, no product change, no code change. Review record: the two-lane artifact filed with this amendment's PR. **— Superseded as live commerce 2026-09-02 by the v0.71 free amendment below: the ledger stands as the historical record of the paid era's prices; nothing is priced now.**

**v0.71 free amendment (the product is completely free, controller order, 2026-09-02).** On the controller's explicit words — "let's make it completely free … No more gumroad this is fun" — the storefront retires in full and the entire product opens for every device. This supersedes AS LIVE COMMERCE every price, offer, checkout, entitlement-gate and storefront clause in this constitution — the §1.D tier ladder as a PAID ladder, §4.B's offers and sprint presentation, §1.J's rung pricing and offer activation, §5.B Call 2's checkout mechanism, the v0.55 ownership model, the v0.69 ledger — all preserved verbatim per L17 as the record of the product's commercial era; the ledger's prices become historical facts, not live ones. What ships: (1) **The free ceiling.** The single density resolver answers the ceiling for every device — `getRenderTier()` returns `t5`, storage consulted never — so every render carries the complete sheet, the meanings, the written entry, domain fit, and the dyad's paired read, with nothing sealed anywhere. The tier vocabulary and `TIER_COORDS` stay as the RENDER REGISTRY (composition, not price), and every gate keeps its single predicate seam — which now always answers yes. (2) **The commerce surface is deleted, not hidden:** the paywall modal and its specimen preview, the lock icon, the sprint offer control, the dyad offer anchor and its disclosure, every checkout URL, the `?paid=` return handler, purchase staging, and the tier/credits/pending storage shims — with a mechanical absence guard pinning zero checkout tokens across every shipped source file. (3) **§5 storage shrinks.** The three retired commerce keys (`eight_ball_tier_v1`, `eight_ball_credits_v1`, `eight_ball_pending_profile_v1`) are actively removed by a read-verified boot scrub; the pending key held a staged name+DOB payload across checkout and gets the v0.64 personal-data standard — erased, not left inert (this subsumes the v0.64 pending-gender scrub: removing the payload removes the token). No key is added. (4) **What stays, as registry, per the kua-retirement precedent:** `core/payments.js` (the tier and state machine with its full engine suite), the facet-rotation machinery (never commerce), and `ui/payments.js` itself as the storage/status module carrying the lineage in place — the §7 stage-6 payments leg keeps running as the state-machine suite plus the free-surface suite that pins the storefront's absence. The product-audit's blocking t4-migration probe is repointed to the free ceiling (resolves `t5` over legacy storage, writes nothing, scrub verified) with the auditor's assurance suite updated in the same commit. (5) **Owner impact, named per the v0.58/v0.60 precedent:** every device that bought a rung keeps everything it had and gains the rest like every other device — no one loses access; the purchase bought the product during its paid era, and no refund mechanism exists or is implied. (6) **The revenue thread closes.** The reach-bottleneck monetization work, the storefront truths, and the operator-hand steps (publish `neysyv`, re-point its button) are all MOOT: the listing never published, no dyad checkout ever completed, and the controller's stated ground is the product's own nature — "this is fun". Any future re-commercialization is a fresh controller decision requiring its own amendment, storefront, wiring and release gates; nothing in the superseded clauses restores by default. Review record: the two-lane artifact filed with this amendment's PR.

If a line lands but you can't tell whether it crosses any of the above, it crosses. Cut it.

**Safety-patch carve-out.** Locked-decision #9 (immutable v1 pools) protects against silent flavor drift. It does NOT protect a doctrine-rule violation caught post-ship. If a shipped trait, template, or card line violates §4, it is cut in a patch release; the journal records the cut and the diff carries the doctrine note. Immutability is for taste discipline, not error preservation.

## §5. Privacy primitive

The product persists only user-entered profile fields, in `localStorage` only, on the user's own device:

- `eight_ball_profile_v1` — JSON-stringified profile object containing the user-entered fields below. Persists the profile payload across loads; written by the onboarding submit handler and by `applyPaidReturn()` when consuming `eight_ball_pending_profile_v1` on a paid round-trip.
- `name` (string)
- `dob` (ISO date YYYY-MM-DD)
- optional `time` (HH:MM string, 24-hour)
- optional `country` (country/zone entry code from `core/countries.js`)
- optional `lat` (decimal latitude)
- optional `lng` (decimal longitude)
- optional `city` (city display name from `assets/cities.json` — user's birthplace selection. Added v0.2.7.2.)
- optional `cc` (ISO 3166-1 alpha-2 country code, paired with `city` for display rehydration. Added v0.2.7.2.)
- optional `tz` (IANA timezone string — drives DST-aware rising-sign computation per §1.A v0.21. Added v0.2.7.2.)
- `eight_ball_labels_revealed_v1` — separate string flag (`'true'` or `'false'`) tracking whether the user has chosen to reveal the symbol-name labels on the result card. UI preference only; independent of the profile payload. Added v0.2.7.
- `eight_ball_tries_used_v1` — integer (serialized as string). Monotonic counter incrementing on every new-pair shake regardless of paid state. Read against the constant `3` for the §4.B cap check. Added v0.3.0.
- `eight_ball_credits_v1` — integer (serialized as string). `+= 3` on each `?paid=t1` handler invocation; decrements by 1 on each new-pair shake when `> 0`. Hits zero → next new-pair triggers paywall or locked render depending on `tries_used`. Non-negative invariant. Added v0.3.0.
- `eight_ball_pending_profile_v1` — JSON-stringified profile object, transient. Written when the paywall modal opens (both Path A new-pair shake and Path B lock-tap on already-rendered card per §5.C). Consumed on the `?paid=t1` return: one credit decremented, profile saved to `eight_ball_profile_v1`, key deleted. Lifetime is one round-trip; either absent or present-and-valid. Added v0.3.0.
- `eight_ball_tier_v1` — string tier flag (`'t1' | 't2' | 't3'`), the highest rung purchased per §1.D; absent = free. Monotonic — written only by the `?paid=t1|t2|t3` return handler as max(current, purchased) by ladder rank, plus the one-time §1.D legacy-grandfather write (credits with no tier key persist as `'t3'` on first render); never downgrades. UI preference-free, profile-free: it stores which density the device has bought, nothing about the person. Added v0.6.0 (§1.D / §4.B v0.36).
- `eight_ball_facet_index_v2` — **RETIRED by §1.H v0.62 (calc v4); superseded by the `_v3` bullet below and preserved verbatim as lineage per L17. Never read, never written; named in `ui/payments.js` and here solely for the one-shot clear.** integer string (`'0' | '1' | '2'`) naming the currently visible v1 note position for the t3 written entry, never the next position. Profile-free and PII-free. New profile and consumed pending-profile return reset it to the life-path anchor; reload and same-profile submission preserve it; `forget` clears it. It changes only alongside a funded explicit t3 `flip again`; a zero-credit paywall opening leaves it unchanged. Added v0.49 as `eight_ball_facet_index_v1` (§1.H); versioned to `_v2` by the v0.54 calc-v3 amendment — the retired `_v1` key is never read, is cleared once on the first facet read after calc-v3 load, and stays named in code (and on this list) only for that clear.
- `eight_ball_facet_index_v3` — **the active facet key as of §1.H v0.62 (calc v4).** Same shape, same semantics and same reset/preserve/clear rules as the `_v2` bullet above; only the name moves. The two retired generations `_v1` and `_v2` are never read and never written — both are cleared once on the first facet read after calc-v4 load — and they stay named in `ui/payments.js` and on this list solely for that clear (the v0.48 retired-key precedent, exercised twice now). Versioning rather than migrating is the point: the single stored index cannot represent how a device arrived at a position, so a value stored against a life path that calc v4 has moved (`1970-01-04`: `4 → 22`, `mid → high`) has nothing to migrate INTO. **No other key is added, read or written by this amendment.**
- `eight_ball_saved_readings_v1` — JSON array for the user-initiated Saved Readings archive. Each entry contains local metadata (`id`, user-editable `title`, ISO `savedAt`) plus a `profile` object restricted to the same user-entered reconstruction fields allowed for `eight_ball_profile_v1` (`name`, `dob`, optional `time`/`city`/`cc`/`tz`/`country`/`lat`/`lng`). No derived coordinate, catalog result, tier, credit, card prose, or payment data is stored. Added v0.50 (§5.E).

**v0.36 amendment (v0.6.0, allow-list semantics).** The `?paid=t1` references in the `eight_ball_credits_v1` and `eight_ball_pending_profile_v1` bullets above are preserved verbatim from their v0.3.0 wording per L17. As of v0.6.0 the same semantics apply to every `?paid=t1|t2|t3` return: +3 credits on any rung, identical pending-profile round-trip consumption (§1.D / §4.B / §5.B Call 2 v0.36). No other change to either key's behavior.

**v0.49 amendment (§1.H).** The §1.D "Credits unchanged" / β-idempotence language and the v0.3.0/v0.36 bullets above remain lineage for the form-submit read state machine: same-pair form submission is still credit-preserving. The t3 result-screen `flip again` is a separate state machine and now decrements `eight_ball_credits_v1` by 1 on each funded advance. A zero-credit t3 top-up clears `eight_ball_pending_profile_v1` before opening the paywall so an abandoned Path A/B intent cannot auto-consume on return. No other change to either key's behavior.

**v0.55 amendment (ownership — counters retired, 2026-07-26).** The counter semantics in the `eight_ball_tries_used_v1`, `eight_ball_credits_v1`, `eight_ball_pending_profile_v1`, `eight_ball_tier_v1`, and `eight_ball_facet_index_v2` bullets above (and their v0.36/v0.49 amendments) are superseded as follows. `eight_ball_tries_used_v1` is **retired** — never read, never written; the code names no such key and the scanner allow-list drops it (the v0.48 retired-key precedent; stale values on old devices are inert integers). `eight_ball_credits_v1` is **frozen as a read-only legacy signal**: never granted, never decremented; its sole consumer is the §1.D R2 grandfather read at render time, and once that persists the tier the value is ignored forever. `eight_ball_pending_profile_v1` keeps its one-round-trip lifetime but is written only by the Path B lock-tap (Path A is retired, §4.B v0.55) and its consumption no longer decrements anything. `eight_ball_tier_v1` is unchanged in shape and remains monotonic — it is now the entire purchase record. `eight_ball_facet_index_v2` advances on any explicit t3 flip (no funding requirement, §1.H v0.55); reset/preserve/clear rules unchanged.

Nothing else. No derived profile is stored — it's recomputed on each load. No analytics. No third-party endpoints. No third-party scripts. System fonts only.

**v0.50 allow-list amendment.** The "Nothing else" inventory above is superseded only by `eight_ball_saved_readings_v1` as specified in §5.E. The no-derived-profile rule remains literal: reopening an entry runs its stored input payload through the existing `profileFromPayload` → `buildProfile` and render path, so calculation outputs continue to have one authority.

**v0.63 gender-field amendment (§1.D v0.63, controller override, 2026-08-03).** The `eight_ball_profile_v1` field list above gains the one optional `gender` bullet, under the strict two-token vocabulary stated there. The field is user-entered and calc-driving — the §1.D v0.63 kua block reads it — so it forwards through `optsFromPayload` like the other calculation inputs; the no-derived-profile rule is untouched. The §5.E "same user-entered reconstruction fields" parenthetical (`name`, `dob`, optional `time`/`city`/`cc`/`tz`/`country`/`lat`/`lng`) is superseded to include optional `gender` under the same strict vocabulary — the archive must reconstruct the same reading the fresh submit produced, and a gender dropped there would silently re-key a saved kua on reopen. The privacy-copy update this section requires before merge shipped in the same PR: the about-modal storage sentence names the optional input, its single consumer, and that it can stay blank.

**v0.64 gender-field removal (§1.D v0.64, controller decision, 2026-08-30).** The v0.63 gender-field amendment above is superseded in full: the optional `gender` bullet leaves the `eight_ball_profile_v1` field list (removed from the list above in the same edit), and nothing writes, forwards, or rehydrates the field anywhere. Deleted rather than kept as a RETIRED entry — deliberately unlike the facet-key lineage bullets, which stay because code still names those keys for one-shot clears: this list is the §5 privacy inventory of what IS stored, gender is not stored and is actively erased, and a lingering bullet would overstate the stored surface; the lineage lives in this paragraph and §1.D. The §5.E "same user-entered reconstruction fields" parenthetical returns to its original list (`name`, `dob`, optional `time`/`city`/`cc`/`tz`/`country`/`lat`/`lng`). The v0.63 rationale — a gender dropped from the archive would silently re-key a saved kua on reopen — no longer holds: the kua read is gender-independent (§1.D v0.64, both classical values for every profile), so reopen and fresh submit are identical without the token. Devices that stored a gender under v0.63 are cleaned at boot: three single-key scrubs (`eight_ball_profile_v1`, `eight_ball_pending_profile_v1`, `eight_ball_saved_readings_v1`) delete exactly the stale `gender` keys — read-verified, pure reads when there is nothing to strip, surgical by mutation-pinned contract (no other field, entry, or ordering may change), and silent-fail-safe on storage errors because the token is inert to every reader. No new localStorage key is added. The privacy copy shipped in the same PR: the about-modal and paywall disclosures no longer name a gender input.

**Network behavior is scope-restricted, not absolute** (v0.21 amendment). The product makes no telemetry calls, no third-party calls, no fingerprinting calls, no out-of-band data flow.

**First-party server-side observability is not telemetry** (v0.35 clarification). Passive aggregate request logs generated by the hosting platform (e.g. Netlify's own server-side analytics) are server-side, carry no client-injected tracker, no fingerprint, no third-party script, and no identifier the product emits. Reading the operator's own origin logs is permitted. The ban is on out-of-band / third-party data flow the product *creates* — not on the operator observing first-party traffic to their own site. This clarification adds no code surface and changes no scanner: `tests/privacy_scan.test.js` still forbids `fetch()`/`XMLHttpRequest`/`navigator.sendBeacon`/third-party scripts in tracked source, because first-party server logs are produced by the host platform, never by tracked source.

The following same-origin asset loads are permitted:

- **Initial page load.** HTML and ES modules served from the deployed origin, plus same-origin static assets (favicons, og:image).
- **Same-origin lazy loads.** Dynamic ES `import()` of same-origin JSON or modules. As of v0.2.7.2, `assets/cities.json` is lazy-loaded the first time the user opens the rising-sign `<details>` element. Lazy loads carry no user state — they fetch a static asset whose bytes are identical for every visitor. *Trigger superseded (2026-08-30, pr214, mechanical):* the rising-sign `<details>` surface is long retired; since then the load trigger has been the first debounced birthplace search, and it is now the first focus of a birthplace field (`warmCities()`, once per session across both city fields, sharing `loadCities()`' cache and its three bounded same-origin specifiers). The accepted cost is named: a visitor who focuses the optional field and never types now fetches the asset where before they would not have. Everything the permission rests on is unchanged — same asset, no user state, bytes identical for every visitor, no new mechanism.
- **§5.B user-initiated network calls.** Two outbound network calls, both user-initiated (see §5.B): (1) feedback form POST to Netlify Forms, (2) Gumroad Buy Link redirect for paid-tier unlock. Added v0.3.0 (LS path); swapped to Gumroad at v0.3.0.3.

`fetch()`, `XMLHttpRequest`, and `navigator.sendBeacon` remain forbidden in tracked source under `core/`, `content/`, and `index.html` — enforced by `tests/privacy_scan.test.js`. Dynamic `import()` with import attributes (`with: { type: 'json' }`) is the permitted same-origin lazy-load mechanism for static JSON; it does not trip the `fetch(` ban because it is an ES module-loader call, not an XHR.

The v0.14 wording "zero network requests after page load" is retired by this amendment. The intent — no third-party traffic, no telemetry, no out-of-band data flow — is preserved verbatim.

If a future feature requires storing or transmitting more, that feature requires a doctrine amendment to §5 and a privacy-policy update before merge.

## §5.B — User-initiated network calls (feedback v0.2.5, LS redirect v0.3.0 retired, Gumroad redirect v0.3.0.3)

The site permits exactly two user-initiated network calls:

1. **Feedback POST** — submission of a feedback form to the Netlify Forms endpoint at the same origin (added v0.2.5).
2. **Gumroad Buy Link redirect** — navigation away to the operator's hosted Gumroad checkout when the user taps the paywall CTA (added v0.3.0 as Lemon Squeezy path; swapped to Gumroad at v0.3.0.3).

Both calls are out-of-band — neither echoes user profile state. These are the only network calls permitted in the product runtime; everything else in §5 stands.

### Call 1 — Feedback POST

Constraints:

- **User-initiated only.** The submission fires only on user click of the explicit submit button. No timer, no auto-fire, no submission on page load, on shake, on result-render, or on any other implicit trigger.
- **User-authored content only.** The submitted payload contains exactly two fields: a free-text `message` and an optional free-text `contact`, both typed by the user inside the form. No localStorage profile data (`name`, `dob`, `time`, `country`, `lat`, `lng`, `city`, `cc`, `tz`) is included. No derived coordinates. No name, no DOB, no IP enrichment from the page, no analytics tags, no UTM params, no fingerprint. The form has no hidden field carrying user-data state.
- **Single named endpoint.** The `<form>` action is the same-origin Netlify Forms handler — either no `action` attribute (Netlify intercepts the POST and serves its branded thanks page) or `action="/?sent=1"` for a same-origin success redirect that the v0.2.5.2 banner JS detects and swaps to the in-page "thanks. read." state, then `replaceState` strips the query. The v0.2.5.2 success redirect is the shipped form (`tests/feedback_surface.test.js` locks `action="/?sent=1"`). No third-party form provider, no webhook fan-out, no secondary destination.
- **Native form post, no JavaScript fetch.** The form uses HTML `<form method="POST" data-netlify="true">` semantics. No `fetch()`, no `XMLHttpRequest`, no `navigator.sendBeacon`. The privacy scan (`tests/privacy_scan.test.js`) is unchanged and remains the enforcement.
- **Fail-silent.** On submission failure (offline, provider outage, network error), the browser handles the error natively. No retry queue, no remote error reporting, no telemetry of failure. If the user's browser is offline, the submit fails and the user can decide what to do.
- **Submission visibility.** Submissions land in the Netlify dashboard under the Forms tab. The operator may configure email notifications per Netlify defaults. Submissions are not echoed back to the page or persisted in localStorage.
- **No retention by 8ball.** 8ball-the-codebase does not retain submissions. Netlify-the-platform does (for as long as operator keeps them in the dashboard); that is operator's responsibility, not 8ball's runtime concern.

The about-modal copy is updated to disclose this surface honestly. Disclosure is the load-bearing piece: anyone reading the modal must learn that a feedback form exists and what happens when they submit it.

### Call 2 — Gumroad Buy Link redirect (v0.3.0 LS path → v0.3.0.3 Gumroad path)

User taps `unlock · $3` on the paywall modal → browser navigates to `https://theeightball.gumroad.com/l/rzqezp` (hosted by Gumroad, which acts as Merchant of Record). Unlike the v0.3.0 Lemon Squeezy path, Gumroad does not accept a URL-encoded `success_url` query parameter on the Buy Link href; post-purchase redirect to `/?paid=t1` is handled entirely on Gumroad's side via the product's Content-tab "→ unlock your reads" Button (configured 2026-05-16 chat-27 directive F) which links to `https://the-eight-ball.netlify.app/?paid=t1`. The redirect mechanism is therefore single-source (Gumroad-side Content Button), not the belt-and-suspenders pattern of the prior LS path. Two-click post-purchase flow: paywall tap → Gumroad hosted checkout → Content-tab Button tap → `/?paid=t1`.

Gumroad processes payment via Stripe (Gumroad's internal Stripe Connect integration; operator's verified Connect account cleared 2026-05-16 per `~/Desktop/8ball/audits/gumroad_post_confirm_recheck_2026-05-16.md`). PayPal optional at buyer choice per Gumroad's default checkout. On successful payment, Gumroad displays the product Content page with the "→ unlock your reads" Button; on Button-tap, browser navigates to `https://the-eight-ball.netlify.app/?paid=t1`. The page-load JS handler at the same `/?paid=t1` query parser calls `applyPaidReturn()` from `core/payments.js` to credit the device (`+= 3`) and — if `eight_ball_pending_profile_v1` is present — consume it (decrement one credit, save the profile, render unlocked), then `replaceState({}, '', window.location.pathname)` strips the query for a clean URL.

Constraints:

- **User-initiated only.** Tap of the explicit paywall CTA. No timer, no auto-fire, no redirect on page load, on shake, on result-render, or on any other implicit trigger.
- **Hosted redirect, not in-page overlay.** Plain `<a href>` navigation. The Gumroad overlay/embed script is permanently rejected per §5's third-party-script ban — same constraint as the prior LS path, operator decision logged in the v0.3.0.3 cycle commit.
- **No user data leaves on the redirect.** The Buy Link URL is static — no profile fields encoded, no UTM tags, no tracking identifiers appended. The bare URL contains no query parameters at all (distinct from the v0.3.0 LS path which carried URL-encoded `checkout[success_url]`); `tests/payments_markup.test.js` v0.3.0.3 assertions lock this shape.
- **No SDK, no `fetch`, no webhook.** 8ball-the-codebase never talks to Gumroad programmatically; the redirect IS the entire payment surface.
- **Two-click post-purchase return (Gumroad-specific).** The post-purchase flow is two clicks (paywall → Gumroad checkout → Content-tab Button → `/?paid=t1`), structurally different from the LS path's single-click `checkout[success_url]` redirect. The Content-tab Button text is operator-controlled and ships as "→ unlock your reads" per chat-27 directive F.
- **Trust-based return.** `/?paid=t1` is unsigned. Manual URL entry grants credits without payment (see §5.C). Acceptable for the arcade-toy model and disclosed in the about-modal.
- **No customer object retained.** Gumroad captures buyer email for its own receipt and tax compliance; 8ball never sees it. No localStorage entry for purchase metadata beyond `eight_ball_credits_v1` count.
- **Disclosure.** The about-modal names Gumroad as the payment processor and the data-flow boundary (payment + email to Gumroad; reading stays on-device). The paywall modal carries a compact `.modal-disclosure` line mirroring the same disclosure.

**v0.36 amendment (v0.6.0, tier ladder).** Call 2 generalizes to **three Gumroad products — one per rung**, same mechanism per product: t1 · $3 `https://theeightball.gumroad.com/l/rzqezp` → Content-tab Button → `/?paid=t1`; t2 · $6 `https://theeightball.gumroad.com/l/neysyv` → `/?paid=t2`; t3 · $9 `https://theeightball.gumroad.com/l/xjpvp` → `/?paid=t3`. Each paywall-ladder CTA is a plain bare-href Buy Link (no query parameters, no UTM, no tracking identifiers — `tests/payments_markup.test.js` locks all three shapes); the post-purchase redirect stays single-source on Gumroad's side via each product's Content-tab Button. The page-load handler is generalized to `?paid=t1|t2|t3`: `applyPaidReturn` credits the device +3 and raises the stored tier to max(current, purchased) per §1.D; unknown `?paid=` values are ignored (replay-safe branch preserved). All Call 2 constraints — user-initiated only, hosted redirect not overlay, no user data on redirect, no SDK/fetch/webhook, trust-based return, no customer object retained, disclosure — apply to each product unchanged. This remains one mechanism with three product instances; the §5.B "exactly two user-initiated network calls" enumeration is unchanged in kind (feedback POST + Gumroad Buy Link redirect).

**v0.55 amendment (ownership repricing, 2026-07-26).** The three products repriced on Gumroad 2026-07-26: **t1 · $1 / t2 · $2 / t3 · $3** — product IDs, Buy Link URLs, Content-tab Buttons, and `?paid=` params all unchanged; the v0.36 prices above are lineage. The base Call 2 prose's `unlock · $3` CTA copy and `+= 3` credit language are likewise lineage: the handler grants no credits — `applyPaidReturn` writes the monotonic tier (its entire effect) and hands back a pending profile if one is present. The listings now promise the purchase is permanent ("yours for good"); the code honours that per §1.D v0.55. The **trust-based return** constraint reads accordingly: manual `/?paid=tN` entry grants the tier flag without payment — still acceptable, still disclosed (§5.C). The **no customer object retained** constraint's "beyond `eight_ball_credits_v1` count" reads "beyond the `eight_ball_tier_v1` flag" — the legacy credit key persists only as the R2 grandfather signal. Everything else — mechanism, constraints, two-call enumeration — unchanged.

If a future feature would require a third endpoint, additional fields, or any non-user-initiated network behavior, that requires a further §5 amendment.

## §5.C — Content-delivery transparency (added v0.3.0)

The card-content layer (`content/cards.v1.full.js` — `name`, `type`, `habit`, `note` × 144 cards) ships in the public bundle. JS visibility is gated by the `eight_ball_credits_v1` flag; the data itself is inspectable via View Source, and `/?paid=t1` is not signed — manual URL entry will grant `+= 3` credits without payment. **The lock is a UI convention; the deck is in the source — we trust adults to tip when they enjoyed the toy.**

**v0.55 amendment (ownership, 2026-07-26).** The gate in the paragraph above is now the stored tier: JS visibility of the written entry is gated by `eight_ball_tier_v1` at t3 (the §1.D density rule; the credits flag survives only as the R2 grandfather signal). `/?paid=tN` remains unsigned — manual URL entry sets the tier flag, permanently, without payment. Same trust posture, now with a smaller blast radius in the other direction too: there is nothing to top up, so the only thing a replay can ever grant is a rung the toy sells for at most $3. Disclosure unchanged.

Backend-gated delivery (Netlify Functions + Stripe webhook verification, or any processor's server-validated license-key issuance) is doctrinally permitted as a future amendment but not built in v0.3.0. The about-modal discloses the bundle shape so payment is informed-tip, not deception. The disclosure line "the deck is visible in source; the lock is a convention, not a vault" is the load-bearing piece — users tip the operator for the toy, not for content scarcity.

## §5.D — Share surface (added v0.4.0)

The result surface MAY offer a user-initiated share affordance that renders the free card to a raster image (PNG) entirely on-device (SVG → canvas → `toBlob`; no network). Sharing uses the Web Share API where available (`navigator.share` with the image file); otherwise it falls back to a local file download plus a clipboard copy of the bare production URL. Invariants:

- **(a)** the shared artifact contains only the free coordinate symbols, their section labels, the catalog number, the 8ball wordmark, and the bare production URL — never the paid card-content layer (name/type/habit/note), never name or DOB;
- **(b)** no per-result link or query parameter encodes any profile field;
- **(c)** no telemetry, no share counting, and no network call of any kind is introduced — §5 same-origin/no-beacon and §7 no-telemetry are preserved;
- **(d)** the affordance is offered on the free surface to maximize organic reach.

The image builder reads only the rendered free coordinate symbol nodes (and their section titles + catalog number) from the live DOM; it never reads a profile object or the unlocked card-content slots, which is how invariants (a)/(b) are mechanically guaranteed. DOM-touching code lives in `ui/share.js` (`initShareUI({refs}, {hooks})` per §6), not in `index.html`. This clause is the privacy review contemplated by the §12 "until a privacy review explicitly clears it" wording.

**v0.36 amendment (v0.6.0, tier-aware share).** The share affordance renders the card **at the user's current render tier** — paid users emit denser PNGs. Invariant (a)'s "free coordinate symbols" generalizes to "the rendered coordinate symbols at the current tier"; every other protection in (a)–(d) is preserved verbatim and applies at every tier: labeled coordinate symbols + section labels + catalog number + 8ball wordmark + bare production URL only; never the written card-content layer (name/type/habit/note); never name or DOB; no per-result link or query parameter encoding any profile field; no telemetry, no share counting, no network call of any kind; affordance offered on the free surface (and every paid render). Mechanically the builder still reads only rendered coordinate symbol nodes from the live DOM and now skips tier-hidden rows (the `ui/tiers.js` gating sets them hidden); it still never reads a profile object or the card-content slots.

**v0.39 amendment (reach pass v1 — full-sheet share artifact).** The share artifact renders the FULL specimen sheet at every tier: the rendered coordinate values at the user's tier PLUS the SEALED STRUCTURE of the higher-tier compartments — section label + seal hatch, the coordinate VALUE absent. Rendering is **per compartment, not per row**: a mixed row (e.g. `SUN · RISING` on the free card) surfaces its open value beside its sealed compartment(s); the free PNG shows all 14 compartments (open values + seal hatches), mirroring the on-card v0.37 compartment sheet. This resolves the v0.37 logged deferral ("sealed-structure-in-PNG is a logged journal deferral; `ui/share.js` UNTOUCHED"). Every invariant (a)–(d) is preserved verbatim: a sealed coordinate's VALUE never appears in the artifact; never the written card-content layer (name/type/habit/note); never name or DOB; no per-result link or query parameter encodes any profile field; no telemetry, no share counting, no network call of any kind; the affordance is offered on the free surface. Invariant (a)'s "rendered coordinate symbols at the current tier" generalizes to "the rendered coordinate values at the current tier PLUS the seal-hatch structure (section label only, value absent) of the tier-hidden compartments." The mechanical guarantee is unchanged and is what makes the added structure safe: the builder reads ONLY the live DOM, where a sealed cell renders `textContent === ''` (the v0.37 compartment contract — absent, not hidden), so the sealed VALUE does not exist in the snapshot the builder serializes; `ui/tiers.js shareRowRefs` exposes a per-cell `{state, value}` snapshot derived from that same live cell state, never from a profile object or a tier constant (the prior tier-hidden row filter in `ui/share.js` is removed). NEW — share caption: the artifact MAY be accompanied by a clinical caption (the catalog numeral + the open coordinate VALUES + a `sealed remainder` marker + the bare production URL) carried in `navigator.share`'s text field where supported, and copied to the clipboard on the desktop fallback in place of the prior bare-URL-only copy. The caption is built from the SAME DOM snapshot and is bound by every (a)/(b) invariant — no name/DOB, no profile read, no per-result parameter, no sealed coordinate value — and stays in the §2 clinical register (no sales language). Sibling tracked-file changes co-versioned: `ui/share.js` (full-sheet builder — sealed cells draw an SVG `<pattern>` hatch + label, value absent; the per-cell `sealed` state replaces the tier-hidden row filter; `buildCaptionFromSnapshot` + caption coupled into the share flow), `ui/tiers.js` (`shareRowRefs` per-cell `{state, value}` snapshot, DOM-derived; the sealed value is never passed), `index.html` (one-line cold-landing `#mechanism-strip` near the registry header — clinical, no pricing/CTA; share-wiring comment updated; 1499/1500 held by offsetting compaction), `tests/share_surface.test.js` (full-sheet eight-row render, sealed = hatch + label + no value, aggregate paid-value-absence sentinel, F4 unresolved-vs-sealed in the PNG, caption PII/voice, share-flow caption coupling, mechanism-strip placement/no-CTA). Ships within the 0.7.x line — `package.json` deliberately not bumped (flagged for Procedure 4 version-truth review, as in v0.38). No calc change, no `content/` deck touch, no `core/` touch, no new dependency, no new localStorage key, no network call introduced — `privacy_scan` FORBIDDEN list + `LOCALSTORAGE_KEY_ALLOW` unchanged; `pii_scan` DOCTRINE_ALLOW unchanged. Trigger per §13: reach pass — the verified reach bottleneck (the 06-15 ship-gate read; orchestrator dissent on record since v0.38). Codex pre-build audit cleared this scope 2026-06-29 (P1 hook H2 → this amendment; disposition `~/Desktop/8ball/audits/codex_prebuild_reach_pass_v1_2026-06-29_response.md`); brief `~/Desktop/8ball/sessions/brief_reach_pass_v1_2026-06-29.md`.

## §5.E — Saved Readings (v0.50)

The result surface MAY offer `save reading` after a completed render. Saving is explicit and user-initiated; generating, rehydrating, sharing, opening the archive, or loading the page never saves an archive entry implicitly. The Previous Readings screen lists entries newest-first by `savedAt` and permits reopen, inline rename, confirmed individual deletion, and confirmed clear-all. `forget this device` clears both the current `eight_ball_profile_v1` payload and the complete Saved Readings archive.

**Storage minimum.** Each entry stores only `{ id, title, savedAt, profile }`. `profile` is a copy of the user-entered reconstruction payload allowed under §5; `id`, `title`, and `savedAt` exist only to identify, label, and order the local archive. Exact input profiles deduplicate to one saved entry. Derived coordinates, catalog/card output, card prose, entitlement tier, credits, tries, purchase metadata, and share artifacts are never copied into the archive.

**Revisit contract.** Opening a saved entry makes its profile the current local profile and recomputes the reading through the existing `profileFromPayload` / `buildProfile` / `showResult` path at the device's current entitled density. It does not call or alter the new-reading state machine, does not increment `eight_ball_tries_used_v1`, and does not decrement `eight_ball_credits_v1`. This is the same deterministic-rehydration class as cold-boot profile restoration and same-pair idempotence, not a new calculation or entitlement path. `core/`, `content/`, and `tests/fixtures.json` remain untouched.

**Failure and retention behavior.** Missing storage reads as an empty archive. Malformed outer JSON fails closed and is never silently overwritten by Save; the UI offers clear-all as recovery. Partially malformed arrays display only valid entries and disclose that some entries could not be read; the archive remains read-only until clear-all so an ordinary mutation cannot silently discard unreadable entries. Read, write, quota, or removal errors never crash generation or the calculation pipeline; the UI reports that saving/clearing is unavailable. Entries persist until the user deletes them, clears all, uses `forget this device`, clears site data, or the browser evicts local storage.

**Privacy boundary.** No account, backend, sync, analytics event, remote database, third-party storage, new endpoint, `fetch`, `XMLHttpRequest`, or beacon is introduced. The about modal and Previous Readings screen disclose the device-only scope and deletion controls. `tests/privacy_scan.test.js` allow-lists exactly `eight_ball_saved_readings_v1`; any second archive key or broader payload requires another §5 amendment.

## §5.F — Registry + Concordance (v0.51)

Concordance uses the Saved Readings archive as its only selector. Selecting two entry ids is transient UI state held in memory only. Comparison does not add a localStorage key, add a field to `eight_ball_saved_readings_v1`, create pair history, cache derived coordinates, store relation output, create a share artifact, or alter either saved entry. Closing or reloading the page discards the selection and result. The existing archive retention and deletion controls in §5.E remain the complete data-retention surface.

Both entries are validated by the existing archive loader and recomputed through the same calculation pipeline before comparison. Corrupt or partially valid archives preserve §5.E's fail-closed/read-only behavior; only valid displayed entries may be selected. If reconstruction or relation lookup fails, the UI reports that comparison is unavailable and leaves storage unchanged. Blocked/unavailable storage yields no selectable entries and does not affect ordinary reading generation. No account, backend, sync, analytics event, remote database, third-party storage, endpoint, network call, runtime dependency, or calculation-engine change is introduced. `tests/privacy_scan.test.js`'s key allow-list is unchanged from v0.50.

## §6. Architecture

- Single repo, private on GitHub as of v0.2.0 (was public through v0.1.4; flipped private to protect the future paid card-content layer at `~/dev/8ball-private/`).
- **Visibility correction (2026-08-01; L17).** The sentence above is preserved as historical lineage but superseded as current state: the GitHub repository is **PUBLIC**, verified against authenticated GitHub state on 2026-08-01. The separate authoring source at `~/dev/8ball-private/` remains private. Repository visibility is not content security: §1 v0.22 already records that the public bundle contains the full deck bytes and that its lock is a UI convention.
- Static site. Deployed on Netlify Pro ($20/mo) from `main` branch. (Was misdocumented as free tier; corrected 2026-07-20 to match 8BALL.md / README ops truth — mechanical, no product change.)
- ES modules in the browser. No build step.
- Four-folder source layout: `core/` (pure-logic ES modules), `ui/` (DOM-touching ES modules with `init*UI({refs}, {hooks})` DI shape per the v0.23 amendment below), `content/` (versioned static data — card deck, coordinate meanings, and the §1.I concordance registry), `tests/` (gates + `fixtures.json`). This clause pins the layout *shape*, not the file list — **v0.44 correction: no per-folder counts are hardcoded here anymore.** They drifted twice already (v0.41's "6 ui / 24 tests" was stale within two content cycles, per the 2026-07-04 drift-sweep) precisely because this clause restates a number it says elsewhere shouldn't live here. The canonical module/test inventory lives in `CLAUDE.md` (with `README.md`) — that is the single place to check current counts; this clause will not restate them again. Plus `index.html` and config. The private location at `~/dev/8ball-private/cards.v1.full.js` remains the authoring source; the public `content/cards.v1.full.js` is a verbatim copy maintained via `cp` at release-prep time.
- The single-file rule: `index.html` may not exceed 1500 lines. If it would, split into `ui/*.js` modules. Not before. **v0.23 amendment: the split-target shape is locked. A `ui/*.js` module is an ES module with pure exports (no DOM, vitest-testable without jsdom) alongside DOM-touching helpers gated by an `init*UI({refs}, {hooks})` injection point. Host-side `let` bindings stay in `index.html`; the module mutates them via host-injected setter callbacks (e.g. `setCurrentProfile`, `setSelectedCity`). Module-internal localStorage keys are bare-string `const KEY = '...'` definitions inside the module so `tests/privacy_scan.test.js`'s same-file identifier resolution catches them. Concrete precedents: `ui/payments.js` (v0.3.0 step 6, 165 LOC) and `ui/profile.js` (v0.3.0 step 7, 153 LOC). Future splits inherit the shape rather than re-deriving it.**
  **Shell-stylesheet amendment (2026-08-31, controller-ordered).** Two clarifications to the sentence above, which stands verbatim per L17. (1) The v0.23 shape governs LOGIC and MARKUP splits; the host's own static styles have a second sanctioned split target — a plain `ui/*.css` stylesheet linked from the head, the shape `ui/experience.css` established. Under this amendment the whole inline `<style>` block moved verbatim to `ui/shell.css` (linked FIRST, before `ui/experience.css`, preserving the cascade order the inline block had); `index.html` went 1455 → 683 lines. A stylesheet move is delivery, not architecture: no rule changed, feature modules keep injecting their own scoped styles at init, and "shared rules stay in the shell" now reads shell = `index.html` + `ui/shell.css`. (2) "Not before" bound the agents, not the controller: this split ran below the 1500 cap on the controller's explicit order, per the standing instruction that the next sizable feature must start by splitting rather than trimming. (3) With the block moved, no inline `<style>` element may return to `index.html` — the headroom this split bought erodes exactly there, and `tests/meanings_ui.test.js` pins the ban; inlining a critical-CSS block would be a fresh amendment to this clause, not an exception to it.

## §7. CI gate

`.github/workflows/ci.yml` runs on every push and PR to main. The gate has these stages:

1. **Calculation contract + engine pipeline** — `tests/profile.test.js` must pass (calc cases against `tests/fixtures.json`, `getCard` pipeline across the 144 positional catalog cells, `resolveBracket` cases). **v0.22 extension:** additionally verifies `content/cards.v1.full.js` parses as an ES module exporting `CARDS` with exactly 144 entries (12 sun signs × 12 animals), and runs the existing `BANNED_VOICE_REGISTER` scan against the deck's content strings (`name`, `type`, `habit`, `note.{low, mid, high}` fields). **v0.44 extension:** `tests/meanings_content.test.js` runs the same `BANNED_VOICE_REGISTER` + `BANNED_PATTERNS` tables (imported from `tests/profile.test.js`, not duplicated) against every `content/meanings.v1.js` entry, plus a completeness check against `SUN_SIGNS`/`ANIMALS`/`MAJOR_ARCANA` and a zero-tolerance second-person-address scan (§1.G). **Coverage catch-up (2026-07-20; documents shipped test reality, no rule change — see journal):** stage 1's enumeration above under-describes the live §2 voice-register surface — the same banned-term scans also run against the concordance registry and its assembled output (`tests/concordance.test.js`, #101/#107), the provenance placards (`tests/provenance.test.js`), and the atlas legend (`tests/atlas.test.js`), and as of the #104→#109 chain every `BANNED_VOICE_REGISTER`-based scan routes through the shared case-insensitive substring matcher `voiceRegisterHits()` with its explicit `SUBSTRING_SAFELIST`, while the second-person / diagnostic-framing regexes and the word-bounded `BANNED_PATTERNS` are separate voice-policy checks the helper also owns, applied only by the scans that name them (deck / meanings / concordance — not provenance/atlas) — `tests/helpers/voice-register.js` is now the canonical table home, superseding the `tests/profile.test.js` attribution in the v0.44 note above (preserved verbatim per L17).
   **v0.53 extension:** `tests/meanings_content.test.js` also checks all five `content/meanings.v2.js` element entries, all fourteen context roles, and every number-theme alias for shape, completeness, the canonical banned register/slur tables, and zero second-person address; `tests/meanings_behavior.test.js` pins the deterministic three-theme synthesis and the absence of derivation copy in resolved panels.
   **v0.54 extension:** calculation fixtures and direct tests pin strict `1..9` reduction across all six resolved numerology outputs and `null` for an empty contributing letter subset; tier tests require `—` for null/zero/out-of-range inputs so no tenth number can render; bracket/facet tests reject retired master values; meaning tests require exactly nine active entries and numerology-only partners; Concordance tests require all 36 distinct `1..9` pairs to remain unfiled and reject values outside the active range.
   **v0.62 extension (calc v4).** The v0.54 extension is superseded; its text stands as lineage per L17. Every gate above is re-pointed at the twelve-value terminal domain, and each is stated as the PAIR it has to be, because a domain widening is the one change an all-negative assertion greens on: calculation fixtures and direct tests pin the master-stop reduction (`29 → 11`, `38 → 11`, `44 → 8`) and a 5,000-value sweep that nothing escapes the domain, alongside the retained `null` guard; tier tests require `—` for null/zero/out-of-range inputs **and** require `11 / 22 / 33` to render as themselves on all six numerology cells; bracket/facet tests pin masters to `high`/third position, pin bracket and anchor agreement across the whole domain, and still reject `10 / 12 / 44`; meaning tests require exactly twelve active entries, byte-identity of the three master entries with immutable v1, and still refuse a value the calculator cannot produce; Concordance tests require exactly three registered master-reduction links out of 66 distinct pairs with the other 63 unfiled and none `adjacent`; dyad tests pin master inputs, master combined outputs, and the `sum !== combined` frame rule; public-tier tests pin that the birthday keeps its master value, that the mode table stays nine entries, that the bridge is reported in the returned reading, and — the pin the first implementation lacked — that the note is RENDERED for both reachable master birthdays on the host block and on both dyad sheets, cleared on the sealed branch, on `clear()`, and on a following unbridged render. The facet-key migration pin covers both retired generations (`_v1` and `_v2`), and `LOCALSTORAGE_KEY_ALLOW` adds `eight_ball_facet_index_v3` while retaining both retired names for the one-shot clear.
2. **Privacy scan** — `tests/privacy_scan.test.js` must pass (no unpermitted network calls — only §5-permitted same-origin lazy loads and §5.B user-initiated calls are allowed; no third-party fonts/scripts; system fonts only). **v0.22 extension:** `LOCALSTORAGE_KEY_ALLOW` extended with `eight_ball_tries_used_v1`, `eight_ball_credits_v1`, `eight_ball_pending_profile_v1` per §5. **v0.49 extension:** the allow-list also includes the PII-free current-position key `eight_ball_facet_index_v1` per §1.H / §5. **v0.50 extension:** the allow-list adds exactly `eight_ball_saved_readings_v1`; `tests/readings.test.js` locks its minimal schema, save/load/deduplication, rename, deletion, clear-all, corruption, and unavailable-storage behavior. **v0.54 extension:** the allow-list adds the versioned `eight_ball_facet_index_v2` and retains `eight_ball_facet_index_v1` solely because `ui/payments.js` still names the retired key for its one-shot calc-v3 clear; `tests/facet_rotation.test.js` pins the migration (a stale pre-v3 position never renders; `forget` scrubs both generations). **v0.55 amendment:** the allow-list retires `eight_ball_tries_used_v1` — the ownership model (§5 v0.55) reads and writes no such key; retired-key precedent v0.48.

**v0.51 test extension.** `tests/concordance.test.js` pins the complete finite registered inventory, matching-axis status/output shape, paid element entitlement, transient retention marker, host-side `profileFromPayload` reconstruction, no profile mutation, and absence of storage/network capability. The privacy allow-list remains unchanged; `core/` and `tests/fixtures.json` must have an empty feature diff.
3. **PII scan** — `tests/pii_scan.test.js` must pass (operator-name leakage, SIRR cross-reference leakage, labeled-DOB leakage). **v0.22 extension:** scans `content/cards.v1.full.js` for the same banned patterns; no DOB strings should appear in card content but the scan codifies the invariant.
4. **Dependency discipline** — `tests/dependency_discipline.test.js` must pass (npm runtime dependencies absent/empty, no build step, devDependencies ≤ 5).
5. **Single-file rule** — `index.html` ≤ 1500 lines.
6. **Payments state machine** (added v0.3.0) — `tests/payments_state.test.js` must pass (`isNewPair`, `nextShakeState`, `applyPaidReturn` transitions covering credits, cap, paywall trigger, replay-attack `no-pending` branch, same-profile idempotence, pending-profile round-trip). **v0.49 extension:** `tests/facet_rotation.test.js` covers `nextFacetState`, life-path anchors, strict round-robin rotation, current-position persistence, debit coupling, t3-only host wiring, and the zero-credit no-hidden-burn path (§1.H). **v0.55 amendment:** both suites rewritten to the ownership model — `nextShakeState` renders unconditionally (render / render-idempotent only), `applyPaidReturn` covers the monotonic tier write + pending render with no credit grant, the R2 grandfather and replay-safe `no-pending` branches stay pinned, and `tests/facet_rotation.test.js` covers unfunded always-advance rotation; the credit/cap/paywall-trigger transitions above are lineage. Density assertions in `tests/tiers.test.js` must keep proving the four distinct §1.D surfaces (free = 5 of 15 · t1 = 10 · t2 = 14 · t3 = 15 + `cardEntry`). Markup-static assertions for the paywall surface in `index.html` are covered by `tests/payments_markup.test.js` (counted under stage 5's file budget; runs in the same vitest pass).

**v0.61 extension (the dyad rung, §1.J).** Three suites join the gate, and they map onto the stages above rather than forming a fourth thing. **Stage 1** gains `tests/dyad.test.js` (the relation engine: determinism, table totality with every `kind` re-derived from the sheng/ke cycles rather than trusted, the byte-identity of each single side, and the branch-pair expansion — the one remaining deliberate fork, pinned against its original `ui/concordance.js` table walk by a differential test, because `core/` must not import `ui/`. The day master is NOT a fork — the F4 remediation (line 153 above) reads it off `profile.dayPillar` rather than recomputing it, and the suite pins CONSUMPTION, a supplied pillar that contradicts the raw date still winning, not a second calculation agreeing with the first; this paragraph called it a fork for a day after that fix landed, and PR #187 P2-R4 is the correction.) and the dyad half of `tests/content_shape.test.js` (the §1.J grammar pins, held to the same classes of rule the 144-card deck is). **Stage 1's voice surface** gains `tests/dyad_content.test.js`, which runs the canonical `voiceRegisterHits` / second-person / diagnostic-framing / `BANNED_PATTERNS` tables over both the frozen tables and the ASSEMBLED runtime output (the #101/#107 convention), plus two scans this surface needs and no single-reading surface does: a compatibility-claim scan and a person-as-subject scan, each carrying a positive-fire sentinel so neither can false-green. **Stages 2 and 6** gain `tests/dyad_surface.test.js`: no new `localStorage` key and no network capability in `ui/dyad.js`, the fail-closed offer (`T5_PRODUCT_URL` empty ⇒ no `href`, hidden CTA), the §1.D v0.37 sealed-DOM contract driven through a real render, and the ladder append itself — `RETIREMENT_COLLISIONS` empty, a stored `t4` still migrating to `t3`, the R2 grandfather still capped at `t3`, and the §1.F census identical at t3 and t5. Density assertions in `tests/tiers.test.js` keep proving the four distinct §1.D surfaces; t5 adds no coordinate, so the four-surface count is unchanged and that is itself pinned.

(History note: the `cards.v*.js` content-scan retired in v0.2.0 when the deck moved private; it returns as of v0.3.0 against the new public-tracked `content/cards.v1.full.js` — see stage 1's v0.22 extension and stage 3's. The private content-authoring pipeline at `~/dev/8ball-private/` continues to run its own scans before each release-prep `cp` to the public path.)

Netlify is configured to auto-deploy on push to `main`. CI runs in parallel on the same push. Failed CI does not currently block the auto-deploy itself — this gap is acknowledged and acceptable while traffic is operator-only. Wiring a Netlify required-check on the GitHub Actions status is queued for the first traction milestone (see `journal.md`); at that point, this paragraph gets re-tightened to "a failed gate blocks the deploy."

**v0.43 amendment (A3 — Netlify-required-check trigger bound per §13).** "The first traction milestone" above is an unobservable park, against §13 v0.35's named-trigger rule. Bind it to the concrete proxy already used as the journal's ship-gate: **≥5 paid purchases plus ≥1 strong divination-niche signal**. Until that threshold is met, the failed-CI-does-not-block-deploy gap is an **indefinitely-accepted gap** while traffic is operator-scale — not parked work awaiting an unnamed event. (At the mid-June ship-gate read the count was 0 sales — reach is the verified bottleneck — so the trigger is nowhere near firing; this is wording precision, not a deploy-risk change.) When the threshold is met, the paragraph above re-tightens to "a failed gate blocks the deploy." Per L17 the original wording is preserved verbatim above.

## §8. Release ritual

Every release, however small, has automated gates and ritual gates.

**Automated gates (CI, blocking):**

1. CI green (6 stages — calc+pipeline / privacy / PII / dependency / single-file / payments-state). See §7 for the per-stage breakdown.
2. **Doctrine/content change requires journal entry.** PRs touching `DOCTRINE.md` or `content/*.js` must also touch `journal.md`. Enforced in `.github/workflows/ci.yml`.

**Ritual gates (operator/reviewer responsibility):**

3. PR opened with a one-line summary. (PR title discipline; CI doesn't validate title quality.)
4. Operator runs local PII audit (`audits/run_local_audit.sh`). The public CI cannot see the operator's personal data; the local audit closes that gap. Skipping it is the failure mode, so it's a checklist item, not a vibe.
5. Reviewer reads the diff. Asks: any new line cross §4? Any new path cross §5?
6. Cross-model audit on doctrine or content changes. See §10. Solo authority IS the failure mode. Doctrine and content changes go through Codex (or ChatGPT for content batches) before merge. Mechanical edits do not.
7. Merge.
8. Append to `journal.md` with the `===== YYYY-MM-DD · Title =====` shape: what shipped, what was rejected, what's deferred.
9. **Live-fire on local deploy preview.** Static audit cannot simulate DOM/CSS or engine runtime. Releases touching `index.html`, `core/engine.js`, or content batches require an operator-run local-server pass against the changed surface before merge. Phase-2F-1 made this load-bearing — the flip-state inversion bug was invisible to Codex's static audit and only surfaced when the operator shook the deploy preview.
10. Confirm Netlify auto-deployed. Open the live URL. Shake it.

**v0.43 amendment (A1 self-firing rule-kill step + B4 journal-header shape).** Two corrections to the ritual above:
- **Self-firing rule-kill step (A1, see §13).** Folded into gate 8 (the journal append, where the last-review date is both read and written): **at every doctrine-touching ship, if the newest rule-kill review entry in `journal.md` is more than 30 days old, run the §13 rule-kill review as part of this close-out and log it.** This is the event-based accelerator for the §13 cadence — the monthly date is the floor, the ship-event is the accelerator. The check is mechanical (read the newest review date from `journal.md`), not a remembered cadence.
- **Journal-header shape (B4).** Gate 8's `===== YYYY-MM-DD · Title =====` example names a retired header shape; the live journal uses `## YYYY-MM-DD — Title — STATUS`. Read the example as that shape. Per L17 the `=====` form is preserved verbatim above and superseded here.

## §9. The SIRR boundary

Tracked content of this repo MAY NOT contain the string "SIRR" (case-sensitive) or any SIRR-domain vocabulary (`sirr.studio`, `abjad`, `hebrew_gematria`, etc.) outside the boundary-naming files allow-listed in `tests/pii_scan.test.js` (`DOCTRINE_ALLOW`). SIRR is a sibling project, not a parent or child of `8ball`; the two repos share no code and no imports. Enforced by `tests/pii_scan.test.js`.

## §10. Multi-model lane system

**The author-judge problem is real.** A single instance writing AND reviewing its own work has a structural blind spot. SIRR's session L4 named this directly: "Solo authority on doctrine IS the failure mode."

For 8ball at its current scale, the system runs **five agent roles plus one always-on controller**. Full role docs live at `~/dev/8ball/agents/*.md`; platform-specific constraints at `~/dev/8ball/agents/PLATFORMS.md`; this § is the constitutional summary.

**Core agent roles** (v0.29):

| Role | Tool | Role doc | Read access | Write access |
|---|---|---|---|---|
| **Orchestrator** — context, briefs, dispositions, sequencing | Claude (chat) | [`agents/orchestrator.md`](agents/orchestrator.md) | Everywhere in `~/dev/8ball/` and `~/Desktop/8ball/` | Pragmatic for fast initial work; routes through implementer for production-grade edits |
| **Implementer** — multi-file production edits, git ops, repo filesystem | Claude Code (CC) — `/usr/local/bin/claude` | [`agents/implementer.md`](agents/implementer.md) | Repo + `~/Desktop/8ball/` | Repo + `~/Desktop/8ball/` |
| **Auditor** — adversarial pre-merge review on doctrine, content, release-gates | Codex — Mac desktop app, paste-and-relay | [`agents/auditor.md`](agents/auditor.md) | Pasted briefs only | Returns categorized verdicts (PASS / P3 / P2 / P1 / P0) |
| **Verifier** — live UX on deployed product, structured findings + screenshots | Claude in Chrome (CiC) — browser extension | [`agents/verifier.md`](agents/verifier.md) | Per-session domain allowlist (default = read-only/static; non-default requires controller approval) | Reports text + screenshots only; no irreversible action clicks |
| **Inspector** — read-only state on operator infrastructure dashboards (Gumroad, Netlify, GitHub, social), structured observations | Claude in Chrome (CiC) + osascript + operator-screenshot adjacents | [`agents/inspector.md`](agents/inspector.md) | Operator's own dashboards (project-operational scope, never personal accounts); per-session CiC allowlist for non-default domains | Reports observations only; no irreversible action clicks, no credentials, no payment entries, no terms acceptance |

**Controller** (always-on, never an agent): the **Operator**. Sole authority for merges, deploys, payments, account changes, sharing-permission changes, TOS acceptance, identity verification flows, and any irreversible third-party action. Full role doc at [`agents/controller.md`](agents/controller.md).

**Adjunct lanes** (not core agents; invoked ad-hoc, no project-specific role doc): **ChatGPT** for content/copy review (paste-relay); **Perplexity** for web search; **Gemini** for second opinion. Per `~/MUHAB.md` §3.

**Lane discipline (v0.24):**

- The orchestrator does NOT autonomously override §4 or §5. Borderline content gets flagged for ChatGPT review before merge.
- Doctrine amendments go through the auditor before merge. Mechanical edits do not.
- Periodic corpus drift-sweep audits run when state-doc age + cumulative shipped surface change warrant it. Drift-sweep brief shape, paste workflow, verdict format, and pre-budget-absorb expectation codified at `agents/auditor.md` Procedure 6. (v0.27)
- All filesystem and git operations route to the implementer for any change touching ≥3 files or modifying `core/`. Single-file documentation tweaks may stay in chat.
- Live-UX verification routes to the verifier post-deploy-preview (pre-merge for surface changes) and post-merge for the first 24–48h on production for any significant ship.
- Operational-dashboard state reads route to the inspector when canonical files (`8BALL.md`, journal) need to assert state of an operator-controlled third-party surface — ship-gate verification, processor activation banner state, deploy-status confirmation, social-profile state. Inspector directives originate from a named canonical-file-consumption purpose; never open-ended exploration. (v0.29)
- The controller is always the final approver. No agent auto-merges. Per L48: explicit audit-cleared signal before merge; five-minute-CI-green-to-merge windows are the L48 failure shape.

For per-cycle artifact locations (briefs, audits, directives, reports), see `agents/PLATFORMS.md` "Artifact-location matrix". For SIRR-specific lane discipline, see `~/dev/SIRR/SIRR.md` §7.

## §11. PII rule

Personal data of the operator, family, friends, or any other identifiable person is NEVER tracked content. The repo is private as of v0.2.0, but this rule is independent of repo visibility — tracked content is the durable boundary, not the current ACL state. Repos can flip; the rule survives the flip.

**Visibility correction (2026-08-01; L17).** The historical current-state sentence above is superseded: the repository is **PUBLIC**, verified against authenticated GitHub state on 2026-08-01. The PII rule is unchanged and remains deliberately visibility-independent; public visibility makes the tracked-content boundary immediately observable, not newly applicable.

Specifically forbidden in any tracked file:

- The operator's full name in association with personal data (DOB, address, ID numbers).
- Test fixtures using real-person DOBs labeled to a real person.
- Any string matching combination patterns: `(operator-name) + (YYYY-MM-DD)` within 40 characters.
- SIRR cross-references outside of explicit boundary-naming doctrine.

The `tests/pii_scan.test.js` enforces a public-banned subset. The local audit at `audits/LOCAL_PII_AUDIT.md` closes the gap for personal data the public CI cannot know about.

**Fixture DOB rule (sub-rule of §11):** Fixture DOBs are chosen for the calc path they exercise, not anchored to any real person. If a calc path accidentally hits a real-person DOB, the fixture date is shifted by 12 years (preserves Chinese zodiac animal and life path mod-9 distribution while breaking real-DOB anchor) and the calc path is re-verified.

## §12. Out of scope

Two tiers. **[PERMANENT]** = load-bearing invariant (privacy, no-backend, no-oracle, SIRR); relaxing it changes what the object is — do not revisit. **[GUARDED]** = parked behind a named evidence trigger, not forbidden forever; revisit only on that trigger, never as idle feature-creep. (v0.35.)

- **[PERMANENT]** Account systems
- **[PERMANENT]** Email capture
- **[PERMANENT]** Push notifications
- **[PERMANENT]** AI-generated trait phrases at runtime (latency, cost, repeatability all break)
- **[GUARDED]** Multi-language until v2 of the trait pool exists in any language
- **[GUARDED]** Astrology charts, planetary aspects, anything beyond sun sign + optional rising sign — revisit only if it serves the object, never as feature-creep, and never in a way that crosses into horoscope/guidance identity
- **[PERMANENT]** Daily/weekly horoscopes ("today's reading")
- **[PERMANENT]** Sharing/social-card export of unconstrained content. The original wording — "Sharing/social-card export until a privacy review explicitly clears it" — is preserved here as lineage: that privacy review landed at v0.31 (§5.D), clearing a user-initiated, client-side, no-network export of the **free** card (labeled coordinate symbols + catalog number, no paid content, no PII). Permanently out of scope remains any export that carries the paid card-content layer (name/type/habit/note), any PII (name/DOB), or any per-result link / query parameter encoding a profile field.
- **[PERMANENT]** Anything touching the SIRR engine, codebase, or vocabulary

**v0.43 amendment (A2 — [GUARDED] gated-work pass, P1; corrects a 3-review-inherited error).** §13 v0.35 requires every [GUARDED] item to carry a NAMED, OBSERVABLE evidence trigger. There are exactly two; both fail it and are corrected here. Per L17 the two [GUARDED] lines above are preserved verbatim and superseded:
- **Astrology charts, planetary aspects → reclassified [GUARDED] → [PERMANENT].** Not genuinely guarded: §12 already permanently bans the identity they belong to — the [PERMANENT] header names *no-oracle* as a load-bearing invariant whose relaxation "changes what the object is," and "[PERMANENT] Daily/weekly horoscopes" bans the reading/guidance register. Charts and planetary aspects ARE that horoscope/guidance identity. The only genuinely-guarded astrology surface — optional rising sign — already shipped and cleared (§1.A). Relaxing the line would change what the object is, so it is [PERMANENT], not parked.
- **Multi-language trigger → re-keyed to a live observable.** Its trigger "until v2 of the trait pool exists in any language" references the trait-pool / template-pool system (`traits.v1.js` / `templates.v1.js`) **retired and DELETED in Phase-2F-2** (§4). A condition keyed to re-versioning a deleted, never-recurring system can never fire — the exact "parked with no defined release condition = drift" case §13 v0.35 exists to catch. (Flagged at the 2026-05-15 inaugural review; wrongly reversed 2026-05-30 — "string still present" conflated with "trigger valid"; inherited until the 2026-06-30 cross-verification caught it.) Re-keyed: **revisit multi-language only if a v2 of the cards deck (`cards.v2.*`) exists in any language.** Remains [GUARDED].

## §13. Refresh discipline

This file is **maintained by hand**, not generated. Update it when:

- A locked rule is added under any §-number
- A canonical path changes
- A doctrine version locks (note the version)
- A blocker is resolved or added
- A new tool/relay is automated (kill the old manual entry)

**Rule-kill review:** every locked rule that hasn't fired in 30 days → archive. Doctrine that doesn't breathe accumulates cost without producing safety. ~15 min/month.

**Gated-work discipline (v0.35):** parked/gated work carries a NAMED evidence trigger. "Parked" with no defined release condition is drift — at rule-kill review, either name the trigger or kill the item.

**Amendment discipline (L17, codified in-document, v0.42):** amendments **supersede, never edit** — historical clause text and prior footer version entries are preserved verbatim; a change is a new dated amendment block (or footer entry) that supersedes the old, and §-numbers are preserved under substance rewrite. This is the rule the in-document "per L17" citations refer to; its origin is the journal L17 lessons entry. Sibling standing lessons this constitution cites: **L48** — no merge before an explicit cross-model audit-cleared signal (§10); **L53** — describing labeled-DOB events in prose can re-fire the PII scan, so keep operator/role tokens away from ISO dates in files not on the scan's allow-list.

Cadence is **monthly** (amended v0.32, 2026-05-30 — was weekly). Rationale: weekly review at a solo cadence produced zero pruning (every review 2026-05-08 through 2026-05-30 was all-KEEP calibration — rules younger than the 30-day archive threshold have no firing surface to evaluate), and the weekly cadence silently lapsed for two consecutive Fridays (2026-05-22 + 2026-05-29) — the prune-discipline became the rule that stopped firing. Monthly aligns the review interval to the 30-day threshold it enforces, so each review actually has rules old enough to bite. Co-locate the monthly firing with other monthly discipline beats where possible (e.g. the v0.3.1 gate-close read 2026-06-15) so it has a recurring trigger and does not lapse.

First rule-kill review fired the first Friday after the doctrine aged 7 days (2026-05-15, inaugural). The catch-up review covering the two lapsed weekly slots fired 2026-05-30 (`~/Desktop/8ball/sessions/friday_rule_kill_review_2026-05-30.md`). Next review: 2026-06-15 (first monthly).

**v0.43 amendment (A1 — self-firing review trigger; the date cadence failed twice).** The monthly date cadence (v0.32, preserved verbatim above as lineage) is superseded as the *primary* trigger. It lapsed twice: first the weekly slots that prompted weekly→monthly, then the monthly review itself — due mid-June, it did not self-fire and ran 15 days late, firing only because a handoff carried it. A calendar a solo operator never watches cannot self-fire; swapping one date for another does not change the failure class. Replace it with an **event-based trigger backed by a date floor**, both first-class:
- **Accelerator (event).** Every doctrine-touching ship runs the rule-kill review in its §8 close-out (new ritual step, §8 v0.43) **if the last logged review is more than 30 days old** — binding the review to the journal-append ritual that already fires on every ship.
- **Floor (date).** Absent any doctrine-touching ship, the review still fires monthly as a backstop (the v0.32 cadence, retained as the floor).
- **Mechanical read, not memory.** "Last review date" is the date of the newest `## YYYY-MM-DD — … rule-kill review …` entry in `journal.md`; the >30-day check reads that date. This review logged 2026-06-30, so the next fires on the first doctrine-touching ship after 2026-07-30, or 2026-07-30 itself as the floor.

If you find yourself adding more locked rules than you're killing at review time, that's the recursion firing through the orchestrator. Stop drafting and ship something.

## §14. Operator calibration

Carried from an 8ball close-out session (2026-07-05), refined across a short review pass into fail-closed mechanisms rather than judgment calls. This section governs how the orchestrator (and any Claude lane) reads the operator and calibrates itself; it sits alongside §10's agent-role/lane architecture rather than replacing it.

**Objective gate (before the first file of any new cycle).**
1. Declare the objective in writing: money, mastery, or practice. One answer — it kills the others.
2. The slowest feedback loop that matters must resolve in <24h. If it doesn't, change the channel before writing code.
3. Name what gets deleted when this works. No subtraction plan = collection, not product.

**Reading the operator.**
- Reconstruct, don't clarify — but only for compression-trust: terseness that reads as deliberate shorthand on a reversible, low-blast-radius ask with an established acceptance criterion. Terseness that instead reads as unresolved ambiguity is not compression, whatever its length, and still gets a stated assumption or a clarifying question. Either way, an override-risk flag below still fires first.
- Override-risk flags, checked against the message, the current transcript, and canonical doctrine — not inferred tone or assumed fatigue, but not claimed as mechanically observable in isolation either. (a) and (b) require reading against standing law and thread history, not a keyword match; a close call fires the flag rather than waiting for certainty: (a) two asks in one message that contradict each other or a standing law; (b) scope doubling mid-thread without a new objective stated; (c) a directive that reverses a decision made <2h earlier in the same session; (d) a client-surfaced timestamp past midnight local on an irreversible-adjacent ask. Any flag fires → restate the interpretation and offer to defer to the morning read before executing. Supersedes an earlier draft of this rule keyed to "typos rising," which a chat transcript never actually exposes.
- Operational definitions, since these decide when (d) fires: "local" is the operator's own local clock, not server/UTC time; "irreversible-adjacent" means the ask sits within one step of something on the circuit-break list below (posts, merges, payments, deploys), not any midnight decision generally; "the morning read" is the operator's own re-read of the same thread after normal waking hours, not a fixed clock time.
- Irreversible-adjacent decisions flagged under (d) auto-defer to the morning read. Non-negotiable.
- Morning reads outrank midnight directives when the two conflict. Say so plainly — directness lands here, deference doesn't.
- Friction is designed, not narrated: the right action stays one tap; the wrong action requires a written sentence of justification. Naming a failure pattern is not the intervention.
- Taste calls (register, restraint, subtraction) get fast deference, even against conventional advice.

**Self-calibration (Claude, every lane).**
- Named failure mode: executing to avoid questioning — momentum reads as progress while direction drifts. The on-record instance is #69/#70 (2026-07-04): the same merge-before-audit-cleared-signal gap (L48) fired twice in one session, which is what made it a pattern rather than a slip.
- Weekly strategic check — "are we pointed at the right thing" — is exempt from terse-mode and is never requested; it fires on its own trigger. Mechanism, not memory: the newest `journal.md` entry carries a `next_strategic_read: YYYY-MM-DD` line; every session bootstrap reads it, and if it's overdue or missing the check runs before any task work, then the date rolls forward 7 days. Mirrors the §13 v0.43 self-firing rule-kill trigger exactly — a cadence a solo operator never watches cannot self-fire, so the trigger lives in a file that already gets read, not in memory.
- On unanimous-counsel overrides (all lanes agree, operator overrides anyway): the real objective must be stated out loud before executing. The override may be right, or may be revealing an unstated goal — lanes lack the operator's private context either way, so don't assume the override was correct going in. State the real objective, then judge the override against it.
- Own outcomes, not tasks.

**Standing laws.**
- Circuit-break on irreversibles: posts, merges, payments, deploys stay operator hands. Restates §10's controller-authority list; "posts" is the one item that list doesn't already name, since §10 predates the social-reach work.
- Dissent-then-BLOCK below a named evidence threshold (not dissent-then-execute). The threshold is a number or named condition pinned in writing for the specific decision at hand, never inferred per-session. If none is pinned, the absence is the answer: no threshold defined = below threshold, fail-closed rather than fail-open — same instinct as §12's gated-work discipline and the §13 amendment discipline, where an undefined gate is drift, not permission. Worked example already on file: the reach/build-B gate at `journal.md` 2026-06 ("build-B parked behind a NAMED evidence gate `[reach ≥ ~2–3k qualified] OR [≥1 strong divination-niche inflection signal]`") — not "N≥400," which is the *sub-threshold*, unmeasurable case logged the same week, not a gate value. Each gated decision pins its own number; none generalizes to a universal bar. This threshold rule governs empirical claims, gated-work decisions, and taste calls — it does not extend to safety, privacy, or violations of this doctrine's other standing rules. Those aren't waived by declaring an objective or invoking taste, no matter how firmly stated.
- Prototype before batch. Dry-run before install.
- ONE canonical file per domain, repo-resident, committed — no desktop vault, no split-truth layer. `DOCTRINE.md` is canon for product/process law, `journal.md` is canon for current state; this section is that file for operator calibration and is not re-pasted into chats as a parallel copy. Ephemeral review artifacts — audit briefs, relay run output, one-off critiques — can live outside the repo until their findings are reduced into canon; that's scratch space, not a second truth layer.
- Park = delete with a log line. WIP cap: 3.
- Audit intensity scales with blast radius. User-visible falsehoods outrank all polish.
- Refinement requires a named signal + hypothesis. No signal, no pass.
- One subtractive pass per cycle, scored by what it deletes.
- English only on public surfaces (western audience).
- No metric enters canon unless pinned from a dashboard. Estimates never get formatted as data.
- Weekly analytics read: file-backed like the strategic-read trigger above — a `next_analytics_read: YYYY-MM-DD` line in `journal.md`, checked at session bootstrap; overdue or missing = read runs now, then the date rolls forward 7 days. A calendar cadence nobody's watching doesn't self-fire — this section already made that argument once above; it applies here too.

**Build physics.**
- Generator-first: everything derivable derives from one data source; edit one line, regenerate all.
- Golden-master the full output space when it's deterministic — CI backbone, not a someday.
- Separate change-classes architecturally (copy / presentation / entitlement / engine) so cheap changes are audit-exempt by construction.
- When a constraint creates friction, reach for the primitive (worktree, script, file-drop) — never a new rule. Rules cost compliance forever; primitives don't.
- Agents communicate by file-drop from day one. Operator relays only where auth requires it.
- Post-merge close-out is a script, not a ritual.
- The distribution loop is one tap before any build automation exists.
- Automate nothing before three manual cycles of the same friction.

**Baseline.** This partnership's fluency starts at July-speed, not May-speed. Hold it there.

---

**calc version:** v4 (master-preserving Pythagorean numerology — terminal domain `1..9, 11, 22, 33` · tropical sun · real lunar new year tables · real solar-term tables · canonical Asia/Shanghai date-precision for cusps) — decision record: `journal.md` entry «2026-07-31 — calc v4: master numbers 11/22/33 restored — controller word». Supersedes calc v3 (`journal.md` «2026-07-20 — §3 row 8 SUPERSESSION: nine terminal numbers (calc v3) — controller word»), which stands as history.
**content version:** v0.3.0-public-tier-1 + meanings-v5 + concordance-v3 + dyad-v2 + public-v3 (the immutable v1 deck and every historical registry remain on disk, unedited; current numerology meanings expose all twelve terminal values with the three master entries reused byte-for-byte from v1 (meanings-v5 carries the v4 exports unedited — themselves the v3 tables plus the per-slot numerology lines and the theme-tension registry — and adds the rising/private-animal placement lines; additive prose, no shipped entry edited), active Concordance restores exactly the three master-reduction links, the dyad tables carry v1 unedited but for the two combined-path frames and one provenance line, and the public tables carry v1/v2 unedited plus the declared master-to-base mode bridge; all other catalog, tier, interpretation, share, and privacy behavior remains as specified above)
**content interaction amendment:** §1.G v0.54 supersedes the v0.44 “four tappable values,” the v0.52 provenance-only limitation, and v0.53's cross-system fallback for numerology: all fourteen sheet compartments are interactive; every resolved value receives a registered meaning plus deterministic context through `content/meanings.v5.js` (v0.62 tables carried unedited via re-export — the registry widened back to all twelve terminal values at v3, 2026-08-30's v4 adds the per-slot numerology lines and the theme-tension registry, and v5 the rising/private-animal placement lines; `meanings.v2.js` through `meanings.v4.js` stay immutable on disk); each numerology coordinate is read only beside other numerology coordinates under `with the other numbers`; unresolved/sealed states and catalog exclusion remain explicit.
**doctrine version:** 2026-09-02 · v0.71 (§4.B/§1.D/§1.J/§5 — **the product is completely free.** On the controller's order the storefront retires in full: the free ceiling renders the complete sheet plus the dyad for every device (`getRenderTier()` = `t5`, storage never consulted), the paywall/lock/offers/checkout URLs/`?paid=` handler are deleted with a zero-checkout-token absence guard over every shipped source, and the three commerce keys are boot-scrubbed (the pending key held staged name+DOB — the v0.64 personal-data standard). `core/payments.js` and the facet machinery stay as tested registries (the kua-retirement precedent); `TIER_COORDS` becomes a render registry; the auditor's blocking probe is repointed to the free ceiling with its assurance suite in the same commit. Owner impact named: buyers keep everything and gain the rest like everyone else. The v0.67 operator-hand steps and the whole revenue thread are MOOT — the ledger stands as the paid era's historical record, and any re-commercialization is a fresh controller decision from zero. Markers appended to the v0.56, v0.67 and v0.69 clauses. §10 read: the pr-numbered two-lane artifact filed with this amendment's PR. **This entry claims no merge authority — the merge word stays with the controller per §10/L48.**)

**doctrine version, prior:** 2026-09-01 · v0.70 (§1.D — **the kua block is retired from the render, controller decision.** The block leaves the t3 ceiling and every sheet on the controller's screen-space order: `kuaRead` out of the coordinate list, `ui/kua.js` deleted, no kua section on the host face, the dyad sheets or the specimen sheet, the paid copy no longer names the kua line. `content/kua.v1.js` stays unedited per §4 and `core/kua.js` stays as the tested method registry (the v0.60 t4 precedent: a render retires, its tables stand); the v0.64 gender-token boot scrubs keep running. Prices, entitlements and the census are untouched. Supersedes the v0.63 grant and the v0.64 both-values contract as renders — markers appended to both. §10 read: the pr-numbered two-lane artifact filed with this amendment's PR. **This entry claims no merge authority — the merge word stays with the controller per §10/L48.**)

**superseded:** 2026-08-31 · v0.69 (§1.J — **the price-column reconciliation closes, adopting nothing.** The open doctrine work v0.61 filed and v0.67 kept open is resolved on the controller's order by reading the Price column as what v0.55 made it: a ledger of controller-set, one-time, FROM-ZERO rung prices, monotone up the ladder, never derived by formula and never prorated on upgrade (a t1 owner pays the full $3 to reach t3 — established behavior, now disclosed). t5's from-zero price is $6 — two complete sheets' worth at the ladder's own $3, the relation layer deliberately unpriced; the presented t3→t5 path pays the unprorated rung price like every upgrade before it, and subtraction has never priced anything in this column, so the defense v0.60 found impossible for the retired rung is never owed. The brief's entity-count ladder stays unadopted; the four-rung reading is t1 · $1 / t2 · $2 / t3 · $3 / t5 · $6, stated with its dominance fact (direct $6 beats the $9 path for a strict superset — surfacing the direct path stays the reserved v0.61/v0.67 controller decision) and with the storefront kept distinct (only $3 live today; $6 staged pending the v0.67 operator-hand steps). The locked table stays verbatim per L17; the §1.J, v0.67 and v0.58 open-work sentences all carry appended sighting-corrected markers, so open price reconciliations count zero on the page. No price, product, or code change. §10 read: the pr-numbered two-lane artifact filed with this amendment's PR (both lanes broke the first draft's unit derivation on the upgrade path; the ledger account is the reconciliation). **This entry claims no merge authority — the merge word stays with the controller per §10/L48.**)

**superseded:** 2026-08-31 · v0.68 (§1.G — **the body/context boundary is drawn: three authored layers.** The question the meanings-v4/v5 routing notes recorded as an open controller call is resolved on the controller's order: a §1.G meaning panel is constituted of exactly three authored layers — the registered body (v0.44's citation contract, keyed by value, immutable per §4, byte-identical wherever reused), the NEW-NAMED placement layer (`NUMEROLOGY_SLOT_LINES` + `PLACEMENT_LINES`, keyed by position × value, stating the value read in the position's registered office, appended after the unedited body, versioned separately under §4's new-file rule), and the `in this sheet` context synthesis (v0.53, with `THEME_TENSIONS` classified as its authored table input). The §4 immutability unit is per-layer, so the body's byte-identity pin enforces the boundary mechanically; no product behavior, table, pin, or scan changes — the amendment names what the shipped mechanics already hold. Both routing notes carry sighting-corrected resolution markers per the v0.48–v0.61 footer precedent. §10 read: the pr-numbered two-lane artifact filed with this amendment's PR. **This entry claims no merge authority — the merge word stays with the controller per §10/L48.**)

**superseded:** 2026-08-31 · v0.67 (§1.J/§4.B/§5.B — **the dyad rung goes on offer.** `T5_PRODUCT_URL` = the live `neysyv` Buy Link ($6, bare), shipped together with its result-rail offer anchor (`#dyad-offer-link`, plain checkout `<a href>` + the gumroad disclosure beside it, §5.B Call 2) per the PR #187 R6 contract — the entry control stays entitlement-only, the offer is its own predicate and click path scoped to t3 exactly (the rung its delta label is true of), the two swap on one render sync with the href stripped when hidden. `?paid=t5` return handling already existed (v0.61); the monotonic tier write is the entire grant (§1.D v0.55 — no credits exist). Blocked-storage exposure decided and named: no preflight, because the return path is retry-preserving by design. Fail-closed degradation retained and now runtime-driven (injectable url parameter). Paywall modal untouched (still exactly one Gumroad CTA); `index.html` byte-unchanged; the dead in-screen placeholder CTA deleted. Named operator-hand remainder per §10, with its trap stated: publish `neysyv` and RE-POINT its existing Content-tab button from `/?paid=t2` (its t2-era wiring) to `/?paid=t5` — an unchanged button silently grants a $6 buyer t2. §10 read: the pr-numbered two-lane artifact filed with the activating PR. **This entry claims no merge authority — the merge word stays with the controller per §10/L48.**)

**superseded:** 2026-08-31 · v0.66 (§6 — **the shell stylesheet.** The whole of `index.html`'s inline `<style>` block (771 lines) moves VERBATIM to `ui/shell.css`, linked from the head BEFORE `ui/experience.css` so the cascade order the inline block had is preserved exactly; byte-identity of the moved rules is sha256-proven and the live-fire computed-style diff against the pre-split tree is empty across three viewports and the full submit flow. `index.html` goes 1455 → 683 lines under the unchanged 1500 cap. The §6 shell-stylesheet amendment sanctions a plain `ui/*.css` linked from the head as the second split target (the v0.23 `ui/*.js` shape still governs logic/markup), records that this below-cap split ran on the controller's explicit order, and bans inline `<style>` from returning. Test surface hardened with the move: every CSS pin repointed (positives to the stylesheet, negatives to the combined shell source), the monochrome palette scan gains a `ui/*.css` leg that had never covered `ui/experience.css`, the §5 privacy scan gains `.css` files and CSS egress tokens (`@import`, remote `url(`), and five dead negative guards the split would have silenced were revived (kua/tiers/labels/payments ×2 — all mutation-proven first). §10 read: the pr215 two-lane in-container review, artifact `audits/claude_relay_pr215_premerge_audit_2026-08-31_response.md`. **This entry claims no merge authority — the merge word stays with the controller per §10/L48.**)

**superseded:** 2026-08-30 · v0.65 (§1.I — **the registries read within one sheet.** The §1.G meaning panels gain a `filed relation` section: the tapped coordinate checked against a fixed short pair list (sun × rising, public × private animal, year × day-pillar element, the three master-reduction links between numerology values), REGISTERED records only, five of the six Register-law emissions verbatim with the status narrowed to `registered` because an unfiled pair renders nothing — on a panel, absence claims nothing. Adverse records (chong/hai/xing · square/quincunx/opposition · the ke cycle) suppress the harmony algebra in the same panel; the panel closes on card re-render so a stale citation cannot attest what the current values do not support; the tier boundary is the §1.D v0.37 sealed-DOM contract (sealed values are absent from the card, so their pairs do not exist to look up). Shipped with the same PR: four deterministic §1.G harmony frames replacing the single context skeleton, closing clause byte-identical across frames. `content/concordance.v3.js` keeps its sole importer. §10 read: the pr213 two-lane in-container review (both lanes MERGE WITH FIXES; all fix-class findings landed pre-merge — the full emission set, the adverse suppression, the stale-panel close, per-pair-kind sealed pins, real-domain frame reachability, and a token-order pairing oracle after a demonstrated coverage loss). **This entry claims no merge authority — the merge word stays with the controller per §10/L48.**)

**superseded:** 2026-08-30 · v0.64 (§1.D + §5 — **the gender ask is retired.** The optional gender input leaves every surface: the primary form, the dyad's second-person entry, storage, the recompute paths, and all privacy copy (PR #208, squash-merged to `main` as `4c99ab4` on 2026-08-30). The kua block stays at t3 with exactly ONE read for every profile — the both-classical-values render v0.63's rule (3) specified as the no-gender mode, both values labeled, the method's gender dependence named in the register note; the disclosed raw-5 remap and the single continuous formula with its named post-2000 fork stand unchanged, and `core/kua.js` keeps the gendered arithmetic as method registry, consumed only through `getKuaBoth`. Profiles and archives carry no `gender`; stale v0.63 tokens are erased at boot by three read-verified single-key scrubs whose only-the-gender-key contract is mutation-pinned; no localStorage key is added and `content/kua.v1.js` is untouched per §4. **Doctrine follows code here, not the other way around** — the product change merged first and this amendment closes the divergence the pre-merge audit flagged (its F5); the sequencing is named rather than smoothed over. §10 cross-model audit: the codex/grok lanes were unreachable from the implementing container, so the relay ran as three independent Claude-family lanes (opus + sonnet + haiku), fresh context each, reconciled by the authoring session — composition stated plainly in the artifact, `audits/claude_relay_pr208_premerge_audit_2026-08-30_response.md`. Reconciled verdict MERGE WITH FIXES; all six fix-class findings landed pre-merge, including the MAJOR blur race (a slow first city search resolving after blur re-hid the sole mobile submit under the same PR's new reveal contract) and three pin-hardening findings proven by mutation. One open product call recorded in §1.D v0.64: the §1.G citation body (`KUA_TRIGRAMS[n].body`) is currently rendered nowhere. **— resolved same date, controller word RENDER, shipped by PR #210 (`d4de8aa`); corrected on sighting per the footer precedent (mechanical edit), see the §1.D v0.64 resolution marker.** Suite 57 files / **1950** tests green; product audit PASS, 0 blocking; `index.html` 1455/1500. **This entry claims no merge authority for the PR that carries it — the merge word stays with the controller per §10/L48.**)

**superseded:** 2026-08-04 · v0.63 (§1.D + §5 — **the kua block: the product's first gender-keyed surface, first non-DOB demographic input.** The t3 ceiling gains a third BLOCK, `kuaRead`: the Eight Mansions personal trigram (Ba Zhai Ming Jing), computed by `core/kua.js` from the SOLAR birth year (Li Chun boundary, at-or-after, per `core/calendar.js`'s authority-pinned term table) and the profile's optional `gender`. Gender is OPTIONAL forever — strict `'male' | 'female'`, off-vocabulary dropped at every write seam, never required by any surface, never touches the free tier (DOB-only invariant untouched, §1.D v0.38). A profile with no gender on file renders BOTH classical values, labeled, never a silent default. A raw 5 has no trigram and files male→2 / female→8, RETURNED by the engine and rendered as a note, not silent (§1.B v0.62 anti-silent-substitution rule). `content/kua.v1.js` carries citation-only registry facts in the §1.G shape, zero directional guidance, mechanically pinned by `tests/kua_content.test.js`. Ships as a **controller override of the product-surface freeze** — the operator's explicit 2026-08-03 decision on vehicle (kua), tier (t3 block) and process (override + cross-model audit before merge), per the §1.G v0.44 precedent, override authority not re-litigated. §8 gate 6 / §10 cross-model audit: the Codex lane is parked, so the audit ran as an operator relay (codex + grok + claude fan-out, gemini errored on auth, reconciled by claude) per the PR #198 precedent — recorded at `audits/relay_pr199_premerge_audit_2026-08-04_response.md`. **The relay caught one real regression pre-merge:** person B's gender survived a dyad close/reopen (`ui/dyad.js` `clearEntryFields()` omitted `dyad-gender-input`) — the exact stale-hidden-DOM defect class PR #187's F1 fix exists to close; fixed, with a dedicated regression test. It also caught a citation-accuracy defect: the NAMED LIMITATION's "competing post-2000 school" contrast was computed with the wrong S-scoping (full-year digit sum instead of the last-two-digits sum the cited formula actually uses) — hand-checked by exhaustive sweep across 2000–2099 (`tests/kua.test.js`), the two formulas do not diverge anywhere in that range once correctly scoped, so `core/kua.js`, `content/kua.v1.js`'s `KUA_SOURCES.limitation`, and the test citation were corrected to say so rather than repeat an unverified contrast. Suite 54 files / **1889** tests green (1887 before the two regression tests this response added); product audit PASS 12/0/1/0; `index.html` 1486/1500; local PII scan clean (848 files). **SAFE TO MERGE per the relay's reconciled verdict, with the above fixes landed and re-verified — operator merge word still required per §10/L48; this entry claims no push, PR, merge or deploy authority.**)

**superseded:** 2026-07-31 · v0.62 (§1.B + §1.G + §1.H + §1.I + §1.J + §1.D + §3 + §5 + §7 — **calc v4: the master numbers are restored.** Active Pythagorean numerology has TWELVE terminal values, `1..9` plus `11 / 22 / 33`, across all six coordinates; the reducer digit-sums while above 9 and stops at a master (`29 → 11`, `38 → 11`, `44 → 8`), and maturity keeps its method and can itself land on a master (`22 + 11 = 33`). This SUPERSEDES the v0.54 nine-number amendment and is a controller product/commercial decision — *"master numbers sell so keep them"* — forced by a truth defect rather than a preference: the locked X-offer specimen shows `11` and `22` while the shipped calculator reduced them, and the 2026-07-31 Codex preflight returned STOP on exactly that mismatch (`audits/codex_x_offer_preflight_2026-07-31_response.md`). The repair the controller refused is deleting the masters from the creative; the product is made truthful instead. What v0.54 got right survives untouched: an absent contributing letter class is still `null` / `—`, never a displayed `0`, and the versioning discipline that kept every content file immutable through the narrowing is exactly why widening costs four new active files and no edit to a shipped one. Masters resolve to the `high` bracket and the THIRD facet position — the pre-calc-v3 contract, restored; there is no fourth bracket, because a catalog cell has three note positions. The facet key versions `_v2` → **`_v3`** and both retired generations are cleared once on the first v4 read, because a position stored under calc v3 was computed against a life path that has since moved (`1970-01-04`: `4 → 22`, `mid → high`) — no other key is added. Active content becomes `meanings.v3` (twelve entries; the three master bodies reused BYTE-FOR-BYTE from immutable v1 and pinned by identity, plus one added `theme` field each), `concordance.v3` (twelve-value domain; exactly the three v1 master-reduction links registered, the other 63 pairs unfiled, none `adjacent`), `dyad.v2` (v1 tables unedited; two combined-path frames and one provenance line reworded, and the frame rule moves from `sum > 9` to `sum !== combined` so a sum that STOPS at 11 no longer announces that 11 reduces to 11), and `public.v3` (v1/v2 tables unedited; a master birthday keeps its value and reaches the nine-entry mode table through the declared `MASTER_MODE_BRIDGE`, DISCLOSED as `mode.modeKey` / `mode.bridged` / `mode.bridgeNote` AND rendered as a fourth line on the host block and both dyad sheets — no master work mode is authored, and CI pins the table at nine entries so none can be). **Corrected pre-merge on an independent audit (2026-07-31):** the first implementation disclosed the bridge in the returned object and discarded it in the sole production formatter, so the rendered surface performed the exact silent substitution this amendment names as the wrong fix; every existing surface test used a non-master birthday and could not catch it. Two P2s closed with it — active state statements still naming the retired `_v2` facet key, and a `meanings.v3` that restated v2's nine themes instead of carrying them structurally. `index.html` is byte-identical at 1497 lines; no new dependency, no network call, no new persisted key beyond the versioned facet name, no tier/entitlement change, no deck edit, and the tarot birth-card reduction (§1.C, `22 → 0 · the fool`) is a separate contract this does not touch. Suite 51 files / **1826** tests green (1814 at the first-audited HEAD `d03a768`; the remediation above added ten — the master-birthday disclosure at both surfaces, and the cross-version registry pin; the final-worktree P2 absorb added two production-seam regression tests); auditor assurance 93 green; product audit PASS 13/0/1/0. **STAGED — a fresh independent Codex audit is mandatory before merge consideration per §10/L48; the earlier X-offer preflight is NOT implementation clearance. No push, PR, merge or deploy authority is claimed by this entry.**)

**superseded:** 2026-07-30 · v0.61 (§1.D + new §1.J — the DYAD rung. A fourth ladder rung `t5`, param `?paid=t5`, price $6, appended so every existing rung keeps its rank and no t1/t2/t3 owner is affected; the R2 legacy grandfather deliberately stays at t3. **The ladder skips `t4` and the skip is load-bearing**: `t4` is retired and lives in `RETIRED_TIERS`, so a dyad shipped under that token would sit in both tables and `normalizeTier` would rewrite every paying dyad device down to t3 before `isTier` saw it. `RETIREMENT_COLLISIONS` derives that invariant from both tables at module load and is pinned empty, and rank is ladder POSITION rather than the digit in the token. t5 buys a SECOND complete specimen sheet — every coordinate, its own written 144-card entry, its own public read — plus a relation layer of three axes computed by `core/dyad.js` from the two already-calculated profiles: the directed five-element relation between the two day masters (one authored passage per direction), the combined life path (a neutral reduction clause beside the existing nine-number registry's own body, carried byte-for-byte), and the card pair (year branches through §1.I's five named families × the ordered life-path bracket pair). Like `cardEntry` and `publicRead` it is a BLOCK, so the §1.F census is byte-identical at t3 and t5. Entitlement is all-or-nothing: below t5 the entry control is absent, the screen refuses to open, a submission is refused and the render produces nothing. Commercially NOT live and fail-closed — the Gumroad product does not exist, `T5_PRODUCT_URL` ships empty, and the §4.B v0.56 single-$3 sprint surface is byte-untouched with exactly one Gumroad URL still in `index.html`. §1.J binds the relation layer to §1.I's register law verbatim and adds a no-person-as-subject rule, both mechanically scanned over the tables AND the assembled runtime output with positive-fire sentinels. **First implementation was REFUSED** by a Codex pre-merge audit with six P1 findings (ungated entry and second sheet; person B's data left in hidden DOM on close; a second-person form with none of the primary form's validation; a recomputed day master; a narrowed two-column output instead of two standalone readings; re-authored number meanings) — all closed, and the corrections are recorded in the clauses themselves rather than only in the journal. Two limits on the record: the dyad sheets are a second DOM writer over one shared value mapping rather than a second instance of `ui/tiers.js`, and are therefore not tappable; and **no cross-model audit has cleared the corrected branch** — per L48 a fresh independent read is required before merge. Suite 51 files / 1758 tests green. STAGED — merge is its own word. **SHIPPED — squash-merged to `main` as `2cdaa3c` (#187) on 2026-07-30; the STAGED language and this entry's “no cross-model audit has cleared the corrected branch” sentence are superseded by that merge and its audit chain, corrected on sighting per the v0.48–v0.56 footer precedent (mechanical edit, see `audits/mechanical_footer_correction_v061_2026-07-30.md`). L48 was met, not overridden: Codex DO NOT MERGE at `9c749ef` (six P1, two P2) → correction commits `98792b1` + `e34458a` → independent Codex re-audit SAFE TO MERGE (`audits/codex_pr187_premerge_reaudit_2026-07-30_response.md`), then merge.**)

**superseded:** 2026-07-29 · v0.60 (§1.D — the public rung is RETIRED and its read folds into t3. `TIER_ORDER` returns to three rungs; `?paid=t4` is no longer a tier; the public read becomes a t3 ceiling block beside the written entry, still a BLOCK so the §1.F census is unmoved at 15 of 15. Controller ruling on three grounds: the tier was named for an exposure radius it never delivered (the block renders privately and is absent from the §5.D share surface); the price could not be defended ($9 for five derived strings computing no new coordinate, above a $3 rung selling fifteen coordinates plus the written 144-card entry); and there are no sales to build on (the revenue-reset diagnosis on record is zero outside orders). Nobody is downgraded: the rung was never buyable but its unsigned `?paid=t4` return was live, so `RETIRED_TIERS = { t4: 't3' }` maps a stored `'t4'` onto the rung that absorbed it and the UI persists the rewrite — without which a genuine t3 buyer who opened that URL once would drop to free, since the stored tier is the only record of a purchase. The Price column stops contradicting itself. The engine, tables, render module and birthday keying all survive; only the rung, its price and its unopened offer are withdrawn. Suite 45 files / 1585 tests green. Cleared by controller override, L48 sighting #19 — still no cross-model read. STAGED — merge is its own word.)

**superseded:** 2026-07-29 · v0.59 (§1.D public rung — the t4 mode driver moves OFF the free surface. v0.58 keyed the mode of work by the life path, free since §1.D v0.38, so the paid rung's only new content was a re-reading of a coordinate every visitor already had; that clause named the problem in its own text and shipped anyway. The driver is now the **birthday** number — day of the month under the §1.B v0.54 nine-number reduction — which is date-only (input contract unchanged), has the same 1..9 domain (the authored mode table carries over unedited), and is a **t2** coordinate, so the driver is itself paid information. §1.G already names the birthday "the recurring skill" and the life path "the long route"; a mode of work is a recurring skill. `content/public.v1.js` stays immutable per §4 — `content/public.v2.js` imports every table byte-for-byte and changes only the mode provenance string and the modes' self-naming key field. Ladder, price, entitlement, census, sealed-DOM rule and fail-closed offer are all unchanged; `T4_PRODUCT_URL` is still empty, so no purchaser ever received the superseded keying. Suite 45 files / 1578 tests green. Cleared by controller override, L48 sighting #17 — still no cross-model read of the engine, the rung or this clause. STAGED — merge is its own word.)

**superseded:** 2026-07-29 · v0.58 (§1.D public rung — a fourth ladder rung `t4`, param `?paid=t4`, price $9, appended so every existing rung keeps its rank and no t1/t2/t3 owner is affected; the R2 legacy grandfather deliberately stays at t3. t4 adds no coordinate — the sheet is complete at t3 — but a reading OF it: three ranked domain families, one anti-fit, one shape-of-role line, computed offline by `core/public.js` from the birth date alone. It is a block like `cardEntry`, excluded from the §1.F census, governed by the §1.D v0.37 sealed-DOM rule. Commercially NOT live and fail-closed: the Gumroad product does not exist, `T4_PRODUCT_URL` ships empty, and the CTA carries no href while it is; the §4.B v0.56 $3 sprint is byte-untouched. Three limits on the record: the mode driver is the life path, a coordinate the free sheet already shows (`PUBLIC_TIER_SPEC.md` §6.1); strength is the month-branch seasonal-state model only; and no cross-model audit has read the engine or this rung — the clearance is an explicit controller override, L48 sighting #15. The $9 price comes from the brief's exposure-radius ladder, which no tracked file records and which this clause does not adopt; reconciling it with the v0.55 $1/$2/$3 column stays open. Suite 45 files / 1560 tests green. STAGED — merge is its own word.)

**superseded:** 2026-07-29 · v0.57 (§3 calc v3.1 — era-correct civil offset for lunar-year and solar-term cusps. calc v2 evaluated the whole 1900–2100 table at a flat UTC+8; China kept Beijing local mean time, UTC+7:45:40, until China Standard Time was adopted in 1929, so a new moon or solar term falling in the first ~14 minutes after midnight was filed one day late. Four dates move, all pre-1929: lunar new year 1916 Feb 4 → Feb 3, and solar terms 1911 lixia, 1912 xiaohan, 1927 bailu each one day earlier. The visible defect was a 1916-02-03 birth filed under the previous year's animal — rabbit instead of dragon. `core/calendar.js` is the only source file changed; numerology, sun sign, rising sign, birth card, pillars ~~and the catalog index are untouched~~ **are untouched — the catalog index is NOT among them, erratum below**, no stored reading needs migration, and no dependency, localStorage key, network call or scanner list changes. `tests/fixtures.json` gains two pre-1929 boundary cases per §3; `tests/calendar.test.js` pins 1916 → Feb 3 and drops it from the ICU-divergence list, which now holds only the three years where ICU is the one in error. Verified against three independent implementations — sxtwl, lunardate, borax — unanimous with each other on 1900–2050 and with this change on 1900–2100. NOTE: the Hong Kong Observatory tables named under calc v2 could not be reached from the implementing environment; egress policy blocked `hko.gov.hk`, so the three-library consensus substitutes and wants HKO re-confirmation. Suite 43 files / 1500 tests green. STAGED on `claude/calendar-pre1929-lmt-fix`; per §10/L48 a cross-model audit is REQUIRED before merge — this is a calc change and a DOCTRINE change, the case the don't-do list names explicitly, and an override is not an appropriate substitute here. **SHIPPED — squash-merged to `main` as `8b51c38` (#140) on 2026-07-29; the STAGED language above is superseded by that merge, corrected on sighting per the v0.48/v0.50/v0.51/v0.55 footer precedent (mechanical edit, see `audits/mechanical_footer_correction_2026-07-29.md`). Corrected, NOT vindicated: the requirement stated above was not satisfied. No cross-model audit was run, and the gate was cleared by explicit controller override — `audits/L48_override_pr140_2026-07-29.md`, L48 sighting #12, which opens by calling itself the weakest use of that instrument in the chain and records that one author wrote the change, its evidence and its clearance. A retroactive cross-model read has since been commissioned (brief at `audits/codex_pr140_retroactive_audit_2026-07-29_brief.md`, #145, merged `a9957b5`); its verdict is outstanding, and it is deliberately named so that it cannot retroactively satisfy any gate. The HKO re-confirmation the NOTE above asks for also remains open. **Erratum, 2026-07-29 P1-D: the "catalog index... untouched" claim above was false. The catalog is `(sunSign, animal)`-driven (§1) using the public year-pillar animal — exactly the coordinate this entry moved for 1916-02-03 (rabbit → dragon). Its catalog index therefore moved too: Aquarius+rabbit `cxxiv` → Aquarius+dragon `cxxv`, confirmed against shipped `core/engine.js`. Numerology, sun sign, rising sign, birth card, and pillars genuinely are untouched. Blast-radius/truth defect in the record; the shipped catalog computation itself was already correct. Also: the HKO re-confirmation this entry left open has now happened (2026-07-29, P1-D) — see the NOTE above in this same paragraph and journal.md. ~~It found eight further boundary mismatches against the authority this entry could not reach, outside the four dates this entry corrected; `core/calendar.js` now carries a sourced correction table for exactly those eight, with zero mismatches remaining against the full HKO 1901–2100 index.~~ **— erratum, 2026-07-30 (Codex pre-merge audit, finding F), struck per L17 and superseded by the fuller erratum under §3 calc v3.1: "outside the four dates this entry corrected" is FALSE — 1911 lixia and 1912 xiaohan are two OF those four, and the correction table restores for them the very HKO values this entry moved away from, so calc v3.1's pre-1929 offset was a regression at those two boundaries. Six of the eight are genuinely new. "The full HKO 1901–2100 index" also overstates the scope: the comparison covers the 12 month-starting terms plus the lunar-new-year date per year (2,400 solar + 200 lunar, zero mismatches), not HKO's full 24-term index — the 12 major terms carry no direct coverage. The eight override values themselves are unaffected and remain verified against the 200 official HKO source files.**)
- v0.71: §4.B/§1.D/§1.J/§5 — the product goes completely free on the controller's order: free ceiling (`t5` for every device, storage never consulted), storefront deleted with an absence guard (no paywall, offers, checkout URLs, `?paid=` handler), three commerce keys boot-scrubbed (pending held staged personal data), registries stay (`core/payments.js`, facet machinery, `TIER_COORDS` as render registry), auditor probe repointed, owner impact named, revenue thread closed as MOOT with markers on the v0.56/v0.67/v0.69 clauses. Review: the two-lane artifact filed with the amending PR.
- v0.70: §1.D — the kua block retired from the render on the controller's screen-space order: `kuaRead` leaves t3, `ui/kua.js` deleted, no kua section on any sheet, paid copy updated; `content/kua.v1.js` (per §4) and `core/kua.js` (method registry + engine suite) stay, gender-token boot scrubs stay; prices, entitlements, census untouched; markers appended to the v0.63/v0.64 clauses. Review: the two-lane artifact filed with the amending PR.
- v0.69: §1.J — the price-column reconciliation closes by adopting nothing: the column read as v0.55's ledger of controller-set from-zero rung prices (monotone, never derived, never prorated — disclosed), t5's $6 = two complete sheets' worth with the relation layer deliberately unpriced, subtraction prices nothing in this column, the brief's ladder stays unadopted, the four-rung reading is $1/$2/$3/$6 with its dominance fact stated and the storefront kept distinct (only $3 live), and all three open-work sentences carry markers — zero open on the page. Review: the two-lane artifact filed with the amending PR.
- v0.68: §1.G — the body/context boundary drawn on the controller's order: three authored layers (registered body · placement layer · context synthesis), the placement layer named for the appended slot/placement lines (position × value, the value read in the position's registered office, appended after the immutable body, §4-versioned separately), THEME_TENSIONS homed as context-layer table input; per-layer immutability stated, no product change; both routing notes carry resolution markers. Review: the two-lane artifact filed with the amending PR.
- v0.67: §1.J/§4.B/§5.B — the dyad rung goes on offer: `T5_PRODUCT_URL` carries the live `neysyv` Buy Link and the t3-scoped rail offer anchor ships with it (own predicate, own plain-anchor click path + disclosure, entry stays entitlement-only per R6, href stripped when hidden); `?paid=t5` handling pre-existed and the tier write is the entire grant (no credits exist, §1.D v0.55); fail-closed degradation is runtime-driven and the single-CTA paywall retained; the operator-hand remainder is publishing `neysyv` and RE-pointing its t2-era Content-tab button to `/?paid=t5` — the unchanged button silently grants t2.
- v0.66: §6 — the shell stylesheet: the inline `<style>` block moves verbatim to `ui/shell.css` (linked before `ui/experience.css`, cascade preserved, byte-identity proven), `index.html` 1455 → 683 under the unchanged 1500 cap; `ui/*.css` becomes the second sanctioned split target, inline `<style>` is banned from returning, and the scan surface widens (palette scan over `ui/*.css`, privacy scan over `.css` + CSS egress tokens, five silenced negative guards revived). Review: the pr215 two-lane artifact.
- v0.65: §1.I — the registries read within one sheet: the meaning panels gain a `filed relation` section (fixed pair list, registered records only, five Register-law emissions verbatim + status narrowed by the absence-claims-nothing rule), adverse records suppress the harmony algebra, stale panels close on re-render, the sealed-DOM contract carries the tier boundary; plus four deterministic §1.G harmony frames. Review: the pr213 two-lane artifact.
- v0.64: §1.D + §5 — the gender ask is retired: the input leaves every surface, profiles and archives carry no `gender`, boot scrubs erase the stale v0.63 tokens from all three keys (read-verified, surgical by mutation-pinned contract), and the kua block's single read is the both-classical-values render with the method's gender dependence disclosed; `core/kua.js` keeps the gendered arithmetic as method registry. Records PR #208 (merged `4c99ab4`) after the fact — doctrine follows code, named openly; cross-model audit ran as the in-container three-lane relay, artifact `audits/claude_relay_pr208_premerge_audit_2026-08-30_response.md`.
- v0.63: §1.D + §5 — the kua block (Eight Mansions personal trigram): the product's first gender-keyed surface and first non-DOB demographic input; optional strict `'male'|'female'` field feeds a new t3 BLOCK computed from the Li Chun-bound solar year, no-gender-on-file renders both classical values, a raw 5 remap is disclosed never silent; shipped as a controller override of the product-surface freeze (2026-08-03 decision, §1.G v0.44 precedent), cross-model audit run as an operator relay per the PR #198 precedent.
- v0.62: §1.B + §1.G/§1.H/§1.I/§1.J/§1.D + §3/§5/§7 — calc v4 restores the master numbers `11 / 22 / 33` across all six numerology coordinates, supersedes the v0.54 nine-number amendment on controller word, anchors masters to the `high` bracket and third facet position, versions the facet key to `_v3`, and ships `meanings.v3` / `concordance.v3` / `dyad.v2` / `public.v3` — every prior content file immutable and untouched.
- v0.61: §1.D + §1.J — the dyad rung `t5` ($6): a second complete sheet plus a named relation layer between two readings, gated all-or-nothing, fail-closed while unbuyable; the ladder skips the retired `t4` and `RETIREMENT_COLLISIONS` pins that it must.
- v0.60: §1.D — the public rung is retired; the read folds into t3 as a ceiling block, the ladder returns to three rungs, and `RETIRED_TIERS` migrates any stored `t4` to `t3` so no device is downgraded.
- v0.59: §1.D public rung — the t4 mode driver moves off the free surface: keyed by the birthday (t2) rather than the life path (free); `content/public.v2.js` carries the v1 tables unedited.
- v0.58: §1.D public rung — fourth rung `t4` ($9, `?paid=t4`) appended; adds a reading of the complete sheet (domain families + anti-fit + role line) as a sealed block, not a coordinate; fail-closed while its product does not exist.
- v0.57: §3 calc v3.1 — era-correct civil offset for lunar-year and solar-term cusps (Beijing LMT before 1929, UTC+8 after); corrects lunar new year 1916 and three pre-1929 solar terms. **Erratum 2026-07-30 (finding F): two of those three solar terms — 1911 lixia and 1912 xiaohan — were moved the wrong way and are restored to their HKO values by `HKO_SOLAR_TERM_CORRECTIONS`; only 1927 bailu stands. See the §3 calc v3.1 erratum.**
- v0.56: 2026-07-26 (14-day single-offer sprint presentation — brief `~/Documents/Codex/2026-07-25/ca/work/claude_revenue_reset_brief_2026-07-26.md`. §4.B v0.56 amendment: buyer-facing paywall = one CTA (`complete 8ball · $3 once`, product `xjpvp`, `?paid=t3`) for 2026-07-26 → 2026-08-08; t1/t2 products + returns + stored tiers + R2 grandfather unchanged — every existing buyer keeps exactly what they own; `core/payments.js` / `ui/payments.js` / `ui/tiers.js` byte-untouched. Adds: labeled fixed specimen preview in the paywall (no. v sheet image + written entry from the public bundle), result-rail offer control on the shared Path B trigger (hidden at t3), plain-English mechanism strip + head metadata (`8 ball · specimen registry`), five non-forced `netlify.toml` 200 rewrites (`/r` `/x` `/ig` `/tt` `/pin`) for first-party server-log attribution (§5 v0.35). index.html 1401 → 1465 (≤1500). Tests: `payments_markup` re-pinned (one-offer + t1/t2 retained-ownership regressions), `prose_coordinate_count` meta re-pin, NEW `tests/channel_routes.test.js`; suite 41 files / 1438 tests green. Expiry after 2026-08-08 unless renewed; revert mechanical. No calc/content/core touch, no new dependency, no new localStorage key, no network call; scanners unchanged. STAGED on `claude/revenue-reset-single-offer`; §10/L48 cross-model audit required before merge. **SHIPPED — squash-merged to `main` as `c471e92` (#130) on 2026-07-26; the STAGED language above is superseded by that merge, corrected on sighting per the v0.48/v0.50/v0.51/v0.55 footer precedent (mechanical edit, see `audits/mechanical_footer_correction_v056_2026-07-29.md`). The cross-model requirement stated here was **met, not overridden** — the distinction from the v0.57 clause corrected in the same change. Chain on file: Codex verdict MERGE WITH FIXES, 3 Medium truth/control-plane findings and 2 Low (`audits/codex_pr130_premerge_audit_2026-07-26_response.md`), all three Mediums closed in the in-branch fix cycle; then a re-audit of the fix delta returning MERGE WITH FIXES with a single Low (`audits/codex_pr130_premerge_reaudit_2026-07-26_response.md`), R1, the stale journal snapshots, itself reconciled in place. Live note rather than staleness: the sprint presentation this clause defines expires **2026-08-08** unless renewed, and revert is mechanical.**)
- v0.55: 2026-07-26 (Ownership + open free surface — brief `~/8ball/sessions/cc_brief_pricing_model_v2_2026-07-26.md`. Purchases become permanent and unlimited: `applyPaidReturn` = monotonic tier write + pending-profile render only; `nextShakeState` renders unconditionally; the three-free-tries cap, tries counter, credit machinery, Path A paywall trigger, funded t3 flip, and reads chip are deleted. Rungs repriced $1 / $2 / $3 (Gumroad live 2026-07-26; IDs/links unchanged). §1.D density ladder + `TIER_COORDS` provably unchanged — free = 5 of 15 · t1 = 10 · t2 = 14 · t3 = 15 + `cardEntry`; R2 grandfather survives (one $9 t3 buyer on record, never downgraded). `eight_ball_tries_used_v1` retired from allow-list per v0.48 precedent; `eight_ball_credits_v1` frozen as the read-only R2 signal. index.html copy: paywall "three rungs · yours for good", about-modal "readings are free and unlimited", banner "rung opened. yours for good." — cap/credits copy gone. Suites rewritten to the ownership contract; privacy CI FORBIDDEN list unchanged; pii_scan DOCTRINE_ALLOW unchanged. Trigger: operator brief (commercial repricing already live on Gumroad — code catches up to the listings' "yours for good" promise). STAGED on `claude/pricing-model-1-2-3`; §10/L48 cross-model audit required before merge. **SHIPPED — squash-merged to `main` as `94cf1bc` (#127) on 2026-07-26 with the in-PR Codex verdict on file (MERGE WITH FIXES — 1 Med DECLINED with rationale, 1 Low absorbed in-PR; `audits/codex_pr127_premerge_audit_2026-07-26_response.md`); the STAGED language above is superseded by that merge, corrected in the dedicated footer-flip cycle the journal's #127 close-out queued, per the v0.48–v0.51 footer precedent.**)
- v0.54: 2026-07-20 (Strict nine-number numerology / calc v3. `core/profile.js` repeatedly reduces all six numerology coordinates to `1..9`; `core/engine.js` and `core/payments.js` accept the nine active life paths only. `tests/fixtures.json` and direct regression tests update in lockstep. `content/meanings.v2.js` exposes an exact nine-entry active view and numerology-only context partners; `content/concordance.v2.js` retires active master links while preserving immutable v1 history. No stored-data migration because only user inputs persist. No commit/push/PR/merge/deploy authority; STAGED on `codex/all-coordinates-clickable`, cross-model audit required.)
- v0.53: 2026-07-20 (§1.G meaning-layer correction — replaces the v0.52 provenance-only panels with value-specific meanings and an `in this sheet` synthesis across fixed neighboring roles. Adds versioned `content/meanings.v2.js`, reusing v1 tables and adding five element entries plus finite context metadata. No advice/prediction/diagnosis/score/certainty/second-person language; no `index.html`, `core/`, fixture, tier, persistence, share, network, or dependency change. Direct controller correction; no push/PR/merge/deploy authority. STAGED on `codex/all-coordinates-clickable`; independent cross-model audit required before merge.)
- v0.52: 2026-07-20 (§1.G interaction-layer amendment — all fourteen sheet compartments become pointer- and keyboard-interactive; four versioned meanings unchanged, ten provenance/status-only; sealed-value DOM purity and catalog exclusion preserved. Runtime/test scope: `ui/meanings.js`, `tests/meanings_behavior.test.js`, `tests/meanings_ui.test.js`; canon synchronized. No `index.html`, `core/`, `content/`, fixture, tier, persistence, network, or dependency change. Direct controller request authorized the local implementation and Codex lane exception; no push/PR/merge/deploy authority. STAGED on `codex/all-coordinates-clickable`; cross-model audit required before merge per §10/L48.)
- v0.51: 2026-07-19 (Registry + Concordance MVP — one-time controller/doctrine + Codex-implementer override. Exactly two valid Saved Readings may be selected; each is recomputed through the existing profile pipeline, then matching axes are looked up in the finite `content/concordance.v1.js` registry. Output is limited to `registered | adjacent | unfiled` plus relation/citation metadata and `recorded, not certified.` No scores, compatibility claim, forecast, advice, diagnosis, or invented synthesis. Selection and results are transient: no new key, schema field, pair history, derived-output cache, share artifact, account, backend, sync, analytics, endpoint, dependency, or engine change. `core/` and `tests/fixtures.json` remain untouched. Trigger: explicit one-time controller override; STAGED on `feature/registry-concordance-mvp`; normal cross-model doctrine audit remains required before any merge per §10/L48. **SHIPPED — squash-merged to `main` as `b1cfd32` (#95); the STAGED language above is superseded by that merge, corrected on sighting per the v0.48/v0.50 footer precedent (mechanical edit, see audits/mechanical_footer_correction_2026-07-20.md).**)
- v0.50: 2026-07-19 (Saved Readings MVP — one-time controller/doctrine + Codex-implementer override. The §2 "no save your reading" phrase is narrowly superseded by §5.E's explicit browser-local archive: save completed readings; reopen through the existing deterministic pipeline; rename; confirmed individual delete; confirmed clear-all; forget-device erases current profile + archive. New allow-listed key `eight_ball_saved_readings_v1` stores only `{id,title,savedAt,profile}` where `profile` is restricted to the existing user-entered reconstruction fields; no derived coordinate/card output, tier, credit, try, payment, or share data. No account/backend/sync/analytics/endpoint/dependency; `core/`, `content/`, and `tests/fixtures.json` untouched. New `ui/readings.js` follows the §6 DI split and injects its page/dialog/CSS; `tests/readings.test.js` covers save/load/deduplication/rename/delete/clear/corruption/unavailable storage. User-facing local-retention + deletion disclosure added to Previous Readings, about, and forget-device surfaces. Trigger: explicit one-time controller override in the implementation brief, exempt from §13's evidence gate by the controller-override precedent. STAGED on `feature/saved-readings-mvp`; requires the normal cross-model doctrine audit before any merge per §10/L48. **SHIPPED — squash-merged to `main` as `4863cd0` (#94); the STAGED language above is superseded by that merge, corrected on stacking per the v0.49 footer precedent.**)
- v0.49: 2026-07-19 (§1.H NEW — t3 written-entry rotation, STAGED under explicit controller override. A funded explicit t3 `flip again` alone advances `note.low → note.mid → note.high → note.low` and debits one credit; lower tiers remain cosmetic and credit-preserving. The current visible zero-based position is persisted in the new §5 allow-listed `eight_ball_facet_index_v1`; new/consumed-pending profiles reset to the life-path anchor, reload/same-profile preserve, and forget clears. A zero-credit flip clears any stale pending intent and opens the paywall without advancing or auto-consuming on return. Content bytes and `tests/fixtures.json` are untouched: this is c.1 mechanical reuse of immutable v1 slots, not the previously designed c.2 lateral reauthoring, and no outward/inward/returning taxonomy is claimed. On 2026-07-19 the controller explicitly waived both the Codex read-only lane restriction and the standing traction/c.2-content prerequisite. Code/test scope: `core/payments.js`, `ui/payments.js`, `index.html`, `tests/facet_rotation.test.js`, and coordinated static/privacy pins; docs/release/journal/audit records co-versioned; package 0.7.0 → 0.7.1. No new dependency, network call, content edit, fixture edit, or derived-profile persistence. `privacy_scan` FORBIDDEN unchanged; `LOCALSTORAGE_KEY_ALLOW` gains the PII-free facet key; `pii_scan` DOCTRINE_ALLOW unchanged. Cross-model gate: Grok + Claude round 1 returned MERGE WITH FIXES; the credits/stale-pending/copy/test absorbs landed; delta re-audit returned SAFE TO MERGE on 2026-07-19. The feature remains STAGED until separately authorized commit/PR/merge/deploy actions clear the ordinary release gates. **SHIPPED — squash-merged to `main` as `0e49773` (#93); the STAGED language above is superseded by that merge, corrected on stacking per the v0.48 footer precedent.**)
- v0.48: 2026-07-06 (§4.A RETIRED — the 18+ acknowledgment gate, live since v0.2.6, is removed: the modal, `isAgeAcknowledged`, the `eight_ball_age_ack_v1` localStorage flag, and the about-modal's disclosure line are all gone. Operator call, not a rule-kill or audit finding — the gate's "for adults" / "toy for adults" copy read as adult-content-warning theatre, the opposite of what §4.A's own mechanics clause required ("no health-product warning theatre"). No external mandate depended on it: the v0.2.6 rationale was purely "kids shouldn't use it," and its one later citation (a Lemon Squeezy KYC pre-emptive response doc) is moot since that payment path was retired for Gumroad at v0.3.0.3. §4.A's mechanics preserved verbatim as history per L17; the §4 "no targeting minors" bullet amended to no longer depend on a mechanical gate. `core/profile.js getMaturity` also fixed the same day (unrelated) — see journal 2026-07-06. `index.html` boot sequencing simplified: `initAfterAck` renamed `boot`, called unconditionally, no more gate branch. `privacy_scan` FORBIDDEN unchanged; `LOCALSTORAGE_KEY_ALLOW` loses `eight_ball_age_ack_v1` (retired key, not a new one); `pii_scan` DOCTRINE_ALLOW unchanged. Trigger: operator content-voice call, not a §13 named-trigger gate. This DOCTRINE.md touch went through cross-model audit before merge per CLAUDE.md's don't-do list (journal.md 2026-07-06 entry, Codex+Claude relay, verdict MERGE WITH FIXES, four stale-reference findings corrected in a follow-up commit) — **SHIPPED, squash-merged to `main` as `1a70756` ([#80](https://github.com/appleeatsapples-lang/8ball/pull/80)).** This "STAGED, not yet audited" note was stale in-file (journal.md flipped STAGED→SHIPPED on 2026-07-06 in the same cycle; this footer entry did not) and is corrected here on sighting, not silently, per the v0.44 footer entry's own precedent below.)
- v0.47: 2026-07-05 (§14 REVISED — Operator calibration, mechanical closure of a Grok post-fix re-audit P2: the weekly strategic-read trigger bullet said "if it's overdue the check runs," lacking the explicit missing-line handling its sibling weekly-analytics-read bullet already had ("overdue or missing = read runs now") despite both claiming the same file-backed mechanism ("mirrors... exactly"). Now reads "if it's overdue or missing the check runs." Three-word fix, no new concept — closes the asymmetry rather than introducing one. Confirming re-audit context: v0.46 (below) was itself the fix for the original gate-6 Grok+Codex audit; a follow-up confirming re-read (Codex: PASS, no findings; Grok: PASS-with-findings, this one P2) verified both v0.46 fixes hold with no new contradiction/regression, and surfaced this asymmetry. No product/calc/content/core touch, no code change, no new dependency, no new localStorage key. `privacy_scan` FORBIDDEN + `LOCALSTORAGE_KEY_ALLOW` unchanged; `pii_scan` DOCTRINE_ALLOW unchanged. Trigger: post-audit mechanical closure, not a §13 named-trigger gate. Codex Procedure 4 + journal per §10/L48 — shipped as `46585ae` (#77), superseded by v0.48 above.)
- v0.46: 2026-07-05 (§14 REVISED — Operator calibration, addressing a two-model cross-model audit (Grok + Codex, both PASS-with-findings, neither a refusal) run before merge per gate 6: (1) the unanimous-override bullet no longer presumes the override was right, just that the objective must be named; (2) override-risk flags (a)/(b) no longer claim to be mechanically observable in isolation — they're checked against message + transcript + doctrine, with a close call firing the flag; (3) terse-trust is scoped to compression-trust (deliberate shorthand, reversible/low-blast-radius, established acceptance criterion) vs. ambiguity, which still gets a clarifying question; (4) added operational definitions for "local," "irreversible-adjacent," and "the morning read"; (5) the evidence-threshold rule now carries an explicit carve-out — it cannot waive safety, privacy, or this doctrine's other standing rules; (6) weekly analytics read is now file-backed (`next_analytics_read`) instead of an unwatched calendar cadence, fixing an internal contradiction Codex caught against this same section's own strategic-read mechanism; (7) canon-vs-ephemeral scope clarified for audit briefs and relay output. v0.45's text and both full model reviews are preserved in `journal.md`. Cross-references §10 controller authority and §12/§13 fail-closed/named-trigger discipline. No product/calc/content/core touch, no code change, no new dependency, no new localStorage key. `privacy_scan` FORBIDDEN + `LOCALSTORAGE_KEY_ALLOW` unchanged; `pii_scan` DOCTRINE_ALLOW unchanged. Trigger: operator-initiated doctrine formalization, not a §13 named-trigger gate. Codex Procedure 4 + journal per §10/L48 — superseded by v0.47 above the same day.)
- v0.44: 2026-07-04 (§1.G NEW — coordinate meanings, controller override shipped against the active features-frozen state; see §1.G for the full override citation, dissent-on-record, and the purged-lab alternative it replaced. No historical or operator-locked text edited elsewhere. `privacy_scan` FORBIDDEN + `LOCALSTORAGE_KEY_ALLOW` unchanged; `pii_scan` DOCTRINE_ALLOW unchanged — `content/meanings.v1.js` and `ui/meanings.js` carry no operator/PII-shaped strings. Trigger: controller override, not a §13 named-trigger gate (overrides are exempt from that discipline by the same precedent as v0.38). Codex Procedure 4 + journal per §10/L48 — merged as `bdfe2ec` (#70); this "audit pending" note was stale in-file and is corrected here on sighting, not silently — worth a dedicated coherence pass if other version lines have the same drift.)
- v0.43: 2026-07-01 (rule-kill review enactment — the 7 cross-verified AMENDs from the 2026-06-30 §13 rule-kill review (#49), additive/L17-clean; no historical or operator-locked text edited. **Governance/trigger (3):** §13 cadence → event-based self-firing trigger (every doctrine-touching ship runs the review in its §8 close-out when the last logged review is >30d old; the monthly date retained as the floor; "last review date" read mechanically from the newest rule-kill review entry in `journal.md`) + sibling §8 ritual step (gate 8); §12 [GUARDED] gated-work pass (P1) — astrology-charts/planetary-aspects reclassified [GUARDED]→[PERMANENT] (already the permanently-banned no-oracle/horoscope identity), multi-language trigger re-keyed off the deleted `traits.v1.js`/`templates.v1.js` system to a live observable (`cards.v2.*` exists in any language); §7 Netlify-required-check trigger bound to the concrete ≥5-paid + 1-signal ship-gate proxy (else an indefinitely-accepted gap). **Coherence/L17-staleness (4):** §1.A "lands at v0.2.8" → shipped/past-tense; §1.B numerology "three-number triplet" → name-derived pair (life path split free at v0.38); §1.B personality/birthday/maturity "reserved for v0.3.0+" → surfaced at t2 (v0.6.0); §8 gate-8 journal-header example `===== … =====` → the live `## … — STATUS` shape. No calc/content/core/code touch; `package.json` unbumped. `privacy_scan` FORBIDDEN + `LOCALSTORAGE_KEY_ALLOW` unchanged; `pii_scan` DOCTRINE_ALLOW unchanged. Trigger per §13: §13 rule-kill review enactment. Codex Procedure 4 + journal per §10/L48.)
- v0.42: 2026-06-30 (doctrine coherence — refinement campaign #3, additive/L17-clean. §1 gains a current-state anchor routing readers to the §1.D ladder (as amended through v0.38) ahead of the retired v0.2.3/v0.36 lineage prose; a table-currency note under the operator-locked §1.D table records that free is now FIVE coordinates (life path joined at v0.38) and t1 numerology is the name-derived pair; §6 architecture counts reconciled to 9 core / 6 ui / 24 tests with the file inventory deferred to CLAUDE.md (canonical) so they cannot re-drift in doctrine; §13 codifies L17 (supersede-not-edit + §-number preservation) in-document with L48/L53 sibling pointers, so the constitution no longer depends on externally-defined rules. No historical or operator-locked text edited; no calc/content/core/code touch; `package.json` unbumped. DEFERRED to a dedicated cut: the overdue §13 rule-kill review + a self-firing review trigger. `privacy_scan` FORBIDDEN + `LOCALSTORAGE_KEY_ALLOW` unchanged; `pii_scan` DOCTRINE_ALLOW unchanged. Trigger per §13: doc-coherence refinement. Codex Procedure 4 + journal per §10/L48.)
- v0.41: 2026-06-30 (Coordinate Legibility Pack cuts 2–3 doctrine coverage [NEW §1.F]. Documents the ATLAS system-name legend (per-coordinate, labels-gated, rendered in `.coord-atlas` outside `.coord-title`, off the §5.D PNG) and the DENSITY census (always-on aggregate `N of 15 coordinates open · M sealed at paid tiers` in the result rail, OUTSIDE `#card-face`, computed purely from `TIER_COORDS` + `CELL_COORD` via `tierDensitySummary`, never a profile; base 15 = the 14 sheet cells + the always-open catalog numeral, so the free count 5 matches §1.D). Both surface-only, catalog-isolated, no coordinate VALUE / no PII, no new localStorage key, §2 clinical / no-FOMO. Discharges the cut-2 Codex Hook-7 §1.F/§1.E-broaden debt — ATLAS (cut 2) and DENSITY (cut 3) both shipped clause-free under §1.D v0.37 + §5.D v0.39 + the §1.E precedent; §1.F now gives them literal coverage. `index.html` about-modal "the trick" copy updated to name what reveal-labels surfaces (derivation + system) and the result census. **Doc/copy only — no calc/content/core touch, no code-behavior change beyond the already-shipped cuts 2–3; `package.json` not bumped.** `privacy_scan` FORBIDDEN + `LOCALSTORAGE_KEY_ALLOW` unchanged; `pii_scan` DOCTRINE_ALLOW unchanged. Trigger per §13: §8 brand-coherence / legibility. Codex Procedure 4 + journal per §10/L48.)
- v0.40: 2026-06-29 (Coordinate Legibility Pack — provenance placards [NEW §1.E]. Each coordinate may carry a labeled, surface-only PROVENANCE PLACARD naming its derivation method — clinical, never interpretation (§2) — e.g. `digit-sum reduction`, `tropical zodiac`, `ascendant`, `solar term`, `letter-value sum`, rendered per row in cell order, gated by the existing `.card.labels-revealed` toggle (no new key). Tier-invariant + catalog-isolated: keyed to the coordinate, never a profile value or the catalog driver `(sunSign, animal)`; `getCard` unchanged. Adds NO coordinate VALUE (free VALUE count stays five per §1.D v0.38) and NO PII (names a method, never name/DOB/value). A sealed compartment's placard names its derivation (label/structure only, value absent per the v0.37 contract) — a hatched cell reads as a real coordinate withheld at the user's tier, not a failure (the F1-WATCH legibility fix). NOT serialized into the §5.D share PNG: the share path reads `.coord-val` + `.coord-title` (via `ui/tiers.js shareRowRefs`, consumed by `ui/share.js` as a snapshot), never `.coord-prov`. Step-0 §6 budget move: about/forget/age-gate modals + escape-to-close extracted to `ui/modals.js` (AGE_ACK_KEY relocated, no new key); index.html 1496 → ~1444. Sibling tracked-file changes co-versioned: `ui/tiers.js` (`PROV_NOTE` + `provText` + init-time `.coord-prov` write pass), `ui/modals.js` (NEW — modal controllers), `index.html` (`.coord-prov` CSS gate + modal extraction), `tests/` (NEW `tests/modals.test.js` (DI shape + no-new-key + boot wiring), NEW `tests/provenance.test.js` (derivation pins + paid-value-absence sentinel + clinical-voice pin + DOM-write/sealed-row F1-WATCH/idempotence), `tests/share_surface.test.js` placard-exclusion pin, `tests/age_gate.test.js` key re-home). Ships within the 0.7.x line — `package.json` not bumped (flagged for Procedure 4 version-truth, as v0.38/v0.39). No calc / `content/` / `core/` touch, no new dependency, no new localStorage key, no network call — `privacy_scan` FORBIDDEN + `LOCALSTORAGE_KEY_ALLOW` unchanged; `pii_scan` DOCTRINE_ALLOW unchanged. Trigger per §13: §8 brand-coherence / legibility — retires the F1-WATCH "hatch reads as broken" risk; explicitly NOT a measured conversion fix. Brief `~/Desktop/8ball/sessions/brief_clp_provenance_2026-06-29.md`.)
- v0.39: 2026-06-29 (reach pass v1 — full-sheet share artifact. **§5.D:** the share PNG renders the FULL specimen sheet — open coordinate values at the user's tier PLUS the sealed structure of higher-tier compartments (section label + seal hatch, the VALUE absent), resolving the v0.37 sealed-structure-in-PNG deferral; the sealed value never exists in the DOM snapshot the builder serializes (sealed cells hold `textContent === ''`), so invariants (a)–(d) hold; `ui/tiers.js shareRowRefs` exposes a DOM-derived per-cell `{state, value}` snapshot and the `ui/share.js` tier-hidden filter is removed. NEW share caption: catalog + open coordinate values + `sealed remainder` + bare URL, carried in `navigator.share` text and copied on the desktop fallback (replacing the bare-URL copy), same (a)/(b) PII bounds, §2 clinical voice. **Cold-landing:** one-line `#mechanism-strip` near the registry header — clinical, no pricing/CTA. Sibling tracked-file changes co-versioned: `ui/share.js` (full-sheet builder + SVG hatch `<pattern>` + `buildCaptionFromSnapshot` coupled into the flow), `ui/tiers.js` (`shareRowRefs` per-cell `{state, value}` snapshot, DOM-derived), `index.html` (mechanism strip + share-wiring comment, 1499/1500 held by offsetting compaction), `tests/share_surface.test.js` (full-sheet render, sealed = hatch + label + no value, aggregate paid-value-absence sentinel, F4 unres-vs-seal, caption PII/voice, share-flow coupling, mechanism strip). Ships within the 0.7.x line — `package.json` not bumped (flagged for Procedure 4 version-truth, as v0.38). No calc / `content/` / `core/` touch, no new dependency, no new localStorage key, no network call — `privacy_scan` FORBIDDEN + `LOCALSTORAGE_KEY_ALLOW` unchanged; `pii_scan` DOCTRINE_ALLOW unchanged. Reach lever per the 06-15 read; Codex pre-build audit cleared 2026-06-29 (P1 H2 → §5.D amendment). Trigger per §13: reach pass; brief `~/Desktop/8ball/sessions/brief_reach_pass_v1_2026-06-29.md`.)
- v0.38: 2026-06-25 (free life-path split — override #3, controller-authorized 2026-06-12, HELD by operator 2026-06-14, resumed 2026-06-25 — §1.D numerology splits by derivation: `lifePath` (DOB-derived) joins the free surface (free = DOB-only invariant preserved) so the free render carries FIVE coordinate VALUES — birth card · sun · public animal · life path · catalog numeral; the name-derived pair (expression/name number + soul urge) stays t1, the t1 "numerology triplet" demoted to lineage per L17. Surface-only / catalog-isolated — life path drives low/mid/high bracket resolution per §1, never the catalog index `(sunSign, animal)`; `getCard` unchanged. §5.D: the free share-PNG now carries the life-path numeral — a DOB-derived number with no name/DOB string, so §5.D PII invariants hold, same basis as the birth card and catalog numeral. One-line `TIER_COORDS` rollback (`ui/tiers.js`). Sibling tracked-file changes co-versioned: `ui/tiers.js` (FREE_COORDS + CELL_COORD split + renderTierSections lifePath cell), `index.html` about-modal + og/twitter/description meta (four→five coordinates; life path moved to the free list, out of the t1 list), test re-pins in `tiers` / `payments_markup` / `prose_coordinate_count`. No calc / `content/` / `core/` touch, no new dependency, no new localStorage key, no network call — `privacy_scan` FORBIDDEN + `LOCALSTORAGE_KEY_ALLOW` unchanged; `pii_scan` DOCTRINE_ALLOW unchanged. Orchestrator dissent on record: reach is the verified bottleneck (06-15 read), conversion is not the constraint; LOW public-perception reversibility — the free share-PNG exposes the life-path number, a later clawback reads as bait-and-switch. Trigger per §13: controller override (override #3); brief `~/Desktop/8ball/sessions/override3_free_lifepath_split_2026-06-12.md`.)
- v0.37: 2026-06-12 (compartment-render amendment, v0.7.0 — the card renders the full coordinate sheet at every tier; coordinates above the device's render tier appear as sealed compartments — row, label, and cell structure visible, the coordinate value ABSENT from the DOM (absent, not hidden). **§1.D:** v0.37 amendment appended — free-surface invariant unchanged in substance (the free render carries exactly four coordinate VALUES; v0.7.0 adds the visible sealed structure of the remaining sheet); sealed ≠ unresolvable (paywalled field → seal treatment; entitled-but-uncomputable field — rising without birth time/place, hour pillar without birth time — → `—` empty field at any entitled tier); the written 144-card entry stays a t3-only unlock, its sealed block carries no entry text at any lower tier. Sibling tracked-file changes co-versioned: `ui/tiers.js` render-contract rewrite (hidden→sealed, per-cell granularity — `setCell` state ∈ value | sealed | unres; paired-row title grammar: dot form names structure, relation glyph marks a resolved entitled pair — `SUN · RISING` / `SUN ↑ RISING`, `PUBLIC · PRIVATE` / `PUBLIC ⇌ PRIVATE`, bare sun-only title never renders; pure unseal-trigger decision `newlyEntitledCells` + `primeUnsealBaseline` — upgrade renders only, β-idempotent; §5.D `shareRowRefs` row-snapshot proxies — the share PNG keeps rendering OPEN coordinates only, sealed-structure-in-PNG is a logged journal deferral, `ui/share.js` UNTOUCHED); `index.html` per-cell compartment markup (14 `.coord-cell` + the sealed `#card-entry` block), F1 hatch seal as the single root token `.card.seal-hatch` (gradient defined once), `.locked-extras` bars retired, unseal keyframes + `prefers-reduced-motion` instant fallback, written-entry slots constant-skeleton and cleared below t3 (1499/1500); `tests/tiers.test.js` rewrite (constant skeleton, DOM purity at every rung, seal-iff-above-tier, F4 distinction, title grammar, unseal idempotence, share-row snapshots; tier-resolution/grandfather/monotonic suites carried unchanged); `tests/payments_markup.test.js` seal tokens + v0.6.1 geometry assertions kept; re-pins in labels_reveal / numerology_display / birthcard; `package.json` + lock 0.6.0 → 0.7.0. No calc change, no `content/` deck touch, no `core/` touch, no new dependency, no new localStorage key, no network call introduced — privacy_scan FORBIDDEN list + LOCALSTORAGE_KEY_ALLOW unchanged; pii_scan DOCTRINE_ALLOW unchanged. Trigger per §13: §8 brand-coherence on the tier surface, controller override recorded 2026-06-12 (same exception class as Build B 2026-06-11); brief `~/Desktop/8ball/sessions/brief_v070_compartment_card_2026-06-12.md`.)
- v0.36: 2026-06-11 (tier-ladder amendment, v0.6.0 — the paid surface becomes a three-rung coordinate-density ladder per the operator-locked table; the free surface shrinks to four coordinates. **§1:** free-surface invariant amended — free card = birth card · sun (bare) · public animal (year) · catalog numeral; the v0.2.3 seven-coordinate free surface demoted to lineage verbatim per L17; v0.22 unlocked card-content layer relocates to t3. **§1.D (NEW, mirrors §1.A/§1.B/§1.C):** tier-ladder surface clause — tiers reveal coordinates per the locked table; tier coordinates are surface-only; catalog driver stays `(sunSign, animal)`; day/hour pillars never enter the catalog; stored tier monotonic via `eight_ball_tier_v1`; +3 credits on any rung; density = f(stored tier, credit state) resolved by one helper at every render, never boot circumstance (PR #36 Codex remediation R1); legacy credits-without-tier grandfathered to t3 and persisted on first detection (remediation R2); `TIER_COORDS` rollback flag in `ui/tiers.js`; specimen-sheet §9.3 element reconciliation untouched (no §3 calc-version bump). **§2:** arcade-toy carve-out explicit re-amendment — `dollars == tries` superseded (every rung buys three reads; dollars buy density); one-time/fixed-credit/no-subscription unchanged. **§4.B:** paywall = three-rung ladder; 3-free-tries cap unchanged; t3 carries the card-content unlock. **§5:** allow-list + `eight_ball_tier_v1`. **§5.B Call 2:** three Gumroad products (rzqezp $3 / neysyv $6 / xjpvp $9), same bare-href + Content-tab-Button mechanism; `?paid=t1|t2|t3` handler, unknown values ignored. **§5.D:** share renders at the current tier; all invariants preserved (a's "free coordinate symbols" generalizes to "rendered coordinate symbols at the current tier"). Sibling tracked-file changes co-versioned: new `ui/tiers.js` (TIER_COORDS + render gating, §6 DI shape); `ui/payments.js` TIER_KEY + generalized handler; `core/payments.js` pure tier functions (TIER_ORDER/isTier/tierRank/maxTier + applyPaidReturn tier extension); `ui/share.js` tier-aware row filter + count-adaptive geometry; `index.html` three new pillar/number rows + paywall ladder + about/meta tier disclosure (1494/1500); new `tests/tiers.test.js`; re-pins in payments_state / payments_markup / labels_reveal / prose_coordinate_count / share_surface / numerology_display / pillars / birthcard tests; `tests/privacy_scan.test.js` LOCALSTORAGE_KEY_ALLOW + `eight_ball_tier_v1` (the §5-amendment-gated extension). No calc change, no `content/` deck touch, no new dependency, no network call introduced — privacy_scan FORBIDDEN list unchanged; pii_scan DOCTRINE_ALLOW unchanged. Trigger per §13: tier-model §8 brand-coherence exception, controller override recorded 2026-06-11; brief `~/Desktop/8ball/sessions/brief_v060_tier_ladder_2026-06-11.md`.)
- v0.35: 2026-06-02 (governance-repair amendment — decouples over-extended constraints from their load-bearing values without removing any privacy / voice / SIRR / no-backend invariant. **§2:** analytics bullet clarified (first-party server-side host logs are not "telemetry") + new review-discipline bullet "honest invitation permitted; manipulation forbidden" — supersedes "not a funnel / no CTA / no link in bio" language that lived only in operating/session plans, never in this file. **§5:** new "first-party server-side observability is not telemetry" clarification — reading the operator's own origin logs is permitted; the product still injects no client / makes no call / emits no beacon, and `tests/privacy_scan.test.js` is unchanged because first-party logs are produced by the host platform, never by tracked source. **§12:** retitled "Out of scope" with a two-tier **[PERMANENT]** vs **[GUARDED]** marking — accounts / email / push / runtime-AI-phrases / daily-weekly-horoscopes / unconstrained-export / SIRR stay PERMANENT (load-bearing); only multi-language + astrology-charts move to GUARDED. No item deleted. **§13:** gated-work-needs-a-named-trigger discipline added. Sibling state-doc co-edits in `8BALL.md`: §3 telemetry row (first-party logs ≠ telemetry), §1 deniability-layer clarified (privacy/identity split, not a status claim — 8ball may be a first-class public object; §9 no-SIRR-string rule unchanged), build-B parked behind a named evidence gate. No code, no calc, no `content/` deck touch, no shipped-surface change, no new dependency, no new localStorage key, no network call introduced — `tests/privacy_scan.test.js` FORBIDDEN list + `LOCALSTORAGE_KEY_ALLOW` unchanged; `tests/pii_scan.test.js` DOCTRINE_ALLOW unchanged. Product stays 0.5.2. Derivation: `~/Desktop/8ball/sessions/{constraint_decoupling_analysis,doctrine_patch_plan}_2026-06-02.md` + Codex constraint-audit package 2026-06-02.)
- v0.34: 2026-06-02 (§1.A amended — v0.5.2 legacy rising determinism. Stored v0.2.1–v0.2.7.1 country-code profiles now resolve their country/zone code to a representative IANA timezone before using the same `computeRising` path as fresh city profiles; legacy `getRisingSign(..., utcOffsetMinutes, ...)` is retained only as a low-level fixed-offset helper and both APIs remain polar-safe. §5.D(a) share-artifact wording now truthfully includes the 8ball wordmark. Sibling tracked-file changes co-versioned: `core/countries.js` exports `LEGACY_COUNTRY_TIMEZONES` for exact-keyset tests; `core/profile.js` legacy country path routes through representative IANA tz; `ui/share.js` stale fixed-count comment scrubbed; `tests/countries.test.js` adds exact keyset, alpha-2 largest-city parity, split-code snapshots, and DST/historical sign-impact regressions; `tests/prose_coordinate_count.test.js` derives the prose count from rendered coordinate rows plus share refs; `tests/rising.test.js` adds invalid-tz null regression; `package.json` and `package-lock.json` version truth reconciled to product v0.5.2. No calc-version change, no `content/` deck touch, no new dependency, no new localStorage key, no network call introduced — `tests/privacy_scan.test.js` FORBIDDEN list + `LOCALSTORAGE_KEY_ALLOW` unchanged; `tests/pii_scan.test.js` DOCTRINE_ALLOW unchanged.)
- v0.33: 2026-05-30 (§1.C added — tarot birth-card free coordinate. Major Arcana numeral + lowercase name (e.g. `XXI · the world`), computed DOB-only in new `core/birthcard.js` (`getBirthCard`), carried as additive `birthCard` field on `buildProfile`. Lead row of the free card, above five-element. Free-surface coordinate inside the §1 symbols-only invariant — labeled coordinate, never interpretation; the paid card-content layer is untouched. Surface-only like §1.A rising: does not enter `getCard` / `resolveBracket` / catalog computation; catalog driver remains `(sunSign, animal)`. Reduction tradition: digit-sum (sum all DOB digits, reduce ≤22), distinct from the master-number-stopping life-path reduction; 22 maps to `0 · the fool` so every DOB resolves to a real arcana. Included in the §5.D share surface (free coordinate → shared PNG matches the on-screen free card, five coordinates); no PII, no paid content, §5.D invariants (a)/(b) hold. Ships with product v0.5.0. Sibling tracked-file changes co-versioned: new `core/birthcard.js`; `core/profile.js` import + additive field; `index.html` arcana lead `.coord-section` + render wiring + share symbols array extended 4→5; new `tests/birthcard.test.js` (14 tests); `tests/labels_reveal.test.js` coordinate-count assertions 4→5. No calc-version change (DOB-only coordinate, no catalog/bracket effect), no `content/` deck touch, no new dependency, no new localStorage key, no network call introduced — `tests/privacy_scan.test.js` FORBIDDEN list + `LOCALSTORAGE_KEY_ALLOW` unchanged; `tests/pii_scan.test.js` DOCTRINE_ALLOW unchanged.)
- v0.32: 2026-05-30 (§13 cadence amended + §7 mirror — rule-kill review weekly → monthly. Weekly review at solo cadence produced zero pruning (all reviews 2026-05-08 → 2026-05-30 were all-KEEP calibration; rules younger than the 30-day archive threshold have no firing surface) and silently lapsed two consecutive Fridays (2026-05-22 + 2026-05-29) — the prune-discipline became the rule that stopped firing. Monthly aligns the review interval to the 30-day threshold it enforces and co-locates with other monthly beats (next: 2026-06-15, with the v0.3.1 gate-close read). Sibling change: `agents/orchestrator.md` gains Procedure 9 (re-grep canonical state before orienting/drafting) per the orchestrator-stale-context L promoted same session (2 sightings: chat-25 §5.C duplicate-analysis / 2026-05-30 handoff-orientation-shortcut). The Friday pre-stage's §12 "trait pool / template expansion" AMEND leg was DROPPED on verification — that string no longer exists in DOCTRINE (retired earlier); the surviving §12 "trait pool" multi-language clause is accurate, not drift. Doctrine-governance-only cycle: no code, no calc, no `content/` deck touch, no shipped-surface change, no new dependency, no test logic change — `tests/pii_scan.test.js` DOCTRINE_ALLOW unchanged, `tests/privacy_scan.test.js` unchanged.)
- v0.31: 2026-05-29 (§5.D added + §12 amended — share surface cleared. New §5.D codifies a user-initiated, on-device (SVG → canvas → `toBlob`) PNG export of the free card (labeled coordinate symbols + catalog number, no paid content, no PII), shared via the Web Share API with a local-download + bare-URL-clipboard fallback; invariants lock no-paid-content, no-PII, no-per-result-link, no-network, no-telemetry. §12 "Sharing/social-card export until a privacy review explicitly clears it" amended (line preserved as lineage per L17): the contemplated privacy review landed here — the §5.D-constrained free-card export is cleared; unconstrained / paid-content / PII / per-result-link export stays permanently out of scope. Sibling tracked-file changes co-versioned: new `ui/share.js` (`initShareUI` DI module per §6); `index.html` `#share-btn` + share-status surface + boot wiring; new `tests/share_surface.test.js`. Ships with product v0.4.0 — first new surface since the v0.3.0 paid layer. No calc change, no `content/` deck touch, no new dependency, no new localStorage key, no network call introduced — `tests/privacy_scan.test.js` FORBIDDEN list and `LOCALSTORAGE_KEY_ALLOW` both unchanged; `tests/pii_scan.test.js` DOCTRINE_ALLOW unchanged.)
- v0.30: 2026-05-17 (§5.C + §10 amended — Lemon Squeezy permanently retired from the project. LS rejected the operator's application 2026-05-17 after Step 3 KYC/KYB review (chat-25 logged Step 2 cleared; chat-28 chose Gumroad path per "whichever clears first becomes primary funnel" framing). The v0.28 lineage clause "LS path preserved at lemon-squeezy.com store but not wired through 8ball runtime; future re-route possible if LS Step 3 clears" is hereby superseded: LS path is permanently dead, no re-route possible, Gumroad is the single permanent processor at runtime. §5.C future-amendment example (license-key issuance via LS) updated to processor-agnostic backend-gated framing. §10 inspector dashboard list drops LS as a project surface (operator no longer has reason to check LS dashboards post-rejection). Sibling tracked-file scrub of ~13 active LS references in state docs (8BALL.md §4 inspector lane row + §11.11 (b) ship-gate row + §11.16 Live-fire testing row), agent docs (orchestrator.md / auditor.md / verifier.md / controller.md / PLATFORMS.md / inspector.md / implementer.md), README.md §5.B privacy scan description, ui/payments.js comment, tests/payments_markup.test.js comment header, audits/RELEASE_CHECKLIST.md §5.B + post-merge-smoke-test wording (mix of chat-28 Codex Procedure 4 P1 tier-1 backlog + post-rejection cleanup). All historical breadcrumbs in DOCTRINE §5 / §5.B Call 2 that explain Gumroad mechanism via comparison with prior LS path preserved — they're not drift, they're how the doctrine documents the transition. All lineage entries (v0.28 through v0.22) preserved verbatim per L17. All agents/*.md audit-history / firing-log blocks documenting past LS-dashboard inspector firings + LS domain-allowlist block at v0.3.0 first non-default-domain directive + L51 sightings against LS dashboard state-row drift preserved verbatim per L17. No code touch, no calc change, no test logic change, no shipped-surface change — v0.3.0.3 already removed LS from runtime; this cycle is the doctrine + state-doc + agent-doc cleanup that v0.3.0.3 deliberately did not bundle. `tests/pii_scan.test.js` DOCTRINE_ALLOW unchanged.)
- v0.29: 2026-05-16 (§10 amended — core-agent table extended from 4 rows to 5 rows; new **Inspector** row inserted between Verifier and Controller, codifying read-only state reads on operator-controlled third-party dashboards — payment processors (Gumroad, LS), hosting (Netlify), code (GitHub), social (IG / TikTok / Threads). Inspector promoted from sketch `~/Desktop/8ball/sessions/inspector_sketch_2026-05-13.md` after 9/9 directive-shape firings (8 fired, 7 unambiguously clean) across 8 distinct surfaces; T1 promotion gate over-satisfied by 4-firing buffer. §10 main paragraph "four agent roles" updated to "five agent roles"; Lane discipline gains new bullet routing operational-dashboard state reads to the inspector with canonical-file-consumption purpose requirement. Boundary clauses (no irreversible actions, no credentials, no scope-expansion synthesis) inherited from `agents/verifier.md` (adapted to inspector surface; meaning preserved) via chat-15 c13-c14-c15 L-mitigation bundle. Sibling tracked-file changes co-versioned: new `agents/inspector.md` role doc; `agents/PLATFORMS.md` new H2 "Inspector (operational dashboard read)" section between CiC (verifier) and ChatGPT entries; `agents/AGENTS.md` role table 4 → 5 core agents + opening-paragraph count + new audit history entry. No code touch, no calc change, no shipped-surface change, no test logic change — `tests/pii_scan.test.js` DOCTRINE_ALLOW extended in this cycle to add `agents/inspector.md` explicitly (sibling-to-existing-role-doc entries from v0.24 cycle); test change is one-line list addition.)
- v0.28: 2026-05-16 (§5.B Call 2 rewritten — Lemon Squeezy Buy Link redirect path retired, Gumroad Buy Link redirect codified as the live processor mechanism. §2 + §5 preamble + §5.B section header + §5.B Call 2 numbered item all updated. Mechanism differences: bare URL with no `checkout[success_url]` query parameter; post-purchase redirect handled single-source via Gumroad's product Content-tab Button rather than belt-and-suspenders LS dashboard Confirmation + URL-encoded query; two-click post-purchase flow paywall → Gumroad checkout → Content Button → `/?paid=t1` rather than single-click LS redirect; Stripe under Gumroad's Connect rather than Stripe under LS. Constraints preserved: user-initiated only, hosted redirect not overlay, no user data on redirect, no SDK/fetch/webhook, trust-based return, no customer object retained, disclosure in about-modal + paywall modal. Trigger: chat-25 LS Step 3 KYC/KYB review still vendor-staff-pending after 1-3 BD SLA window; chat-28 Gumroad Stripe Connect verification cleared 2026-05-16 same day after operator submission of corrected business-website + product-description fields. Per chat-27 "whichever clears first becomes primary funnel" — Gumroad cleared first, became live processor. LS path preserved at lemon-squeezy.com store but not wired through 8ball runtime; future re-route possible if LS Step 3 clears, but doctrine codifies single-processor-at-a-time at runtime.)
- v0.27: 2026-05-13 (§10 amended additively — periodic corpus drift-sweep audit procedure codified at `agents/auditor.md` Procedure 6 after three clean T1 firings, chat-8 + chat-9 + chat-10; absorbs the standing "Codex drift-sweep" tentative lane into a real auditor procedure. No new lanes, no role-doc additions; the existing auditor role gains an additional invocation entry point. No code touch, no calc change, no shipped-surface change.)
- v0.26: 2026-05-12 (drift-sweep tier-2 cleanup: §5.B Call 1 retires the stale `action="/?sent=1"` ban — the v0.2.5.2 same-origin success redirect is the shipped form; §5.B Call 2 documents the URL-encoded `checkout[success_url]` query-param mechanism shipped at v0.3.0.2 paired with the dashboard Button link as belt-and-suspenders; §6 main paragraph re-aligned with shipped `ui/` folder + 7-module `core/` + 14-test `tests/`. State docs (8BALL.md §1/§2/§8, README.md, root AGENTS.md, root CLAUDE.md, agents/auditor.md) reconciled with shipped reality. 8BALL.md §1 SIRR-paragraph metaphor/architecture split applied per controller disposition B (chat-10). No new doctrine clauses added; existing v0.21 / v0.22 / v0.23 / v0.25 amendments now reflected consistently across §5.B / §6 main bodies + state docs.)
- v0.25: 2026-05-12 (drift-sweep tier-1 cleanup: §1 main paragraph aligned with v0.22 amendment paid-content reality; §2 voice scan paragraph aligned with v0.22 §7 stage-1 CI scan extension; §2 third bullet + §7 stage 2 absolute "no network calls" wording replaced with scoped-model wording matching §5; §5 allow-list extended to declare `eight_ball_profile_v1` wrapper key explicitly. No new doctrine clauses added; existing v0.22 / v0.23 amendments now reflected consistently in §1 / §2 / §5 / §7 main bodies.)
- v0.24: 2026-05-12 (§10 restructured as 4 core agents + 1 always-on controller + adjuncts; agents/ folder formalized with `AGENTS.md`, `PLATFORMS.md`, and 5 role docs at `agents/orchestrator.md` / `implementer.md` / `auditor.md` / `verifier.md` / `controller.md`; v0.23 5-row table reshaped, lane labels aligned with role docs, ChatGPT moved from core lane to adjunct, verifier (Claude in Chrome) added as the fourth agent role).
- v0.23: 2026-05-11 (§1.B amended — numerology triplet always space-separated, v0.22 hasMaster branch retired post-CiC Fix A; §4.B amended — about-modal discloses cap shape, not the localStorage-wipe reset; §6 amended — ui/*.js split pattern locked with `ui/payments.js` + `ui/profile.js` as concrete precedents and DI shape specified).
- v0.22: §1 / §2 / §4 / §5 / §5.B / §5.C / §6 / §7 amended or added — paid surface (3 free + $3/3 reads via LS Buy Link redirect; public-bundle JS-gated deck delivery; arcade-toy carve-out; payments state-machine CI stage).
- v0.21: §1.A amended — DST + historical-tz handling in scope; city-level birthplace via core/cities.js; |lat|>66.5° returns null. §5 amended — same-origin lazy loads permitted, fetch() ban preserved. §5 allow-list extended with city/cc/tz.
- v0.20: §3 calc v2 — real lunar new year + solar-term tables; getInnerAnimal signature change; date-precision Asia/Shanghai cusp resolution.
- v0.19: §5 allow-list extended with `eight_ball_labels_revealed_v1`.
- v0.18: §4.A added — 18+ acknowledgment gate, click-through (no verification), one-time, persists via separate localStorage flag.
- v0.17: §5.B added — feedback surface, user-initiated only, single named endpoint, native form POST, fail-silent.
